---
phase: 10-refine-actions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/sara-extract/SKILL.md
autonomous: true
requirements:
  - WIKI-03
must_haves:
  truths:
    - "sara-extract action pass defines a positive signal: any passage implying work needs to happen"
    - "Each extracted action artifact contains act_type (deliverable or follow-up), owner (distinct from raised_by), and due_date fields"
    - "Owner-not-resolved warning appears once before the artifact summary block for actions with unresolved owner, not inside the Discuss loop"
  artifacts:
    - path: ".claude/skills/sara-extract/SKILL.md"
      provides: "Rewritten action pass (Step 3) + owner warning injection (Step 4)"
      contains: "act_type"
  key_links:
    - from: ".claude/skills/sara-extract/SKILL.md Step 3 action pass"
      to: "sara-artifact-sorter.md"
      via: "act_artifacts JSON array"
      pattern: "act_artifacts"
    - from: ".claude/skills/sara-extract/SKILL.md Step 4"
      to: "AskUserQuestion call"
      via: "plain-text warning before artifact block"
      pattern: "Owner not resolved"
---

<objective>
Rewrite the sara-extract action extraction pass (Step 3) and add the owner-not-resolved warning to the Step 4 approval loop.

Purpose: The current action pass is a single vague paragraph with no signal definition, no type classification, and no owner/due_date fields. This plan replaces it with a structured pass mirroring the decisions pass (Phase 9 pattern), and adds the D-13 owner warning to the approval loop.

Output: `.claude/skills/sara-extract/SKILL.md` with two targeted changes:
1. Action pass (lines 138–146) replaced with the D-01 through D-05 compliant pass.
2. Owner warning injected before the `--- Artifact {N} ---` block in Step 4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-refine-actions/10-CONTEXT.md
@.planning/phases/10-refine-actions/10-RESEARCH.md
@.planning/phases/10-refine-actions/10-PATTERNS.md

<interfaces>
<!-- Key patterns from the existing skill file that the executor must replicate -->

From .claude/skills/sara-extract/SKILL.md (decisions pass, lines 95–136) — the direct analog for the action pass replacement:
```
A passage IS a decision if it contains commitment language OR misalignment language from the
signal lists below.

  COMMITMENT language — these passages ARE decisions → status: accepted
  - "we decided to", "we chose", "we agreed on", "we went with", "we will use", "the approach is"

  MISALIGNMENT language — these passages ARE decisions → status: open
  - disagreement, contested views, unresolved choices, "we need to decide", competing preferences

  EXCLUDE — these passages are NOT decisions (do NOT extract them):
  - Option exploration: "we could use X or Y"
  - Aspiration/wish: "it would be good to have Z"
  - Requirement/obligation: "the system must support A"

For each decision found:
- Extract the exact verbatim passage as `source_quote` (MANDATORY — skip any decision without a quotable passage)
- Write a short (≤10 words) noun-phrase `title`
- Set `raised_by` to the STK-NNN ID if identifiable from the source or discussion_notes; otherwise use "STK-NNN" placeholder
- Set `status` to "accepted" if commitment language; "open" if misalignment language
- Set `dec_type` to one of the six types above
- Set `chosen_option`, `alternatives`
- Set `action` = "create", `type` = "decision", `id_to_assign` = "DEC-NNN", `related` = [], `change_summary` = ""
- Do NOT extract `context` or `rationale` — synthesised by sara-update

Collect results as `{dec_artifacts}` (JSON array; empty array if none found).
```

Current action pass being replaced (lines 138–146):
```
**Actions pass**

Extract every passage that describes an action item — a concrete task or follow-up with an
implied or explicit owner (something that must be done, not a general statement of intent). For each action found:
- Extract the exact verbatim passage as `source_quote` (MANDATORY)
- Write a short (≤10 words) imperative-phrase `title` (e.g. "Send updated proposal to client")
- Set `raised_by` to the STK-NNN ID of the person who will own the action if identifiable; otherwise `"STK-NNN"` placeholder
- Set `action` = `"create"`, `type` = `"action"`, `id_to_assign` = `"ACT-NNN"`, `related` = `[]`, `change_summary` = `""`

Collect results as `{act_artifacts}` (JSON array; empty array if none found).
```

Step 4 approval loop presentation block (lines 219–227) — owner warning is injected BEFORE this:
```
  Present the artifact as plain text before the AskUserQuestion call:
  ```
  --- Artifact {artifact_index} ---
  Type:   {type}
  Title:  {title}
  Action: CREATE new {TYPE}-NNN  /  UPDATE {existing_id}
  Source: "{source_quote}"
  [If update] Change: {change_summary}
  ```
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite sara-extract action pass (Step 3)</name>
  <files>.claude/skills/sara-extract/SKILL.md</files>
  <read_first>
    - .claude/skills/sara-extract/SKILL.md (MANDATORY — read the full file before editing; identify exact lines 138–146 to replace)
    - .planning/phases/10-refine-actions/10-CONTEXT.md (D-01 through D-05 locked decisions)
    - .planning/phases/10-refine-actions/10-PATTERNS.md (Pattern Assignments section for action pass)
  </read_first>
  <action>
Replace lines 138–146 (the current **Actions pass** block) with the following text verbatim. Use the Edit tool to replace from `**Actions pass**` through `Collect results as \`{act_artifacts}\` (JSON array; empty array if none found).` — including both boundary lines.

The replacement text is:

```
**Actions pass**

A passage IS an action if it describes any work that needs to happen — a concrete task,
follow-up, or deliverable that someone is expected to do. Cast the broadest possible net:
extract passages with or without a named owner, with or without a due date, with or without
explicit assignment language. The existing sorter handles cross-type disambiguation.

  INCLUDE — these passages ARE actions (extract them):
  - Explicit assignment: "Alice will send the updated proposal by Friday"
  - Implicit task: "someone needs to chase the sign-off on this"
  - Deliverable commitment: "we need a revised spec before next sprint"
  - Follow-up required: "Bob to confirm the budget allocation"
  - Unowned task: "this needs to be documented" (no owner named — still an action)

  EXCLUDE — these passages are NOT actions (do NOT extract them):
  - Background context: "historically we have used vendor X" (describes past, not future work)
  - Risk mitigations: passages that describe a contingency plan — these are captured by the Risks pass
  - Requirements: passages with modal verbs describing system capabilities ("the system must support…") — captured by the Requirements pass
  - Decisions: passages describing a concluded choice — captured by the Decisions pass

For each action found, classify it into one of two types inline:
  - `deliverable` — a concrete output or artefact to produce (report, document, implementation, fix)
  - `follow-up`   — a check-in, response, or update required from someone (confirm, reply, chase, update)

For each action found:
- Extract the exact verbatim passage as `source_quote` (MANDATORY — skip any action without a quotable passage)
- Write a short (≤10 words) imperative-phrase `title` (e.g. "Send updated proposal to client", "Confirm budget allocation with finance")
- Set `act_type` to `"deliverable"` or `"follow-up"` (see classification above)
- Set `owner` to the STK-NNN ID of the person assigned to do the work if identifiable from source or discussion_notes; if a name is mentioned but not yet in the stakeholder registry, write the raw name string (e.g. `"Alice"`); if no person is identified, set to `""`
- Set `raised_by` to the STK-NNN ID of the person who surfaced or mentioned this action (may be the same as owner); otherwise use `"STK-NNN"` placeholder
- Set `due_date` to the raw string from the source if a due date or deadline is mentioned (e.g. `"by Friday"`, `"EOW"`, `"before next sprint"`); otherwise set to `""`
- Set `action` = `"create"`, `type` = `"action"`, `id_to_assign` = `"ACT-NNN"`, `related` = `[]`, `change_summary` = `""`
- Do NOT extract Description or Context — these are synthesised by sara-update from the full source document, not extracted here

Collect results as `{act_artifacts}` (JSON array; empty array if none found).
```

Key points to verify after editing:
- The `raised_by` field is preserved and distinct from `owner` (per D-05)
- `act_type`, `owner`, `due_date` are new fields added alongside existing mandatory fields
- The "Do NOT extract Description or Context" note is present at the end of the field list
- The `Collect results as {act_artifacts}` collect line is preserved verbatim at the end
- No other passes (requirements, decisions, risks) are modified
  </action>
  <verify>
    <automated>grep -n "act_type" /home/george/Projects/sara/.claude/skills/sara-extract/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "act_type" .claude/skills/sara-extract/SKILL.md` returns >= 2 (appears in classification block and in field extraction list)
    - `grep -c "due_date" .claude/skills/sara-extract/SKILL.md` returns >= 1
    - `grep -n "owner.*STK-NNN\|owner.*raw name\|owner.*assigned" .claude/skills/sara-extract/SKILL.md` returns a match (owner field definition present)
    - `grep -n "raised_by.*STK-NNN ID of the person who surfaced" .claude/skills/sara-extract/SKILL.md` returns a match (raised_by distinction preserved)
    - `grep -n "deliverable.*follow-up\|follow-up.*deliverable" .claude/skills/sara-extract/SKILL.md` returns a match (two-type classification present)
    - `grep -n "Do NOT extract Description or Context" .claude/skills/sara-extract/SKILL.md` returns a match
    - `grep -n "Collect results as.*act_artifacts" .claude/skills/sara-extract/SKILL.md` returns a match (collect line preserved)
    - The old text `"concrete task or follow-up with an implied or explicit owner"` does NOT appear in the file
  </acceptance_criteria>
  <done>Action pass contains positive signal definition, INCLUDE/EXCLUDE examples, two-type classification, and all five per-artifact fields (act_type, owner, raised_by, due_date, source_quote) with clear instructions. Old vague pass is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Add owner-not-resolved warning to Step 4 approval loop</name>
  <files>.claude/skills/sara-extract/SKILL.md</files>
  <read_first>
    - .claude/skills/sara-extract/SKILL.md (re-read after Task 1 to see current state of Step 4)
    - .planning/phases/10-refine-actions/10-CONTEXT.md (D-13 owner warning decision)
    - .planning/phases/10-refine-actions/10-RESEARCH.md (Pitfall 3: warning placement)
  </read_first>
  <action>
In the Step 4 approval loop, insert the owner-not-resolved warning block immediately BEFORE the `Present the artifact as plain text before the AskUserQuestion call:` line (which begins the `--- Artifact {artifact_index} ---` block).

Use the Edit tool to insert the following text BEFORE the presentation block. The insertion point is the line that reads `  Present the artifact as plain text before the AskUserQuestion call:`.

Insert this block immediately before that line:

```
  If `artifact.type == "action"` AND (`artifact.owner == ""` OR `artifact.owner` does not match the pattern `STK-\d{3}`):
    Output as plain text:
    ⚠ Owner not resolved — assign manually after /sara-update, or run /sara-add-stakeholder first.

```

Critical placement rules (per D-13 and RESEARCH Pitfall 3):
- The warning is output ONCE, before the `--- Artifact {N} ---` block
- It is NOT inside the Discuss retry loop
- It does NOT repeat on subsequent Discuss cycles for the same artifact
- The condition checks `artifact.type == "action"` first, so non-action artifacts are never affected
  </action>
  <verify>
    <automated>grep -n "Owner not resolved" /home/george/Projects/sara/.claude/skills/sara-extract/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Owner not resolved" .claude/skills/sara-extract/SKILL.md` returns a match at a line BEFORE the line containing `"--- Artifact {artifact_index} ---"`
    - `grep -n "STK-\\\\d{3}" .claude/skills/sara-extract/SKILL.md` returns a match (pattern check present)
    - `grep -n "artifact.type.*action" .claude/skills/sara-extract/SKILL.md` returns a match (type guard present)
    - The warning text `"⚠ Owner not resolved — assign manually after /sara-update, or run /sara-add-stakeholder first."` appears exactly once in the file
    - The warning block appears before `Present the artifact as plain text before the AskUserQuestion call:` — not inside the Discuss cycle block
  </acceptance_criteria>
  <done>Owner-not-resolved warning is injected once before the artifact presentation block in Step 4, conditional on action type and unresolved owner. It does not appear inside the Discuss loop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → skill file | The executor writes text to a markdown skill file; no code execution, no external I/O |
| Skill file → LLM runtime | The updated skill file is read by Claude Code as a prompt at runtime; no injection risk beyond the existing skill file trust model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Tampering | sara-extract/SKILL.md edit | accept | File is a markdown prompt, not executable code; changes are review-visible in git diff |
| T-10-01-02 | Information Disclosure | owner field captures raw name strings | accept | Raw name strings already present in pipeline-state.json from discussion phase; no new PII surface |
| T-10-01-03 | Elevation of Privilege | skill file prompt injection | accept | Skill files are authored content loaded by the user's own Claude Code instance; no external input path |
</threat_model>

<verification>
After both tasks complete:

1. `grep -c "act_type" .claude/skills/sara-extract/SKILL.md` — must return >= 2
2. `grep -c "due_date" .claude/skills/sara-extract/SKILL.md` — must return >= 1
3. `grep -n "Owner not resolved" .claude/skills/sara-extract/SKILL.md` — must return exactly 1 match
4. `grep -n "concrete task or follow-up with an implied or explicit owner" .claude/skills/sara-extract/SKILL.md` — must return 0 matches (old text gone)
5. `grep -n "Do NOT extract Description or Context" .claude/skills/sara-extract/SKILL.md` — must return 1 match
6. `grep -n "Collect results as.*act_artifacts" .claude/skills/sara-extract/SKILL.md` — must return 1 match (collect line preserved)
</verification>

<success_criteria>
- Action pass produces artifacts with act_type, owner, raised_by, due_date, source_quote, title — all five new/updated fields present in the field extraction list
- INCLUDE/EXCLUDE examples cover: unowned task, background context, risk mitigation, requirement, decision
- Owner-not-resolved warning appears exactly once in Step 4, before the artifact block, with STK pattern check
- No other extraction passes (requirements, decisions, risks) are modified
- git diff shows only lines 138–146 and the Step 4 warning insertion — no collateral edits
</success_criteria>

<output>
After completion, create `.planning/phases/10-refine-actions/10-01-SUMMARY.md` using the summary template.
</output>
