---
phase: 10-refine-actions
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01-PLAN.md
  - 10-02-PLAN.md
files_modified:
  - .claude/skills/sara-update/SKILL.md
  - .claude/agents/sara-artifact-sorter.md
autonomous: true
requirements:
  - WIKI-03
must_haves:
  truths:
    - "sara-update action create branch writes owner from artifact.owner (not artifact.raised_by), type from artifact.act_type, due-date from artifact.due_date, schema_version: '2.0'"
    - "sara-update action create branch produces six-section body: Source Quote, Description, Context, Owner, Due Date, Cross Links"
    - "sara-update action update branch upgrades any existing ACT page to v2.0 schema: adds type, changes owner source to artifact.owner, adds due-date, rewrites body to six sections"
    - "summary content rule for ACT reads 'owner, due-date, type, status' in both create branch and update branch"
    - "sara-artifact-sorter has explicit pass-through rule for act_type, owner, due_date action fields"
  artifacts:
    - path: ".claude/skills/sara-update/SKILL.md"
      provides: "v2.0 action create branch + update branch"
      contains: "artifact.owner"
    - path: ".claude/agents/sara-artifact-sorter.md"
      provides: "action field pass-through rule documentation"
      contains: "act_type"
  key_links:
    - from: ".claude/skills/sara-update/SKILL.md action create branch"
      to: "wiki/actions/ACT-NNN.md"
      via: "Write tool"
      pattern: "artifact.owner"
    - from: ".claude/skills/sara-update/SKILL.md action update branch"
      to: "wiki/actions/ACT-NNN.md"
      via: "Read then Write tool"
      pattern: "artifact.act_type"
---

<objective>
Rewrite the sara-update action create branch and add the action update branch v2.0 upgrade. Add the action field pass-through documentation rule to sara-artifact-sorter.

Purpose: Plan 01 updated sara-extract to produce act_type, owner, due_date fields. Plan 02 updated sara-init to write the v2.0 template. This plan closes the loop by updating sara-update to consume the new fields and write v2.0 wiki pages with the six-section body. It also adds explicit sorter pass-through documentation for the new action fields (documentation-only — no behaviour change).

Output:
1. `.claude/skills/sara-update/SKILL.md` — action create branch body (lines ~262–281) rewritten to six-section format; frontmatter field mapping (lines ~99–116) updated for action artifacts; action update branch added after decision update branch.
2. `.claude/agents/sara-artifact-sorter.md` — action field pass-through rule appended to the rules section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-refine-actions/10-CONTEXT.md
@.planning/phases/10-refine-actions/10-RESEARCH.md
@.planning/phases/10-refine-actions/10-PATTERNS.md
@.planning/phases/10-refine-actions/10-01-SUMMARY.md
@.planning/phases/10-refine-actions/10-02-SUMMARY.md

<interfaces>
<!-- Key lines from sara-update/SKILL.md that must be located and edited -->

Current frontmatter mapping block — action-specific lines (lines ~99, 106–107 in sara-update/SKILL.md):
```
    - `schema_version` = `"1.0"` for action and risk artifacts (always double-quoted)
    ...
    - For action artifacts: set `status` = `"open"`, `owner` = `artifact.raised_by` if it is a resolved STK ID (e.g. `"STK-001"`); otherwise set `owner` = `""` (empty — leave unassigned; do not write a placeholder ID)
```

Current summary content rule — action line (line ~114 in sara-update/SKILL.md):
```
      - ACT: owner, due-date, status (open/in-progress/done/cancelled)
```

Current action create branch body (lines ~262–281 in sara-update/SKILL.md):
```
    **action:**
    ```
    ## Description
    > "{artifact.source_quote}" — [[{artifact.raised_by}|{stakeholder_name}]]

    {synthesised summary of what needs to be done, who is responsible, and any relevant
     deadlines or dependencies resolved during /sara-discuss}

    ## Notes
    {synthesised blockers, dependencies, follow-up context, or related items from discussion
     notes — leave empty if none available}

    ## Cross Links
    {Generate one wiki link per entry in artifact.related...}
    ```
```

Existing decision update branch (sara-update/SKILL.md lines ~341–376) — direct structural analog for the action update branch:
```
    For decision artifacts (`artifact.type == "decision"`): after applying the change_summary
    to frontmatter fields and regenerating the summary, also update the frontmatter to include
    the v2.0 fields from the artifact object:
    - Set `type` = `artifact.dec_type` ...
    - Set `status` = `artifact.status` ...
    - Set `schema_version` = `'2.0'` (single-quoted string)
    - Remove the following v1.0 frontmatter fields if present: `context`, `decision`, `rationale`, `alternatives-considered`

    Then rewrite the full body to the v2.0 structured section format ...
```

Existing sorter rules section (sara-artifact-sorter.md lines ~151–155):
```
- For requirement artifacts, preserve `priority` and `req_type` exactly as received from the
  extraction pass. Do not modify, reclassify, or drop these fields. Pass them through unchanged
  to `cleaned_artifacts`.
- For decision artifacts, preserve `status`, `dec_type`, `chosen_option`, and `alternatives`
  exactly as received from the extraction pass. Do not modify, reclassify, or drop these fields.
  Pass them through unchanged to `cleaned_artifacts`.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update sara-update frontmatter mapping + rewrite action create branch body</name>
  <files>.claude/skills/sara-update/SKILL.md</files>
  <read_first>
    - .claude/skills/sara-update/SKILL.md (MANDATORY — read the full file; locate lines ~99–116 for frontmatter mapping and lines ~262–281 for action create branch body)
    - .planning/phases/10-refine-actions/10-CONTEXT.md (D-06 owner from artifact.owner; D-07 schema_version; D-08 type; D-09 owner; D-10 due-date; D-12 synthesis rules)
    - .planning/phases/10-refine-actions/10-PATTERNS.md (Action frontmatter field mapping section; Action create branch section)
    - .planning/phases/10-refine-actions/10-RESEARCH.md (Pitfall 1: owner vs raised_by; Pitfall 2: schema_version quoting)
  </read_first>
  <action>
Make two targeted edits to sara-update/SKILL.md:

**Edit A — Frontmatter mapping block (three sub-edits):**

1. Locate the line: `- \`schema_version\` = \`"1.0"\` for action and risk artifacts (always double-quoted)`
   Replace it with:
   ```
   - `schema_version` = `'2.0'` for action artifacts (single-quoted — matches requirement and decision convention; prevents YAML float parsing)
   - `schema_version` = `"1.0"` for risk artifacts (unchanged)
   ```

2. Locate the line: `- For action artifacts: set \`status\` = \`"open"\`, \`owner\` = \`artifact.raised_by\` if it is a resolved STK ID (e.g. \`"STK-001"\`); otherwise set \`owner\` = \`""\` (empty — leave unassigned; do not write a placeholder ID)`
   Replace it with:
   ```
   - For action artifacts: set `status` = `"open"`, `type` = `artifact.act_type` (one of: `deliverable`, `follow-up`), `owner` = `artifact.owner` (STK-NNN or raw name string or `""`), `due-date` = `artifact.due_date` (raw string or `""`)
   ```
   CRITICAL: The replacement sets `owner` from `artifact.owner`, NOT `artifact.raised_by`. This is the Pitfall 1 fix.

3. Locate the summary content rule line: `      - ACT: owner, due-date, status (open/in-progress/done/cancelled)`
   Replace it with:
   ```
         - ACT: owner, due-date, type, status
   ```

**Edit B — Action create branch body (lines ~262–281):**

Locate the `**action:**` body block. It begins with `    **action:**` followed by a triple-backtick fence and ends with the closing triple-backtick of that fence (just before `    **risk:**`).

Replace the entire block from `    **action:**` through its closing triple-backtick with:

```
    **action:**
    ```
    ## Source Quote
    > "{artifact.source_quote}" — [[{artifact.raised_by}|{stakeholder_name}]]

    ## Description
    {Synthesised from {source_doc} and {discussion_notes}: 2–4 sentences describing what needs
     to be done. Ground the description in the source quote. Leave empty (heading only) if
     nothing relevant is available — never fabricate.}

    ## Context
    {Synthesised from {source_doc} and {discussion_notes}: why this action was raised —
     triggering event, dependency, or decision it relates to. Leave empty (heading only)
     if nothing relevant is available — never fabricate.}

    ## Owner
    {Written from artifact.owner — NOT synthesised:
     - If artifact.owner is a valid STK-NNN ID (matches pattern STK-\d{3}): write "[[STK-NNN|Stakeholder Name]]" — read wiki/stakeholders/{artifact.owner}.md to resolve the name.
     - If artifact.owner is a raw name string (not empty, not STK-NNN): write it as-is with note "(not yet registered — run /sara-add-stakeholder)"
     - If artifact.owner is empty ("" or absent): write "Not assigned — set manually."}

    ## Due Date
    {Written from artifact.due_date — NOT synthesised:
     - If artifact.due_date is non-empty: write the raw string as-is (e.g. "by Friday", "EOW")
     - If artifact.due_date is empty ("" or absent): write "Not specified — set manually."}

    ## Cross Links
    {Generate one wiki link per entry in artifact.related. Resolve display text per wikilink rule:
     - STK entities: [[STK-NNN|name]] — read wiki/stakeholders/{ID}.md for the name field
     - REQ/DEC/ACT/RSK entities: [[ID|ID Title]] — read wiki/index.md for the title
     - If title/name cannot be resolved: fall back to bare [[ID]]
     Write each link on its own line. If artifact.related is empty, write this heading with no
     content (heading-only — consistent with the established empty-section pattern for this skill).}
    ```
```

Critical synthesis rule (D-12): Description and Context are synthesised from source_doc + discussion_notes. Owner and Due Date are written from extracted artifact fields — NOT synthesised. This distinction must be explicit in the Owner and Due Date section instructions (the "Written from artifact.X — NOT synthesised:" prefix).
  </action>
  <verify>
    <automated>grep -n "artifact.owner" /home/george/Projects/sara/.claude/skills/sara-update/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "owner.*artifact.owner" .claude/skills/sara-update/SKILL.md` returns at least 2 matches (frontmatter mapping line + Owner section body)
    - `grep -n "owner.*artifact.raised_by" .claude/skills/sara-update/SKILL.md` returns 0 matches for action artifacts (the old mapping is gone; risk artifact mapping may still exist separately)
    - `grep -n "artifact.act_type" .claude/skills/sara-update/SKILL.md` returns at least 1 match (frontmatter mapping `type` = `artifact.act_type`)
    - `grep -n "artifact.due_date" .claude/skills/sara-update/SKILL.md` returns at least 1 match (frontmatter mapping + Due Date section)
    - `grep -n "schema_version.*'2.0'.*action\|action.*schema_version.*'2.0'" .claude/skills/sara-update/SKILL.md` returns a match OR `grep -n "schema_version.*'2.0'" .claude/skills/sara-update/SKILL.md` returns >= 3 matches total (requirement, decision, action)
    - `grep -n "ACT: owner, due-date, type, status" .claude/skills/sara-update/SKILL.md` returns exactly 1 match (summary rule updated)
    - `grep -n "ACT: owner, due-date, status (open" .claude/skills/sara-update/SKILL.md` returns 0 matches (old summary rule gone)
    - `grep -n "## Source Quote" .claude/skills/sara-update/SKILL.md` returns a match in the action body block (new six-section body)
    - `grep -n "## Notes" .claude/skills/sara-update/SKILL.md` returns 0 matches in the action body block (old Notes section gone from action; may still exist in risk body block)
    - `grep -n "NOT synthesised" .claude/skills/sara-update/SKILL.md` returns matches for Owner and Due Date sections (D-12 synthesis rule explicit)
  </acceptance_criteria>
  <done>sara-update action create branch: frontmatter mapping sets type/owner/due-date/schema_version correctly from artifact fields; body has six sections; Owner and Due Date sections are written from extracted fields (not synthesised); old Description+Notes body is gone; old raised_by owner mapping is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Add action v2.0 update branch + sorter action pass-through rule</name>
  <files>
    .claude/skills/sara-update/SKILL.md
    .claude/agents/sara-artifact-sorter.md
  </files>
  <read_first>
    - .claude/skills/sara-update/SKILL.md (re-read after Task 1; locate the decision update branch around lines ~341–376 as the insertion anchor; locate the update branch summary content rule around line ~320)
    - .claude/agents/sara-artifact-sorter.md (read in full; locate the rules section ending with the decision pass-through rule around lines ~151–155)
    - .planning/phases/10-refine-actions/10-CONTEXT.md (D-06; RESEARCH Pitfall 4 — incomplete update branch)
    - .planning/phases/10-refine-actions/10-PATTERNS.md (Action update branch section; Sorter pass-through rule section)
  </read_first>
  <action>
Make three targeted edits:

**Edit A — Update branch summary content rule (sara-update/SKILL.md):**

Locate the update branch's summary regeneration rule. It reads (in the `artifact.action == "update"` section):
```
     ACT: owner/due-date/status
```
(or similar wording with the ACT content rule). Replace the ACT content rule to add `type`:
```
     ACT: owner/due-date/type/status
```

**Edit B — Add action update branch block (sara-update/SKILL.md):**

Locate the decision update branch block. It begins with `    For decision artifacts (\`artifact.type == "decision"\`):` and ends before `    Use the Write tool to overwrite ...`.

Immediately AFTER the decision update branch block (after its `Use the Write tool...` line for decision artifacts), insert the following action update branch block:

```
    For action artifacts (`artifact.type == "action"`): after applying the change_summary
    to frontmatter fields and regenerating the summary, also update the frontmatter to include
    the v2.0 fields from the artifact object:
    - Set `type` = `artifact.act_type` (one of: `deliverable`, `follow-up`) — add if absent
    - Set `owner` = `artifact.owner` (STK-NNN or raw name string or `""`) — REPLACE any existing value; do NOT use `artifact.raised_by`
    - Set `due-date` = `artifact.due_date` (raw string or `""`) — add if absent
    - Set `schema_version` = `'2.0'` (single-quoted string — prevents YAML float parsing)

    Then rewrite the full body to the v2.0 structured section format (Source Quote, Description,
    Context, Owner, Due Date, Cross Links) using the same synthesis rules as the create branch.
    Synthesise Description and Context from the updated frontmatter, artifact.source_quote,
    artifact.change_summary, and {discussion_notes}. Write Owner and Due Date from artifact
    fields — do NOT synthesise these sections.

    ## Source Quote
    > "{artifact.source_quote}" — [[{artifact.raised_by}|{stakeholder_name}]]

    ## Description
    {Synthesised from {source_doc}, {discussion_notes}, and artifact.change_summary: updated
     summary of what needs to be done. Ground in source quote. Never fabricate.}

    ## Context
    {Synthesised from {source_doc}, {discussion_notes}: why this action was raised, including
     any new context from artifact.change_summary. Leave empty (heading only) if nothing
     relevant — never fabricate.}

    ## Owner
    {Written from artifact.owner — NOT synthesised:
     - If valid STK-NNN ID: write "[[STK-NNN|Stakeholder Name]]"
     - If raw name string: write as-is with "(not yet registered — run /sara-add-stakeholder)"
     - If empty: write "Not assigned — set manually."}

    ## Due Date
    {Written from artifact.due_date — NOT synthesised:
     - If non-empty: write the raw string as-is
     - If empty: write "Not specified — set manually."}

    ## Cross Links
    {Generate one wiki link per entry in artifact.related (after merging with the existing
     related[] array). Use the wikilink rule: STK → [[STK-NNN|name]], REQ/DEC/ACT/RSK →
     [[ID|ID Title]], fallback to [[ID]] if title/name cannot be resolved.
     Write each link on its own line. Write heading only if artifact.related is empty after merge.}

```

**Edit C — Action pass-through rule (sara-artifact-sorter.md):**

Locate the end of the rules section in sara-artifact-sorter.md. The last rule currently reads:
```
- For decision update artifacts (`action=update`, `type=decision`), `status`, `dec_type`, `chosen_option`, and `alternatives` MUST be present...
```

Append the following two rules immediately after the last existing decision rule (before the closing `</output_format>` tag):
```
- For action artifacts, preserve `act_type`, `owner`, and `due_date` exactly as received from
  the extraction pass. Do not modify, reclassify, or drop these fields. Pass them through unchanged
  to `cleaned_artifacts`.
- For action update artifacts (`action=update`, `type=action`), `act_type`, `owner`, and `due_date`
  MUST be present — copy them from the incoming create artifact unchanged. sara-update reads these
  fields for all action artifacts regardless of action.
```
  </action>
  <verify>
    <automated>grep -n "artifact.act_type\|act_type" /home/george/Projects/sara/.claude/skills/sara-update/SKILL.md | head -10 && grep -n "act_type" /home/george/Projects/sara/.claude/agents/sara-artifact-sorter.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "For action artifacts.*artifact.type.*action" .claude/skills/sara-update/SKILL.md` returns a match (update branch block present)
    - `grep -n "artifact.act_type" .claude/skills/sara-update/SKILL.md` returns at least 2 matches (frontmatter mapping from Task 1 + update branch)
    - `grep -n "ACT: owner/due-date/type/status" .claude/skills/sara-update/SKILL.md` returns a match (update branch summary rule updated)
    - `grep -n "ACT: owner/due-date/status" .claude/skills/sara-update/SKILL.md` returns 0 matches (old update branch summary rule without type is gone)
    - `grep -n "do NOT use.*artifact.raised_by" .claude/skills/sara-update/SKILL.md` returns a match in the action update branch (explicit raised_by exclusion)
    - `grep -n "act_type" .claude/agents/sara-artifact-sorter.md` returns at least 2 matches (both action pass-through rules added)
    - `grep -n "due_date" .claude/agents/sara-artifact-sorter.md` returns at least 1 match (sorter rule references due_date)
    - The action update branch block appears AFTER the decision update branch block in the file (correct insertion order)
  </acceptance_criteria>
  <done>sara-update has an action update branch that upgrades any existing ACT page to v2.0 schema (type, owner from artifact.owner, due-date, schema_version '2.0', six-section body rewrite). The update branch summary rule includes `type`. sara-artifact-sorter has explicit pass-through documentation for act_type, owner, due_date.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → skill file | The executor writes text to markdown skill files; no code execution, no external I/O |
| Skill file → LLM runtime | Updated skill files are read by Claude Code as prompts at runtime |
| artifact.owner field → wiki page | Raw name strings written to frontmatter; no validation at write time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Tampering | sara-update/SKILL.md edit | accept | File is a markdown prompt; changes are review-visible in git diff |
| T-10-03-02 | Information Disclosure | artifact.owner raw name in wiki frontmatter | accept | Same exposure level as raised_by field which already writes raw names; user controls the wiki repo |
| T-10-03-03 | Spoofing | owner field set to arbitrary raw string | accept | Owner field is user-visible in wiki; reconciliation is explicit user workflow; no privilege escalation path |
| T-10-03-04 | Tampering | Incorrect update branch insertion point | mitigate | Acceptance criteria verify the action update branch appears AFTER the decision branch and includes the `artifact.act_type` pattern |
</threat_model>

<verification>
After both tasks complete, run all checks across both modified files:

**sara-update/SKILL.md:**
1. `grep -c "artifact.owner" .claude/skills/sara-update/SKILL.md` — must return >= 3 (frontmatter mapping + create branch Owner section + update branch Owner section)
2. `grep -c "artifact.act_type" .claude/skills/sara-update/SKILL.md` — must return >= 2 (frontmatter mapping + update branch)
3. `grep -c "artifact.due_date" .claude/skills/sara-update/SKILL.md` — must return >= 2 (frontmatter mapping + update branch)
4. `grep -n "schema_version.*\"1.0\".*action\|action.*schema_version.*\"1.0\"" .claude/skills/sara-update/SKILL.md` — must return 0 matches (old double-quoted 1.0 for action is gone)
5. `grep -n "ACT: owner, due-date, type, status" .claude/skills/sara-update/SKILL.md` — must return 1 match (create branch summary rule)
6. `grep -n "ACT: owner/due-date/type/status" .claude/skills/sara-update/SKILL.md` — must return 1 match (update branch summary rule)
7. `grep -n "NOT synthesised" .claude/skills/sara-update/SKILL.md` — must return >= 2 matches (Owner + Due Date in create and/or update branch)

**sara-artifact-sorter.md:**
8. `grep -c "act_type" .claude/agents/sara-artifact-sorter.md` — must return >= 2
9. `grep -c "due_date" .claude/agents/sara-artifact-sorter.md` — must return >= 1
</verification>

<success_criteria>
- sara-update action create branch: six-section body, owner from artifact.owner, type from artifact.act_type, due-date from artifact.due_date, schema_version '2.0' single-quoted
- sara-update action update branch: mirrors create branch, adds v2.0 fields to existing pages, body rewritten to six sections, owner from artifact.owner (not raised_by)
- sara-update summary content rules: create branch reads "ACT: owner, due-date, type, status"; update branch reads "ACT: owner/due-date/type/status"
- sara-artifact-sorter: two action pass-through rules appended (one for create, one for update artifacts)
- git diff shows only: action-specific lines in sara-update/SKILL.md + appended rules in sara-artifact-sorter.md — no risk, requirement, or decision sections modified
</success_criteria>

<output>
After completion, create `.planning/phases/10-refine-actions/10-03-SUMMARY.md` using the summary template.
</output>
