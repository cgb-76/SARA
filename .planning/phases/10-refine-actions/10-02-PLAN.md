---
phase: 10-refine-actions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/sara-init/SKILL.md
autonomous: true
requirements:
  - WIKI-03
must_haves:
  truths:
    - "sara-init CLAUDE.md schema block for Action shows v2.0 frontmatter with type, owner (raw name comment), due-date (raw string comment), schema_version: '2.0' (single-quoted)"
    - "sara-init action.md template write contains v2.0 frontmatter and six-section body"
    - "summary comment reads '# ACT: owner, due-date, type, status' in both CLAUDE.md block and template"
  artifacts:
    - path: ".claude/skills/sara-init/SKILL.md"
      provides: "v2.0 action schema block (Step 9) + v2.0 action template (Step 12)"
      contains: "schema_version: '2.0'"
  key_links:
    - from: ".claude/skills/sara-init/SKILL.md CLAUDE.md schema block"
      to: "sara-update action write branch"
      via: "template consumed at project init time"
      pattern: "action.md"
---

<objective>
Update the sara-init action schema block (CLAUDE.md, Step 9) and action template (`.sara/templates/action.md`, Step 12) to v2.0 frontmatter and six-section body.

Purpose: At project init time, sara-init writes the CLAUDE.md behavioral contract and entity templates. Both the action schema block and the action template are currently v1.0 (two-section body, no type field, double-quoted schema_version). This plan bumps them to v2.0 to align with what sara-update will write in Plan 03.

Output: `.claude/skills/sara-init/SKILL.md` with two targeted changes:
1. CLAUDE.md action schema block (Step 9, lines ~253–272) replaced with v2.0 content.
2. Action template write (Step 12, lines ~472–491) replaced with v2.0 content.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-refine-actions/10-CONTEXT.md
@.planning/phases/10-refine-actions/10-RESEARCH.md
@.planning/phases/10-refine-actions/10-PATTERNS.md

<interfaces>
<!-- Key content from sara-init/SKILL.md that the executor must locate and replace -->

Current action schema block in Step 9 (sara-init/SKILL.md lines ~253–272):
```
### Action

```yaml
---
id: ACT-000
title: ""
status: open  # open | in-progress | done | cancelled
summary: ""  # ACT: owner, due-date, status (open/in-progress/done/cancelled)
owner: ""      # stakeholder ID (e.g. STK-001)
due-date: ""   # ISO 8601
source: []     # list of ingest IDs (e.g. [MTG-001, MTG-003])
schema_version: "1.0"
tags: []
related: []
---

## Description

## Notes
```
```

Current action template in Step 12 (sara-init/SKILL.md lines ~472–491):
```
`.sara/templates/action.md`:

```markdown
---
id: ACT-000
title: ""
status: open  # open | in-progress | done | cancelled
summary: ""  # ACT: owner, due-date, status (open/in-progress/done/cancelled)
owner: ""      # stakeholder ID (e.g. STK-001)
due-date: ""   # ISO 8601
source: []     # list of ingest IDs (e.g. [MTG-001, MTG-003])
schema_version: "1.0"
tags: []
related: []
---

## Description

## Notes
```
```

Analog pattern — decision schema block (already v2.0 in sara-init Step 9):
```
### Decision

```yaml
---
id: DEC-000
title: ""
status: accepted  # accepted | open | rejected | superseded
summary: ""  # DEC: options considered, chosen option, status, decision date
type: architectural  # architectural | process | tooling | data | business-rule | organisational
date: ""          # ISO 8601 (e.g. 2026-04-29)
deciders: []      # stakeholder IDs (e.g. [STK-001, STK-002])
supersedes: ""    # DEC-NNN or empty
source: []        # ingest IDs (e.g. [MTG-001])
schema_version: '2.0'
tags: []
related: []
---
```
Body follows the v2.0 structured section format (Source Quote, Context, Decision, Alternatives
Considered, Rationale).
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md action schema block in sara-init Step 9</name>
  <files>.claude/skills/sara-init/SKILL.md</files>
  <read_first>
    - .claude/skills/sara-init/SKILL.md (MANDATORY — read the full file; locate the `### Action` heading in Step 9 around lines 253–272)
    - .planning/phases/10-refine-actions/10-CONTEXT.md (D-07 through D-10 frontmatter decisions; D-11 six-section body)
    - .planning/phases/10-refine-actions/10-PATTERNS.md (CLAUDE.md action schema block replacement section)
  </read_first>
  <action>
Locate the `### Action` block inside the CLAUDE.md schema write instruction (Step 9) of sara-init/SKILL.md. It begins with `### Action` and ends just before `### Risk`.

Use the Edit tool to replace the entire block from `### Action` through the closing triple-backtick of the code fence (the block that ends with `## Notes` and a closing ` ``` `) with the following text verbatim:

```
### Action

```yaml
---
id: ACT-000
title: ""
status: open  # open | in-progress | done | cancelled
summary: ""   # ACT: owner, due-date, type, status
type: deliverable  # deliverable | follow-up
owner: ""     # STK-NNN or raw name string
due-date: ""  # raw string from source (e.g. "by Friday") or ISO date once resolved
source: []    # ingest IDs (e.g. [MTG-001])
schema_version: '2.0'
tags: []
related: []
---
```

Body follows the v2.0 structured section format (Source Quote, Description, Context, Owner,
Due Date, Cross Links). Description and Context are synthesised by sara-update from the source
document and discussion notes. Owner and Due Date are written from extracted artifact fields.
```

Key changes to verify:
- `summary` comment updated from `# ACT: owner, due-date, status (open/in-progress/done/cancelled)` to `# ACT: owner, due-date, type, status`
- `type` field added: `type: deliverable  # deliverable | follow-up`
- `owner` comment changed from `# stakeholder ID (e.g. STK-001)` to `# STK-NNN or raw name string`
- `due-date` comment changed from `# ISO 8601` to `# raw string from source (e.g. "by Friday") or ISO date once resolved`
- `source` comment updated (shorter form `# ingest IDs (e.g. [MTG-001])`)
- `schema_version: '2.0'` — single quotes, NOT double quotes
- Body description updated to six-section format name
- `## Description` and `## Notes` body stub removed (replaced by prose description of the six sections)
  </action>
  <verify>
    <automated>grep -n "schema_version.*2.0" /home/george/Projects/sara/.claude/skills/sara-init/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "schema_version: '2.0'" .claude/skills/sara-init/SKILL.md` returns at least 2 matches (one in the action block, at minimum — plus pre-existing decision/requirement matches)
    - `grep -n "type: deliverable" .claude/skills/sara-init/SKILL.md` returns a match in the action schema block
    - `grep -n "ACT: owner, due-date, type, status" .claude/skills/sara-init/SKILL.md` returns a match (updated summary comment)
    - `grep -n "ACT: owner, due-date, status (open" .claude/skills/sara-init/SKILL.md` returns 0 matches (old summary comment gone)
    - `grep -n "STK-NNN or raw name string" .claude/skills/sara-init/SKILL.md` returns a match in the action block
    - `grep -n "ISO 8601" .claude/skills/sara-init/SKILL.md` at the action block location returns 0 matches (old due-date comment gone from action block; may still exist elsewhere in the file for other artifact types)
    - The action block no longer contains `schema_version: "1.0"` (double-quoted)
  </acceptance_criteria>
  <done>CLAUDE.md action schema block in Step 9 shows v2.0 frontmatter: type field, updated owner comment, updated due-date comment, updated summary comment with type, schema_version: '2.0' single-quoted. Old two-section body stub replaced with six-section format description.</done>
</task>

<task type="auto">
  <name>Task 2: Update action.md template write in sara-init Step 12</name>
  <files>.claude/skills/sara-init/SKILL.md</files>
  <read_first>
    - .claude/skills/sara-init/SKILL.md (re-read after Task 1; locate the `.sara/templates/action.md` write block in Step 12 around lines 472–491)
    - .planning/phases/10-refine-actions/10-CONTEXT.md (D-07 through D-11; D-12 synthesis rules)
    - .planning/phases/10-refine-actions/10-PATTERNS.md (Action template replacement section)
  </read_first>
  <action>
Locate the `.sara/templates/action.md` block inside Step 12 of sara-init/SKILL.md. It begins with the line `` `.sara/templates/action.md`: `` and ends just before the `` `.sara/templates/risk.md`: `` block.

Use the Edit tool to replace the entire block from `` `.sara/templates/action.md`: `` through the closing triple-backtick of the markdown code fence (the block that ends with `## Notes` and a closing ` ``` `) with the following text verbatim:

````
`.sara/templates/action.md`:

```markdown
---
id: ACT-000
title: ""
status: open  # open | in-progress | done | cancelled
summary: ""   # ACT: owner, due-date, type, status
type: deliverable  # deliverable | follow-up
owner: ""     # STK-NNN or raw name string
due-date: ""  # raw string from source (e.g. "by Friday") or ISO date once resolved
source: []    # ingest IDs (e.g. [MTG-001])
schema_version: '2.0'
tags: []
related: []
---

## Source Quote
> [exact verbatim passage from source document] — [[STK-NNN|Stakeholder Name]]

## Description

[Synthesised by sara-update: 2–4 sentences describing what needs to be done, grounded in
 source quote and discussion notes]

## Context

[Synthesised by sara-update: why this action was raised — triggering event, dependency, or
 decision it relates to]

## Owner

[Written from artifact.owner — who is responsible. If empty: "Not assigned — set manually."]

## Due Date

[Raw due date string from extraction, or "Not specified — set manually." if empty]

## Cross Links
[One wiki link per related[] entry — see wikilink rule in sara-update SKILL.md]
```
````

Key changes to verify:
- Frontmatter matches v2.0 shape exactly (same fields as CLAUDE.md block updated in Task 1)
- `schema_version: '2.0'` — single quotes
- Body has six sections in order: Source Quote, Description, Context, Owner, Due Date, Cross Links
- `## Description` + `## Notes` old two-section body is gone
- Instructional comments in brackets for Description, Context, Owner, Due Date sections
  </action>
  <verify>
    <automated>grep -n "Due Date" /home/george/Projects/sara/.claude/skills/sara-init/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "## Due Date" .claude/skills/sara-init/SKILL.md` returns a match inside the action.md template block
    - `grep -n "## Source Quote" .claude/skills/sara-init/SKILL.md` returns a match inside the action.md template block (in addition to any existing matches in other template blocks)
    - `grep -n "## Notes" .claude/skills/sara-init/SKILL.md` at the action template location returns 0 matches (old Notes section gone from action template; may still exist in risk template which is unchanged)
    - `grep -n "ACT: owner, due-date, type, status" .claude/skills/sara-init/SKILL.md` returns 2 matches total (CLAUDE.md block from Task 1 + template from Task 2)
    - The action template block no longer contains `schema_version: "1.0"` (double-quoted)
    - `grep -n "Not assigned — set manually" .claude/skills/sara-init/SKILL.md` returns a match (Owner section instructional comment present)
    - `grep -n "Not specified — set manually" .claude/skills/sara-init/SKILL.md` returns a match (Due Date section instructional comment present)
  </acceptance_criteria>
  <done>Action template write in Step 12 produces a file with v2.0 frontmatter and six-section body (Source Quote, Description, Context, Owner, Due Date, Cross Links). Old Description + Notes two-section body is gone. Frontmatter matches the CLAUDE.md schema block updated in Task 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → skill file | The executor writes text to a markdown skill file; no code execution, no external I/O |
| Skill file → LLM runtime | The updated skill file is read by Claude Code as a prompt at runtime; no injection risk beyond the existing skill file trust model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Tampering | sara-init/SKILL.md edit | accept | File is a markdown prompt; changes are review-visible in git diff |
| T-10-02-02 | Tampering | Incorrect schema_version quoting | mitigate | Acceptance criteria explicitly check for single-quoted `'2.0'`; double-quoted form fails the criteria check |
| T-10-02-03 | Information Disclosure | Template instructional comments | accept | Comments are authoring guidance for sara-update, not user-facing; no PII |
</threat_model>

<verification>
After both tasks complete:

1. `grep -c "schema_version: '2.0'" .claude/skills/sara-init/SKILL.md` — must return >= 2 (action block + decision block minimum; requirement block also present)
2. `grep -c "ACT: owner, due-date, type, status" .claude/skills/sara-init/SKILL.md` — must return exactly 2 (CLAUDE.md block + template)
3. `grep -c "type: deliverable" .claude/skills/sara-init/SKILL.md` — must return >= 1 (action block)
4. `grep -c "## Due Date" .claude/skills/sara-init/SKILL.md` — must return >= 1 (template section heading)
5. `grep -n "schema_version: \"1.0\"" .claude/skills/sara-init/SKILL.md` — must return 0 matches in the action section (risk section still has "1.0" — that is correct and unchanged)
6. `grep -n "## Notes" .claude/skills/sara-init/SKILL.md` — must NOT appear in the action block (may appear in risk block)
</verification>

<success_criteria>
- CLAUDE.md action schema block in Step 9 shows v2.0 frontmatter with type, updated comments, schema_version: '2.0' (single-quoted)
- Action template in Step 12 has v2.0 frontmatter and six-section body matching D-11 exactly
- summary comment reads `# ACT: owner, due-date, type, status` in both locations
- No changes to requirement, decision, risk, or stakeholder blocks
- git diff shows only the action block in Step 9 and the action.md template in Step 12
</success_criteria>

<output>
After completion, create `.planning/phases/10-refine-actions/10-02-SUMMARY.md` using the summary template.
</output>
