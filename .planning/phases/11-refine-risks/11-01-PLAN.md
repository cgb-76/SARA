---
phase: 11-refine-risks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/sara-extract/SKILL.md
autonomous: true
requirements:
  - WIKI-04
must_haves:
  truths:
    - "sara-extract risk pass defines a tightened signal: uncertain future event or condition with a potential negative effect; confirmed problems already happening are actions, not risks"
    - "Each extracted risk artifact contains risk_type (one of six values), owner (distinct from raised_by), likelihood, impact (extracted from source signal or empty string), and status (open/mitigated/accepted, signal-based)"
    - "Owner-not-resolved warning appears once before the artifact summary block for risks with empty or raw-name owner, not inside the Discuss loop"
  artifacts:
    - path: ".claude/skills/sara-extract/SKILL.md"
      provides: "Rewritten risk pass (Step 3) + owner warning injection for risk type (Step 4)"
      contains: "risk_type"
  key_links:
    - from: ".claude/skills/sara-extract/SKILL.md Step 3 risk pass"
      to: "sara-artifact-sorter.md"
      via: "risk_artifacts JSON array"
      pattern: "risk_artifacts"
    - from: ".claude/skills/sara-extract/SKILL.md Step 4"
      to: "AskUserQuestion call"
      via: "plain-text warning before artifact block"
      pattern: "Owner not resolved"
---

<objective>
Rewrite the sara-extract risk extraction pass (Step 3) and extend the Step 4 approval loop owner warning to cover risk artifacts.

Purpose: The current risk pass is a vague paragraph with no type classification, no owner field, no likelihood/impact capture, and no signal-based status assignment. This plan replaces it with a structured pass consistent with D-01 through D-06 locked decisions, and extends the existing D-16 owner warning (currently action-only) to also trigger for risk artifacts.

Output: `.claude/skills/sara-extract/SKILL.md` with two targeted changes:
1. Risk pass (the "**Risks pass**" block in Step 3) replaced with the D-01 through D-06 compliant pass.
2. Step 4 owner warning block extended so it fires for `artifact.type == "risk"` in addition to `artifact.type == "action"`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-refine-risks/11-CONTEXT.md

<interfaces>
<!-- Key patterns from the existing skill file that the executor must replicate -->

From .claude/skills/sara-extract/SKILL.md — current risk pass (Step 3) being replaced:
```
**Risks pass**

Extract every passage that describes a risk — an uncertain event or condition with a potential negative effect (threat, concern, or "what if" scenario). A confirmed problem is an action item, not a risk. For each risk found:
- Extract the exact verbatim passage as `source_quote` (MANDATORY)
- Write a short (≤10 words) noun-phrase `title` (e.g. "Budget overrun on infrastructure")
- Set `raised_by` to the STK-NNN ID if identifiable; otherwise `"STK-NNN"` placeholder
- Set `action` = `"create"`, `type` = `"risk"`, `id_to_assign` = `"RSK-NNN"`, `related` = `[]`, `change_summary` = `""`

Collect results as `{risk_artifacts}` (JSON array; empty array if none found).
```

From .claude/skills/sara-extract/SKILL.md — Step 4 existing owner warning block (action-only, to be extended to also cover risk):
```
  If `artifact.type == "action"` AND `artifact.owner == ""`:
    Output as plain text:
    ⚠ Owner not set — assign manually after /sara-update, or run /sara-add-stakeholder first.

  If `artifact.type == "action"` AND `artifact.owner` is a non-empty string that does not match the pattern `STK-\d{3}`:
    Output as plain text:
    ⚠ Owner '{artifact.owner}' is a raw name — run /sara-add-stakeholder to register them before or after /sara-update.
```

The actions pass pattern (Phase 10) — direct analog for the risk pass structure:
```
**Actions pass**

A passage IS an action if it describes any work that needs to happen...

  INCLUDE — these passages ARE actions (extract them):
  ...

  EXCLUDE — these passages are NOT actions (do NOT extract them):
  ...

For each action found, classify it into one of two `act_type` values inline:
  - `deliverable` — ...
  - `follow-up`   — ...

For each action found:
- Extract the exact verbatim passage as `source_quote` (MANDATORY ...)
- ...
- Set `act_type` to ...
- Set `owner` to ...
- Set `raised_by` to ...
- Set `due_date` to ...
- Set `action` = `"create"`, `type` = `"action"`, `id_to_assign` = `"ACT-NNN"`, `related` = `[]`, `change_summary` = `""`
- Do NOT extract Description or Context — these are synthesised by sara-update ...

Collect results as `{act_artifacts}` (JSON array; empty array if none found).
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite sara-extract risk pass (Step 3)</name>
  <files>.claude/skills/sara-extract/SKILL.md</files>
  <read_first>
    - .claude/skills/sara-extract/SKILL.md (MANDATORY — read the full file before editing; locate the **Risks pass** block to identify exact replacement boundaries)
    - .planning/phases/11-refine-risks/11-CONTEXT.md (D-01 through D-06 locked decisions — signal definition, type taxonomy, owner vs raised_by, likelihood/impact capture, status assignment, artifact schema)
  </read_first>
  <action>
Replace the entire **Risks pass** block in Step 3 — from the line `**Risks pass**` through the line `Collect results as \`{risk_artifacts}\` (JSON array; empty array if none found).` (inclusive of both boundary lines) — with the following text verbatim. Use the Edit tool.

The replacement text is:

```
**Risks pass**

A passage IS a risk if it describes an **uncertain future event or condition with a potential
negative effect** — a threat, concern, "what if" scenario, or exposure that has not yet
materialised. A confirmed problem already happening is an action item, not a risk.

  INCLUDE — these passages ARE risks (extract them):
  - Future uncertainty: "we might not get budget approval for the second phase"
  - Concern or exposure: "there's a risk the integration vendor won't hit the API delivery date"
  - What-if scenario: "if the key architect leaves, we'd have no one to maintain this"
  - Dependency risk: "we're dependent on the client's data team being available for testing"
  - Regulatory exposure: "we haven't confirmed GDPR compliance for the new data store"

  EXCLUDE — these passages are NOT risks (do NOT extract them):
  - Confirmed problem already happening: "the build pipeline is broken right now" (→ action item)
  - Background context: "historically the vendor has been slow" (descriptive fact, not an uncertain future event)
  - Aspiration: "it would be great if the system could handle more load" (desire, not a risk event)
  - Mitigation or contingency already captured as an action: "Alice will set up redundancy this week"

For each risk found, classify it into one of six `risk_type` values inline:
  - `technical`    — system, architecture, technology, integration risks
  - `financial`    — budget, cost, funding, pricing risks
  - `schedule`     — timeline, deadline, dependency, sequencing risks
  - `quality`      — accuracy, completeness, reliability, performance risks
  - `compliance`   — regulatory, legal, policy, contractual risks
  - `people`       — staffing, skills, availability, stakeholder engagement risks

For each risk found:
- Extract the exact verbatim passage as `source_quote` (MANDATORY — skip any risk without a quotable passage)
- Write a short (≤10 words) noun-phrase `title` (e.g. "Budget overrun on infrastructure phase", "Vendor API delivery delay")
- Set `risk_type` to one of the six values above (use `risk_type` — not `type` — to avoid collision with the envelope `type: "risk"` field)
- Set `owner` to the STK-NNN ID of the person responsible for tracking and mitigating this risk if identifiable from source or discussion_notes; if a name is mentioned but not yet in the stakeholder registry, write the raw name string (e.g. `"Alice"`); if no person is identified, set to `""`
- Set `raised_by` to the STK-NNN ID of the person who surfaced or raised this risk in the source (may be the same as owner); otherwise use `"STK-NNN"` placeholder
- Set `likelihood` to `"high"`, `"medium"`, or `"low"` if the source contains an explicit signal (e.g. "very likely", "low probability", "significant concern", "minor risk"); otherwise set to `""`. Do not invent a likelihood value if none is stated.
- Set `impact` to `"high"`, `"medium"`, or `"low"` if the source contains an explicit signal (e.g. "catastrophic if it happens", "minor inconvenience", "could derail the project"); otherwise set to `""`. Do not invent an impact value if none is stated.
- Set `status` based on explicit source language only:
  - `"mitigated"` — source says "controls already in place", "this is being handled by X", "we've addressed this with Y"
  - `"accepted"`  — source says "we've accepted this risk", "we're comfortable with this", "no action needed, we'll live with it"
  - `"open"`      — all other risks (default; do not require explicit language for open)
- Set `action` = `"create"`, `type` = `"risk"`, `id_to_assign` = `"RSK-NNN"`, `related` = `[]`, `change_summary` = `""`
- Do NOT extract IF/THEN statement or Mitigation — these are synthesised by sara-update from the full source document, not extracted here

Collect results as `{risk_artifacts}` (JSON array; empty array if none found).
```

Key points to verify after editing:
- `risk_type` field is present with all six taxonomy values (technical, financial, schedule, quality, compliance, people)
- `owner` is a distinct field from `raised_by` with the same raw-name/empty-string pattern as actions
- `likelihood` and `impact` instructions say to set `""` if no signal — do not invent values
- `status` defaults to `"open"` with only two override keywords (mitigated, accepted)
- "Do NOT extract IF/THEN statement or Mitigation" note is present
- The `Collect results as {risk_artifacts}` collect line is preserved verbatim
- No other passes (requirements, decisions, actions) are modified
  </action>
  <verify>
    <automated>grep -n "risk_type" /home/george/Projects/sara/.claude/skills/sara-extract/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "risk_type" .claude/skills/sara-extract/SKILL.md` returns >= 2 (in type taxonomy block and in per-artifact field list)
    - `grep -n "technical.*financial.*schedule\|technical\|financial\|schedule\|quality\|compliance\|people" .claude/skills/sara-extract/SKILL.md` returns at least 6 matches (all six taxonomy values present)
    - `grep -n "owner.*responsible for tracking" .claude/skills/sara-extract/SKILL.md` returns a match (owner defined as distinct from raised_by)
    - `grep -n "raised_by.*surfaced or raised" .claude/skills/sara-extract/SKILL.md` returns a match (raised_by distinction preserved)
    - `grep -n "likelihood" .claude/skills/sara-extract/SKILL.md` returns >= 2 matches (likelihood field defined and referenced)
    - `grep -n "impact" .claude/skills/sara-extract/SKILL.md` returns >= 2 matches (impact field defined and referenced)
    - `grep -n "mitigated\|accepted.*risk\|open.*default" .claude/skills/sara-extract/SKILL.md` returns matches (status assignment logic present)
    - `grep -n "Do NOT extract IF/THEN" .claude/skills/sara-extract/SKILL.md` returns a match
    - `grep -n "Collect results as.*risk_artifacts" .claude/skills/sara-extract/SKILL.md` returns a match
    - The old text `"Extract every passage that describes a risk — an uncertain event"` does NOT appear in the file
  </acceptance_criteria>
  <done>Risk pass contains tightened signal definition, INCLUDE/EXCLUDE examples, six-type taxonomy, and all seven per-artifact fields (risk_type, owner, raised_by, likelihood, impact, status, source_quote) with explicit instructions. Old vague pass is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Extend Step 4 owner warning to cover risk artifacts</name>
  <files>.claude/skills/sara-extract/SKILL.md</files>
  <read_first>
    - .claude/skills/sara-extract/SKILL.md (read after Task 1 to see current state of the Step 4 owner warning block)
    - .planning/phases/11-refine-risks/11-CONTEXT.md (D-16 — owner warning decision for risks, same pattern as Phase 10 action warning)
  </read_first>
  <action>
In Step 4, find the existing two-condition owner warning block that currently checks only `artifact.type == "action"`. It reads:

```
  If `artifact.type == "action"` AND `artifact.owner == ""`:
    Output as plain text:
    ⚠ Owner not set — assign manually after /sara-update, or run /sara-add-stakeholder first.

  If `artifact.type == "action"` AND `artifact.owner` is a non-empty string that does not match the pattern `STK-\d{3}`:
    Output as plain text:
    ⚠ Owner '{artifact.owner}' is a raw name — run /sara-add-stakeholder to register them before or after /sara-update.
```

Use the Edit tool to replace this block with the following extended version that covers both action and risk artifact types:

```
  If (`artifact.type == "action"` OR `artifact.type == "risk"`) AND `artifact.owner == ""`:
    Output as plain text:
    ⚠ Owner not set — assign manually after /sara-update, or run /sara-add-stakeholder first.

  If (`artifact.type == "action"` OR `artifact.type == "risk"`) AND `artifact.owner` is a non-empty string that does not match the pattern `STK-\d{3}`:
    Output as plain text:
    ⚠ Owner '{artifact.owner}' is a raw name — run /sara-add-stakeholder to register them before or after /sara-update.
```

Critical placement rules (per D-16):
- The warning is output ONCE, before the `--- Artifact {N} ---` block
- It is NOT inside the Discuss retry loop
- It does NOT repeat on subsequent Discuss cycles for the same artifact
- The condition now covers both action and risk types; all other artifact types are unaffected
  </action>
  <verify>
    <automated>grep -n "artifact.type.*risk" /home/george/Projects/sara/.claude/skills/sara-extract/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "artifact.type.*risk" .claude/skills/sara-extract/SKILL.md` returns at least 1 match in the Step 4 section (the warning condition)
    - `grep -n "artifact.type.*action.*OR.*artifact.type.*risk\|artifact.type.*risk.*OR.*artifact.type.*action" .claude/skills/sara-extract/SKILL.md` returns a match (combined OR condition present)
    - The warning text `"⚠ Owner not set"` appears exactly once in the file (not duplicated)
    - The warning text `"⚠ Owner.*is a raw name"` appears exactly once in the file (not duplicated)
    - The warning block appears before `Present the artifact as plain text before the AskUserQuestion call:` — not inside the Discuss cycle block
    - `grep -n "STK-\\\\d{3}" .claude/skills/sara-extract/SKILL.md` returns a match (STK pattern check preserved)
  </acceptance_criteria>
  <done>Step 4 owner warning fires for both action and risk artifacts when owner is empty or an unresolved raw name. Warning appears once before the artifact block, not inside the Discuss loop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → skill file | The executor writes text to a markdown skill file; no code execution, no external I/O |
| Skill file → LLM runtime | The updated skill file is read by Claude Code as a prompt at runtime; no injection risk beyond the existing skill file trust model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Tampering | sara-extract/SKILL.md edit | accept | File is a markdown prompt, not executable code; changes are review-visible in git diff |
| T-11-01-02 | Information Disclosure | owner field captures raw name strings | accept | Raw name strings already present in pipeline-state.json from discussion phase; no new PII surface |
| T-11-01-03 | Elevation of Privilege | skill file prompt injection | accept | Skill files are authored content loaded by the user's own Claude Code instance; no external input path |
</threat_model>

<verification>
After both tasks complete:

1. `grep -c "risk_type" .claude/skills/sara-extract/SKILL.md` — must return >= 2
2. `grep -n "technical\|financial\|schedule\|quality\|compliance\|people" .claude/skills/sara-extract/SKILL.md` — must return 6+ matches in the risk pass taxonomy block
3. `grep -n "artifact.type.*risk" .claude/skills/sara-extract/SKILL.md` — must return >= 1 match in Step 4
4. `grep -n "Owner not set" .claude/skills/sara-extract/SKILL.md` — must return exactly 1 match
5. `grep -n "is a raw name" .claude/skills/sara-extract/SKILL.md` — must return exactly 1 match
6. `grep -n "Do NOT extract IF/THEN" .claude/skills/sara-extract/SKILL.md` — must return 1 match
7. `grep -n "Collect results as.*risk_artifacts" .claude/skills/sara-extract/SKILL.md` — must return 1 match
8. `grep -n "Extract every passage that describes a risk — an uncertain event" .claude/skills/sara-extract/SKILL.md` — must return 0 matches (old text gone)
</verification>

<success_criteria>
- Risk pass produces artifacts with risk_type, owner, raised_by, likelihood, impact, status, source_quote — all seven new/updated fields present in the field extraction list
- INCLUDE/EXCLUDE examples cover: confirmed problem (action, not risk), background context, aspiration, already-captured mitigation
- Six-type taxonomy (technical, financial, schedule, quality, compliance, people) present with descriptions
- likelihood and impact instruction explicitly says set "" if no source signal — do not invent
- status defaulting rules: open by default; mitigated/accepted only on explicit source language
- Owner-not-resolved warning in Step 4 covers both action and risk types via OR condition
- No other extraction passes (requirements, decisions, actions) are modified
</success_criteria>

<output>
After completion, create `.planning/phases/11-refine-risks/11-01-SUMMARY.md` using the summary template.
</output>
