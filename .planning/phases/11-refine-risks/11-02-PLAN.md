---
phase: 11-refine-risks
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/sara-init/SKILL.md
autonomous: true
requirements:
  - WIKI-04
must_haves:
  truths:
    - "sara-init Step 9 CLAUDE.md risk schema block reflects v2.0 frontmatter: type, owner, likelihood, impact, status, schema_version '2.0'; mitigation field removed"
    - "sara-init Step 12 risk.md template reflects v2.0 frontmatter and four-section body (Source Quote, Risk IF/THEN, Mitigation, Cross Links)"
    - "schema_version in risk template and CLAUDE.md schema block uses single-quoted '2.0' (not double-quoted) to prevent YAML float parsing"
  artifacts:
    - path: ".claude/skills/sara-init/SKILL.md"
      provides: "Updated Step 9 risk schema block + updated Step 12 risk.md template write"
      contains: "risk_type"
  key_links:
    - from: ".claude/skills/sara-init/SKILL.md Step 9"
      to: "CLAUDE.md Risk schema block written at runtime"
      via: "Write tool call for CLAUDE.md"
      pattern: "schema_version: '2.0'"
    - from: ".claude/skills/sara-init/SKILL.md Step 12"
      to: ".sara/templates/risk.md written at runtime"
      via: "Write tool call for risk.md template"
      pattern: "## Source Quote"
---

<objective>
Update sara-init to write the v2.0 risk schema: update the Risk section in the Step 9 CLAUDE.md write, and update the risk.md template in the Step 12 template writes.

Purpose: The current sara-init writes a v1.0 risk schema block (mitigation frontmatter field, Description/Mitigation/Notes body, schema_version "1.0"). This plan upgrades both the CLAUDE.md schema contract and the risk.md template to v2.0 — matching the frontmatter shape and body structure locked in D-07 through D-15.

Output: `.claude/skills/sara-init/SKILL.md` with two targeted changes:
1. Step 9 Risk schema block in the CLAUDE.md write: updated to v2.0 frontmatter (type, owner, raised-by, likelihood, impact, status, schema_version '2.0'; mitigation removed).
2. Step 12 risk.md template write: updated to v2.0 frontmatter and four-section body (Source Quote, Risk IF/THEN, Mitigation, Cross Links).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-refine-risks/11-CONTEXT.md

<interfaces>
<!-- Current risk schema blocks in sara-init/SKILL.md that are being replaced -->

From .claude/skills/sara-init/SKILL.md — Step 9 CLAUDE.md Risk schema block (current v1.0, to be replaced):
```yaml
### Risk

```yaml
---
id: RSK-000
title: ""
status: open  # open | mitigated | accepted | closed
summary: ""  # RSK: likelihood, impact, mitigation approach, status
likelihood: ""  # low | medium | high
impact: ""      # low | medium | high
owner: ""       # stakeholder ID (e.g. STK-001)
mitigation: ""
source: []      # list of ingest IDs (e.g. [MTG-001, MTG-003])
schema_version: "1.0"
tags: []
related: []
---

## Description

## Mitigation

## Notes
```
```

From .claude/skills/sara-init/SKILL.md — Step 12 `.sara/templates/risk.md` content (current v1.0, to be replaced):
```
---
id: RSK-000
title: ""
status: open  # open | mitigated | accepted | closed
summary: ""  # RSK: likelihood, impact, mitigation approach, status
likelihood: ""  # low | medium | high
impact: ""      # low | medium | high
owner: ""       # stakeholder ID (e.g. STK-001)
mitigation: ""
source: []      # list of ingest IDs (e.g. [MTG-001, MTG-003])
schema_version: "1.0"
tags: []
related: []
---

## Description

## Mitigation

## Notes
```

Phase 9 action.md template pattern (direct analog for the template structure):
```
---
id: ACT-000
title: ""
status: open  # open | in-progress | done | cancelled
summary: ""   # ACT: owner, due-date, type, status
type: deliverable  # deliverable | follow-up
owner: ""     # STK-NNN or raw name string
due-date: ""  # raw string from source (e.g. "by Friday") or ISO date once resolved
source: []    # ingest IDs (e.g. [MTG-001])
schema_version: '2.0'
tags: []
related: []
---

## Source Quote
...
## Description
...
## Context
...
## Owner
...
## Due Date
...
## Cross Links
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Step 9 CLAUDE.md risk schema block to v2.0</name>
  <files>.claude/skills/sara-init/SKILL.md</files>
  <read_first>
    - .claude/skills/sara-init/SKILL.md (MANDATORY — read the full file before editing; locate the "### Risk" section inside the Step 9 CLAUDE.md write block to identify exact replacement boundaries)
    - .planning/phases/11-refine-risks/11-CONTEXT.md (D-07 through D-13 — v2.0 frontmatter shape; D-14 — four-section body structure; D-15 — synthesised sections)
  </read_first>
  <action>
Find the "### Risk" section inside the Step 9 CLAUDE.md write block. It starts with `### Risk` and ends before `### Stakeholder`. Replace the entire Risk section (from `### Risk` through the closing triple-backtick of the YAML block and the subsequent body lines `## Description`, `## Mitigation`, `## Notes`) with the following v2.0 content verbatim. Use the Edit tool.

The replacement text is:

```
### Risk

```yaml
---
id: RSK-000
title: ""
status: open  # open | mitigated | accepted
summary: ""   # RSK: likelihood, impact, type, status, mitigation approach
type: technical  # technical | financial | schedule | quality | compliance | people
likelihood: ""   # low | medium | high (extracted from source, or empty)
impact: ""       # low | medium | high (extracted from source, or empty)
owner: ""        # STK-NNN or raw name string
raised-by: ""    # STK-NNN or raw name string
source: []       # ingest IDs (e.g. [MTG-001])
schema_version: '2.0'
tags: []
related: []
---
```

Body follows the v2.0 structured section format (Source Quote, Risk IF/THEN, Mitigation, Cross Links).
The Risk section uses `IF <trigger condition> THEN <adverse event>` with IF and THEN in caps.
Mitigation and Risk sections are synthesised by sara-update from the source document and discussion notes.

```

Key points to verify after editing:
- `schema_version: '2.0'` uses single quotes (not double-quotes "1.0")
- `mitigation: ""` frontmatter field is ABSENT (removed per D-08)
- `type:` field is present with `technical` as example value and all six type values in the comment
- `owner:` comment says "STK-NNN or raw name string" (per D-10)
- `raised-by:` field is present (new in v2.0 — distinct from owner)
- `status:` comment shows three values only: `open | mitigated | accepted` (no `closed` per D-12)
- Body description mentions "Source Quote, Risk IF/THEN, Mitigation, Cross Links" (the four sections)
- `## Description`, `## Mitigation`, `## Notes` body headings from v1.0 are GONE
  </action>
  <verify>
    <automated>grep -n "schema_version.*2.0" /home/george/Projects/sara/.claude/skills/sara-init/SKILL.md | grep -v "action\|decision\|requirement"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "schema_version.*'2.0'" .claude/skills/sara-init/SKILL.md` returns matches that include the risk section (single-quoted, not double-quoted)
    - `grep -n "mitigation:" .claude/skills/sara-init/SKILL.md` returns 0 matches in the Risk schema block context (mitigation frontmatter field removed; body section "## Mitigation" is fine)
    - `grep -n "type:.*technical" .claude/skills/sara-init/SKILL.md` returns a match in the risk section
    - `grep -n "raised-by:" .claude/skills/sara-init/SKILL.md` returns a match in the risk section
    - `grep -n "open | mitigated | accepted" .claude/skills/sara-init/SKILL.md` returns a match (three-value status lifecycle — no 'closed')
    - `grep -n "schema_version.*\"1.0\"" .claude/skills/sara-init/SKILL.md` returns 0 matches for risk (stakeholder may still have "1.0" double-quoted — that is correct and unchanged)
    - `grep -n "Source Quote.*Risk IF/THEN\|four-section" .claude/skills/sara-init/SKILL.md` returns a match (body structure described)
  </acceptance_criteria>
  <done>Step 9 CLAUDE.md risk schema block shows v2.0 frontmatter with type, raised-by, schema_version '2.0'; mitigation frontmatter field absent; status shows open/mitigated/accepted only; body description references four-section format.</done>
</task>

<task type="auto">
  <name>Task 2: Update Step 12 risk.md template to v2.0</name>
  <files>.claude/skills/sara-init/SKILL.md</files>
  <read_first>
    - .claude/skills/sara-init/SKILL.md (read after Task 1 to see current state of Step 12 risk.md template block)
    - .planning/phases/11-refine-risks/11-CONTEXT.md (D-07 through D-15 — full v2.0 frontmatter shape and four-section body structure with exact IF/THEN format and synthesis notes)
  </read_first>
  <action>
Find the `.sara/templates/risk.md` content block inside Step 12. It starts with the line `` `.sara/templates/risk.md`: `` and contains the current v1.0 frontmatter and body. Replace the full content written for this template (the markdown block between the backtick fences for risk.md) with the following v2.0 content verbatim. Use the Edit tool.

The full replacement content for the risk.md template write is:

```markdown
---
id: RSK-000
title: ""
status: open  # open | mitigated | accepted
summary: ""   # RSK: likelihood, impact, type, status, mitigation approach
type: technical  # technical | financial | schedule | quality | compliance | people
likelihood: ""   # low | medium | high (extracted from source, or empty)
impact: ""       # low | medium | high (extracted from source, or empty)
owner: ""        # STK-NNN or raw name string
raised-by: ""    # STK-NNN or raw name string
source: []       # ingest IDs (e.g. [MTG-001])
schema_version: '2.0'
tags: []
related: []
---

## Source Quote
> [exact verbatim passage from source document] — [[STK-NNN|Stakeholder Name]]

## Risk

IF <trigger condition> THEN <adverse event>

[Synthesised by sara-update: the IF/THEN statement is the primary risk description.
IF and THEN are written in caps. The rest of the statement is sentence case.
Example: IF the integration vendor delays API delivery THEN the go-live milestone slips by 4+ weeks.]

## Mitigation

[Synthesised by sara-update from source doc + discussion notes: controls, contingencies, or
mitigation approaches mentioned. If nothing was discussed: "No mitigation discussed — define
action items to address this risk."]

## Cross Links
[One wiki link per related[] entry — see wikilink rule in sara-update SKILL.md]
```

Key points to verify after editing:
- `schema_version: '2.0'` uses single quotes
- `mitigation: ""` frontmatter field is ABSENT
- `type:` field present with `technical` example and all six values in comment
- `raised-by:` field present (distinct from `owner:`)
- `status:` comment shows `open | mitigated | accepted` (no `closed`)
- Four body sections present: `## Source Quote`, `## Risk`, `## Mitigation`, `## Cross Links`
- `## Description` and `## Notes` from v1.0 are ABSENT
- IF/THEN format note is present under `## Risk`
- Synthesis notes are present under both `## Risk` and `## Mitigation`
  </action>
  <verify>
    <automated>grep -n "## Risk\|## Source Quote\|## Mitigation\|## Cross Links" /home/george/Projects/sara/.claude/skills/sara-init/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "## Source Quote" .claude/skills/sara-init/SKILL.md` returns a match in the risk template block
    - `grep -n "## Risk$" .claude/skills/sara-init/SKILL.md` returns a match in the risk template block (exact heading)
    - `grep -n "IF.*THEN\|THEN.*adverse" .claude/skills/sara-init/SKILL.md` returns a match (IF/THEN format instruction present)
    - `grep -n "## Cross Links" .claude/skills/sara-init/SKILL.md` returns a match in the risk template block
    - `grep -n "## Notes" .claude/skills/sara-init/SKILL.md` returns 0 matches in the risk template block (v1.0 section gone; other templates may have Notes — check context)
    - `grep -n "## Description" .claude/skills/sara-init/SKILL.md` returns 0 matches in the risk template block (v1.0 section gone)
    - `grep -n "mitigation:.*\"\"" .claude/skills/sara-init/SKILL.md` returns 0 matches in the risk template block (mitigation frontmatter field removed)
    - `grep -n "No mitigation discussed" .claude/skills/sara-init/SKILL.md` returns a match (fallback text present in Mitigation section)
  </acceptance_criteria>
  <done>Step 12 risk.md template write uses v2.0 frontmatter (type, raised-by, schema_version '2.0', no mitigation field, status open/mitigated/accepted) and four-section body (Source Quote, Risk IF/THEN, Mitigation, Cross Links). Old v1.0 Description/Mitigation/Notes body is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → skill file | The executor writes text to a markdown skill file; no code execution, no external I/O |
| Skill file → LLM runtime | The updated skill file is read by Claude Code as a prompt at runtime; no injection risk beyond existing skill file trust model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Tampering | sara-init/SKILL.md edit | accept | File is a markdown prompt, not executable code; changes are review-visible in git diff |
| T-11-02-02 | Tampering | schema_version float risk | mitigate | Use single-quoted '2.0' in all risk schema blocks — prevents YAML float parsing in Obsidian; verified by acceptance criteria grep |
| T-11-02-03 | Elevation of Privilege | skill file prompt injection | accept | Skill files are authored content loaded by the user's own Claude Code instance; no external input path |
</threat_model>

<verification>
After both tasks complete:

1. `grep -n "schema_version.*'2.0'" .claude/skills/sara-init/SKILL.md` — must return matches that include the risk section (single quotes)
2. `grep -n "schema_version.*\"1.0\"" .claude/skills/sara-init/SKILL.md` — must return 0 matches for risk context (stakeholder "1.0" is unrelated and fine)
3. `grep -n "## Risk$" .claude/skills/sara-init/SKILL.md` — must return 1 match (the template body section)
4. `grep -n "IF.*THEN" .claude/skills/sara-init/SKILL.md` — must return >= 1 match (IF/THEN format instruction)
5. `grep -n "open | mitigated | accepted" .claude/skills/sara-init/SKILL.md` — must return >= 1 match in risk context
6. `grep -n "mitigation:" .claude/skills/sara-init/SKILL.md` — must return 0 matches in risk frontmatter context
7. `grep -n "## Notes" .claude/skills/sara-init/SKILL.md` — must return 0 matches in risk template context
8. `grep -n "No mitigation discussed" .claude/skills/sara-init/SKILL.md` — must return 1 match
</verification>

<success_criteria>
- Step 9 CLAUDE.md risk schema block shows: type, raised-by, schema_version '2.0'; status open/mitigated/accepted; no mitigation frontmatter field; body description references four-section format
- Step 12 risk.md template shows: same v2.0 frontmatter; four body sections (Source Quote, Risk IF/THEN, Mitigation, Cross Links); IF/THEN synthesis note under Risk; fallback text under Mitigation; no Description/Notes sections
- single-quoted schema_version: '2.0' consistent with requirement, decision, action convention
- No other templates (requirement, decision, action, stakeholder) are modified
</success_criteria>

<output>
After completion, create `.planning/phases/11-refine-risks/11-02-SUMMARY.md` using the summary template.
</output>
