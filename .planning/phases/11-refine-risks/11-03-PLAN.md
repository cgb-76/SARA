---
phase: 11-refine-risks
plan: 03
type: execute
wave: 2
depends_on:
  - 11-01
  - 11-02
files_modified:
  - .claude/skills/sara-update/SKILL.md
autonomous: true
requirements:
  - WIKI-04
must_haves:
  truths:
    - "sara-update risk create branch writes v2.0 frontmatter: type from artifact.risk_type, owner from artifact.owner, likelihood from artifact.likelihood, impact from artifact.impact, status from artifact.status, schema_version '2.0'; mitigation frontmatter field absent"
    - "sara-update risk create branch writes four-section body: Source Quote, Risk (IF/THEN synthesised), Mitigation (synthesised), Cross Links"
    - "sara-update risk update branch applies same v2.0 frontmatter fields and rewrites body to the same four-section format"
    - "The RSK summary generation rule reads: 'RSK: likelihood, impact, type, status, mitigation approach'"
  artifacts:
    - path: ".claude/skills/sara-update/SKILL.md"
      provides: "v2.0 risk create branch + v2.0 risk update branch"
      contains: "artifact.risk_type"
  key_links:
    - from: ".claude/skills/sara-update/SKILL.md risk create branch"
      to: "wiki/risks/{assigned_id}.md"
      via: "Write tool call"
      pattern: "schema_version.*'2.0'"
    - from: ".claude/skills/sara-update/SKILL.md risk update branch"
      to: "wiki/risks/{artifact.existing_id}.md"
      via: "Write tool call (overwrite)"
      pattern: "artifact.risk_type"
---

<objective>
Rewrite the sara-update risk create and update branches to produce v2.0 wiki pages.

Purpose: The current sara-update risk write branches produce v1.0 pages (schema_version "1.0", mitigation frontmatter field, Description/Mitigation/Notes body). This plan replaces both the create and update branches with v2.0 implementations — mapping artifact.risk_type → type, artifact.owner → owner, artifact.likelihood/impact/status → frontmatter fields, schema_version '2.0' (single-quoted), and writing the four-section body (Source Quote, Risk IF/THEN, Mitigation, Cross Links).

Output: `.claude/skills/sara-update/SKILL.md` with two targeted changes:
1. Risk create branch: v2.0 frontmatter mapping + four-section body specification.
2. Risk update branch: v2.0 frontmatter migration + four-section body rewrite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-refine-risks/11-CONTEXT.md

<interfaces>
<!-- Current risk write branches in sara-update/SKILL.md being replaced -->

Current risk create branch body section in sara-update/SKILL.md (the **risk:** block inside "If artifact.action == 'create'"):
```
    **risk:**
    ```
    ## Description
    > "{artifact.source_quote}" — [[{artifact.raised_by}|{stakeholder_name}]]

    {synthesised summary of the risk, its likelihood/impact context, and any relevant
     triggers or conditions identified during /sara-discuss}

    ## Mitigation
    {synthesised mitigation approaches or controls mentioned in the source or discussion
     notes — leave empty if none were discussed}

    ## Notes
    {synthesised monitoring notes, triggers, owners, or related context from discussion
     notes — leave empty if none available}

    ## Cross Links
    {Generate one wiki link per entry in artifact.related...}
    ```
```

Current risk frontmatter mapping in sara-update (inside the create branch generic substitution block):
```
    - `schema_version` = `"1.0"` for risk artifacts (unchanged)
    - For risk artifacts: set `status` = `"open"`, `owner` = `artifact.raised_by` if it is a resolved STK ID...
```

Current risk update branch in sara-update/SKILL.md:
```
    For risk artifacts (`artifact.type == "risk"`): after applying the change_summary
    to frontmatter fields and regenerating the summary, also update the frontmatter to include
    the v2.0 fields from the artifact object:
    - Set `schema_version` = `"1.0"` (unchanged from risk schema)

    Then rewrite the full body to the v2.0 structured section format (Description, Mitigation,
    Notes, Cross Links)...
```

Action create branch body pattern (direct analog for the risk body structure):
```
    **action:**
    ```
    ## Source Quote
    > "{artifact.source_quote}" — [[{artifact.raised_by}|{stakeholder_name}]]

    ## Description
    {Synthesised from {source_doc} and {discussion_notes}: 2–4 sentences describing what needs
     to be done. Ground the description in the source quote.}

    ## Context
    ...

    ## Owner
    {Written from artifact.owner — NOT synthesised: ...}

    ## Due Date
    {Written from artifact.due_date — NOT synthesised: ...}

    ## Cross Links
    ...
    ```
```

Action update branch pattern for adding v2.0 frontmatter fields:
```
    For action artifacts (`artifact.type == "action"`): after applying the change_summary
    to frontmatter fields and regenerating the summary, also update the frontmatter to include
    the v2.0 fields from the artifact object:
    - Set `type` = `artifact.act_type` ...
    - Set `owner` = `artifact.owner` ...
    - Set `due-date` = `artifact.due_date` ...
    - Set `schema_version` = `'2.0'` (single-quoted string — prevents YAML float parsing)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite risk create branch in sara-update</name>
  <files>.claude/skills/sara-update/SKILL.md</files>
  <read_first>
    - .claude/skills/sara-update/SKILL.md (MANDATORY — read the full file before editing; locate both the generic frontmatter substitution block that mentions risk artifacts AND the **risk:** body section block)
    - .planning/phases/11-refine-risks/11-CONTEXT.md (D-05 owner mapping, D-07 schema_version, D-08 remove mitigation field, D-09 type field, D-10 owner field, D-11 likelihood/impact, D-12 status lifecycle, D-13 status from artifact, D-14 four-section body, D-15 synthesised sections)
  </read_first>
  <action>
Two targeted edits are needed in the create branch:

**Edit A — Fix frontmatter mapping for risk artifacts**

In the generic create branch substitution block (the bulleted list with `- schema_version = '2.0' for decision artifacts`, etc.), find the two lines that govern risk artifacts:
```
    - `schema_version` = `"1.0"` for risk artifacts (unchanged)
    ...
    - For risk artifacts: set `status` = `"open"`, `owner` = `artifact.raised_by` if it is a resolved STK ID (e.g. `"STK-001"`); otherwise set `owner` = `""` (empty — leave unassigned; do not write a placeholder ID)
```

Replace these two risk-specific lines with the following:
```
    - `schema_version` = `'2.0'` for risk artifacts (single-quoted — prevents YAML float parsing; consistent with requirement, decision, action convention)
    - For risk artifacts: set `type` = `artifact.risk_type` (one of: technical, financial, schedule, quality, compliance, people); set `owner` = `artifact.owner` (STK-NNN or raw name string or `""`); set `raised-by` = `artifact.raised_by` (note: template field is `raised-by`; artifact schema field is `raised_by`); set `likelihood` = `artifact.likelihood` (`"high"`, `"medium"`, `"low"`, or `""`); set `impact` = `artifact.impact` (`"high"`, `"medium"`, `"low"`, or `""`); set `status` = `artifact.status` (`"open"`, `"mitigated"`, or `"accepted"` — signal-based from extraction; default is `"open"`). Do NOT write a `mitigation:` frontmatter field — it is removed in v2.0.
```

**Edit B — Replace the risk body section**

Find the `**risk:**` body block (inside the create branch, after the action body block). It starts with `    **risk:**` and ends after the Cross Links block for risks (before `    Use the Write tool to create...`). Replace the entire `**risk:**` block with the following verbatim:

```
    **risk:**
    ```
    ## Source Quote
    > "{artifact.source_quote}" — [[{artifact.raised_by}|{stakeholder_name}]]

    ## Risk

    IF <trigger condition> THEN <adverse event>

    {Synthesised by sara-update from {source_doc} and {discussion_notes}: write the risk as a
     single IF/THEN statement. IF and THEN are written in caps; the rest is sentence case.
     The trigger condition (IF clause) describes what must happen or fail for the risk to
     materialise. The adverse event (THEN clause) describes the negative outcome.
     Example: IF the integration vendor delays API delivery THEN the go-live milestone slips by 4+ weeks.
     Ground the statement in the source_quote and surrounding context. Never fabricate conditions
     not supported by the source.}

    ## Mitigation

    {Synthesised by sara-update from {source_doc} and {discussion_notes}: describe controls,
     contingencies, or mitigation approaches explicitly mentioned. If nothing was discussed,
     write exactly: "No mitigation discussed — define action items to address this risk."
     Never fabricate mitigation that is not grounded in {source_doc} or {discussion_notes}.}

    ## Cross Links
    {Generate one wiki link per entry in artifact.related. Resolve display text per wikilink rule:
     - STK entities: [[STK-NNN|name]] — read wiki/stakeholders/{ID}.md for the name field
     - REQ/DEC/ACT/RSK entities: [[ID|ID Title]] — read wiki/index.md for the title
     - If title/name cannot be resolved: fall back to bare [[ID]]
     Write each link on its own line. If artifact.related is empty, write this heading with no
     content (heading-only — consistent with the established empty-section pattern for this skill).}
    ```
```

Key points to verify after editing:
- `schema_version` = `'2.0'` for risk (single quotes)
- `type` = `artifact.risk_type` (not `artifact.type`)
- `owner` = `artifact.owner` (not `artifact.raised_by`)
- `raised-by` = `artifact.raised_by` (the hyphen vs underscore mapping note is present)
- `likelihood` and `impact` written from artifact fields
- `status` written from `artifact.status` (open/mitigated/accepted)
- "Do NOT write a `mitigation:` frontmatter field" note is present
- Four body sections: Source Quote, Risk, Mitigation, Cross Links (no Description, no Notes)
- IF/THEN synthesis instruction is present with example
- Mitigation fallback text is specified: "No mitigation discussed — define action items to address this risk."
  </action>
  <verify>
    <automated>grep -n "artifact.risk_type" /home/george/Projects/sara/.claude/skills/sara-update/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "artifact.risk_type" .claude/skills/sara-update/SKILL.md` returns >= 1 match (type mapping present)
    - `grep -n "schema_version.*'2.0'.*risk\|risk.*schema_version.*'2.0'" .claude/skills/sara-update/SKILL.md` returns a match OR `grep -n "'2.0'.*risk artifacts" .claude/skills/sara-update/SKILL.md` returns a match (single-quoted for risk)
    - `grep -n "schema_version.*\"1.0\".*risk\|risk.*\"1.0\"" .claude/skills/sara-update/SKILL.md` returns 0 matches (old double-quoted 1.0 for risk is gone)
    - `grep -n "artifact.owner.*risk\|risk.*artifact.owner\|Set.*owner.*artifact.owner" .claude/skills/sara-update/SKILL.md` returns a match (owner from artifact.owner, not raised_by)
    - `grep -n "artifact.likelihood" .claude/skills/sara-update/SKILL.md` returns >= 1 match
    - `grep -n "artifact.impact" .claude/skills/sara-update/SKILL.md` returns >= 1 match
    - `grep -n "artifact.status.*open.*mitigated.*accepted\|open.*mitigated.*accepted" .claude/skills/sara-update/SKILL.md` returns a match in the risk context
    - `grep -n "Do NOT write.*mitigation.*frontmatter\|mitigation.*frontmatter.*removed" .claude/skills/sara-update/SKILL.md` returns a match
    - `grep -n "## Risk$" .claude/skills/sara-update/SKILL.md` returns a match in the body section block
    - `grep -n "IF.*caps.*THEN\|IF and THEN.*caps\|THEN.*caps" .claude/skills/sara-update/SKILL.md` returns a match (IF/THEN synthesis instruction)
    - `grep -n "No mitigation discussed" .claude/skills/sara-update/SKILL.md` returns a match (fallback text)
    - `grep -n "## Description" .claude/skills/sara-update/SKILL.md` returns 0 matches in the risk body block (v1.0 section gone)
    - `grep -n "## Notes" .claude/skills/sara-update/SKILL.md` returns 0 matches in the risk body block (v1.0 section gone)
  </acceptance_criteria>
  <done>Risk create branch maps artifact.risk_type → type, artifact.owner → owner, artifact.likelihood/impact/status → frontmatter fields, schema_version '2.0'; body has four sections (Source Quote, Risk IF/THEN, Mitigation, Cross Links); mitigation frontmatter field absent.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite risk update branch in sara-update</name>
  <files>.claude/skills/sara-update/SKILL.md</files>
  <read_first>
    - .claude/skills/sara-update/SKILL.md (read after Task 1 to see the current state of the risk update branch; locate "For risk artifacts (`artifact.type == \"risk\"`):" in the update section)
    - .planning/phases/11-refine-risks/11-CONTEXT.md (D-07 through D-15 — all frontmatter and body decisions; update branch must apply same v2.0 migration as create branch)
  </read_first>
  <action>
Find the risk update branch block. It starts with the line `    For risk artifacts (\`artifact.type == "risk"\`):` inside the `If artifact.action == "update":` section. It ends before the next artifact-type-specific block or the `Use the Write tool to overwrite` instruction for risk artifacts. Replace the entire risk update branch block with the following verbatim. Use the Edit tool.

The replacement text is:

```
    For risk artifacts (`artifact.type == "risk"`): after applying the change_summary
    to frontmatter fields and regenerating the summary, also update the frontmatter to include
    the v2.0 fields from the artifact object:
    - Set `type` = `artifact.risk_type` (one of: technical, financial, schedule, quality, compliance, people) — add if absent
    - Set `owner` = `artifact.owner` (STK-NNN or raw name string or `""`) — REPLACE any existing value; do NOT use `artifact.raised_by`
    - Set `raised-by` = `artifact.raised_by` — add if absent (note: template field is `raised-by`; artifact field is `raised_by`)
    - Set `likelihood` = `artifact.likelihood` (`"high"`, `"medium"`, `"low"`, or `""`) — add or replace
    - Set `impact` = `artifact.impact` (`"high"`, `"medium"`, `"low"`, or `""`) — add or replace
    - Set `status` = `artifact.status` (`"open"`, `"mitigated"`, or `"accepted"`) — replace any existing value
    - Set `schema_version` = `'2.0'` (single-quoted string — prevents YAML float parsing)
    - Remove the `mitigation` frontmatter field if present (it is a v1.0 field removed in schema v2.0)

    Then rewrite the full body to the v2.0 structured section format (Source Quote, Risk, Mitigation,
    Cross Links) using the same synthesis rules as the create branch:

    ## Source Quote
    > "{artifact.source_quote}" — [[{artifact.raised_by}|{stakeholder_name}]]

    ## Risk

    IF <trigger condition> THEN <adverse event>

    {Synthesised from {source_doc}, {discussion_notes}, and artifact.change_summary: updated
     IF/THEN risk statement with IF and THEN in caps. Revise the trigger condition and adverse
     event based on any new information from change_summary. Never fabricate conditions not
     grounded in {source_doc} or {discussion_notes}.}

    ## Mitigation

    {Synthesised from {source_doc}, {discussion_notes}, and artifact.change_summary: updated
     controls, contingencies, or mitigation approaches. If nothing relevant: "No mitigation
     discussed — define action items to address this risk." Never fabricate.}

    ## Cross Links
    {Generate one wiki link per entry in artifact.related (after merging with the existing
     related[] array). Use the wikilink rule: STK → [[STK-NNN|name]], REQ/DEC/ACT/RSK →
     [[ID|ID Title]], fallback to [[ID]] if title/name cannot be resolved.
     Write each link on its own line. Write heading only if artifact.related is empty after merge.}
```

Also update the RSK summary generation guidance in the update branch regeneration rule. Find the line in the regeneration block that reads:
```
     RSK: likelihood/impact/mitigation/status;
```
Replace with:
```
     RSK: likelihood, impact, type, status, mitigation approach;
```

Key points to verify after editing:
- All eight v2.0 frontmatter field operations are listed (type, owner, raised-by, likelihood, impact, status, schema_version, remove mitigation)
- `schema_version` = `'2.0'` (single-quoted)
- `owner` = `artifact.owner` with REPLACE note (not raised_by)
- `raised-by` = `artifact.raised_by` with "add if absent" note and the hyphen/underscore mapping note
- `Remove the mitigation frontmatter field if present` instruction is present
- Body rewrite produces four sections: Source Quote, Risk (IF/THEN), Mitigation, Cross Links
- IF/THEN synthesis note references artifact.change_summary for updates
- Mitigation fallback text is identical to the create branch
  </action>
  <verify>
    <automated>grep -n "Remove.*mitigation.*frontmatter\|mitigation.*v1.0.*removed" /home/george/Projects/sara/.claude/skills/sara-update/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Remove.*mitigation.*frontmatter\|mitigation.*v1.0 field\|v1.0.*mitigation" .claude/skills/sara-update/SKILL.md` returns a match in the risk update branch (migration instruction present)
    - `grep -c "artifact.risk_type" .claude/skills/sara-update/SKILL.md` returns >= 2 (appears in both create and update branches)
    - `grep -c "No mitigation discussed" .claude/skills/sara-update/SKILL.md` returns >= 2 (fallback text in both create and update branches)
    - `grep -n "raised-by.*artifact.raised_by\|artifact.raised_by.*raised-by" .claude/skills/sara-update/SKILL.md` returns >= 2 matches (both branches have the raised-by mapping note)
    - `grep -n "likelihood.*impact.*type.*status.*mitigation approach\|RSK:.*likelihood.*impact.*type" .claude/skills/sara-update/SKILL.md` returns a match (RSK summary rule updated)
    - `grep -n "schema_version.*'2.0'.*risk\|risk.*'2.0'\|single-quoted.*risk\|risk.*single-quoted" .claude/skills/sara-update/SKILL.md` returns >= 2 matches (both branches single-quoted)
    - `grep -n "schema_version.*\"1.0\"" .claude/skills/sara-update/SKILL.md` returns 0 matches for risk context
    - `grep -n "risk.*artifact.owner\|artifact.owner.*risk\|owner.*REPLACE.*raised_by" .claude/skills/sara-update/SKILL.md` returns a match in the update branch (REPLACE instruction present)
  </acceptance_criteria>
  <done>Risk update branch applies all v2.0 frontmatter field migrations (including removing mitigation field), sets schema_version '2.0' (single-quoted), rewrites body to four-section format with IF/THEN Risk section and synthesised Mitigation. RSK summary generation rule updated to reflect v2.0 field set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → skill file | The executor writes text to a markdown skill file; no code execution, no external I/O |
| Skill file → LLM runtime | The updated skill file is read by Claude Code as a prompt at runtime; no injection risk beyond existing skill file trust model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Tampering | sara-update/SKILL.md edit | accept | File is a markdown prompt, not executable code; changes are review-visible in git diff |
| T-11-03-02 | Tampering | schema_version float risk | mitigate | Use single-quoted '2.0' in all risk write paths — verified by acceptance criteria grep for absence of "1.0" double-quoted pattern |
| T-11-03-03 | Information Disclosure | owner/raised-by field mapping | accept | These fields map from already-extracted artifact data in pipeline-state.json; no new PII surface beyond what exists in the pipeline |
| T-11-03-04 | Elevation of Privilege | skill file prompt injection | accept | Skill files are authored content loaded by the user's own Claude Code instance; no external input path |
</threat_model>

<verification>
After both tasks complete:

1. `grep -c "artifact.risk_type" .claude/skills/sara-update/SKILL.md` — must return >= 2 (create and update branches)
2. `grep -c "No mitigation discussed" .claude/skills/sara-update/SKILL.md` — must return >= 2 (create and update branches)
3. `grep -n "schema_version.*\"1.0\"" .claude/skills/sara-update/SKILL.md` — must return 0 matches for risk context (single-quoted '2.0' everywhere for risk)
4. `grep -n "Remove.*mitigation.*frontmatter" .claude/skills/sara-update/SKILL.md` — must return 1 match (update branch migration instruction)
5. `grep -n "## Risk$" .claude/skills/sara-update/SKILL.md` — must return >= 1 match (body section heading present)
6. `grep -n "IF and THEN.*caps\|IF.*THEN.*caps" .claude/skills/sara-update/SKILL.md` — must return >= 2 matches (IF/THEN instruction in both branches)
7. `grep -n "RSK:.*likelihood.*impact.*type" .claude/skills/sara-update/SKILL.md` — must return 1 match (updated summary rule)
8. `grep -n "## Description" .claude/skills/sara-update/SKILL.md` — must return 0 matches in the risk body block (v1.0 section gone; other entity body blocks may still have Description — verify context)
9. `grep -n "## Notes" .claude/skills/sara-update/SKILL.md` — must return 0 matches in the risk body block
</verification>

<success_criteria>
- Risk create branch: type from artifact.risk_type, owner from artifact.owner, likelihood/impact/status from artifact fields, schema_version '2.0' (single-quoted), mitigation frontmatter field absent, four-section body with IF/THEN Risk and synthesised Mitigation
- Risk update branch: all eight v2.0 frontmatter migrations applied, mitigation field removal instruction present, body rewrites to four-section format, IF/THEN and Mitigation synthesis reference artifact.change_summary
- RSK summary generation rule updated to read "likelihood, impact, type, status, mitigation approach"
- No other artifact type branches (requirement, decision, action) are modified
</success_criteria>

<output>
After completion, create `.planning/phases/11-refine-risks/11-03-SUMMARY.md` using the summary template.
</output>
