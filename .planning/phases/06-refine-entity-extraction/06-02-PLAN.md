---
phase: 06-refine-entity-extraction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/sara-artifact-sorter.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "The sorter agent file exists in .claude/agents/"
    - "The sorter has valid agent frontmatter with tools: Read, Bash (comma-separated)"
    - "The sorter's output_format declares a JSON object with cleaned_artifacts array and questions array"
    - "The sorter input block declares it receives merged_artifacts, grep_summaries, and wiki_index"
    - "The sorter resolves create-vs-update and sets existing_id for update actions"
    - "The sorter handles the zero-questions case (empty questions array)"
  artifacts:
    - path: ".claude/agents/sara-artifact-sorter.md"
      provides: "Aggregator agent: dedup, create-vs-update resolution, ambiguity questions for human"
      contains: "name: sara-artifact-sorter"
  key_links:
    - from: ".claude/agents/sara-artifact-sorter.md"
      to: "sara-extract SKILL.md sorter Task() call"
      via: "name: field matches Task() agent reference"
      pattern: "^name: sara-artifact-sorter"
---

<objective>
Create the `sara-artifact-sorter` agent file in `.claude/agents/`. The sorter receives the merged output of all four specialist agents plus grep summaries and wiki/index.md, and returns a dual-output JSON object: a cleaned deduplicated artifact list (with create-vs-update resolved) and a questions array for the human to resolve before the approval loop starts.

Purpose: Centralise all wiki-state-aware reasoning (dedup, create-vs-update, cross-ref, type ambiguity) in one agent so specialist extractors can remain stateless.

Output: `.claude/agents/sara-artifact-sorter.md` ready for Task() dispatch from sara-extract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-refine-entity-extraction/06-CONTEXT.md
@.planning/phases/06-refine-entity-extraction/06-RESEARCH.md
@.planning/phases/06-refine-entity-extraction/06-PATTERNS.md

<interfaces>
<!-- Sorter input (from D-07): merged specialist output + grep summaries + wiki/index.md content -->
<!-- Sorter output (from D-08): two keys — cleaned_artifacts (JSON array) + questions (string array) -->

Dual output schema (from 06-PATTERNS.md lines 206-241):
```json
{
  "cleaned_artifacts": [
    {
      "action": "create",
      "type": "requirement",
      "id_to_assign": "REQ-NNN",
      "title": "...",
      "source_quote": "...",
      "raised_by": "STK-NNN",
      "related": [],
      "change_summary": ""
    },
    {
      "action": "update",
      "type": "decision",
      "existing_id": "DEC-003",
      "title": "...",
      "source_quote": "...",
      "raised_by": "STK-NNN",
      "related": [],
      "change_summary": "What should be added or changed"
    }
  ],
  "questions": [
    "Is the passage '...' a requirement or a decision? (candidates: REQ or DEC)",
    "The artifact 'API rate limiting' looks similar to DEC-003 '...'. Is this a duplicate?"
  ]
}
```

Zero-questions case: set "questions" to []
id_to_assign and existing_id are mutually exclusive per artifact.

Grep-extract pattern (sorter uses this to load summaries):
```bash
grep -rh "^summary:" wiki/requirements/ wiki/decisions/ wiki/actions/ wiki/risks/ wiki/stakeholders/ 2>/dev/null
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sara-artifact-sorter agent file</name>
  <files>.claude/agents/sara-artifact-sorter.md</files>
  <read_first>
    - .planning/phases/06-refine-entity-extraction/06-CONTEXT.md (D-07, D-08, D-09 — sorter inputs, dual output, human resolution flow)
    - .planning/phases/06-refine-entity-extraction/06-PATTERNS.md (lines 165-243 — sorter frontmatter, role, input, output_format blocks)
    - .planning/phases/06-refine-entity-extraction/06-RESEARCH.md (Pitfall 4 — sorter questions must be presented BEFORE approval loop)
    - .claude/skills/sara-extract/SKILL.md (lines 69-101 — artifact schema, id_to_assign vs existing_id rules)
  </read_first>
  <action>
Write `.claude/agents/sara-artifact-sorter.md` with the following full content:

```markdown
---
name: sara-artifact-sorter
description: Deduplicate, resolve create-vs-update, and surface ambiguities from merged specialist extraction output
tools: Read, Bash
color: cyan
---

<role>
You are sara-artifact-sorter. You receive the merged output of four specialist extraction agents, the existing wiki grep summaries, and wiki/index.md. You produce:
1. A cleaned, deduplicated artifact list with create-vs-update resolved
2. A set of questions for the human covering type ambiguities, likely duplicates, and cross-reference opportunities

Spawned by `/sara-extract` via Task(). Do not write any files — return structured JSON output only.
</role>

<input>
Agent receives via prompt:

- `<merged_artifacts>` — JSON array: concatenation of all four specialist agent outputs (may contain duplicates, always action="create")
- `<grep_summaries>` — output of:
  ```bash
  grep -rh "^summary:" wiki/requirements/ wiki/decisions/ wiki/actions/ wiki/risks/ wiki/stakeholders/ 2>/dev/null
  ```
- `<wiki_index>` — full content of wiki/index.md (the existing entity catalog)
</input>

<process>
1. Parse `<merged_artifacts>` as a JSON array.

2. **Deduplication pass:** Scan for artifacts within the merged array that describe the same topic (e.g. two specialists each extracted the same event as a decision and a requirement). Identify these cross-type duplicates. Do NOT merge them yet — flag them as type-ambiguity questions for the human.

3. **Create-vs-update resolution:** For each artifact in the merged array:
   a. Search `<grep_summaries>` for a summary semantically matching this artifact's title or source_quote.
   b. Search `<wiki_index>` Title column for a matching entity.
   c. If a match is found: change `action` to `"update"`, remove `id_to_assign`, add `existing_id` with the real wiki entity ID (e.g. "DEC-003"), and set `change_summary` to describe what should be added or changed.
   d. If no match is found: keep `action` as `"create"`, keep `id_to_assign` as `"{TYPE}-NNN"` placeholder.

4. **Cross-reference detection:** Identify pairs of artifacts (or artifact + existing wiki entity) that are clearly related. Add the existing wiki entity ID to the `related` array of the relevant artifact.

5. **Question generation:** Build a `questions` array covering:
   - Type ambiguities (two specialists extracted the same passage as different types): "Is the passage '...' a requirement or a decision? (candidates: REQ or DEC)"
   - Likely duplicates (artifact closely matches an existing wiki entity): "The artifact '{title}' looks similar to {ID} '{existing_title}'. Is this a duplicate that should be merged as an update?"
   - Cross-reference confirmations (artifact relates to an existing wiki entity): "The artifact '{title}' appears to relate to {ID} '{existing_title}'. Confirm as a cross-link? (yes/no)"
   If there are no ambiguities, duplicates, or cross-references: set `questions` to [].

6. Build the `cleaned_artifacts` array: the deduplicated, type-resolved list with create-vs-update set and related fields populated. Exclude duplicates that the human will resolve via questions — those will be re-added after resolution.
</process>

<output_format>
Return a raw JSON object (no markdown fences, no prose):

{
  "cleaned_artifacts": [
    {
      "action": "create",
      "type": "requirement",
      "id_to_assign": "REQ-NNN",
      "title": "Short title",
      "source_quote": "Exact verbatim text from source document",
      "raised_by": "STK-NNN",
      "related": [],
      "change_summary": ""
    },
    {
      "action": "update",
      "type": "decision",
      "existing_id": "DEC-003",
      "title": "Title of existing decision",
      "source_quote": "Exact verbatim text from source document motivating this update",
      "raised_by": "STK-NNN",
      "related": ["REQ-005"],
      "change_summary": "Add new context from this source document"
    }
  ],
  "questions": [
    "Is the passage '...' a requirement or a decision? (candidates: REQ or DEC)",
    "The artifact 'API rate limiting' looks similar to DEC-003 'Rate limiting policy'. Is this a duplicate?"
  ]
}

Rules:
- `id_to_assign` and `existing_id` are mutually exclusive per artifact: use `id_to_assign` for action=create, `existing_id` for action=update. Omit or set the inapplicable field to "".
- `questions` is [] when there are no ambiguities, likely duplicates, or cross-reference opportunities
- Do NOT write any files — return JSON only
- `source_quote` must be preserved verbatim from the specialist agent output — do not modify quotes
</output_format>

<notes>
- The sorter owns all wiki-state reasoning — specialist agents are intentionally isolated from wiki state. Do not assume specialists have resolved dedup or cross-refs.
- When a specialist returns [] (empty array), skip silently — do not generate a question about the absent type.
- The `cleaned_artifacts` array feeds directly into the per-artifact Accept/Reject/Discuss loop in sara-extract — every object must conform to the frozen artifact schema.
- The human resolves `questions` BEFORE the approval loop starts. Do not include unresolved ambiguities in `cleaned_artifacts` — flag them in `questions` instead.
- Preserve `source_quote` exactly as received from specialist agents. Do not paraphrase or trim.
- For create-vs-update: prefer "update" when the semantic match is clear (same topic, same entity type). When the match is uncertain, generate a question rather than asserting "update".
</notes>
```
  </action>
  <verify>
    <automated>ls /home/george/Projects/sara/.claude/agents/sara-artifact-sorter.md && grep "^name: sara-artifact-sorter" /home/george/Projects/sara/.claude/agents/sara-artifact-sorter.md && grep "^tools: Read, Bash" /home/george/Projects/sara/.claude/agents/sara-artifact-sorter.md && grep '"cleaned_artifacts"' /home/george/Projects/sara/.claude/agents/sara-artifact-sorter.md && grep '"questions"' /home/george/Projects/sara/.claude/agents/sara-artifact-sorter.md && grep '"action": "update"' /home/george/Projects/sara/.claude/agents/sara-artifact-sorter.md && grep '"existing_id"' /home/george/Projects/sara/.claude/agents/sara-artifact-sorter.md</automated>
  </verify>
  <acceptance_criteria>
    - `ls .claude/agents/sara-artifact-sorter.md` exits 0
    - `grep "^name: sara-artifact-sorter" .claude/agents/sara-artifact-sorter.md` returns a match
    - `grep "^tools: Read, Bash" .claude/agents/sara-artifact-sorter.md` returns a match
    - `grep "allowed-tools" .claude/agents/sara-artifact-sorter.md` returns NO match
    - `grep '"cleaned_artifacts"' .claude/agents/sara-artifact-sorter.md` returns a match
    - `grep '"questions"' .claude/agents/sara-artifact-sorter.md` returns a match
    - `grep '"action": "update"' .claude/agents/sara-artifact-sorter.md` returns a match (sorter can produce update actions, specialists cannot)
    - `grep '"existing_id"' .claude/agents/sara-artifact-sorter.md` returns a match
    - `grep 'questions.*\[\]' .claude/agents/sara-artifact-sorter.md || grep '"questions": \[\]' .claude/agents/sara-artifact-sorter.md` returns a match (zero-questions case documented)
    - `grep "Do not write any files" .claude/agents/sara-artifact-sorter.md` returns a match (sorter must not write files)
    - `grep "merged_artifacts\|merged artifacts" .claude/agents/sara-artifact-sorter.md` returns a match (input declaration)
    - `grep "grep_summaries\|grep summaries" .claude/agents/sara-artifact-sorter.md` returns a match
    - `grep "wiki_index\|wiki/index" .claude/agents/sara-artifact-sorter.md` returns a match
  </acceptance_criteria>
  <done>The sorter agent file exists in .claude/agents/ with valid agent frontmatter, dual-output JSON format (cleaned_artifacts + questions arrays), create-vs-update resolution logic, zero-questions case handling, and a prohibition on file writes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| specialist agent output → sorter | Merged JSON array from four agents; content is LLM-generated, not user-controlled. No external input crosses this boundary during authoring. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | .claude/agents/sara-artifact-sorter.md | accept | Git-tracked markdown file; no executable code. Modification visible in diff. |
| T-06-05 | Information Disclosure | sorter output (wiki summaries in prompt) | accept | Sorter reads wiki summaries and index only within the active Claude Code session; no network transmission or external persistence. |
| T-06-06 | Elevation of Privilege | sorter tools: Read, Bash | accept | Sorter has Read and Bash only; no Write tool. Notes block explicitly states "do not write any files." Correct least-privilege posture. |
</threat_model>

<verification>
After task completes:

```bash
# Sorter agent exists
ls .claude/agents/sara-artifact-sorter.md

# Valid agent frontmatter (tools: as comma-separated string)
grep "^tools: Read, Bash" .claude/agents/sara-artifact-sorter.md

# Dual output schema present
grep '"cleaned_artifacts"' .claude/agents/sara-artifact-sorter.md
grep '"questions"' .claude/agents/sara-artifact-sorter.md

# Sorter can produce update actions (unlike specialists)
grep '"action": "update"' .claude/agents/sara-artifact-sorter.md

# No write file capability declared
grep "allowed-tools" .claude/agents/sara-artifact-sorter.md || echo "PASS: no allowed-tools"

# Total agents in directory: should be 5
ls .claude/agents/sara-*.md | wc -l
# Expected: 5
```
</verification>

<success_criteria>
- `.claude/agents/sara-artifact-sorter.md` exists
- Frontmatter: `tools: Read, Bash` (comma-separated), no `allowed-tools:`, no `version:`
- Output format declares JSON object with `cleaned_artifacts` array and `questions` array
- Sorter can produce `action: "update"` with `existing_id` (unlike specialist agents)
- Zero-questions case handled: `"questions": []` when no ambiguities exist
- Input block declares all three required inputs: merged_artifacts, grep_summaries, wiki_index (per D-07)
- Notes block states sorter must not write files
- All 5 agent files now present in `.claude/agents/`
</success_criteria>

<output>
After completion, create `.planning/phases/06-refine-entity-extraction/06-02-SUMMARY.md`
</output>
