---
phase: 06-refine-entity-extraction
plan: 03
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - .claude/skills/sara-extract/SKILL.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "sara-extract Steps 2-3 dispatch to four specialist agents via Task() and one sorter agent via Task()"
    - "The Task() prompt for each specialist explicitly passes source document content and discussion_notes string (per D-03)"
    - "The sorter Task() prompt receives merged array + grep summaries + wiki/index.md content (per D-07)"
    - "Sorter questions are presented to the human and resolved BEFORE the approval loop starts (per D-09)"
    - "Step 4 (per-artifact approval loop) is preserved verbatim"
    - "Step 5 (write extraction_plan, advance stage) is preserved verbatim"
    - "Step 1 (stage guard) is preserved verbatim"
  artifacts:
    - path: ".claude/skills/sara-extract/SKILL.md"
      provides: "Orchestrator skill with multi-agent dispatch in Steps 2-3"
      contains: "sara-requirement-extractor"
  key_links:
    - from: ".claude/skills/sara-extract/SKILL.md Step 3"
      to: ".claude/agents/sara-requirement-extractor.md"
      via: "Task() call referencing agent name"
      pattern: "sara-requirement-extractor"
    - from: ".claude/skills/sara-extract/SKILL.md Step 3"
      to: ".claude/agents/sara-artifact-sorter.md"
      via: "Task() call referencing agent name"
      pattern: "sara-artifact-sorter"
---

<objective>
Rewrite `sara-extract` SKILL.md Steps 2 and 3 to replace the monolithic LLM extraction step with a multi-agent dispatch pipeline: four parallel specialist Task() calls followed by one sorter Task() call, sorter question resolution, then the existing approval loop.

Purpose: Steps 4 and 5 are preserved verbatim. Only Steps 2-3 change — the source-loading and artifact-generation logic is replaced with agent dispatch.

Output: Updated `.claude/skills/sara-extract/SKILL.md` with new Steps 2-3 and objective block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-refine-entity-extraction/06-CONTEXT.md
@.planning/phases/06-refine-entity-extraction/06-RESEARCH.md
@.planning/phases/06-refine-entity-extraction/06-PATTERNS.md

<interfaces>
<!-- Current sara-extract SKILL.md structure (from file read above): -->
<!-- Lines 1-11: frontmatter — PRESERVE VERBATIM -->
<!-- Lines 13-15: <objective> block — UPDATE wording -->
<!-- Lines 17-18: <process> open tag — PRESERVE -->
<!-- Lines 19-38: Step 1 stage guard — PRESERVE VERBATIM -->
<!-- Lines 40-56: Step 2 (current: load source + index + grep) — REPLACE -->
<!-- Lines 48-101: Step 3 (current: monolithic dedup + artifact generation) — REPLACE -->
<!-- Lines 103-145: Step 4 approval loop — PRESERVE VERBATIM -->
<!-- Lines 149-176: Step 5 write plan — PRESERVE VERBATIM -->
<!-- Lines 180-191: <notes> block — PRESERVE + ADD agent dispatch note -->

Replacement Steps 2-3 content (from 06-RESEARCH.md Code Examples):
- Step 2: Read source_path, extract discussion_notes from already-loaded pipeline-state.json, Read wiki/index.md
- Step 3: Spawn 4 parallel Task() calls (specialists), merge arrays, grep summaries, spawn sorter Task(), present questions, collect resolutions, feed cleaned_artifacts to Step 4
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace sara-extract Steps 2-3 with multi-agent dispatch</name>
  <files>.claude/skills/sara-extract/SKILL.md</files>
  <read_first>
    - .claude/skills/sara-extract/SKILL.md (read the FULL file — Steps 4-5 must be copied verbatim; Step 1 preserved; Steps 2-3 are the target for replacement)
    - .planning/phases/06-refine-entity-extraction/06-PATTERNS.md (lines 247-345 — exact replacement Steps 2-3 text and preserved sections map)
    - .planning/phases/06-refine-entity-extraction/06-CONTEXT.md (D-02, D-03, D-04, D-07, D-08, D-09, D-10 — orchestration constraints)
    - .planning/phases/06-refine-entity-extraction/06-RESEARCH.md (Pitfall 1 — discussion_notes must be passed explicitly; Pitfall 4 — sorter questions before loop)
  </read_first>
  <action>
Rewrite `.claude/skills/sara-extract/SKILL.md` preserving all existing content except:
1. The `<objective>` block is updated to reflect agent dispatch
2. Steps 2 and 3 are replaced
3. A note about agent dispatch is added to the `<notes>` block

**Sections to PRESERVE VERBATIM (do not modify one character):**
- Entire frontmatter (lines 1-11): `name:`, `description:`, `argument-hint:`, `allowed-tools:`, `version:`
- `<process>` opening tag
- Step 1 — Stage guard and item lookup (lines 19-38)
- Step 4 — Per-artifact approval loop (lines 103-145) — including the AskUserQuestion header limit comment and the "Discuss" freeform wait pattern
- Step 5 — Write extraction plan and advance stage (lines 149-176)
- All existing notes EXCEPT the first bullet about source_quote (which remains unchanged) — add new notes at the end
- `</process>` and `</notes>` closing tags

**New `<objective>` block** (replace lines 13-15):
```
<objective>
This skill reads the source document and discussion notes for pipeline item N, dispatches four specialist extraction agents (one per entity type) via Task() in parallel, passes their merged output to a sorter agent that deduplicates and resolves create-vs-update decisions, presents the sorter's ambiguity questions to the user, and then runs the per-artifact Accept/Reject/Discuss loop on the cleaned artifact list. Approved artifacts are written to `extraction_plan` in `pipeline-state.json` and the item stage advances to `approved`; no wiki files are created or modified by this skill.
</objective>
```

**New Step 2** (replace current Step 2 which starts at "**Step 2 — Load source, discussion notes, and dedup context**"):
```markdown
**Step 2 — Load source, discussion notes, and wiki index**

Read `{item.source_path}` using the Read tool. This is the source document.

`{discussion_notes}` = `items["{N}"].discussion_notes` (already in memory from the Step 1 read of pipeline-state.json).

Read `wiki/index.md` HERE using the Read tool. Reading the index at this step (not at skill entry) ensures it is fresh — it may have been updated by `/sara-add-stakeholder` during the preceding `/sara-discuss` session. (See notes — Pitfall 4 guard.)
```

**New Step 3** (replace current Step 3 "**Step 3 — Generate artifact list with dedup check**" through the end of Step 3 content before "**Step 4**"):
```markdown
**Step 3 — Dispatch specialist agents and sorter**

Spawn four specialist agents via Task() in parallel, passing the source document content and discussion_notes string explicitly in each prompt. Agents start cold and have no implicit access to pipeline-state.json or the wiki.

Task prompt template for each specialist:
```
You are {agent-name}. Extract {type} artifacts only.

<source_document>
{full content of source file read in Step 2}
</source_document>

<discussion_notes>
{discussion_notes string from pipeline-state.json, or empty string if not set}
</discussion_notes>

Return a JSON array of {type} artifacts. Each artifact must include a source_quote.
```

Spawn all four in parallel (or sequentially if context window is a concern):
- Task(`sara-requirement-extractor`, prompt=source+discussion_notes) → `{req_artifacts}` (JSON array)
- Task(`sara-decision-extractor`, prompt=source+discussion_notes) → `{dec_artifacts}` (JSON array)
- Task(`sara-action-extractor`, prompt=source+discussion_notes) → `{act_artifacts}` (JSON array)
- Task(`sara-risk-extractor`, prompt=source+discussion_notes) → `{risk_artifacts}` (JSON array)

Merge all four arrays:
`{merged}` = req_artifacts + dec_artifacts + act_artifacts + risk_artifacts

Load grep summaries using the Bash tool:
```bash
grep -rh "^summary:" wiki/requirements/ wiki/decisions/ wiki/actions/ wiki/risks/ wiki/stakeholders/ 2>/dev/null
```

Spawn sorter:
- Task(`sara-artifact-sorter`, prompt=merged+grep_summaries+wiki_index) → `{sorter_output}` (JSON object)

Parse `{sorter_output}`:
- `{cleaned_artifacts}` = sorter_output.cleaned_artifacts
- `{sorter_questions}` = sorter_output.questions

If `{sorter_questions}` is non-empty:
  Present all questions to the user as plain text before the approval loop:
  ```
  Before reviewing individual artifacts, please resolve the following:

  {sorter_questions[0]}
  {sorter_questions[1]}
  ...
  ```
  Wait for the user's reply using a plain-text wait. Apply the user's resolutions to `{cleaned_artifacts}` (e.g. confirm/reject a type assignment, merge a duplicate into an update, confirm a cross-link).

Proceed to Step 4 with `{cleaned_artifacts}` as the artifact list. The sorter questions are now fully resolved.
```

**New notes to append** (add at end of existing `<notes>` block, before the closing `</notes>` tag):
```
- Agent dispatch: sara-extract spawns four specialist agents (sara-requirement-extractor, sara-decision-extractor, sara-action-extractor, sara-risk-extractor) via Task() in parallel. Each receives only the raw source content and discussion_notes — no wiki state (D-03, D-04). The sorter agent (sara-artifact-sorter) receives the merged output plus grep summaries and wiki/index.md (D-07). Specialist agents always return action="create"; the sorter resolves create-vs-update (D-06).
- Sorter questions are presented to the human BEFORE the approval loop starts (Step 4). Never present sorter questions inside the artifact loop (Pitfall 4 guard).
- If a specialist agent returns an empty array [], merge it as zero elements — skip silently, do not generate a question about the absent type.
- The `discussion_notes` string MUST be passed explicitly in each specialist Task() prompt. Agents start cold and have no implicit access to pipeline-state.json. An empty discussion_notes string is valid — pass it as an empty string, not omitted. (Pitfall 1 guard.)
```
  </action>
  <verify>
    <automated>grep "sara-requirement-extractor" /home/george/Projects/sara/.claude/skills/sara-extract/SKILL.md && grep "sara-decision-extractor" /home/george/Projects/sara/.claude/skills/sara-extract/SKILL.md && grep "sara-action-extractor" /home/george/Projects/sara/.claude/skills/sara-extract/SKILL.md && grep "sara-risk-extractor" /home/george/Projects/sara/.claude/skills/sara-extract/SKILL.md && grep "sara-artifact-sorter" /home/george/Projects/sara/.claude/skills/sara-extract/SKILL.md && grep "sorter_questions" /home/george/Projects/sara/.claude/skills/sara-extract/SKILL.md && grep "cleaned_artifacts" /home/george/Projects/sara/.claude/skills/sara-extract/SKILL.md && grep "Step 4 — Per-artifact approval loop" /home/george/Projects/sara/.claude/skills/sara-extract/SKILL.md && grep "Step 5 — Write extraction plan" /home/george/Projects/sara/.claude/skills/sara-extract/SKILL.md && grep "^version: 1.0.0" /home/george/Projects/sara/.claude/skills/sara-extract/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "sara-requirement-extractor" .claude/skills/sara-extract/SKILL.md` returns a match
    - `grep "sara-decision-extractor" .claude/skills/sara-extract/SKILL.md` returns a match
    - `grep "sara-action-extractor" .claude/skills/sara-extract/SKILL.md` returns a match
    - `grep "sara-risk-extractor" .claude/skills/sara-extract/SKILL.md` returns a match
    - `grep "sara-artifact-sorter" .claude/skills/sara-extract/SKILL.md` returns a match
    - `grep "sorter_questions" .claude/skills/sara-extract/SKILL.md` returns a match
    - `grep "cleaned_artifacts" .claude/skills/sara-extract/SKILL.md` returns a match
    - `grep "discussion_notes" .claude/skills/sara-extract/SKILL.md` returns matches (discussion_notes passed in Task() prompt)
    - `grep "Step 4 — Per-artifact approval loop" .claude/skills/sara-extract/SKILL.md` returns a match (Step 4 preserved)
    - `grep "Step 5 — Write extraction plan" .claude/skills/sara-extract/SKILL.md` returns a match (Step 5 preserved)
    - `grep "Step 1 — Stage guard" .claude/skills/sara-extract/SKILL.md` returns a match (Step 1 preserved)
    - `grep "^version: 1.0.0" .claude/skills/sara-extract/SKILL.md` returns a match (frontmatter preserved)
    - `grep "For each extractable topic in the source" .claude/skills/sara-extract/SKILL.md` returns NO match (old monolithic Step 3 removed)
    - `grep "Sorter questions are presented to the human BEFORE" .claude/skills/sara-extract/SKILL.md` returns a match (Pitfall 4 guard in notes)
    - `grep "Pitfall 1 guard\|discussion_notes.*must be passed explicitly" .claude/skills/sara-extract/SKILL.md` returns a match (Pitfall 1 guard in notes)
  </acceptance_criteria>
  <done>sara-extract SKILL.md Steps 2-3 replaced with multi-agent dispatch. All four specialist agent names referenced. Sorter referenced. Sorter questions resolved before Step 4. Step 1/4/5 preserved verbatim. Frontmatter unchanged. New agent dispatch notes added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sara-extract → specialist agents (Task()) | Orchestrator passes source document content + discussion_notes string as Task() prompt. Content is from local filesystem files (wiki, pipeline-state.json). |
| specialist agents → sorter (via orchestrator) | JSON arrays from cold agents returned to the orchestrator session, then passed to sorter Task(). No user-controlled data path — all content originates from committed source files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Tampering | sara-extract SKILL.md | accept | Git-tracked; modifications visible in diff. No executable code — markdown instructions only. |
| T-06-08 | Information Disclosure | source document content in Task() prompt | accept | Source documents are project files in the local repo. Task() dispatch is in-process Claude Code session; no network or external persistence. |
| T-06-09 | Denial of Service | parallel Task() calls with large source documents | accept | Claude Code manages context window; sequential fallback is documented as an option in Step 3. Planner has no authority to prevent this — it is Claude's discretion per CONTEXT.md. |
</threat_model>

<verification>
After task completes:

```bash
# Agent names all present in updated skill
grep -c "sara-.*-extractor\|sara-artifact-sorter" .claude/skills/sara-extract/SKILL.md
# Expected: at least 5 lines

# Old monolithic generation step removed
grep "For each extractable topic in the source" .claude/skills/sara-extract/SKILL.md && echo "FAIL: old Step 3 still present" || echo "PASS: old Step 3 removed"

# Approval loop preserved
grep "Step 4 — Per-artifact approval loop" .claude/skills/sara-extract/SKILL.md

# Write plan preserved
grep "Step 5 — Write extraction plan" .claude/skills/sara-extract/SKILL.md

# Version field intact
grep "^version: 1.0.0" .claude/skills/sara-extract/SKILL.md
```
</verification>

<success_criteria>
- Steps 2-3 contain Task() dispatch to all five agent names (four specialists + sorter)
- Specialist Task() prompts explicitly include source document content and discussion_notes string (per D-03)
- Sorter Task() prompt receives merged array + grep summaries + wiki/index.md content (per D-07)
- Sorter questions are presented and resolved BEFORE Step 4 (per D-09)
- Steps 1, 4, 5 preserved verbatim — no modifications
- Frontmatter unchanged (version: 1.0.0, same allowed-tools list)
- Notes block updated with agent dispatch guidance and Pitfall 1/4 guards
</success_criteria>

<output>
After completion, create `.planning/phases/06-refine-entity-extraction/06-03-SUMMARY.md`
</output>
