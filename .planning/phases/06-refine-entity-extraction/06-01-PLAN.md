---
phase: 06-refine-entity-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/sara-requirement-extractor.md
  - .claude/agents/sara-decision-extractor.md
  - .claude/agents/sara-action-extractor.md
  - .claude/agents/sara-risk-extractor.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Four specialist agent files exist in .claude/agents/"
    - "Each specialist agent file has valid YAML frontmatter with tools: Read, Bash (comma-separated, no YAML list)"
    - "Each agent returns a raw JSON array of artifacts with action always 'create' and id_to_assign as TYPE-NNN placeholder"
    - "source_quote is declared mandatory in every specialist agent's output_format block"
    - "No specialist agent receives or references wiki/index.md or grep summaries"
  artifacts:
    - path: ".claude/agents/sara-requirement-extractor.md"
      provides: "Specialist agent for requirement artifact extraction"
      contains: "name: sara-requirement-extractor"
    - path: ".claude/agents/sara-decision-extractor.md"
      provides: "Specialist agent for decision artifact extraction"
      contains: "name: sara-decision-extractor"
    - path: ".claude/agents/sara-action-extractor.md"
      provides: "Specialist agent for action artifact extraction"
      contains: "name: sara-action-extractor"
    - path: ".claude/agents/sara-risk-extractor.md"
      provides: "Specialist agent for risk artifact extraction"
      contains: "name: sara-risk-extractor"
  key_links:
    - from: ".claude/agents/sara-requirement-extractor.md"
      to: "sara-extract SKILL.md Task() dispatch"
      via: "name: field matches Task() agent reference"
      pattern: "^name: sara-requirement-extractor"
---

<objective>
Create the four specialist extraction agent files in `.claude/agents/`. Each agent is an isolated extraction unit that receives a raw source document and discussion_notes string, extracts artifacts of its specific type only, and returns a raw JSON array.

Purpose: Enable sara-extract to dispatch specialised extraction work per entity type via Task(), with each specialist working in isolation from wiki state (dedup belongs to the sorter, not the specialists).

Output: Four `.claude/agents/*.md` files ready for Task() dispatch from sara-extract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-refine-entity-extraction/06-CONTEXT.md
@.planning/phases/06-refine-entity-extraction/06-RESEARCH.md
@.planning/phases/06-refine-entity-extraction/06-PATTERNS.md

<interfaces>
<!-- Agent file frontmatter format (VERIFIED from /home/george/.claude/agents/gsd-codebase-mapper.md) -->
<!-- CRITICAL: use `tools:` as comma-separated string — NOT `allowed-tools:` YAML list (that is SKILL.md format) -->

Correct agent frontmatter:
```yaml
---
name: sara-requirement-extractor
description: Extract requirement artifacts from a source document and discussion notes
tools: Read, Bash
color: cyan
---
```

Frozen artifact schema (from .claude/skills/sara-extract/SKILL.md Step 3):
```json
{
  "action": "create",
  "type": "requirement",
  "id_to_assign": "REQ-NNN",
  "title": "Title of the requirement",
  "source_quote": "Exact verbatim text from source document",
  "raised_by": "STK-NNN",
  "related": [],
  "change_summary": ""
}
```

Type-to-id_placeholder mapping:
- requirement → REQ-NNN
- decision → DEC-NNN
- action → ACT-NNN
- risk → RSK-NNN

Valid type strings: "requirement", "decision", "action", "risk"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sara-requirement-extractor and sara-decision-extractor agent files</name>
  <files>.claude/agents/sara-requirement-extractor.md, .claude/agents/sara-decision-extractor.md</files>
  <read_first>
    - .planning/phases/06-refine-entity-extraction/06-CONTEXT.md (D-03, D-04, D-05, D-06 — agent input/output constraints)
    - .planning/phases/06-refine-entity-extraction/06-PATTERNS.md (specialist agent file structure template, lines 26-131)
    - .claude/skills/sara-extract/SKILL.md (artifact schema at lines 69-99 — must match exactly)
  </read_first>
  <action>
Create `.claude/agents/` directory if it does not exist, then write two agent files.

**sara-requirement-extractor.md** — full content:

```markdown
---
name: sara-requirement-extractor
description: Extract requirement artifacts from a source document and discussion notes
tools: Read, Bash
color: cyan
---

<role>
You are sara-requirement-extractor. You extract requirement artifacts only from a source document and discussion notes.
Spawned by `/sara-extract` via Task(). Return a JSON array only — no prose, no markdown fences.

A requirement is a statement of what the system must do, what a user needs, or what a stakeholder expects — a capability, constraint, or rule that the project must satisfy.
</role>

<input>
Agent receives via prompt:

- `<source_document>` — full content of the source file
- `<discussion_notes>` — discussion_notes string from pipeline-state.json (may be empty string)
</input>

<process>
1. Read the `<source_document>` and `<discussion_notes>` provided in the prompt.
2. Identify every passage that describes a requirement (capability, constraint, or rule the system/project must satisfy).
3. For each requirement found:
   a. Extract the exact verbatim passage from the source as `source_quote`.
   b. Determine the title as a short (≤10 words) noun-phrase label for the requirement.
   c. Set `raised_by` to the STK-NNN ID of the person who raised it, if identifiable from the source or discussion_notes. Use `"STK-NNN"` placeholder if unknown.
   d. Set `related` to an empty array — cross-linking is the sorter's responsibility.
4. If no requirement artifacts are found, return an empty array: []
5. Do NOT access the wiki, wiki/index.md, or run any grep commands — those tasks belong to the sorter.
6. Do NOT produce "update" actions — create-vs-update resolution belongs to the sorter.
</process>

<output_format>
Return a raw JSON array (no markdown fences, no prose):

[
  {
    "action": "create",
    "type": "requirement",
    "id_to_assign": "REQ-NNN",
    "title": "Short noun-phrase title for the requirement",
    "source_quote": "Exact verbatim text from source document supporting this artifact",
    "raised_by": "STK-NNN",
    "related": [],
    "change_summary": ""
  }
]

Rules:
- `action` is always "create" — never "update"
- `id_to_assign` is always "REQ-NNN" placeholder — never a real ID
- `source_quote` is MANDATORY — every artifact must include verbatim text from the source; do not generate any artifact without a source quote
- If no requirement artifacts are found, return: []
</output_format>

<notes>
- discussion_notes are passed explicitly in the prompt — agents start cold and have no implicit access to pipeline-state.json or prior discuss phase context
- Do NOT access the wiki, wiki/index.md, or grep summaries — those belong to the sorter
- Do NOT produce "update" actions — create-vs-update resolution is the sorter's job
- source_quote must be verbatim — copy the exact passage, do not paraphrase
- raised_by: use the STK-NNN ID if identifiable from discussion_notes (e.g. "Alice Wang → STK-002"). If not identifiable, use "STK-NNN" placeholder.
</notes>
```

**sara-decision-extractor.md** — same structure, substituting decision-specific content:

```markdown
---
name: sara-decision-extractor
description: Extract decision artifacts from a source document and discussion notes
tools: Read, Bash
color: cyan
---

<role>
You are sara-decision-extractor. You extract decision artifacts only from a source document and discussion notes.
Spawned by `/sara-extract` via Task(). Return a JSON array only — no prose, no markdown fences.

A decision is a deliberate choice made by the team about how to approach a problem, which technology to use, or how to resolve a conflict — something that was decided, not just discussed.
</role>

<input>
Agent receives via prompt:

- `<source_document>` — full content of the source file
- `<discussion_notes>` — discussion_notes string from pipeline-state.json (may be empty string)
</input>

<process>
1. Read the `<source_document>` and `<discussion_notes>` provided in the prompt.
2. Identify every passage that describes a decision (a deliberate choice made by the team — a conclusion reached, not just a topic discussed).
3. For each decision found:
   a. Extract the exact verbatim passage from the source as `source_quote`.
   b. Determine the title as a short (≤10 words) noun-phrase label for the decision.
   c. Set `raised_by` to the STK-NNN ID of the decision-maker or proposer, if identifiable from the source or discussion_notes. Use `"STK-NNN"` placeholder if unknown.
   d. Set `related` to an empty array — cross-linking is the sorter's responsibility.
4. If no decision artifacts are found, return an empty array: []
5. Do NOT access the wiki, wiki/index.md, or run any grep commands — those tasks belong to the sorter.
6. Do NOT produce "update" actions — create-vs-update resolution belongs to the sorter.
</process>

<output_format>
Return a raw JSON array (no markdown fences, no prose):

[
  {
    "action": "create",
    "type": "decision",
    "id_to_assign": "DEC-NNN",
    "title": "Short noun-phrase title for the decision",
    "source_quote": "Exact verbatim text from source document supporting this artifact",
    "raised_by": "STK-NNN",
    "related": [],
    "change_summary": ""
  }
]

Rules:
- `action` is always "create" — never "update"
- `id_to_assign` is always "DEC-NNN" placeholder — never a real ID
- `source_quote` is MANDATORY — every artifact must include verbatim text from the source; do not generate any artifact without a source quote
- If no decision artifacts are found, return: []
</output_format>

<notes>
- discussion_notes are passed explicitly in the prompt — agents start cold and have no implicit access to pipeline-state.json or prior discuss phase context
- Do NOT access the wiki, wiki/index.md, or grep summaries — those belong to the sorter
- Do NOT produce "update" actions — create-vs-update resolution is the sorter's job
- source_quote must be verbatim — copy the exact passage, do not paraphrase
- A decision must have been concluded — "we will use X" is a decision; "we could use X" is not
- raised_by: use the STK-NNN ID if identifiable from discussion_notes. If not identifiable, use "STK-NNN" placeholder.
</notes>
```
  </action>
  <verify>
    <automated>ls /home/george/Projects/sara/.claude/agents/sara-requirement-extractor.md && ls /home/george/Projects/sara/.claude/agents/sara-decision-extractor.md && grep "^name: sara-requirement-extractor" /home/george/Projects/sara/.claude/agents/sara-requirement-extractor.md && grep "^name: sara-decision-extractor" /home/george/Projects/sara/.claude/agents/sara-decision-extractor.md && grep "^tools: Read, Bash" /home/george/Projects/sara/.claude/agents/sara-requirement-extractor.md && grep "^tools: Read, Bash" /home/george/Projects/sara/.claude/agents/sara-decision-extractor.md && grep '"action": "create"' /home/george/Projects/sara/.claude/agents/sara-requirement-extractor.md && grep '"id_to_assign": "REQ-NNN"' /home/george/Projects/sara/.claude/agents/sara-requirement-extractor.md && grep '"id_to_assign": "DEC-NNN"' /home/george/Projects/sara/.claude/agents/sara-decision-extractor.md && grep "source_quote.*MANDATORY" /home/george/Projects/sara/.claude/agents/sara-requirement-extractor.md</automated>
  </verify>
  <acceptance_criteria>
    - `ls .claude/agents/sara-requirement-extractor.md` exits 0
    - `ls .claude/agents/sara-decision-extractor.md` exits 0
    - `grep "^name: sara-requirement-extractor" .claude/agents/sara-requirement-extractor.md` returns a match
    - `grep "^name: sara-decision-extractor" .claude/agents/sara-decision-extractor.md` returns a match
    - `grep "^tools: Read, Bash" .claude/agents/sara-requirement-extractor.md` returns a match (comma-separated string, not YAML list)
    - `grep "^tools: Read, Bash" .claude/agents/sara-decision-extractor.md` returns a match
    - `grep "allowed-tools" .claude/agents/sara-requirement-extractor.md` returns NO match (SKILL.md format must not appear)
    - `grep '"action": "create"' .claude/agents/sara-requirement-extractor.md` returns a match
    - `grep '"id_to_assign": "REQ-NNN"' .claude/agents/sara-requirement-extractor.md` returns a match
    - `grep '"id_to_assign": "DEC-NNN"' .claude/agents/sara-decision-extractor.md` returns a match
    - `grep "source_quote.*MANDATORY" .claude/agents/sara-requirement-extractor.md` returns a match
    - `grep "source_quote.*MANDATORY" .claude/agents/sara-decision-extractor.md` returns a match
    - `grep "Do NOT.*wiki" .claude/agents/sara-requirement-extractor.md` returns a match (confirms isolation per D-04)
    - `grep "Do NOT.*wiki" .claude/agents/sara-decision-extractor.md` returns a match
  </acceptance_criteria>
  <done>Both agent files exist in .claude/agents/, have valid agent frontmatter (tools: comma-separated, no allowed-tools, no version), declare action always "create", include mandatory source_quote constraint, and explicitly prohibit wiki/index access.</done>
</task>

<task type="auto">
  <name>Task 2: Create sara-action-extractor and sara-risk-extractor agent files</name>
  <files>.claude/agents/sara-action-extractor.md, .claude/agents/sara-risk-extractor.md</files>
  <read_first>
    - .planning/phases/06-refine-entity-extraction/06-CONTEXT.md (D-03, D-04, D-05, D-06)
    - .planning/phases/06-refine-entity-extraction/06-PATTERNS.md (lines 136-162 — action and risk substitution specs)
    - .claude/skills/sara-extract/SKILL.md (lines 69-101 — artifact schema and valid types)
  </read_first>
  <action>
Write two more specialist agent files following the same structure established in Task 1. The only differences from the requirement/decision agents are: entity type name, id_to_assign placeholder, entity definition in the role block, and extraction guidance in the process block.

**sara-action-extractor.md** — full content:

```markdown
---
name: sara-action-extractor
description: Extract action artifacts from a source document and discussion notes
tools: Read, Bash
color: cyan
---

<role>
You are sara-action-extractor. You extract action artifacts only from a source document and discussion notes.
Spawned by `/sara-extract` via Task(). Return a JSON array only — no prose, no markdown fences.

An action is a concrete task or follow-up item assigned to a specific person or team, with an implied or explicit owner and deadline — something that must be done after the meeting/discussion, not a general statement of intent.
</role>

<input>
Agent receives via prompt:

- `<source_document>` — full content of the source file
- `<discussion_notes>` — discussion_notes string from pipeline-state.json (may be empty string)
</input>

<process>
1. Read the `<source_document>` and `<discussion_notes>` provided in the prompt.
2. Identify every passage that describes an action item (concrete task with an implied or explicit owner).
3. For each action found:
   a. Extract the exact verbatim passage from the source as `source_quote`.
   b. Determine the title as a short (≤10 words) imperative-phrase label for the action (e.g. "Send updated proposal to client").
   c. Set `raised_by` to the STK-NNN ID of the person who will own or perform the action, if identifiable from the source or discussion_notes. Use `"STK-NNN"` placeholder if unknown.
   d. Set `related` to an empty array — cross-linking is the sorter's responsibility.
4. If no action artifacts are found, return an empty array: []
5. Do NOT access the wiki, wiki/index.md, or run any grep commands — those tasks belong to the sorter.
6. Do NOT produce "update" actions — create-vs-update resolution belongs to the sorter.
</process>

<output_format>
Return a raw JSON array (no markdown fences, no prose):

[
  {
    "action": "create",
    "type": "action",
    "id_to_assign": "ACT-NNN",
    "title": "Short imperative-phrase title for the action item",
    "source_quote": "Exact verbatim text from source document supporting this artifact",
    "raised_by": "STK-NNN",
    "related": [],
    "change_summary": ""
  }
]

Rules:
- `action` is always "create" — never "update"
- `id_to_assign` is always "ACT-NNN" placeholder — never a real ID
- `source_quote` is MANDATORY — every artifact must include verbatim text from the source; do not generate any artifact without a source quote
- If no action artifacts are found, return: []
</output_format>

<notes>
- discussion_notes are passed explicitly in the prompt — agents start cold and have no implicit access to pipeline-state.json or prior discuss phase context
- Do NOT access the wiki, wiki/index.md, or grep summaries — those belong to the sorter
- Do NOT produce "update" actions — create-vs-update resolution is the sorter's job
- source_quote must be verbatim — copy the exact passage, do not paraphrase
- An action must have a doer — vague statements of intent without an assignable owner should be skipped or noted as low confidence
- raised_by: use the STK-NNN ID if identifiable from discussion_notes. If not identifiable, use "STK-NNN" placeholder.
</notes>
```

**sara-risk-extractor.md** — full content:

```markdown
---
name: sara-risk-extractor
description: Extract risk artifacts from a source document and discussion notes
tools: Read, Bash
color: cyan
---

<role>
You are sara-risk-extractor. You extract risk artifacts only from a source document and discussion notes.
Spawned by `/sara-extract` via Task(). Return a JSON array only — no prose, no markdown fences.

A risk is an uncertain event or condition that, if it occurs, would have a negative effect on the project — expressed as a threat, concern, or "what if" scenario. A risk has likelihood and impact dimensions.
</role>

<input>
Agent receives via prompt:

- `<source_document>` — full content of the source file
- `<discussion_notes>` — discussion_notes string from pipeline-state.json (may be empty string)
</input>

<process>
1. Read the `<source_document>` and `<discussion_notes>` provided in the prompt.
2. Identify every passage that describes a risk (threat, concern, or "what if" scenario with negative consequence).
3. For each risk found:
   a. Extract the exact verbatim passage from the source as `source_quote`.
   b. Determine the title as a short (≤10 words) noun-phrase label for the risk (e.g. "Budget overrun on infrastructure").
   c. Set `raised_by` to the STK-NNN ID of the person who raised or owns the risk, if identifiable from the source or discussion_notes. Use `"STK-NNN"` placeholder if unknown.
   d. Set `related` to an empty array — cross-linking is the sorter's responsibility.
4. If no risk artifacts are found, return an empty array: []
5. Do NOT access the wiki, wiki/index.md, or run any grep commands — those tasks belong to the sorter.
6. Do NOT produce "update" actions — create-vs-update resolution belongs to the sorter.
</process>

<output_format>
Return a raw JSON array (no markdown fences, no prose):

[
  {
    "action": "create",
    "type": "risk",
    "id_to_assign": "RSK-NNN",
    "title": "Short noun-phrase title for the risk",
    "source_quote": "Exact verbatim text from source document supporting this artifact",
    "raised_by": "STK-NNN",
    "related": [],
    "change_summary": ""
  }
]

Rules:
- `action` is always "create" — never "update"
- `id_to_assign` is always "RSK-NNN" placeholder — never a real ID
- `source_quote` is MANDATORY — every artifact must include verbatim text from the source; do not generate any artifact without a source quote
- If no risk artifacts are found, return: []
</output_format>

<notes>
- discussion_notes are passed explicitly in the prompt — agents start cold and have no implicit access to pipeline-state.json or prior discuss phase context
- Do NOT access the wiki, wiki/index.md, or grep summaries — those belong to the sorter
- Do NOT produce "update" actions — create-vs-update resolution is the sorter's job
- source_quote must be verbatim — copy the exact passage, do not paraphrase
- A risk must represent a potential negative outcome — a confirmed problem is an action item, not a risk
- raised_by: use the STK-NNN ID if identifiable from discussion_notes. If not identifiable, use "STK-NNN" placeholder.
</notes>
```
  </action>
  <verify>
    <automated>ls /home/george/Projects/sara/.claude/agents/sara-action-extractor.md && ls /home/george/Projects/sara/.claude/agents/sara-risk-extractor.md && grep "^name: sara-action-extractor" /home/george/Projects/sara/.claude/agents/sara-action-extractor.md && grep "^name: sara-risk-extractor" /home/george/Projects/sara/.claude/agents/sara-risk-extractor.md && grep "^tools: Read, Bash" /home/george/Projects/sara/.claude/agents/sara-action-extractor.md && grep "^tools: Read, Bash" /home/george/Projects/sara/.claude/agents/sara-risk-extractor.md && grep '"id_to_assign": "ACT-NNN"' /home/george/Projects/sara/.claude/agents/sara-action-extractor.md && grep '"id_to_assign": "RSK-NNN"' /home/george/Projects/sara/.claude/agents/sara-risk-extractor.md</automated>
  </verify>
  <acceptance_criteria>
    - `ls .claude/agents/sara-action-extractor.md` exits 0
    - `ls .claude/agents/sara-risk-extractor.md` exits 0
    - `grep "^name: sara-action-extractor" .claude/agents/sara-action-extractor.md` returns a match
    - `grep "^name: sara-risk-extractor" .claude/agents/sara-risk-extractor.md` returns a match
    - `grep "^tools: Read, Bash" .claude/agents/sara-action-extractor.md` returns a match
    - `grep "^tools: Read, Bash" .claude/agents/sara-risk-extractor.md` returns a match
    - `grep "allowed-tools" .claude/agents/sara-action-extractor.md` returns NO match
    - `grep "allowed-tools" .claude/agents/sara-risk-extractor.md` returns NO match
    - `grep '"action": "create"' .claude/agents/sara-action-extractor.md` returns a match
    - `grep '"action": "create"' .claude/agents/sara-risk-extractor.md` returns a match
    - `grep '"id_to_assign": "ACT-NNN"' .claude/agents/sara-action-extractor.md` returns a match
    - `grep '"id_to_assign": "RSK-NNN"' .claude/agents/sara-risk-extractor.md` returns a match
    - `grep "source_quote.*MANDATORY" .claude/agents/sara-action-extractor.md` returns a match
    - `grep "source_quote.*MANDATORY" .claude/agents/sara-risk-extractor.md` returns a match
    - `grep "Do NOT.*wiki" .claude/agents/sara-action-extractor.md` returns a match
    - `grep "Do NOT.*wiki" .claude/agents/sara-risk-extractor.md` returns a match
  </acceptance_criteria>
  <done>Both agent files exist in .claude/agents/, have valid agent frontmatter, action and risk type-specific extraction guidance, mandatory source_quote constraint, and prohibition on wiki access. All four specialist agents are now in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored content → Task() dispatch | Agent files are static markdown authored by the developer and loaded by Claude Code; no external data crosses this boundary during authoring |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | .claude/agents/*.md | accept | Files are git-tracked; any modification is visible in diff. No runtime executable code — markdown only. Low-value target. |
| T-06-02 | Information Disclosure | specialist agent output (JSON array) | accept | Agent outputs raw JSON to the orchestrating sara-extract session only; no persistence or network transmission at this layer. |
| T-06-03 | Elevation of Privilege | agent tools: Read, Bash | accept | Tools are restricted to Read and Bash; no Write tool is listed, preventing agents from modifying wiki files directly. This is the correct least-privilege posture per D-04. |
</threat_model>

<verification>
After both tasks complete:

```bash
# All four specialist agent files exist
ls .claude/agents/sara-requirement-extractor.md \
   .claude/agents/sara-decision-extractor.md \
   .claude/agents/sara-action-extractor.md \
   .claude/agents/sara-risk-extractor.md

# Confirm tools format is comma-separated (not YAML list) in all four
grep "^tools:" .claude/agents/sara-*-extractor.md

# Confirm no agent uses allowed-tools (SKILL.md format)
grep -l "allowed-tools" .claude/agents/sara-*-extractor.md || echo "PASS: no allowed-tools found"

# Confirm all four have mandatory source_quote note
grep -l "source_quote.*MANDATORY" .claude/agents/sara-*-extractor.md | wc -l
# Expected: 4

# Confirm all four prohibit wiki access
grep -c "Do NOT.*wiki" .claude/agents/sara-*-extractor.md
# Each file should show 1 or more matches
```
</verification>

<success_criteria>
- All four specialist agent files created in `.claude/agents/`
- Every agent: `tools: Read, Bash` (comma-separated, no YAML list, no `allowed-tools:`)
- Every agent: output format block contains `"action": "create"` (never "update")
- Every agent: id_to_assign placeholder matches its type (REQ-NNN, DEC-NNN, ACT-NNN, RSK-NNN)
- Every agent: `source_quote` declared MANDATORY in output_format block
- Every agent: notes block explicitly prohibits wiki/index.md access (per D-04)
- Every agent: notes block states discussion_notes are passed explicitly (per D-03)
</success_criteria>

<output>
After completion, create `.planning/phases/06-refine-entity-extraction/06-01-SUMMARY.md`
</output>
