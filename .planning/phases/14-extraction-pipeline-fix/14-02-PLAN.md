---
phase: 14-extraction-pipeline-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/sara-update/SKILL.md
autonomous: true
requirements:
  - XREF-02

must_haves:
  truths:
    - "sara-update resolves temp_ids to real entity IDs before any wiki page is written"
    - "Every created wiki artifact's related[] frontmatter field contains real entity IDs (e.g. REQ-001), not temp_ids"
    - "The counter increment-before-write (Pitfall 1) guard is fully preserved — real counters are only incremented inside the write loop"
    - "Update-action artifacts (which have no temp_id) are skipped during the resolution pass without error"
    - "The Cross Links body section is correctly populated because artifact.related contains real IDs by the time the write loop reaches it"
  artifacts:
    - path: ".claude/skills/sara-update/SKILL.md"
      provides: "Updated skill with temp_id→real_id resolution block at start of Step 2"
      contains: "id_map"
  key_links:
    - from: "sara-update Step 2 (resolution block)"
      to: "artifact.related arrays in extraction_plan"
      via: "id_map substitution pass"
      pattern: "id_map"
    - from: "preview_counters"
      to: "real counters.entity"
      via: "deep copy (read-only peek, no increment)"
      pattern: "preview_counters"
---

<objective>
Modify the sara-update skill to resolve temp_ids to real entity IDs at the start of Step 2, before the write loop begins. This ensures every wiki artifact page written by sara-update has a populated related[] frontmatter field containing real entity IDs.

Purpose: Completes the XREF-01/XREF-02 pipeline — sara-extract writes temp_ids into extraction_plan; sara-update converts them to real IDs (REQ-NNN, DEC-NNN, ACT-NNN, RSK-NNN) via a preview counter simulation before any page is written.

Output: Updated `.claude/skills/sara-update/SKILL.md` with a "Temp ID resolution" block inserted at the start of Step 2, before "Initialize `written_files = []`".
</objective>

<execution_context>
@/home/george/.claude/get-shit-done/workflows/execute-plan.md
@/home/george/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-extraction-pipeline-fix/14-CONTEXT.md
@.planning/phases/14-extraction-pipeline-fix/14-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert temp_id→real_id resolution block at start of Step 2</name>
  <files>.claude/skills/sara-update/SKILL.md</files>
  <read_first>
    - .claude/skills/sara-update/SKILL.md — read the full file before making any changes; Step 2 begins at approximately line 65 with "Initialize `written_files = []` and `failed_files = []`." The insertion point is immediately before this line, after the Step 1b section ends
    - .planning/phases/14-extraction-pipeline-fix/14-CONTEXT.md — decision D-03 defines the resolution algorithm; decisions D-01/D-02 define the temp_id scheme being consumed
    - .planning/phases/14-extraction-pipeline-fix/14-PATTERNS.md — "Copy pattern — temp_id → real_id resolution block at start of Step 2" contains the complete prose block to insert
  </read_first>
  <action>
In `.claude/skills/sara-update/SKILL.md`, locate the start of Step 2: the line that reads:

  `Initialize `written_files = []` and `failed_files = []`.`

Insert the following complete block BEFORE that line (making it the first thing that happens in Step 2, after the Step 1b source-doc read):

```
**Temp ID resolution (before write loop)**

Build the `temp_id → real_id` map by simulating the ID assignment sequence without
incrementing the counters:

  Initialize `id_map` = {} (empty mapping)
  Initialize `preview_counters` = deep copy of `counters.entity` from the in-memory state
    (do NOT modify the real counters — these are read-only preview increments)

  For each artifact in `{extraction_plan}` where `artifact.action == "create"`:
    Determine `{entity_type_key}` from `artifact.type`:
    - `requirement` → `REQ`
    - `decision`    → `DEC`
    - `action`      → `ACT`
    - `risk`        → `RSK`
    Increment `preview_counters.{entity_type_key}` by 1
    Compute `{preview_id}` = `"{entity_type_key}-"` + zero-padded 3-digit preview counter
    Set `id_map[artifact.temp_id]` = `{preview_id}`
    (skip artifacts where `artifact.action == "update"` — they have no temp_id)

Do NOT write `preview_counters` to `pipeline-state.json`. The real counter increments
happen inside the write loop as they always have (Pitfall 1 guard preserved).

**Substitution pass:**

  For each artifact in `{extraction_plan}`:
    For each entry `t` in `artifact.related`:
      If `id_map[t]` exists: replace `t` with `id_map[t]`
      If `id_map[t]` does not exist: leave `t` unchanged (it may already be a real ID
        from a sorter cross-reference resolution in sara-extract Step 3)

After this pass, all `artifact.related` arrays in the in-memory `extraction_plan` contain
real entity IDs. Proceed to "Initialize `written_files = []`" and the write loop.

Do NOT write the substituted `extraction_plan` back to `pipeline-state.json` at this
point — the write loop persists counters on each create-action iteration as before.
The temp_id fields on artifact objects may be left in `pipeline-state.json` (they are
inert after resolution) or stripped — either is acceptable.
```

After inserting, the Step 2 sequence must be:

  **Step 2 — Write wiki artifact files**

  **Temp ID resolution (before write loop)**
  [the block above]

  Initialize `written_files = []` and `failed_files = []`.

  For each artifact in `{extraction_plan}`:
  [existing write loop — unchanged]

Do NOT alter any part of the write loop, the counter increment-before-write pattern, the partial failure report format, or any other content of Step 2.
  </action>
  <verify>
    <automated>grep -n "id_map\|preview_counters\|Temp ID resolution" .claude/skills/sara-update/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Temp ID resolution" .claude/skills/sara-update/SKILL.md` returns exactly 1 match (the block heading)
    - `grep -c "id_map" .claude/skills/sara-update/SKILL.md` returns 5 or more (id_map initialization, id_map assignment in the preview loop, id_map lookup in substitution pass, and inline comments)
    - `grep -n "preview_counters" .claude/skills/sara-update/SKILL.md` returns at least 3 lines (initialization, increment, and the Pitfall 1 guard note)
    - `grep -n "Pitfall 1 guard preserved" .claude/skills/sara-update/SKILL.md` returns 1 match (the critical guard note in the resolution block)
    - `grep -n "Initialize \`written_files" .claude/skills/sara-update/SKILL.md` — verify this line's line number is HIGHER than the "Temp ID resolution" heading line number (resolution block appears before the write loop init)
    - `grep "do NOT modify the real counters" .claude/skills/sara-update/SKILL.md` returns 1 match
    - The existing counter increment-before-write prose at approximately lines 83–87 is unchanged — `grep "Increment \`counters\.entity" .claude/skills/sara-update/SKILL.md` still returns the original line
    - `grep "CRITICAL: Entity counter increments" .claude/skills/sara-update/SKILL.md` returns 1 match (the existing notes section is unchanged)
  </acceptance_criteria>
  <done>Step 2 of sara-update now begins with a temp_id→real_id resolution block. A preview counter simulation builds id_map without touching real counters. A substitution pass replaces temp_ids in all artifact.related arrays with real entity IDs before the write loop starts. The write loop, counter increment-before-write pattern, and all other Step 2 logic are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extraction_plan → sara-update Step 2 | pipeline-state.json content (written by sara-extract) is the input |
| preview_counters → id_map | In-memory read-only simulation; never persisted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Tampering | preview_counters write | mitigate | The resolution block explicitly states "Do NOT write preview_counters to pipeline-state.json" — this is the Pitfall 1 guard. The existing notes section also states the CRITICAL constraint. Two-layer documentation prevents accidental counter persistence. |
| T-14-05 | Tampering | id_map unresolvable temp_id | accept | Substitution pass leaves unresolvable entries unchanged (they may be real IDs from sorter cross-reference resolution). Low risk: worst case is a stale temp_id in related[] which sara-lint (Phase 15) will detect. |
| T-14-06 | Information Disclosure | temp_id fields remaining in pipeline-state.json | accept | temp_ids are random 8-hex strings with no PII or security value. The skill notes that they may be left inert or stripped — either is acceptable. |
</threat_model>

<verification>
After the task completes:
- `grep -n "Temp ID resolution" .claude/skills/sara-update/SKILL.md` returns 1 line
- `grep -n "id_map" .claude/skills/sara-update/SKILL.md` returns multiple lines covering initialization, assignment, and substitution
- `grep -n "preview_counters" .claude/skills/sara-update/SKILL.md` returns lines for initialization, increment, and the Pitfall 1 note
- The insertion is before "Initialize `written_files`" — verified by line number comparison
- The existing CRITICAL notes in the `<notes>` section are unmodified
</verification>

<success_criteria>
- sara-update Step 2 begins with a temp_id→real_id resolution block before the write loop
- id_map correctly maps each create-action artifact's temp_id to its preview real ID (e.g. "a3f2b901" → "REQ-001")
- Substitution pass replaces all temp_id references in artifact.related arrays with real IDs
- update-action artifacts (no temp_id) are skipped without error
- preview_counters is never written to pipeline-state.json — real counter increment-before-write (Pitfall 1 guard) is fully preserved
- The Cross Links body section generation logic is unchanged — it already reads from artifact.related, which now contains real IDs
</success_criteria>

<output>
After completion, create `.planning/phases/14-extraction-pipeline-fix/14-02-SUMMARY.md`
</output>
