---
phase: 14-extraction-pipeline-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/sara-extract/SKILL.md
autonomous: true
requirements:
  - XREF-01

must_haves:
  truths:
    - "Every artifact produced by sara-extract Step 3 carries a unique 8-hex temp_id field"
    - "After the approval loop, every approved artifact's related[] contains the temp_ids of all other approved artifacts in the batch"
    - "A single-artifact batch produces related: [] (empty, not missing)"
    - "Rejected artifacts never appear in any related[] array"
    - "The extraction_plan written to pipeline-state.json contains temp_id and related[] on every approved artifact"
  artifacts:
    - path: ".claude/skills/sara-extract/SKILL.md"
      provides: "Updated skill with temp_id assignment in Step 3 and full-mesh linking in Step 5"
      contains: "temp_id"
  key_links:
    - from: "sara-extract Step 3 (four passes)"
      to: "approved_artifacts array"
      via: "temp_id field on each artifact object"
      pattern: "temp_id"
    - from: "sara-extract Step 5 (full-mesh block)"
      to: "pipeline-state.json extraction_plan"
      via: "A.related = array of other temp_ids"
      pattern: "full-mesh"
---

<objective>
Modify the sara-extract skill to assign a unique 8-hex `temp_id` to each artifact during Step 3 (inline extraction passes) and to compute full-mesh `related[]` links among approved artifacts in Step 5 (before writing to pipeline-state.json).

Purpose: Enables sara-update to resolve cross-references between co-extracted artifacts by providing stable temp_id keys that persist in extraction_plan until real IDs are assigned.

Output: Updated `.claude/skills/sara-extract/SKILL.md` with two new prose blocks — temp_id assignment woven into each of the four extraction passes in Step 3, and a full-mesh linking block inserted in Step 5 before the pipeline-state.json write.
</objective>

<execution_context>
@/home/george/.claude/get-shit-done/workflows/execute-plan.md
@/home/george/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-extraction-pipeline-fix/14-CONTEXT.md
@.planning/phases/14-extraction-pipeline-fix/14-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add temp_id assignment to Step 3 extraction passes</name>
  <files>.claude/skills/sara-extract/SKILL.md</files>
  <read_first>
    - .claude/skills/sara-extract/SKILL.md — read the full file before making any changes; the four pass field-initialization lines are at approximately lines 96, 154, 200, and 253
    - .planning/phases/14-extraction-pipeline-fix/14-CONTEXT.md — decision D-01 specifies the temp_id scheme
    - .planning/phases/14-extraction-pipeline-fix/14-PATTERNS.md — "Step 3: temp_id assignment during inline passes" section contains the exact copy pattern
  </read_first>
  <action>
In `.claude/skills/sara-extract/SKILL.md`, locate the four field-initialization lines in the four inline extraction passes (one per pass). Each currently reads approximately:

  `- Set `action` = `"create"`, `type` = `"requirement"`, `id_to_assign` = `"REQ-NNN"`, `related` = `[]`, `change_summary` = `""`  `

Add a `temp_id` assignment line BEFORE the existing `action`/`type`/`id_to_assign`/`related`/`change_summary` line in each of the four passes. The insertion must appear in all four passes:

Requirements pass (near line 96):
```
- Set `temp_id` = an 8-character lowercase hex string generated at extraction time.
  Use: Bash one-liner `python3 -c "import secrets; print(secrets.token_hex(4))"` OR
  generate inline as a random 8-hex string (e.g. `a3f2b901`). Each artifact gets a
  unique temp_id — do not reuse across artifacts in the same batch.
- Set `action` = `"create"`, `type` = `"requirement"`, `id_to_assign` = `"REQ-NNN"`, `related` = `[]`, `change_summary` = `""`
```

Apply the identical `temp_id` assignment line before the existing field-init line in:
- Decisions pass (near line 154) — existing line ends with `related` = `[]`, `change_summary` = `""`
- Actions pass (near line 200) — existing line ends with `related` = `[]`, `change_summary` = `""`
- Risks pass (near line 253) — existing line ends with `related` = `[]`, `change_summary` = `""`

Do NOT alter any other content of the four passes. The `related` field stays as `[]` at extraction time — it is populated in Step 5, not here. The `temp_id` field is added alongside (not replacing) the existing initialization fields.

Use the Edit tool to make four targeted insertions — one per pass. Do not rewrite unrelated sections.
  </action>
  <verify>
    <automated>grep -c "temp_id" .claude/skills/sara-extract/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "temp_id" .claude/skills/sara-extract/SKILL.md` outputs 4 or more (one occurrence per extraction pass, plus any in Step 5 after Task 2)
    - `grep "temp_id" .claude/skills/sara-extract/SKILL.md` shows the temp_id assignment line in the Requirements, Decisions, Actions, and Risks pass sections
    - `grep "token_hex\|8-character\|8-hex" .claude/skills/sara-extract/SKILL.md` returns at least one match confirming the generation instruction is present
    - The `related` = `[]` field-init lines remain present and unchanged in all four passes — `grep -c "related.*\[\]" .claude/skills/sara-extract/SKILL.md` returns 4 or more
    - No extraction pass text other than the four field-init blocks is altered
  </acceptance_criteria>
  <done>All four inline extraction passes in Step 3 now include a temp_id assignment line before the field-init line, using 8-hex random string generation. related[] remains [] at extraction time.</done>
</task>

<task type="auto">
  <name>Task 2: Insert full-mesh related[] linking block into Step 5</name>
  <files>.claude/skills/sara-extract/SKILL.md</files>
  <read_first>
    - .claude/skills/sara-extract/SKILL.md — read the current Step 5 block (approximately lines 376–406) to identify the exact insertion point: after "After all artifacts have been resolved to 'Accept' or 'Reject': proceed to Step 5." and before "Read `.sara/pipeline-state.json` using the Read tool."
    - .planning/phases/14-extraction-pipeline-fix/14-CONTEXT.md — decisions D-02, D-04, D-05 define the full-mesh algorithm and single/zero artifact edge cases
    - .planning/phases/14-extraction-pipeline-fix/14-PATTERNS.md — "Copy pattern — insert full-mesh linking block BEFORE the write in Step 5" contains the exact prose to insert
  </read_first>
  <action>
In `.claude/skills/sara-extract/SKILL.md`, locate Step 5 — "Write extraction plan and advance stage". The current Step 5 begins with:

  `Read `.sara/pipeline-state.json` using the Read tool.`

Insert the following full-mesh linking block BEFORE that Read line, immediately after the header "**Step 5 — Write extraction plan and advance stage**":

```
**Full-mesh related[] linking**

After all artifacts are resolved to "Accept" or "Reject":

Build the full-mesh related[] for all approved artifacts:
  For each artifact `A` in `approved_artifacts`:
    Set `A.related` = array of `temp_id` values of all OTHER artifacts in `approved_artifacts`
    (i.e. every temp_id in `approved_artifacts` except `A.temp_id` itself)

For a single-artifact batch: `A.related` = `[]` (the other-artifacts set is empty — no special case needed)
For a zero-artifact batch: skip this step entirely (approved_artifacts is empty)

This replaces the `related: []` that was set during Step 3. The temp_id values are stable
cross-reference keys — they persist in extraction_plan until sara-update resolves them to
real IDs at the start of Step 2.
```

After inserting, the Step 5 sequence must be:

  **Step 5 — Write extraction plan and advance stage**

  **Full-mesh related[] linking**
  [the block above]

  Read `.sara/pipeline-state.json` using the Read tool.

  Update `items["{N}"]` in memory:
  [etc — unchanged]

Do NOT alter the existing pipeline-state.json read/write logic, the stage="approved" assignment, or the summary table output at the end of Step 5.
  </action>
  <verify>
    <automated>grep -n "full-mesh\|Full-mesh" .claude/skills/sara-extract/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Full-mesh\|full-mesh" .claude/skills/sara-extract/SKILL.md` returns at least 2 lines (the heading and the body text)
    - `grep -n "A\.related.*temp_id\|temp_id.*A\.related" .claude/skills/sara-extract/SKILL.md` returns at least 1 match confirming the mesh assignment is present
    - `grep -n "single-artifact batch" .claude/skills/sara-extract/SKILL.md` returns 1 match (the edge case note)
    - `grep -n "zero-artifact batch" .claude/skills/sara-extract/SKILL.md` returns 1 match (the skip instruction)
    - The insertion appears BEFORE "Read \`.sara/pipeline-state.json\`" within Step 5 — verify by checking line numbers: the full-mesh block line number is lower than the pipeline-state.json read line number in Step 5
    - `grep -c "Do NOT use Bash shell text-processing" .claude/skills/sara-extract/SKILL.md` still returns 1 (existing constraint unchanged)
  </acceptance_criteria>
  <done>Step 5 of sara-extract now performs full-mesh temp_id linking on approved_artifacts before writing extraction_plan to pipeline-state.json. Single-artifact and zero-artifact batches are handled. The write logic and stage advancement are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → sara-extract | Source documents are user-controlled content processed inline |
| extraction_plan → pipeline-state.json | In-memory JSON written with Read/Write tools only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | temp_id uniqueness | accept | temp_ids are ephemeral cross-reference keys with no security role; collision probability for 8-hex over a single batch is negligible (1/4.3B per pair); batch sizes are bounded by single source document |
| T-14-02 | Tampering | pipeline-state.json write | accept | Read/Write tools only constraint (inherited from existing skill); no shell text-processing; same trust model as existing Step 5 |
| T-14-03 | Information Disclosure | extraction_plan temp_ids | accept | temp_ids are internal state with no PII; they are inert after sara-update resolution; no external exposure |
</threat_model>

<verification>
After both tasks complete:
- `grep -c "temp_id" .claude/skills/sara-extract/SKILL.md` returns 8 or more (4 in Step 3 passes + at least 2 in Step 5 full-mesh block)
- `grep -n "Full-mesh" .claude/skills/sara-extract/SKILL.md` confirms the block exists in Step 5
- `grep "related.*\[\]" .claude/skills/sara-extract/SKILL.md` confirms the four pass field-inits still set related=[] (Step 3 still initializes to empty; Step 5 replaces with mesh)
- The skill file has no lines containing `jq`, `awk`, or `sed` shell commands (the existing false-positive note about `raised_by` containing "sed" as a substring is not a concern — it is a field name, not a shell command)
</verification>

<success_criteria>
- sara-extract Step 3: all four inline passes assign a unique 8-hex temp_id to each extracted artifact
- sara-extract Step 5: full-mesh related[] linking computes A.related = temp_ids of all other approved artifacts, before the pipeline-state.json write
- Single-artifact batch produces related: [] (empty mesh)
- Zero-artifact batch skips the linking step entirely
- Rejected artifacts are never in approved_artifacts and therefore never appear in any related[]
- The existing Read/Write-only constraint, the stage-advancement logic, and the summary table output are all unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/14-extraction-pipeline-fix/14-01-SUMMARY.md`
</output>
