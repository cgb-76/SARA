---
phase: 04-make-installable
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/sara-init/SKILL.md
  - .claude/skills/sara-ingest/SKILL.md
  - .claude/skills/sara-discuss/SKILL.md
  - .claude/skills/sara-extract/SKILL.md
  - .claude/skills/sara-update/SKILL.md
  - .claude/skills/sara-add-stakeholder/SKILL.md
  - .claude/skills/sara-minutes/SKILL.md
  - .claude/skills/sara-agenda/SKILL.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "Every sara-* SKILL.md has a version field in its YAML frontmatter"
    - "The version value is exactly 1.0.0 (plain semver, unquoted)"
    - "All other existing frontmatter fields remain unchanged"
  artifacts:
    - path: ".claude/skills/sara-init/SKILL.md"
      provides: "version field in frontmatter"
      contains: "version: 1.0.0"
    - path: ".claude/skills/sara-ingest/SKILL.md"
      provides: "version field in frontmatter"
      contains: "version: 1.0.0"
    - path: ".claude/skills/sara-discuss/SKILL.md"
      provides: "version field in frontmatter"
      contains: "version: 1.0.0"
    - path: ".claude/skills/sara-extract/SKILL.md"
      provides: "version field in frontmatter"
      contains: "version: 1.0.0"
    - path: ".claude/skills/sara-update/SKILL.md"
      provides: "version field in frontmatter"
      contains: "version: 1.0.0"
    - path: ".claude/skills/sara-add-stakeholder/SKILL.md"
      provides: "version field in frontmatter"
      contains: "version: 1.0.0"
    - path: ".claude/skills/sara-minutes/SKILL.md"
      provides: "version field in frontmatter"
      contains: "version: 1.0.0"
    - path: ".claude/skills/sara-agenda/SKILL.md"
      provides: "version field in frontmatter"
      contains: "version: 1.0.0"
  key_links:
    - from: "install.sh"
      to: ".claude/skills/sara-*/SKILL.md"
      via: "grep for version field"
      pattern: "^version:"
---

<objective>
Add `version: 1.0.0` to the YAML frontmatter of all 8 existing SKILL.md files. This is the version field that install.sh (Plan 02) will read at install time to perform downgrade protection (D-08, D-09).

Purpose: Enable version-aware installs — without this field, the installer has no baseline to compare against.
Output: 8 modified SKILL.md files, each with `version: 1.0.0` added to their YAML frontmatter block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-make-installable/04-CONTEXT.md
</context>

<interfaces>
<!-- Existing SKILL.md frontmatter pattern (from sara-init/SKILL.md): -->
```yaml
---
name: sara-init
description: "Initialise a new SARA wiki in the current directory"
argument-hint: ""
allowed-tools:
  - Read
  - Write
  - Bash
  - AskUserQuestion
---
```

After this plan, the frontmatter MUST look like:
```yaml
---
name: sara-init
description: "Initialise a new SARA wiki in the current directory"
argument-hint: ""
allowed-tools:
  - Read
  - Write
  - Bash
  - AskUserQuestion
version: 1.0.0
---
```

Key constraint from CONTEXT.md code_context: `schema_version: '1.0'` in wiki entity templates is quoted to prevent Obsidian float parse. The skill `version` field is DIFFERENT — it tracks installer version, not schema version — and uses plain unquoted semver `1.0.0`. Do NOT quote it.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add version field to all 8 SKILL.md files</name>
  <files>
    .claude/skills/sara-init/SKILL.md,
    .claude/skills/sara-ingest/SKILL.md,
    .claude/skills/sara-discuss/SKILL.md,
    .claude/skills/sara-extract/SKILL.md,
    .claude/skills/sara-update/SKILL.md,
    .claude/skills/sara-add-stakeholder/SKILL.md,
    .claude/skills/sara-minutes/SKILL.md,
    .claude/skills/sara-agenda/SKILL.md
  </files>
  <read_first>
    Read each of the 8 SKILL.md files listed above before editing any of them. Capture the full
    frontmatter block (between the opening --- and closing ---) for each file so you know the
    current last field and can insert version after it without disrupting existing fields.

    Files to read:
    - /home/george/Projects/llm-wiki-gsd/.claude/skills/sara-init/SKILL.md
    - /home/george/Projects/llm-wiki-gsd/.claude/skills/sara-ingest/SKILL.md
    - /home/george/Projects/llm-wiki-gsd/.claude/skills/sara-discuss/SKILL.md
    - /home/george/Projects/llm-wiki-gsd/.claude/skills/sara-extract/SKILL.md
    - /home/george/Projects/llm-wiki-gsd/.claude/skills/sara-update/SKILL.md
    - /home/george/Projects/llm-wiki-gsd/.claude/skills/sara-add-stakeholder/SKILL.md
    - /home/george/Projects/llm-wiki-gsd/.claude/skills/sara-minutes/SKILL.md
    - /home/george/Projects/llm-wiki-gsd/.claude/skills/sara-agenda/SKILL.md
  </read_first>
  <action>
    For each of the 8 SKILL.md files, add exactly this line as the last field inside the YAML
    frontmatter block (before the closing ---):

      version: 1.0.0

    Rules:
    - The value must be unquoted: `version: 1.0.0` NOT `version: '1.0.0'`
    - Insert after the last existing frontmatter field (after the allowed-tools block)
    - Do NOT change any other field names, values, indentation, or content
    - Do NOT add version to any wiki entity template files — only the 8 SKILL.md files listed above
    - Use the Edit tool to make a targeted insertion at the closing --- of the frontmatter block

    After editing all 8 files, run:
      grep -r "^version:" /home/george/Projects/llm-wiki-gsd/.claude/skills/
    to confirm all 8 have the field.
  </action>
  <verify>
    <automated>grep -r "^version:" /home/george/Projects/llm-wiki-gsd/.claude/skills/ | sort</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "^version:" .claude/skills/` returns exactly 8 lines, one per skill directory
    - Every line contains exactly `version: 1.0.0` (unquoted)
    - The 8 skill names present in output: sara-init, sara-ingest, sara-discuss, sara-extract, sara-update, sara-add-stakeholder, sara-minutes, sara-agenda
    - `grep -r "version: '1.0.0'" .claude/skills/` returns zero lines (quoted form must NOT exist)
    - No other lines in any SKILL.md changed: verify by checking that `name:`, `description:`, `argument-hint:`, `allowed-tools:` are still present in each file
  </acceptance_criteria>
  <done>All 8 SKILL.md files contain `version: 1.0.0` as an unquoted field in their YAML frontmatter. No other content is altered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner→executor | Plan text instructs file edits; executor reads and applies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | SKILL.md frontmatter | mitigate | Read each file before editing; use targeted Edit (not full rewrite) to avoid losing content; grep verification confirms no extraneous changes |
| T-04-02 | Information Disclosure | version field value | accept | Version number is public metadata; no secrets involved |
</threat_model>

<verification>
Run from repo root:
```bash
grep -r "^version:" .claude/skills/ | wc -l   # must output 8
grep -r "^version: 1.0.0$" .claude/skills/ | wc -l  # must output 8
grep -r "version: '1.0.0'" .claude/skills/ | wc -l  # must output 0
```
</verification>

<success_criteria>
- All 8 SKILL.md files have `version: 1.0.0` in their YAML frontmatter
- Value is unquoted plain semver
- No other frontmatter fields were altered in any file
</success_criteria>

<output>
After completion, create `.planning/phases/04-make-installable/04-01-SUMMARY.md`
</output>
