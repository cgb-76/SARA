---
phase: 04-make-installable
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02-PLAN.md
files_modified:
  - README.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "README.md exists at the repo root"
    - "README.md has an Installation section explaining how to run install.sh"
    - "The Installation section covers: clone or download, run install.sh, then run /sara-init in Claude Code"
    - "A human has verified install.sh works end-to-end in a realistic scenario"
  artifacts:
    - path: "README.md"
      provides: "Project overview and installation instructions"
      contains: "install.sh"
    - path: "README.md"
      provides: "Installation section"
      contains: "Installation"
    - path: "README.md"
      provides: "Post-install next step"
      contains: "/sara-init"
  key_links:
    - from: "README.md"
      to: "install.sh"
      via: "documented command"
      pattern: "install.sh"
---

<objective>
Create README.md at the repo root with an Installation section, then run a human verification checkpoint to confirm the end-to-end install flow works correctly in a realistic scenario.

Purpose: Any user who finds this repo (via GitHub or direct share) can understand what SARA is and how to install it. The human checkpoint confirms the full Phase 4 deliverable works before the phase is closed.
Output: README.md at repo root; human-approved end-to-end verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-make-installable/04-CONTEXT.md
@.planning/phases/04-make-installable/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README.md</name>
  <files>README.md</files>
  <read_first>
    Read the PROJECT.md to understand what SARA is — the README should reflect the project
    description accurately:
    /home/george/Projects/llm-wiki-gsd/.planning/PROJECT.md

    Read the CONTEXT.md to confirm the post-install UX message (D-07) and use of /sara-init (D-06):
    /home/george/Projects/llm-wiki-gsd/.planning/phases/04-make-installable/04-CONTEXT.md

    Confirm install.sh exists and is executable before writing README:
    /home/george/Projects/llm-wiki-gsd/install.sh (first 30 lines)
  </read_first>
  <action>
    Create `/home/george/Projects/llm-wiki-gsd/README.md` with the following structure and content:

    **Title:** `# SARA — Solution Architecture Recall Assistant`

    **Brief description (2-3 sentences):**
    SARA is a personal, git-backed knowledge pipeline for solution design, operated entirely
    through Claude Code slash commands. Every meeting, email thread, and document gets permanently
    integrated into a structured wiki — knowledge compounds across sessions instead of
    disappearing into chat history. One SARA instance per project.

    **Section: ## Requirements**
    - [Claude Code](https://claude.ai/code) installed
    - A git-initialised project directory (the target project, not this repo)

    **Section: ## Installation**
    Explain the two-step install:
    1. Clone this repo (or download install.sh)
    2. Run: `bash /path/to/llm-wiki-gsd/install.sh --target /path/to/your-project`
       or, if running from inside the cloned repo against the current directory:
       `./install.sh`
    Include a note about `--backup`: if you have a customised local SKILL.md, pass `--backup`
    to preserve it as SKILL.md.bak before overwriting.
    Include a note about `--help` for full flag reference.

    **Section: ## Setup**
    After installation:
    1. Open Claude Code in your project directory
    2. Run `/sara-init` — this sets up the wiki structure and prompts for your project's
       vertical and department lists

    **Section: ## Commands**
    A brief table of the 8 commands (name + one-line description). Use the `name` and `description`
    fields from the SKILL.md files as the canonical source:
    - sara-init: Initialise a new SARA wiki in the current directory
    - sara-ingest: Register a raw input file as a pipeline item
    - sara-discuss: LLM-guided discussion to agree on extraction intent
    - sara-extract: Present the extraction plan for user approval
    - sara-update: Write approved artifacts to the wiki and commit
    - sara-add-stakeholder: Create a new stakeholder page
    - sara-minutes: Generate meeting minutes from a completed ingest item
    - sara-agenda: Generate a pre-meeting agenda draft

    **Section: ## Updating**
    Re-run install.sh to update. Downgrade protection is built in — if the source version
    is older than what you have installed, a warning is printed and that skill is skipped.
    Use `--force` to override.

    Keep language direct and technical. No marketing language. No emojis. Match the tone
    of the existing PROJECT.md.
  </action>
  <verify>
    <automated>grep -c "install.sh" /home/george/Projects/llm-wiki-gsd/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f README.md && echo ok` — file exists
    - `grep "^# SARA" README.md` returns a match
    - `grep "## Installation" README.md` returns a match
    - `grep "install.sh" README.md` returns at least 2 matches (used in Installation and Updating sections)
    - `grep "/sara-init" README.md` returns a match (post-install next step)
    - `grep "## Commands" README.md` returns a match
    - `grep "sara-minutes" README.md` returns a match (all 8 commands listed)
    - `grep "## Updating" README.md` returns a match
    - File is valid markdown: no unclosed code fences (count of ``` occurrences is even)
  </acceptance_criteria>
  <done>
    README.md exists at repo root with title, Installation, Setup, Commands, and Updating sections,
    all referencing install.sh and /sara-init correctly.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — end-to-end install flow</name>
  <what-built>
    - Plan 01: Added `version: 1.0.0` to all 8 SKILL.md files
    - Plan 02: Created install.sh with git guard, dynamic skill glob, --backup, downgrade protection, and next-step messaging
    - Plan 03 Task 1: Created README.md with Installation, Setup, Commands, and Updating sections
  </what-built>
  <how-to-verify>
    Run the following verification steps manually:

    **Step 1 — Version fields present:**
    ```bash
    grep -r "^version:" .claude/skills/ | sort
    ```
    Expected: 8 lines, each ending in `1.0.0`, one per sara-* skill.

    **Step 2 — install.sh help:**
    ```bash
    ./install.sh --help
    ```
    Expected: usage text printed, exits 0.

    **Step 3 — Guard clause:**
    ```bash
    TMPDIR=$(mktemp -d)
    ./install.sh --target "$TMPDIR" 2>&1 || true
    rm -rf "$TMPDIR"
    ```
    Expected: error message containing "git repository", exits non-zero.

    **Step 4 — Full install to temp target:**
    ```bash
    TMPDIR=$(mktemp -d)
    mkdir "$TMPDIR/.git"
    ./install.sh --target "$TMPDIR"
    ls "$TMPDIR/.claude/skills/"
    rm -rf "$TMPDIR"
    ```
    Expected: 8 sara-* directories listed; output contains "Installed skills:" and
    "Next: open Claude Code in this directory and run /sara-init".

    **Step 5 — Backup flag:**
    ```bash
    TMPDIR=$(mktemp -d)
    mkdir "$TMPDIR/.git"
    ./install.sh --target "$TMPDIR"
    ./install.sh --target "$TMPDIR" --backup
    ls "$TMPDIR/.claude/skills/sara-init/"
    rm -rf "$TMPDIR"
    ```
    Expected: `SKILL.md` and `SKILL.md.bak` both listed for sara-init.

    **Step 6 — README review:**
    Open README.md and confirm it reads clearly. The Installation section should give a
    new user enough information to install SARA without reading any other file.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all steps pass, or describe any issue you found (e.g. "step 4 failed:
    only 6 skills installed") so the executor can fix it before closing the phase.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→README.md | User reads public-facing documentation |
| human verifier→install.sh | Human runs install.sh in a temp directory during verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Information Disclosure | README.md | accept | README is intentionally public; contains no secrets or sensitive paths |
| T-04-09 | Tampering | Verification temp directory | accept | Temp directory is created fresh for each test and deleted after; no persistent state risk |
</threat_model>

<verification>
All verification is performed during the human checkpoint in Task 2. Automated pre-check:
```bash
test -f README.md && grep "## Installation" README.md && grep "install.sh" README.md && echo "README structure OK"
```
</verification>

<success_criteria>
- README.md exists with Installation, Setup, Commands, and Updating sections
- All 6 manual verification steps in Task 2 pass without error
- Human types "approved" at the checkpoint
- Phase 4 is complete and SARA is installable
</success_criteria>

<output>
After completion, create `.planning/phases/04-make-installable/04-03-SUMMARY.md`
</output>
