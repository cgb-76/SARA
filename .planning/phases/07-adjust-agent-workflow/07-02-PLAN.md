---
phase: 7
plan: 02
title: "Delete specialist agent files and update install.sh distribution"
wave: 2
depends_on: ["07-01"]
files_modified:
  - .claude/agents/sara-requirement-extractor.md
  - .claude/agents/sara-decision-extractor.md
  - .claude/agents/sara-action-extractor.md
  - .claude/agents/sara-risk-extractor.md
  - install.sh
autonomous: true
requirements: []
---

<objective>
Delete the four specialist extractor agent files from `.claude/agents/` and update `install.sh` to remove them from the AGENTS distribution array. Only `sara-artifact-sorter.md` remains in the agent distribution.
</objective>

<tasks>

<task id="07-02-T01">
<type>execute</type>
<title>Delete the four specialist agent files</title>
<read_first>
- .claude/agents/sara-requirement-extractor.md — confirm file exists before deleting
- .claude/agents/sara-decision-extractor.md — confirm file exists before deleting
- .claude/agents/sara-action-extractor.md — confirm file exists before deleting
- .claude/agents/sara-risk-extractor.md — confirm file exists before deleting
</read_first>
<action>
Delete the four specialist extractor agent files using Bash:

```bash
rm .claude/agents/sara-requirement-extractor.md
rm .claude/agents/sara-decision-extractor.md
rm .claude/agents/sara-action-extractor.md
rm .claude/agents/sara-risk-extractor.md
```

Do NOT delete `.claude/agents/sara-artifact-sorter.md` — it is retained unchanged.
</action>
<acceptance_criteria>
- `ls .claude/agents/sara-requirement-extractor.md` exits non-zero (file absent)
- `ls .claude/agents/sara-decision-extractor.md` exits non-zero (file absent)
- `ls .claude/agents/sara-action-extractor.md` exits non-zero (file absent)
- `ls .claude/agents/sara-risk-extractor.md` exits non-zero (file absent)
- `ls .claude/agents/sara-artifact-sorter.md` exits zero (sorter retained)
</acceptance_criteria>
</task>

<task id="07-02-T02">
<type>execute</type>
<title>Update install.sh AGENTS array to remove the four specialist files</title>
<read_first>
- install.sh — read the current AGENTS array (lines ~121–130)
</read_first>
<action>
In `install.sh`, replace the AGENTS array block:

```bash
AGENTS=(
  sara-requirement-extractor
  sara-decision-extractor
  sara-action-extractor
  sara-risk-extractor
  sara-artifact-sorter
)
```

With:

```bash
AGENTS=(
  sara-artifact-sorter
)
```

The comment line above the array (`# Known agent files — fixed set for this release`) remains unchanged. All other install.sh content (skills loop, post-install output, flags handling) remains unchanged.
</action>
<acceptance_criteria>
- `grep "sara-requirement-extractor" install.sh` returns no matches
- `grep "sara-decision-extractor" install.sh` returns no matches
- `grep "sara-action-extractor" install.sh` returns no matches
- `grep "sara-risk-extractor" install.sh` returns no matches
- `grep "sara-artifact-sorter" install.sh` returns exactly 1 match (the AGENTS array entry)
- `grep "AGENTS=(" install.sh` returns exactly 1 match (array definition unchanged)
</acceptance_criteria>
</task>

</tasks>

<verification>
1. Run: `ls .claude/agents/` — output must contain only `sara-artifact-sorter.md`
2. Run: `grep -E "sara-(requirement|decision|action|risk)-extractor" install.sh` — must return no matches
3. Run: `grep "sara-artifact-sorter" install.sh` — must return exactly 1 match
4. Run: `bash -n install.sh` — must exit 0 (no syntax errors)
</verification>

<success_criteria>
- `.claude/agents/` directory contains exactly one file: `sara-artifact-sorter.md`
- `install.sh` AGENTS array contains only `sara-artifact-sorter`
- `install.sh` passes bash syntax check (`bash -n install.sh` exits 0)
</success_criteria>

<must_haves>
- All four specialist agent files are deleted
- sara-artifact-sorter.md is NOT deleted
- install.sh AGENTS array contains only sara-artifact-sorter
- install.sh syntax is valid
</must_haves>
