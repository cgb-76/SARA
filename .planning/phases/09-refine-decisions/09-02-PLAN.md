---
phase: 09-refine-decisions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/sara-init/SKILL.md
autonomous: true
requirements:
  - D-07
  - D-08
  - D-09
  - D-10
  - D-11
  - D-12

must_haves:
  truths:
    - "sara-init CLAUDE.md decision schema block (Step 9) reflects v2.0: no context/decision/rationale/alternatives-considered fields, type field present, schema_version single-quoted '2.0', status options are accepted/open/rejected/superseded (no proposed)"
    - "sara-init decision.md template (Step 12) reflects v2.0: same frontmatter shape as schema block, five-section body (Source Quote, Context, Decision, Alternatives Considered, Rationale)"
    - "Both edits are in the same sara-init/SKILL.md file and are consistent with each other"
    - "`organisational` spelling (British English) is used in both schema block and template"
  artifacts:
    - path: ".claude/skills/sara-init/SKILL.md"
      provides: "Updated Step 9 CLAUDE.md decision schema block (v2.0) and Step 12 decision.md template (v2.0)"
      contains: "schema_version: '2.0'"
  key_links:
    - from: ".claude/skills/sara-init/SKILL.md"
      to: ".claude/skills/sara-update/SKILL.md"
      via: "decision.md template defines canonical v2.0 shape; sara-update reads it as the write target"
      pattern: "## Source Quote"
---

<objective>
Update sara-init/SKILL.md in two locations: (1) the decision schema block inside the CLAUDE.md write instruction (Step 9) — replace the v1.0 block with the v2.0 shape; (2) the decision.md template write inside the entity templates instruction (Step 12) — replace the v1.0 template with the v2.0 template including the five-section body.

Purpose: sara-init is the source of truth for the wiki schema. If the template and CLAUDE.md schema block are not updated, every new project initialised after this phase will create decision artifacts with the obsolete v1.0 shape (context/decision/rationale/alternatives-considered frontmatter fields, proposed initial status, schema_version "1.0"). The v2.0 update eliminates this drift.

Output: One modified file — sara-init/SKILL.md with two internal edits (Step 9 schema block and Step 12 decision template block).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-refine-decisions/09-CONTEXT.md
@.planning/phases/09-refine-decisions/09-PATTERNS.md

<interfaces>
<!-- Current decision schema block in sara-init Step 9 CLAUDE.md (lines 210–237) — the block to REPLACE: -->

```
### Decision

```yaml
---
id: DEC-000
title: ""
status: proposed  # proposed | accepted | rejected | superseded
summary: ""  # DEC: options considered, chosen option/recommendation, status, decision date
context: ""
decision: ""
rationale: ""
alternatives-considered: ""
date: ""          # ISO 8601 (e.g. 2026-04-27)
deciders: []      # stakeholder IDs (e.g. [STK-001, STK-002])
supersedes: ""    # DEC-NNN or empty
schema_version: "1.0"
tags: []
related: []
---

## Context

## Decision

## Rationale

## Alternatives Considered
```
```

<!-- Current decision.md template in sara-init Step 12 (lines 420–447) — the block to REPLACE: -->

```
`.sara/templates/decision.md`:

```markdown
---
id: DEC-000
title: ""
status: proposed  # proposed | accepted | rejected | superseded
summary: ""  # DEC: options considered, chosen option/recommendation, status, decision date
context: ""
decision: ""
rationale: ""
alternatives-considered: ""
date: ""          # ISO 8601 (e.g. 2026-04-27)
deciders: []      # stakeholder IDs (e.g. [STK-001, STK-002])
supersedes: ""    # DEC-NNN or empty
schema_version: "1.0"
tags: []
related: []
---

## Context

## Decision

## Rationale

## Alternatives Considered
```
```

<!-- v2.0 frontmatter shape from CONTEXT.md D-07 through D-10 — TARGET for both edits: -->

```yaml
---
id: DEC-NNN
title: ""
status: accepted  # accepted | open | rejected | superseded
summary: ""       # DEC: options considered, chosen option, status, decision date
type: architectural  # architectural | process | tooling | data | business-rule | organisational
date: ""          # ISO 8601 (e.g. 2026-04-29)
deciders: []      # stakeholder IDs (e.g. [STK-001, STK-002])
supersedes: ""    # DEC-NNN or empty
source: []        # ingest IDs (e.g. [MTG-001])
schema_version: '2.0'
tags: []
related: []
---
```

<!-- v2.0 body section order from CONTEXT.md D-11 and D-12 — TARGET for template edit: -->

```markdown
## Source Quote
> [exact verbatim passage from source document] — [[STK-NNN|Stakeholder Name]]

## Context

[Synthesised by sara-update: why this decision was needed, background]

## Decision

[The chosen option — or "No decision reached — alignment required." for open decisions]

## Alternatives Considered

[List of alternatives; for open decisions, list competing positions]

## Rationale

[Synthesised by sara-update: why this option was chosen]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Step 9 — decision schema block in CLAUDE.md write instruction</name>
  <files>.claude/skills/sara-init/SKILL.md</files>
  <read_first>
    - .claude/skills/sara-init/SKILL.md — read the entire file; locate the `### Decision` schema block inside the Step 9 CLAUDE.md write instruction (around line 210). Identify the exact start line (`### Decision`) and end line (the closing triple-backtick after `## Alternatives Considered`). Do not edit any other section.
  </read_first>
  <action>
In sara-init/SKILL.md, find the `### Decision` schema block inside the Step 9 CLAUDE.md write instruction. This block begins with `### Decision` and ends with the closing triple-backtick after the `## Alternatives Considered` section heading.

Replace that entire block (from `### Decision` through the closing triple-backtick) with the following verbatim:

```
### Decision

```yaml
---
id: DEC-000
title: ""
status: accepted  # accepted | open | rejected | superseded
summary: ""  # DEC: options considered, chosen option, status, decision date
type: architectural  # architectural | process | tooling | data | business-rule | organisational
date: ""          # ISO 8601 (e.g. 2026-04-29)
deciders: []      # stakeholder IDs (e.g. [STK-001, STK-002])
supersedes: ""    # DEC-NNN or empty
source: []        # ingest IDs (e.g. [MTG-001])
schema_version: '2.0'
tags: []
related: []
---

## Source Quote
> [exact verbatim passage from source document] — [[STK-NNN|Stakeholder Name]]

## Context

[Synthesised by sara-update: why this decision was needed, background]

## Decision

[The chosen option — or "No decision reached — alignment required." for open decisions]

## Alternatives Considered

[List of alternatives; for open decisions, list competing positions]

## Rationale

[Synthesised by sara-update: why this option was chosen]
```

Body follows the v2.0 structured section format (Source Quote, Context, Decision, Alternatives
Considered, Rationale).
```

Key changes from v1.0:
- REMOVED fields: `context`, `decision`, `rationale`, `alternatives-considered` (narrative fields move to body sections)
- ADDED fields: `type` (six-value taxonomy), `source` (ingest ID list)
- CHANGED: `status` default from `proposed` to `accepted`; status comment removes `proposed` option
- CHANGED: `schema_version` from `"1.0"` (double-quoted) to `'2.0'` (single-quoted)
- ADDED body: five section headings replacing the four v1.0 headings

The `### Action` block that follows the decision block is NOT modified.
  </action>
  <verify>
    <automated>grep -n "schema_version.*'2\.0'\|status.*accepted.*open.*rejected.*superseded\|type.*architectural.*process.*tooling\|## Source Quote" /home/george/Projects/sara/.claude/skills/sara-init/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - sara-init/SKILL.md contains `schema_version: '2.0'` (single-quoted) within the decision schema block
    - sara-init/SKILL.md contains `status: accepted  # accepted | open | rejected | superseded` — no `proposed` in the comment
    - sara-init/SKILL.md contains `type: architectural  # architectural | process | tooling | data | business-rule | organisational` in the decision schema block
    - sara-init/SKILL.md contains `source: []        # ingest IDs` in the decision schema block
    - sara-init/SKILL.md does NOT contain `context: ""` in the decision schema block (removed field)
    - sara-init/SKILL.md does NOT contain `decision: ""` in the decision schema block (removed field)
    - sara-init/SKILL.md does NOT contain `rationale: ""` in the decision schema block (removed field)
    - sara-init/SKILL.md does NOT contain `alternatives-considered: ""` in the decision schema block (removed field)
    - sara-init/SKILL.md does NOT contain `schema_version: "1.0"` in the decision schema block (old double-quoted form)
    - sara-init/SKILL.md contains `## Source Quote` in the decision schema block body
    - sara-init/SKILL.md contains `organisational` (British English) — NOT `organizational`
    - The `### Action` schema block still follows immediately after the decision schema block
  </acceptance_criteria>
  <done>Step 9 decision schema block in sara-init/SKILL.md is fully updated to v2.0. The four narrative frontmatter fields are removed, `type` and `source` are added, `schema_version` is single-quoted '2.0', and the body shows the five-section layout. The rest of the CLAUDE.md write instruction is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Update Step 12 — decision.md template write instruction</name>
  <files>.claude/skills/sara-init/SKILL.md</files>
  <read_first>
    - .claude/skills/sara-init/SKILL.md — the file is already in context from Task 1; locate the `.sara/templates/decision.md` block inside the Step 12 entity templates instruction (around line 420). Identify the exact start line (`.sara/templates/decision.md`:) and end line (the closing triple-backtick after `## Alternatives Considered`).
  </read_first>
  <action>
In sara-init/SKILL.md, find the `.sara/templates/decision.md` template block inside the Step 12 entity templates instruction. This block starts with the line:

```.sara/templates/decision.md`:```

...and ends with the closing triple-backtick after the `## Alternatives Considered` section heading inside the markdown fenced block.

Replace that entire template block with the following verbatim:

```
`.sara/templates/decision.md`:

```markdown
---
id: DEC-000
title: ""
status: accepted  # accepted | open | rejected | superseded
summary: ""  # DEC: options considered, chosen option, status, decision date
type: architectural  # architectural | process | tooling | data | business-rule | organisational
date: ""          # ISO 8601 (e.g. 2026-04-29)
deciders: []      # stakeholder IDs (e.g. [STK-001, STK-002])
supersedes: ""    # DEC-NNN or empty
source: []        # ingest IDs (e.g. [MTG-001])
schema_version: '2.0'
tags: []
related: []
---

## Source Quote
> [exact verbatim passage from source document] — [[STK-NNN|Stakeholder Name]]

## Context

[Synthesised by sara-update: why this decision was needed, background]

## Decision

[The chosen option — or "No decision reached — alignment required." for open decisions]

## Alternatives Considered

[List of alternatives; for open decisions, list competing positions]

## Rationale

[Synthesised by sara-update: why this option was chosen]
```
```

The template frontmatter shape must exactly match the Step 9 CLAUDE.md schema block updated in Task 1. The body section order is: Source Quote → Context → Decision → Alternatives Considered → Rationale.

The `.sara/templates/action.md` template block that follows the decision template is NOT modified.
  </action>
  <verify>
    <automated>grep -n "templates/decision\|schema_version.*'2\.0'\|## Source Quote\|## Alternatives Considered\|organisational" /home/george/Projects/sara/.claude/skills/sara-init/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - sara-init/SKILL.md contains a `.sara/templates/decision.md` template block with `schema_version: '2.0'` (single-quoted)
    - The decision template contains `type: architectural  # architectural | process | tooling | data | business-rule | organisational`
    - The decision template contains `source: []        # ingest IDs`
    - The decision template does NOT contain `context: ""` (removed field)
    - The decision template does NOT contain `decision: ""` (removed field)
    - The decision template does NOT contain `rationale: ""` (removed field)
    - The decision template does NOT contain `alternatives-considered: ""` (removed field)
    - The decision template does NOT contain `schema_version: "1.0"` (old form)
    - The decision template contains `## Source Quote` as the first body section
    - The decision template body sections appear in order: `## Source Quote`, `## Context`, `## Decision`, `## Alternatives Considered`, `## Rationale`
    - `grep -c "## Source Quote" /home/george/Projects/sara/.claude/skills/sara-init/SKILL.md` returns 2 (one in Step 9 schema block, one in Step 12 template)
    - The decision template in Step 12 matches the schema block in Step 9 — same frontmatter fields, same order
    - `grep "proposed" /home/george/Projects/sara/.claude/skills/sara-init/SKILL.md` returns zero results within decision schema block and decision template (both must be `proposed`-free)
  </acceptance_criteria>
  <done>Step 12 decision.md template in sara-init/SKILL.md is fully updated to v2.0, matching the Step 9 schema block exactly. The four narrative frontmatter fields are removed, `type` and `source` are added, schema_version is single-quoted '2.0', and the body has five sections in the correct order. The action template that follows is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sara-init template → new project CLAUDE.md | Template text becomes the auto-loaded schema reference for all future wiki writes; incorrect field names here propagate to every new project |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Tampering | sara-init/SKILL.md decision schema block | accept | Skill files are author-controlled markdown; no runtime injection path. Two-location edit (Step 9 and Step 12) verified independently via grep. |
| T-09-06 | Tampering | YAML float parse (schema_version: 2.0 without quotes) | mitigate | Explicitly use single-quoted form `'2.0'` in both Step 9 and Step 12. Acceptance criteria grep for single-quoted form. If double-quoted (`"2.0"`) or unquoted (`2.0`) appears, YAML parsers treat it as float. |
| T-09-07 | Information Disclosure | Narrative decision content in frontmatter fields | accept | The four removed fields (context, decision, rationale, alternatives-considered) were free-text frontmatter — moving them to body sections is a format improvement with no security impact. |
</threat_model>

<verification>
After both tasks complete:

1. `grep -n "schema_version" /home/george/Projects/sara/.claude/skills/sara-init/SKILL.md` — decision block must show `schema_version: '2.0'` (single-quoted) in both Step 9 and Step 12 locations; no `schema_version: "1.0"` in decision blocks.
2. `grep -n "proposed" /home/george/Projects/sara/.claude/skills/sara-init/SKILL.md` — must return zero results within decision schema block and decision template.
3. `grep -n "context:\|decision:\|rationale:\|alternatives-considered:" /home/george/Projects/sara/.claude/skills/sara-init/SKILL.md` — must return zero results within decision blocks (narrative fields removed).
4. `grep -n "## Source Quote" /home/george/Projects/sara/.claude/skills/sara-init/SKILL.md` — must return 2 results (one in Step 9, one in Step 12).
5. `grep -n "organisational" /home/george/Projects/sara/.claude/skills/sara-init/SKILL.md` — must show British English spelling in both decision blocks; `grep "organizational"` must return zero results.
6. Confirm the `### Action` block in Step 9 and the `.sara/templates/action.md` block in Step 12 are unchanged.
</verification>

<success_criteria>
- schema_version bumped to '2.0' with single quotes in both Step 9 schema block and Step 12 template (D-07)
- Four narrative frontmatter fields (context, decision, rationale, alternatives-considered) removed from both locations (D-08)
- `type` frontmatter field added with six-value taxonomy in both locations (D-09)
- Status initial value is `accepted` with comment listing `accepted | open | rejected | superseded` — no `proposed` (D-10)
- Five-section body (Source Quote, Context, Decision, Alternatives Considered, Rationale) present in both locations (D-11, D-12)
- Step 9 and Step 12 decision shapes are identical and consistent with CONTEXT.md D-07 through D-12
</success_criteria>

<output>
After completion, create `.planning/phases/09-refine-decisions/09-02-SUMMARY.md`
</output>
