---
phase: 09-refine-decisions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/sara-extract/SKILL.md
  - .claude/agents/sara-artifact-sorter.md
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-06

must_haves:
  truths:
    - "sara-extract Step 3 decisions pass only extracts passages containing commitment or misalignment language from the signal lists"
    - "Commitment-language decisions yield status: accepted; misalignment-language decisions yield status: open"
    - "`proposed` never appears as an output value in the decisions pass"
    - "Each extracted decision has dec_type (one of six taxonomy values), chosen_option, and alternatives fields set in the same inline pass"
    - "The extraction prompt contains at least three named negative examples of passages that are NOT decisions"
    - "sara-artifact-sorter output schema decision example includes status, dec_type, chosen_option, and alternatives fields"
    - "Sorter passthrough rule for decision artifacts is present and parallel to the existing requirement passthrough rule"
  artifacts:
    - path: ".claude/skills/sara-extract/SKILL.md"
      provides: "Rewritten decisions pass with two-signal detection, six-type taxonomy, and four new artifact fields"
      contains: "COMMITMENT language"
    - path: ".claude/agents/sara-artifact-sorter.md"
      provides: "Updated decision object examples and passthrough rule for decision-specific fields"
      contains: "dec_type"
  key_links:
    - from: ".claude/skills/sara-extract/SKILL.md"
      to: ".claude/agents/sara-artifact-sorter.md"
      via: "Task() call — merged artifact array includes status, dec_type, chosen_option, alternatives"
      pattern: "dec_type.*chosen_option"
---

<objective>
Rewrite the sara-extract decisions pass to use two-signal detection (commitment language → status: accepted; misalignment language → status: open), add inline six-type classification (dec_type), and capture chosen_option and alternatives per artifact. Fix the sara-artifact-sorter output schema to pass all four new decision fields through to the approval loop.

Purpose: The current decisions pass ("a deliberate choice made by the team that was concluded") produces false positives and misses contested decisions. Two-signal detection captures both aligned and unresolved decisions as first-class artifacts. The sorter schema fix ensures dec_type, status, chosen_option, and alternatives survive the sorter Task() and reach sara-update.

Output: Two modified files — sara-extract/SKILL.md (Step 3 decisions pass block replaced) and sara-artifact-sorter.md (output_format decision examples updated, passthrough rule added).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-refine-decisions/09-CONTEXT.md
@.planning/phases/09-refine-decisions/09-RESEARCH.md
@.planning/phases/09-refine-decisions/09-PATTERNS.md

<interfaces>
<!-- Current decisions pass in sara-extract/SKILL.md (lines 89–97) — the block to REPLACE in full: -->

```
**Decisions pass**

Extract every passage that describes a decision — a deliberate choice made by the team that was
concluded, not just discussed ("we will use X" is a decision; "we could use X" is not). For each
decision found:
- Extract the exact verbatim passage as `source_quote` (MANDATORY)
- Write a short (≤10 words) noun-phrase `title`
- Set `raised_by` to the STK-NNN ID if identifiable; otherwise `"STK-NNN"` placeholder
- Set `action` = `"create"`, `type` = `"decision"`, `id_to_assign` = `"DEC-NNN"`, `related` = `[]`,
  `change_summary` = `""`

Collect results as `{dec_artifacts}` (JSON array; empty array if none found).
```

<!-- Current sorter decision object examples (update example, lines 100–108) — to be EXTENDED: -->

```json
    {
      "action": "update",
      "type": "decision",
      "existing_id": "DEC-003",
      "title": "Title of existing decision",
      "source_quote": "Exact verbatim text from source document motivating this update",
      "raised_by": "STK-NNN",
      "related": ["REQ-005"],
      "change_summary": "Add new context from this source document"
    },
```

<!-- Current requirement passthrough rule (lines 131–134) — the PATTERN to mirror for decisions: -->

```
- For requirement artifacts, preserve `priority` and `req_type` exactly as received from the
  extraction pass. Do not modify, reclassify, or drop these fields. Pass them through unchanged
  to `cleaned_artifacts`.
- For requirement update artifacts (`action=update`, `type=requirement`), `priority` and `req_type`
  MUST be present — copy them from the incoming create artifact unchanged. sara-update reads these
  fields for all requirement artifacts regardless of action.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite sara-extract Step 3 decisions pass</name>
  <files>.claude/skills/sara-extract/SKILL.md</files>
  <read_first>
    - .claude/skills/sara-extract/SKILL.md — read the entire file to locate the `**Decisions pass**` block (lines 89–97); do not edit any other section
  </read_first>
  <action>
Replace the entire `**Decisions pass**` block (from the `**Decisions pass**` header line through `Collect results as {dec_artifacts} (JSON array; empty array if none found).`) with the following new block verbatim. All other Step 3 content — Requirements pass, Actions pass, Risks pass, Merge and sorter dispatch — is unchanged.

New `**Decisions pass**` block to write verbatim:

```
**Decisions pass**

A passage IS a decision if it contains commitment language OR misalignment language from the
signal lists below. Passages lacking both signals are NOT decisions regardless of topic.

  COMMITMENT language — these passages ARE decisions → status: accepted
  - "we decided to", "we decided on"
  - "we chose", "we have chosen"
  - "we agreed on", "we agreed to"
  - "we went with"
  - "we will use" (as a settled commitment, not hypothetical)
  - "the approach is", "our approach is"
  - "we have decided"
  - Similar definitive past- or present-tense alignment phrases where the team is unified

  MISALIGNMENT language — these passages ARE decisions → status: open
  - Explicit disagreement: "Alice prefers X, but Bob argues Y"
  - Unresolved choice: "we need to decide between A and B"
  - Competing preferences: "there are two camps — those who want X vs those who want Y"
  - "we haven't agreed on", "still open", "not yet decided"
  - Documented tension without resolution

  EXCLUDE — these passages are NOT decisions (do NOT extract them):
  - Option exploration: "we could use X or Y" (exploring options, not choosing)
  - Aspiration/wish: "it would be good to have Z" (desire, no concluded choice or tension)
  - Requirement/obligation: "the system must support A" (system obligation, not a team choice)

For each decision found, classify it into one of six types inline based on what the decision is about:
  - `architectural`   — system structure, technology choices, component relationships
  - `process`         — how the team works, workflow, ceremonies, practices
  - `tooling`         — software tools, libraries, platforms selected
  - `data`            — data model, storage, retention, ownership rules
  - `business-rule`   — domain logic, policy decisions
  - `organisational`  — team structure, ownership, roles, responsibilities

For each decision found:
- Extract the exact verbatim passage as `source_quote` (MANDATORY — skip any decision without a quotable passage)
- Write a short (≤10 words) noun-phrase `title`
- Set `raised_by` to the STK-NNN ID if identifiable from the source or discussion_notes; otherwise use `"STK-NNN"` placeholder
- Set `status` to `"accepted"` if commitment language was detected; `"open"` if misalignment language was detected
- Set `dec_type` to one of the six types above (use `dec_type` — not `type` — to avoid collision with the envelope `type: "decision"` field)
- Set `chosen_option` to the selected option text for commitment-language decisions; set to `""` for open/misalignment decisions
- Set `alternatives` to an array of alternatives mentioned in the source (strings); set to `[]` if none are mentioned
- Set `action` = `"create"`, `type` = `"decision"`, `id_to_assign` = `"DEC-NNN"`, `related` = `[]`, `change_summary` = `""`
- Do NOT extract `context` or `rationale` — these are synthesised by sara-update from the full source document, not extracted here

Collect results as `{dec_artifacts}` (JSON array; empty array if none found).
```

Critical: `dec_type` (not `type`) is the artifact JSON field name for the decision taxonomy — this prevents collision with the envelope `type: "decision"` field. sara-update maps `artifact.dec_type` → wiki frontmatter `type`.
  </action>
  <verify>
    <automated>grep -n "COMMITMENT language\|MISALIGNMENT language\|EXCLUDE.*these passages are NOT decisions\|dec_type\|chosen_option\|alternatives" /home/george/Projects/sara/.claude/skills/sara-extract/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - sara-extract/SKILL.md contains the line `COMMITMENT language — these passages ARE decisions → status: accepted`
    - sara-extract/SKILL.md contains the line `MISALIGNMENT language — these passages ARE decisions → status: open`
    - sara-extract/SKILL.md contains `EXCLUDE — these passages are NOT decisions (do NOT extract them):`
    - sara-extract/SKILL.md contains exactly three EXCLUDE negative example types: `Option exploration:`, `Aspiration/wish:`, `Requirement/obligation:`
    - sara-extract/SKILL.md contains `Set \`status\` to \`"accepted"\` if commitment language was detected; \`"open"\` if misalignment language was detected`
    - sara-extract/SKILL.md contains `Set \`dec_type\` to one of the six types above`
    - sara-extract/SKILL.md contains `Set \`chosen_option\``
    - sara-extract/SKILL.md contains `Set \`alternatives\``
    - sara-extract/SKILL.md contains `organisational` (British English spelling) in the type taxonomy
    - sara-extract/SKILL.md does NOT contain `organizational` (American English)
    - sara-extract/SKILL.md does NOT contain `"proposed"` anywhere in the decisions pass block
    - sara-extract/SKILL.md does NOT contain `dec_type.*type` collision — the field is `dec_type`, not `type`
    - The original `Extract every passage that describes a decision — a deliberate choice made by the team that was concluded` sentence is no longer present (replaced)
    - The `**Actions pass**` block remains intact and immediately follows the decisions pass
    - `grep -c "Do NOT extract.*context.*rationale" .claude/skills/sara-extract/SKILL.md` returns 1
  </acceptance_criteria>
  <done>The decisions pass block in sara-extract/SKILL.md Step 3 is fully replaced with the two-signal detector. Both signal groups (COMMITMENT and MISALIGNMENT), three EXCLUDE negative examples, six-type dec_type taxonomy, and four new artifact fields (status, dec_type, chosen_option, alternatives) are present. The block from `**Actions pass**` onward is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Fix sara-artifact-sorter decision schema for passthrough of new fields</name>
  <files>.claude/agents/sara-artifact-sorter.md</files>
  <read_first>
    - .claude/agents/sara-artifact-sorter.md — read the entire file to locate: (a) the decision update object example in output_format (lines ~100–108), (b) the requirement passthrough rule (lines ~131–134)
  </read_first>
  <action>
Three edits to sara-artifact-sorter.md. Do not touch the sorter's deduplication logic, create-vs-update resolution, or question generation.

**Edit A — Add a decision create object example to `<output_format>`:**

Find the decision update example object (the object with `"action": "update", "type": "decision"`). Immediately AFTER that object's closing `},`, insert the following new create decision object:

```json
    {
      "action": "create",
      "type": "decision",
      "id_to_assign": "DEC-NNN",
      "title": "Short title",
      "source_quote": "Exact verbatim text from source document",
      "raised_by": "STK-NNN",
      "related": [],
      "change_summary": "",
      "status": "accepted",
      "dec_type": "architectural",
      "chosen_option": "The selected option",
      "alternatives": ["Alternative A", "Alternative B"]
    },
```

**Edit B — Extend the decision update object example with decision-specific fields:**

Find the existing decision update example:
```json
    {
      "action": "update",
      "type": "decision",
      "existing_id": "DEC-003",
      "title": "Title of existing decision",
      "source_quote": "Exact verbatim text from source document motivating this update",
      "raised_by": "STK-NNN",
      "related": ["REQ-005"],
      "change_summary": "Add new context from this source document"
    },
```

Replace it with:
```json
    {
      "action": "update",
      "type": "decision",
      "existing_id": "DEC-003",
      "title": "Title of existing decision",
      "source_quote": "Exact verbatim text from source document motivating this update",
      "raised_by": "STK-NNN",
      "related": ["REQ-005"],
      "change_summary": "Add new context from this source document",
      "status": "accepted",
      "dec_type": "tooling",
      "chosen_option": "The selected option",
      "alternatives": []
    },
```

**Edit C — Add decision passthrough rule immediately after the existing requirement passthrough rule:**

Find the requirement passthrough rules block (the lines starting with "For requirement artifacts, preserve `priority` and `req_type`..."). Immediately AFTER the last line of that block, add:

```
- For decision artifacts, preserve `status`, `dec_type`, `chosen_option`, and `alternatives` exactly as received from the extraction pass. Do not modify, reclassify, or drop these fields. Pass them through unchanged to `cleaned_artifacts`.
- For decision update artifacts (`action=update`, `type=decision`), `status`, `dec_type`, `chosen_option`, and `alternatives` MUST be present — copy them from the incoming create artifact unchanged. sara-update reads these fields for all decision artifacts regardless of action.
```

The existing requirement passthrough rule lines are NOT modified or removed — only the new decision rule is added after them.
  </action>
  <verify>
    <automated>grep -n "dec_type\|chosen_option\|alternatives.*Alternative\|decision artifacts, preserve" /home/george/Projects/sara/.claude/agents/sara-artifact-sorter.md</automated>
  </verify>
  <acceptance_criteria>
    - sara-artifact-sorter.md contains `"dec_type": "architectural"` in a create decision object example
    - sara-artifact-sorter.md contains `"dec_type": "tooling"` in an update decision object example
    - sara-artifact-sorter.md contains `"status": "accepted"` in at least one decision object example
    - sara-artifact-sorter.md contains `"chosen_option": "The selected option"` in the create decision object example
    - sara-artifact-sorter.md contains `"alternatives": ["Alternative A", "Alternative B"]` in the create decision object
    - sara-artifact-sorter.md contains `For decision artifacts, preserve \`status\`, \`dec_type\`, \`chosen_option\`, and \`alternatives\``
    - sara-artifact-sorter.md still contains the original requirement passthrough rule (`For requirement artifacts, preserve \`priority\` and \`req_type\``) — it was not removed
    - The sorter process steps (1–6) are unchanged — `grep -c "Step [1-6]" .claude/agents/sara-artifact-sorter.md` returns the same count as before
  </acceptance_criteria>
  <done>The sorter output schema includes both create and update decision object examples with all four new fields (status, dec_type, chosen_option, alternatives). A parallel decision passthrough rule is present alongside the existing requirement passthrough rule. The sorter's deduplication and resolution logic is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill text → LLM prompt | The rewritten decisions pass becomes the LLM's instruction; signal phrase lists must be precise enough to avoid false positives and false negatives |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | sara-extract/SKILL.md decisions pass | accept | Skill files are author-controlled markdown; no runtime input injection path. Changes reviewed via git diff before commit. |
| T-09-02 | Information Disclosure | sara-artifact-sorter.md output_format | accept | Agent output is JSON returned in-context; no external persistence or network call. Low risk. |
| T-09-03 | Denial of Service | Extraction pass over-extraction (too many decision artifacts) | mitigate | Three EXCLUDE negative examples in the prompt explicitly block option exploration, aspirations, and requirement obligations — reduces false positives that would bloat the approval loop. |
| T-09-04 | Tampering | dec_type field collision with envelope type | mitigate | Explicit use of `dec_type` (not `type`) as the artifact JSON field name; noted in extraction prompt and sorter passthrough rule. If the field were named `type`, the taxonomy value would overwrite the entity class silently. |
</threat_model>

<verification>
After both tasks complete:

1. `grep -n "COMMITMENT language\|MISALIGNMENT language\|EXCLUDE" /home/george/Projects/sara/.claude/skills/sara-extract/SKILL.md` — must show all three section headers in the decisions pass.
2. `grep -n "proposed" /home/george/Projects/sara/.claude/skills/sara-extract/SKILL.md` — must return zero results in the decisions pass block.
3. `grep -n "dec_type\|chosen_option\|alternatives" /home/george/Projects/sara/.claude/skills/sara-extract/SKILL.md` — must show the field assignment instructions in the decisions pass.
4. `grep -n "dec_type\|chosen_option" /home/george/Projects/sara/.claude/agents/sara-artifact-sorter.md` — must show the decision object examples and passthrough rule.
5. `grep -n "organisational" /home/george/Projects/sara/.claude/skills/sara-extract/SKILL.md` — must show the British English spelling; `grep "organizational"` must return zero results.
6. Confirm `**Actions pass**` block in sara-extract/SKILL.md is intact and immediately follows the decisions pass.
7. Confirm the requirement passthrough rule in sara-artifact-sorter.md (priority/req_type) is still present and unchanged.
</verification>

<success_criteria>
- Decisions pass uses two-signal detection: commitment language → accepted, misalignment language → open (D-01)
- `proposed` is not present as an output value anywhere in the decisions pass (D-02)
- `status` field is set in the artifact JSON by the extraction pass based on detected signal (D-03)
- `source_quote`, `chosen_option`, `alternatives`, and `dec_type` are all captured per artifact in the same inline pass (D-04, D-06)
- Context and Rationale are explicitly excluded from the extraction artifact (D-05)
- `dec_type` (not `type`) is used for the decision taxonomy in artifact JSON to avoid envelope collision
- sara-artifact-sorter will pass all four decision-specific fields through to the approval loop without dropping them
- British English `organisational` spelling used throughout
</success_criteria>

<output>
After completion, create `.planning/phases/09-refine-decisions/09-01-SUMMARY.md`
</output>
