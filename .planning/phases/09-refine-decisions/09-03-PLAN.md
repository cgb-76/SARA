---
phase: 09-refine-decisions
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
  - 09-02
files_modified:
  - .claude/skills/sara-update/SKILL.md
autonomous: true
requirements:
  - D-03
  - D-05
  - D-10
  - D-11
  - D-12

must_haves:
  truths:
    - "sara-update decision create branch writes v2.0 frontmatter: type from artifact.dec_type, status from artifact.status (never hardcoded 'proposed'), schema_version single-quoted '2.0', no context/decision/rationale/alternatives-considered fields"
    - "sara-update decision create branch writes five-section body: ## Source Quote (verbatim with wikilink attribution), ## Context (synthesised), ## Decision (from chosen_option or 'No decision reached — alignment required.' for open), ## Alternatives Considered (from alternatives field or competing positions), ## Rationale (synthesised)"
    - "sara-update decision update branch also rewrites frontmatter and body to v2.0 format (parallel to the requirement update branch pattern)"
    - "sara-update summary generation for DEC has an open-decision variant: 'competing options/positions, alignment not reached, status: open, decision date'"
    - "`proposed` does not appear anywhere in the sara-update decision create or update branches"
  artifacts:
    - path: ".claude/skills/sara-update/SKILL.md"
      provides: "Updated decision create and update branches for v2.0 frontmatter and body structure"
      contains: "artifact.dec_type"
  key_links:
    - from: "artifact.dec_type"
      to: "wiki page frontmatter type:"
      via: "sara-update Step 2 decision create branch field mapping"
      pattern: "type.*artifact\\.dec_type"
    - from: "artifact.status"
      to: "wiki page frontmatter status:"
      via: "sara-update Step 2 decision create branch (replaces hardcoded 'proposed')"
      pattern: "status.*artifact\\.status"
---

<objective>
Rewrite the sara-update Step 2 decision create and update branches to write v2.0 frontmatter and body structure: read status and dec_type from the artifact object (instead of hardcoding), remove four narrative frontmatter fields, add type field, bump schema_version to single-quoted '2.0', and write the five-section body (Source Quote, Context, Decision, Alternatives Considered, Rationale) with conditional content for accepted vs open decisions.

Purpose: sara-update is the only skill that writes decision wiki pages. Without this change, running sara-update after Phase 9 would produce v1.0 pages with the wrong status (proposed), the wrong frontmatter shape, and the wrong body structure — even though sara-extract now correctly produces the new artifact fields.

Output: One modified file — sara-update/SKILL.md with the decision create branch frontmatter rules updated, the decision body block replaced with the five-section v2.0 layout, the update branch extended with the v2.0 field mapping and body rewrite, and the DEC summary generation rule updated with the open-decision variant.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-refine-decisions/09-CONTEXT.md
@.planning/phases/09-refine-decisions/09-RESEARCH.md
@.planning/phases/09-refine-decisions/09-PATTERNS.md
@.planning/phases/09-refine-decisions/09-01-SUMMARY.md
@.planning/phases/09-refine-decisions/09-02-SUMMARY.md

<interfaces>
<!-- Current frontmatter field mapping block in sara-update Step 2 (lines 87–90) — the lines to EDIT: -->

```
    - `schema_version` = `"1.0"` for decision, action, and risk artifacts (always double-quoted); `schema_version` = `'2.0'` for requirement artifacts (single-quoted — prevents YAML float parsing)
    - `type` = `artifact.req_type` for requirement artifacts (one of: functional, non-functional, regulatory, integration, business-rule, data)
    - `priority` = `artifact.priority` for requirement artifacts (one of: must-have, should-have, could-have, wont-have)
    - For decision artifacts: set `status` = `"proposed"`, `date` = today's ISO date
```

<!-- Current decision body block in sara-update Step 2 (lines 205–224) — the block to REPLACE: -->

```
    **decision:**
    ```
    ## Context
    > "{artifact.source_quote}" — {stakeholder_name}

    {synthesised summary of the situation or problem that prompted this decision, drawn from
     the source document and discussion notes}

    ## Decision
    {synthesised statement of what was decided, drawn from the artifact title and any
     resolution captured in discussion notes — leave empty if not clearly stated}

    ## Rationale
    {synthesised explanation of why this decision was made, drawn from discussion notes and
     source context — leave empty if not available}

    ## Alternatives Considered
    {synthesised list of alternatives mentioned in the source or discussion notes — leave
     empty if none were discussed}
    ```
```

<!-- Current DEC summary generation rule (line 98) — the line to EXTEND with open-decision variant: -->

```
      - DEC: options considered, chosen option/recommendation, status, decision date
```

<!-- Requirement update branch (lines 269–290) — the PATTERN to mirror for the decision update branch: -->

```
    For requirement artifacts (`artifact.type == "requirement"`): after applying the change_summary
    to frontmatter fields and regenerating the summary, also update the frontmatter to include
    the v2.0 fields from the artifact object:
    - Set `type` = `artifact.req_type` (one of: functional, non-functional, regulatory, integration, business-rule, data)
    - Set `priority` = `artifact.priority` (one of: must-have, should-have, could-have, wont-have)
    - Set `schema_version` = `'2.0'` (single-quoted string — prevents YAML float parsing)
    - Remove the `description` field from the frontmatter if present (it is a v1.0 field)

    Then rewrite the full body to the v2.0 structured section format (Source Quote, Statement,
    User Story, Acceptance Criteria, BDD Criteria, Context, Cross Links) using the same synthesis
    rules as the create branch. Synthesise section content from the updated frontmatter,
    artifact.source_quote, artifact.change_summary, and {discussion_notes}. Apply the section
    matrix (per artifact.req_type) to determine which sections to include and which to omit.
```

<!-- Wikilink attribution pattern (lines 118–120) — apply verbatim for Source Quote section: -->

```
    > "{artifact.source_quote}" — [[{artifact.raised_by}|{stakeholder_name}]]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update decision create branch — frontmatter rules and body block</name>
  <files>.claude/skills/sara-update/SKILL.md</files>
  <read_first>
    - .claude/skills/sara-update/SKILL.md — read the entire Step 2 section; locate: (a) the frontmatter field mapping block (lines ~87–90), specifically the line `For decision artifacts: set status = "proposed"...`; (b) the decision body block (lines ~205–224); (c) the DEC summary generation rule (line ~98). Do not edit any other section.
  </read_first>
  <action>
Three targeted edits to sara-update/SKILL.md. All other Step 2 content — requirement, action, risk, and stakeholder create branches — is unchanged.

**Edit A — Replace the decision frontmatter lines in the field mapping block:**

Find the line block (lines ~87–90):
```
    - `schema_version` = `"1.0"` for decision, action, and risk artifacts (always double-quoted); `schema_version` = `'2.0'` for requirement artifacts (single-quoted — prevents YAML float parsing)
    - `type` = `artifact.req_type` for requirement artifacts (one of: functional, non-functional, regulatory, integration, business-rule, data)
    - `priority` = `artifact.priority` for requirement artifacts (one of: must-have, should-have, could-have, wont-have)
    - For decision artifacts: set `status` = `"proposed"`, `date` = today's ISO date
```

Replace it with:
```
    - `schema_version` = `'2.0'` for decision artifacts (single-quoted — prevents YAML float parsing; consistent with requirement schema established in Phase 8); `schema_version` = `"1.0"` for action and risk artifacts (always double-quoted)
    - `type` = `artifact.req_type` for requirement artifacts (one of: functional, non-functional, regulatory, integration, business-rule, data)
    - `type` = `artifact.dec_type` for decision artifacts (one of: architectural, process, tooling, data, business-rule, organisational)
    - `priority` = `artifact.priority` for requirement artifacts (one of: must-have, should-have, could-have, wont-have)
    - For decision artifacts: set `status` = `artifact.status` (either `"accepted"` or `"open"` from the extraction pass — NEVER hardcode `"proposed"`), `date` = today's ISO date
    - For decision artifacts: do NOT write `context`, `decision`, `rationale`, or `alternatives-considered` frontmatter fields — these are v1.0 fields removed in schema v2.0
```

**Edit B — Replace the decision body block:**

Find the `**decision:**` body block (lines ~205–224). Replace the entire block (from `**decision:**` through the closing triple-backtick) with:

```
    **decision:**
    ```
    ## Source Quote
    > "{artifact.source_quote}" — [[{artifact.raised_by}|{stakeholder_name}]]

    ## Context
    {Synthesised from {source_doc} and {discussion_notes}: why this decision was needed,
     what problem or situation prompted it, relevant background. Leave empty (heading only)
     if nothing relevant is available — never fabricate.}

    ## Decision
    {If artifact.status == "accepted": write artifact.chosen_option content — the option
     or approach the team selected. If artifact.chosen_option is an empty string, synthesise
     the decision from artifact.title and {discussion_notes}.
     If artifact.status == "open": write exactly "No decision reached — alignment required."}

    ## Alternatives Considered
    {If artifact.status == "accepted": if artifact.alternatives is a non-empty array, list
     each alternative on its own line with a dash prefix, then expand with synthesis if
     the source document or discussion notes mention why each alternative was not chosen.
     If artifact.alternatives is [], write this heading with no content (heading only).
     If artifact.status == "open": list the competing positions detected in the source.
     Each position on its own line with a dash prefix. Example:
     - Position A: [view expressed by stakeholder or group]
     - Position B: [opposing view]}

    ## Rationale
    {Synthesised from {source_doc} and {discussion_notes}: for accepted decisions, why
     this option was chosen over the alternatives. For open decisions, why alignment has
     not been reached (what is blocking agreement). Leave empty (heading only) if nothing
     relevant is available — never fabricate.}
    ```
```

**Edit C — Extend the DEC summary generation rule with open-decision variant:**

Find the line (line ~98):
```
      - DEC: options considered, chosen option/recommendation, status, decision date
```

Replace it with:
```
      - DEC (status=accepted): options considered, chosen option, status: accepted, decision date
      - DEC (status=open): competing options/positions, alignment not reached, status: open, decision date
```
  </action>
  <verify>
    <automated>grep -n "artifact\.dec_type\|artifact\.status\|proposed\|## Source Quote\|No decision reached" /home/george/Projects/sara/.claude/skills/sara-update/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - sara-update/SKILL.md contains `type` = `artifact.dec_type` for decision artifacts in the frontmatter mapping block
    - sara-update/SKILL.md contains `status` = `artifact.status` (either `"accepted"` or `"open"` from the extraction pass — NEVER hardcode `"proposed"`)
    - sara-update/SKILL.md contains `schema_version` = `'2.0'` for decision artifacts (single-quoted)
    - sara-update/SKILL.md does NOT contain `status` = `"proposed"` in the decision create branch (old hardcoded value)
    - sara-update/SKILL.md does NOT contain `context`, `decision`, `rationale`, `alternatives-considered` frontmatter field write instructions in the decision create branch
    - sara-update/SKILL.md contains `## Source Quote` as the first section in the decision body block
    - sara-update/SKILL.md contains `[[{artifact.raised_by}|{stakeholder_name}]]` in the decision Source Quote line (wikilink form)
    - sara-update/SKILL.md contains `No decision reached — alignment required.` as the `## Decision` content for open decisions
    - sara-update/SKILL.md contains `artifact.chosen_option` in the `## Decision` section instructions
    - sara-update/SKILL.md contains `artifact.alternatives` in the `## Alternatives Considered` section instructions
    - sara-update/SKILL.md contains `DEC (status=accepted):` in the summary generation rule
    - sara-update/SKILL.md contains `DEC (status=open):` in the summary generation rule
    - sara-update/SKILL.md contains `organisational` (British English) in the dec_type taxonomy line — NOT `organizational`
    - The requirement create body block (`**requirement:**`) is unchanged
    - The action create body block (`**action:**`) is unchanged
    - `grep -c "proposed" /home/george/Projects/sara/.claude/skills/sara-update/SKILL.md` returns 0 in the decision create branch context
  </acceptance_criteria>
  <done>The decision create branch in sara-update/SKILL.md writes v2.0 frontmatter (type from artifact.dec_type, status from artifact.status, schema_version '2.0', no narrative fields) and a five-section body with conditional content for accepted vs open decisions. The DEC summary generation has two variants for the two status values.</done>
</task>

<task type="auto">
  <name>Task 2: Update decision update branch — v2.0 field mapping and body rewrite</name>
  <files>.claude/skills/sara-update/SKILL.md</files>
  <read_first>
    - .claude/skills/sara-update/SKILL.md — file already in context from Task 1; locate the requirement update branch block (lines ~269–290) which begins `For requirement artifacts (`artifact.type == "requirement"`):`; then find where the analogous decision update branch logic should be inserted immediately after it (or identify if a decision-specific update clause already exists). Also locate line ~268 which applies change_summary to frontmatter fields.
  </read_first>
  <action>
In sara-update/SKILL.md, find the requirement update branch block (the block beginning `For requirement artifacts (`artifact.type == "requirement"`):` and ending before the `Use the Write tool to overwrite` line). This block adds v2.0 fields, removes the description field, and rewrites the full body.

Immediately AFTER the closing of the requirement update branch block (and BEFORE the `Use the Write tool to overwrite` line), insert the following decision update branch block:

```
    For decision artifacts (`artifact.type == "decision"`): after applying the change_summary
    to frontmatter fields and regenerating the summary, also update the frontmatter to include
    the v2.0 fields from the artifact object:
    - Set `type` = `artifact.dec_type` (one of: architectural, process, tooling, data, business-rule, organisational)
    - Set `status` = `artifact.status` (either `"accepted"` or `"open"` from the artifact — do NOT keep any existing `"proposed"` value if present in the existing page)
    - Set `schema_version` = `'2.0'` (single-quoted string — prevents YAML float parsing)
    - Remove the following v1.0 frontmatter fields if present: `context`, `decision`, `rationale`, `alternatives-considered`
    - Add `source: [{item.id}]` if not already present (convert scalar source field to list following existing update branch source-field rule)

    Then rewrite the full body to the v2.0 structured section format using the same synthesis
    rules as the create branch:
    ## Source Quote
    > "{artifact.source_quote}" — [[{artifact.raised_by}|{stakeholder_name}]]

    ## Context
    {Synthesised from {source_doc}, {discussion_notes}, and artifact.change_summary: updated
     background for why this decision exists. Never fabricate.}

    ## Decision
    {If artifact.status == "accepted": write artifact.chosen_option content.
     If artifact.status == "open": write "No decision reached — alignment required."}

    ## Alternatives Considered
    {If artifact.status == "accepted": list from artifact.alternatives (non-empty) or heading-only.
     If artifact.status == "open": list competing positions from source.}

    ## Rationale
    {Synthesised from {source_doc} and {discussion_notes}: why this option was chosen or
     why alignment was not reached. Leave empty (heading only) if nothing relevant — never fabricate.}
```
  </action>
  <verify>
    <automated>grep -n "artifact\.type.*decision\|decision.*artifact\.type\|dec_type.*one of.*architectural\|Remove.*context.*decision.*rationale" /home/george/Projects/sara/.claude/skills/sara-update/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - sara-update/SKILL.md contains `For decision artifacts (\`artifact.type == "decision"\`):` as the header for the decision update branch block
    - sara-update/SKILL.md contains `Set \`type\` = \`artifact.dec_type\`` in the decision update branch
    - sara-update/SKILL.md contains `Set \`status\` = \`artifact.status\`` in the decision update branch
    - sara-update/SKILL.md contains `Set \`schema_version\` = \`'2.0'\`` in the decision update branch (single-quoted)
    - sara-update/SKILL.md contains `Remove the following v1.0 frontmatter fields if present: \`context\`, \`decision\`, \`rationale\`, \`alternatives-considered\`` in the decision update branch
    - sara-update/SKILL.md contains `## Source Quote` in the decision update branch body rewrite instructions
    - sara-update/SKILL.md contains `No decision reached — alignment required.` in the decision update branch
    - The requirement update branch block (`For requirement artifacts`) still precedes the decision update branch block and is unchanged
    - The `Use the Write tool to overwrite` line still follows the decision update branch (not inside it)
    - `grep -c "proposed" /home/george/Projects/sara/.claude/skills/sara-update/SKILL.md` — zero results anywhere in decision update branch context; if `proposed` appears it is only in non-decision sections
    - `grep "organisational" /home/george/Projects/sara/.claude/skills/sara-update/SKILL.md` — returns the British English spelling in the dec_type line of the update branch
  </acceptance_criteria>
  <done>The decision update branch in sara-update/SKILL.md applies v2.0 frontmatter field mapping (type from dec_type, status from artifact.status, schema_version '2.0', removal of four v1.0 fields) and rewrites the full body to the five-section v2.0 layout — parallel to the requirement update branch pattern. Updated decision pages will be indistinguishable in structure from freshly created ones.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| artifact JSON → wiki page write | Field values from the extraction artifact are written directly to the wiki page frontmatter; incorrect field mapping would silently write wrong values |
| sara-update body synthesis → wiki page | Synthesised sections use the full source_doc in context; fabrication risk exists if synthesis instructions are ambiguous |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08 | Tampering | sara-update/SKILL.md decision create branch — hardcoded `proposed` removed | mitigate | Acceptance criteria require `grep -c "proposed"` returns 0 in decision branch context. If `proposed` remains, it silently overwrites the extraction-derived status on every decision artifact. |
| T-09-09 | Tampering | Field name collision: artifact `type` (entity class) vs frontmatter `type` (taxonomy) | mitigate | Explicit mapping instruction: `type` = `artifact.dec_type`. Named field `dec_type` in artifact JSON prevents silent collision. Verified in Task 1 acceptance criteria. |
| T-09-10 | Information Disclosure | Fabricated synthesis in Context/Rationale sections | mitigate | Both sections include explicit `never fabricate` instruction; fallback is `heading only` (empty content). This mirrors the established empty-section pattern from Phase 8. |
| T-09-11 | Denial of Service | Update branch stripping v1.0 fields from existing pages | accept | Removing `context`, `decision`, `rationale`, `alternatives-considered` from existing pages is intentional and non-reversible via the update branch. Pages can be restored from git history if needed. |
</threat_model>

<verification>
After both tasks complete:

1. `grep -n "proposed" /home/george/Projects/sara/.claude/skills/sara-update/SKILL.md` — must return zero results in the decision create or update branch context.
2. `grep -n "artifact.dec_type\|artifact.status" /home/george/Projects/sara/.claude/skills/sara-update/SKILL.md` — must show both field reads in the create branch frontmatter mapping.
3. `grep -n "## Source Quote" /home/george/Projects/sara/.claude/skills/sara-update/SKILL.md` — must return at least one result in the decision body block (create branch) and one in the update branch body rewrite.
4. `grep -n "No decision reached" /home/george/Projects/sara/.claude/skills/sara-update/SKILL.md` — must appear in both the create branch decision body and the update branch body instructions.
5. `grep -n "For decision artifacts" /home/george/Projects/sara/.claude/skills/sara-update/SKILL.md` — must show the update branch header.
6. `grep -n "organisational" /home/george/Projects/sara/.claude/skills/sara-update/SKILL.md` — must show British English spelling; `grep "organizational"` must return zero results.
7. `grep -n "DEC.*status=accepted\|DEC.*status=open" /home/george/Projects/sara/.claude/skills/sara-update/SKILL.md` — must show both summary generation variants.
8. Confirm the requirement create and update branches are unchanged by reading the requirement body block (`**requirement:**`) and the requirement update clause (`For requirement artifacts`).
</verification>

<success_criteria>
- sara-update writes `status` from `artifact.status` — never hardcodes `"proposed"` (D-03, D-10)
- sara-update writes `type` from `artifact.dec_type` to frontmatter `type` field (D-09)
- Context and Rationale are synthesised by sara-update from source_doc and discussion_notes, not extracted (D-05)
- Decision wiki pages have five-section body: Source Quote → Context → Decision → Alternatives Considered → Rationale (D-11)
- Open decisions have `## Decision` = "No decision reached — alignment required." and `## Alternatives Considered` lists competing positions (D-12)
- schema_version written as '2.0' (single-quoted) for decision artifacts (D-07)
- Four v1.0 narrative frontmatter fields not written in create branch; removed in update branch (D-08)
- Both create and update branches implement the same v2.0 body structure
</success_criteria>

<output>
After completion, create `.planning/phases/09-refine-decisions/09-03-SUMMARY.md`
</output>
