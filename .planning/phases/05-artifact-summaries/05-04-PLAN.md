---
phase: 05-artifact-summaries
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/sara-lint/SKILL.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "/sara-lint checks whether wiki/ directory exists and stops with a clear message if not"
    - "/sara-lint scans all five wiki subdirectories for artifacts missing a summary field and presents a count + one preview before writing anything"
    - "/sara-lint asks the user to confirm before batch-writing summaries"
    - "/sara-lint writes back-filled summaries using Read + Write tools only (no jq/sed/awk)"
    - "/sara-lint commits with message 'fix(wiki): back-fill artifact summaries via sara-lint' only after all writes succeed"
    - "SKILL.md has stub sections for Check 2 (orphaned pages) and Check 3 (broken cross-refs) clearly marked as v2"
  artifacts:
    - path: ".claude/skills/sara-lint/SKILL.md"
      provides: "New sara-lint skill: wiki summary back-fill with dry-run confirm and commit"
      contains: "sara-lint"
  key_links:
    - from: "sara-lint Check 1 scan"
      to: "wiki artifact files"
      via: "grep -rL '^summary:' wiki/requirements/ wiki/decisions/ wiki/actions/ wiki/risks/ wiki/stakeholders/"
      pattern: "summary"
    - from: "sara-lint Check 1 write loop"
      to: "artifact frontmatter"
      via: "Read tool + insert summary field + Write tool"
      pattern: "summary_max_words"
    - from: "sara-lint commit"
      to: "git history"
      via: "git commit -m 'fix(wiki): back-fill artifact summaries via sara-lint'"
      pattern: "back-fill artifact summaries"
---

<objective>
Create .claude/skills/sara-lint/SKILL.md — a new maintenance skill that scans all wiki artifact pages for missing summary fields, presents a dry-run preview to the user, generates and writes back-filled summaries upon confirmation, and commits the result.

Purpose: Existing wiki artifacts created before Phase 5 have no summary field. /sara-lint back-fills them so the grep-extract pattern in sara-extract and sara-discuss works for the whole wiki, not just newly created artifacts.

Output: New .claude/skills/sara-lint/SKILL.md implementing Check 1 (summary back-fill) with extensibility stubs for Check 2 and Check 3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-artifact-summaries/05-CONTEXT.md
@.planning/phases/05-artifact-summaries/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .claude/skills/sara-lint/SKILL.md</name>
  <files>.claude/skills/sara-lint/SKILL.md</files>
  <read_first>
    - .planning/phases/05-artifact-summaries/05-CONTEXT.md — D-12 through D-15 define the full sara-lint behaviour: scope, UX pattern (dry-run-first), commit message, extensibility structure
    - .planning/phases/05-artifact-summaries/05-PATTERNS.md — Pattern assignment for sara-lint shows: stage guard (wiki-existence variant), grep -rL scan command, AskUserQuestion confirm call shape, commit pattern from sara-update, extensibility stub structure
    - .claude/skills/sara-update/SKILL.md — borrow the commit pattern from Step 5 (git add + commit + EXIT:$? check + stage advance only after exit 0)
    - .claude/skills/sara-extract/SKILL.md — borrow the AskUserQuestion confirmation call shape from Step 4 (header, question, options)
  </read_first>
  <action>
Create .claude/skills/sara-lint/ directory and write SKILL.md using the Write tool. The file must implement exactly the structure below — no additions, no omissions.

**SKILL.md frontmatter:**
```yaml
---
name: sara-lint
description: "Scan wiki artifact pages for schema issues and fix them — v1: back-fill missing summary fields"
argument-hint: ""
allowed-tools:
  - Read
  - Write
  - Bash
  - AskUserQuestion
version: 1.0.0
---
```

**Objective block:** sara-lint scans all wiki artifact pages, identifies those missing a `summary` field, generates summaries for them using type-specific content rules, writes them back, and commits. v1 implements Check 1 (missing summaries) only. Future checks (orphaned pages, broken cross-refs) are stubbed.

**Process — Step 1: Wiki existence guard**

Run the following Bash command. If it exits non-zero (wiki/ does not exist), output a clear message and STOP:

```bash
if [ ! -d "wiki" ]; then
  echo "No wiki found. Run /sara-init first."
  exit 1
fi
```

If the directory exists, continue.

**Process — Step 2: Load summary_max_words config**

Read `.sara/pipeline-state.json` using the Read tool.

Extract `summary_max_words`. If the field is absent from the JSON, use 50 as the default.

Store as `{summary_max_words}`.

**Process — Check 1: Missing summaries (v1 — implemented)**

Run the following Bash command to find artifact pages that lack a `summary:` field:

```bash
grep -rL "^summary:" wiki/requirements/ wiki/decisions/ wiki/actions/ wiki/risks/ wiki/stakeholders/ 2>/dev/null | grep "\.md$" | grep -v "\.gitkeep"
```

`-L` returns filenames that do NOT contain the pattern — these are the pages to back-fill.

Store the result as `{missing_files}` (a list of file paths).

If `{missing_files}` is empty:
  Output: "Check 1 — Missing summaries: all {total} artifact pages have a summary field. Nothing to do."
  Proceed to Check 2 (stub — output "Check 2 — Orphaned pages: skipped (v2)") and Check 3 (stub) and STOP with success message.

If `{missing_files}` is non-empty:

  Count: `{missing_count}` = number of files in the list.

  Preview: Read the first file in `{missing_files}` using the Read tool. Generate a sample summary for it using the type-specific content rules (determine type from the directory path — `wiki/requirements/` → REQ, `wiki/decisions/` → DEC, `wiki/actions/` → ACT, `wiki/risks/` → RISK, `wiki/stakeholders/` → STK). The preview summary must be within `{summary_max_words}` words.

  Present the dry-run summary to the user as plain text:
  ```
  Check 1 — Missing summaries

  {missing_count} artifact pages are missing a summary field.

  Preview (first artifact):
  File: {first_file}
  Generated summary: "{preview_summary}"

  The full back-fill will:
    - Read each of the {missing_count} files
    - Generate a type-specific summary within {summary_max_words} words
    - Write the summary field into the frontmatter of each file
    - Commit all changes with message: fix(wiki): back-fill artifact summaries via sara-lint
  ```

  Then ask the user to confirm:
  ```
  header: "Confirm lint"
  question: "Back-fill summaries for {missing_count} artifact pages?"
  options: ["Proceed", "Cancel"]
  ```

  If user selects "Cancel":
    Output: "Back-fill cancelled. No files written."
    STOP.

  If user selects "Proceed":
    For each file in `{missing_files}`:
      Read the file using the Read tool.
      Determine type from the directory path.
      Generate a `summary` value as a single prose string within `{summary_max_words}` words, using the type-specific content rules:
        - REQ: title, status, one-line description of what is required
        - DEC: options considered, chosen option/recommendation, status, decision date
        - ACT: owner, due-date, status (open/in-progress/done/cancelled)
        - RISK: likelihood, impact, mitigation approach, status
        - STK: vertical, department, role — enough to distinguish from other stakeholders
      Insert `summary: "{generated_summary}"` into the frontmatter of the file, immediately after the `related:` field.
      Write the file back using the Write tool.
      Do NOT use Bash text-processing tools (jq, sed, awk) — Read and Write tools only.

    After all files are written, run git commit:
    ```bash
    git add wiki/requirements/ wiki/decisions/ wiki/actions/ wiki/risks/ wiki/stakeholders/
    git commit -m "fix(wiki): back-fill artifact summaries via sara-lint"
    echo "EXIT:$?"
    ```

    Check the exit code from "EXIT:$?".

    If exit code 0:
      Run `git log --oneline -1` to capture `{commit_hash}`.
      Output:
      ```
      Check 1 complete.

      Back-filled: {missing_count} artifacts
      Commit: {commit_hash}
      ```
      Proceed to Check 2 (stub) and Check 3 (stub).

    If exit code != 0:
      Output:
      ```
      Check 1 — Commit failed. Files written ({missing_count}) but not committed.
      Stage not changed. Re-run /sara-lint after resolving the git issue.
      ```
      STOP.

**Process — Check 2: Orphaned pages (v2 — stub, not implemented)**

Output: "Check 2 — Orphaned pages: not implemented in v1. Add check here in a future phase."

<!-- Future: scan wiki pages not referenced in wiki/index.md -->

**Process — Check 3: Broken cross-references (v2 — stub, not implemented)**

Output: "Check 3 — Broken cross-references: not implemented in v1. Add check here in a future phase."

<!-- Future: verify all IDs in `related:` fields resolve to existing pages in the wiki -->

**Process — Success report**

Output:
```
/sara-lint complete.

Check 1 — Missing summaries: {result}
Check 2 — Orphaned pages:     skipped (v2)
Check 3 — Broken cross-refs:  skipped (v2)
```

**Notes section must include:**
- pipeline-state.json is read using the Read tool only — never jq/sed/awk
- Wiki artifact files are read and written using Read and Write tools only — never Bash text-processing tools
- The `summary` field is inserted after `related:` in frontmatter — consistent position across all entity types
- If any file write fails mid-loop: output a partial failure report listing written and unwritten files; the git commit has NOT been issued; re-run /sara-lint to retry (it will skip already-summarised files because the grep -rL scan only returns files still missing summary)
- Commit happens only after ALL files in {missing_files} are successfully written
- summary_max_words defaults to 50 if absent from pipeline-state.json (D-07)
  </action>
  <verify>
    <automated>
ls /home/george/Projects/llm-wiki-gsd/.claude/skills/sara-lint/SKILL.md
grep -n "grep -rL" /home/george/Projects/llm-wiki-gsd/.claude/skills/sara-lint/SKILL.md
grep -n "fix(wiki): back-fill" /home/george/Projects/llm-wiki-gsd/.claude/skills/sara-lint/SKILL.md
grep -n "Check 2\|Check 3" /home/george/Projects/llm-wiki-gsd/.claude/skills/sara-lint/SKILL.md
grep -n "summary_max_words" /home/george/Projects/llm-wiki-gsd/.claude/skills/sara-lint/SKILL.md
    </automated>
  </verify>
  <done>
- SKILL.md exists at .claude/skills/sara-lint/SKILL.md
- grep -rL command present (the scan command)
- "fix(wiki): back-fill artifact summaries via sara-lint" present (D-14 commit message)
- Check 2 and Check 3 stubs present and marked "v2 — stub, not implemented"
- summary_max_words appears (config read in Step 2)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wiki files → LLM context (batch) | sara-lint reads all missing-summary files into context; large wikis may load many pages |
| LLM output → wiki frontmatter | Generated summaries are written directly to files without user review of each one (only a single preview is shown) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | batch write of summaries | mitigate | Dry-run preview + explicit user confirmation before any writes; git history provides full rollback via git revert |
| T-05-08 | Denial of Service | large wiki causing context exhaustion mid-loop | accept | Each file is read and written independently; partial failures report written vs unwritten files; re-run is safe (grep -rL only returns files still missing summary) |
| T-05-09 | Repudiation | summary content differs from source material | accept | Summaries are LLM-generated from existing page frontmatter — user can always read the original page; git history preserves pre-lint state |
</threat_model>

<verification>
After execution:
```bash
ls .claude/skills/sara-lint/SKILL.md
grep -c "sara-lint" .claude/skills/sara-lint/SKILL.md
grep -n "grep -rL" .claude/skills/sara-lint/SKILL.md
grep -n "fix(wiki)" .claude/skills/sara-lint/SKILL.md
grep -n "Check 2\|Check 3" .claude/skills/sara-lint/SKILL.md
grep -n "v2.*stub\|stub.*v2" .claude/skills/sara-lint/SKILL.md
```

Expected:
- SKILL.md exists
- sara-lint mentioned at least once
- grep -rL scan command present
- "fix(wiki): back-fill artifact summaries via sara-lint" commit message present
- Check 2 and Check 3 headings present
- stub/v2 markers present for future checks
</verification>

<success_criteria>
.claude/skills/sara-lint/SKILL.md exists with: wiki-existence guard, summary_max_words config read with fallback 50, grep -rL scan across all five wiki subdirectories, dry-run preview of one generated summary, AskUserQuestion confirmation, per-file Read+Write back-fill loop, git commit with "fix(wiki): back-fill artifact summaries via sara-lint" checked for exit code before advancing, Check 2 and Check 3 stubs clearly marked v2.
</success_criteria>

<output>
After completion, create `.planning/phases/05-artifact-summaries/05-04-SUMMARY.md`
</output>
