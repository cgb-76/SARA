---
phase: 05-artifact-summaries
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/sara-update/SKILL.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "sara-update Step 2 create branch generates a summary field for every new artifact using type-specific content rules"
    - "sara-update Step 2 update branch regenerates the summary field after applying change_summary"
    - "summary_max_words is read from pipeline-state.json with fallback to 50 if absent"
  artifacts:
    - path: ".claude/skills/sara-update/SKILL.md"
      provides: "Updated sara-update with summary generation in Step 2"
      contains: "summary_max_words"
  key_links:
    - from: "sara-update Step 2 create branch"
      to: "wiki artifact frontmatter summary field"
      via: "LLM generation using type-specific rules"
      pattern: "summary_max_words"
    - from: "sara-update Step 2 update branch"
      to: "existing wiki artifact summary field"
      via: "refresh after change_summary applied"
      pattern: "summary"
---

<objective>
Update sara-update SKILL.md to generate the `summary` frontmatter field for every artifact write — both create and update actions. The LLM generates type-specific prose within the summary_max_words limit.

Purpose: sara-update is the only pipeline skill that writes wiki artifact files. Summary generation at write time ensures all artifacts produced by the pipeline carry a summary field, enabling the grep-extract pattern in sara-extract and sara-discuss.

Output: Updated .claude/skills/sara-update/SKILL.md with summary generation instructions in Step 2's create branch and update branch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-artifact-summaries/05-CONTEXT.md
@.planning/phases/05-artifact-summaries/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add summary generation to sara-update Step 2 (create and update branches)</name>
  <files>.claude/skills/sara-update/SKILL.md</files>
  <read_first>
    - .claude/skills/sara-update/SKILL.md — read in full; the two target locations are the create branch field-substitution list (~lines 79–92) and the update branch apply-change_summary block (~lines 212–218)
    - .planning/phases/05-artifact-summaries/05-CONTEXT.md — D-03, D-06, D-07 define the type-specific content rules, generation trigger (create + update), and summary_max_words fallback
    - .planning/phases/05-artifact-summaries/05-PATTERNS.md — Pattern assignment for sara-update SKILL.md shows exact before/after content for the create loop substitution list
  </read_first>
  <action>
Make two targeted edits to .claude/skills/sara-update/SKILL.md using the Edit tool. Do NOT rewrite the file.

**Edit 1 — Create branch: add summary to field-substitution list (per D-06)**

Find the field-substitution list in the create branch of Step 2. The current last item in that list is:
```
    - All other fields not supplied by the artifact: use the template default value (empty string `""` or empty array `[]`)
```

Insert the following block immediately after that line (before the next blank line or "Populate the body sections" heading):

```
    - Read `summary_max_words` from the already-loaded pipeline-state.json (field: `summary_max_words`). If the field is absent, use 50 as the default.
    - `summary` = LLM-generated prose string within `summary_max_words` words. Write type-appropriate content:
      - REQ: title, status, one-line description of what is required
      - DEC: options considered, chosen option/recommendation, status, decision date
      - ACT: owner, due-date, status (open/in-progress/done/cancelled)
      - RISK: likelihood, impact, mitigation approach, status
      - STK: vertical, department, role — enough to distinguish from other stakeholders
      Generate the summary from the artifact fields already set (title, status, owner, etc.) and `{discussion_notes}`. Write it as a single prose string — not a list, not bullet points.
```

**Edit 2 — Update branch: regenerate summary after applying change_summary (per D-06)**

Find the update branch block in Step 2. The current text reads:
```
    Read the existing file `{wiki_dir}{artifact.existing_id}.md` using the Read tool.
    Apply `artifact.change_summary` to the relevant field(s) in the frontmatter or body. Update the `source` field to include `{item.id}` in addition to any existing source value. Update the `related` field by merging `artifact.related` with the existing related array (deduplicating by entity ID).
    Use the Write tool to overwrite `{wiki_dir}{artifact.existing_id}.md` with the updated content.
```

After "Update the `related` field by merging..." and before "Use the Write tool to overwrite...", insert:

```
    Regenerate the `summary` field: read `summary_max_words` from pipeline-state.json (already in memory; default 50 if absent). Generate a fresh summary prose string using the same type-specific content rules as the create branch — REQ: title/status/description; DEC: options/chosen option/status/date; ACT: owner/due-date/status; RISK: likelihood/impact/mitigation/status; STK: vertical/department/role. Replace the existing `summary` value in the frontmatter with the newly generated string.
```

After both edits, verify no frontmatter syntax is broken by checking the edit regions are internally consistent (no unclosed backtick blocks, no missing list item dashes).
  </action>
  <verify>
    <automated>
grep -n "summary_max_words" /home/george/Projects/llm-wiki-gsd/.claude/skills/sara-update/SKILL.md
grep -n "summary.*LLM-generated\|Regenerate the.*summary" /home/george/Projects/llm-wiki-gsd/.claude/skills/sara-update/SKILL.md
    </automated>
  </verify>
  <done>
- "summary_max_words" appears at least twice in the file (create branch read + update branch read)
- grep finds the create branch "LLM-generated prose string" line and the update branch "Regenerate the `summary` field" line
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM-generated summary → wiki frontmatter | Summary content is LLM-authored; no user-provided input is interpolated into it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information Disclosure | summary field in wiki | accept | Wiki is a private project repo; summary content is derived from source documents the user already controls |
| T-05-04 | Tampering | summary_max_words in pipeline-state.json | accept | D-05 notes this is user-editable by design; no validation needed in v1 |
</threat_model>

<verification>
After execution:
```bash
grep -n "summary_max_words" .claude/skills/sara-update/SKILL.md
grep -n "LLM-generated prose" .claude/skills/sara-update/SKILL.md
grep -n "Regenerate the" .claude/skills/sara-update/SKILL.md
```

Expected:
- summary_max_words appears in both create and update branches
- "LLM-generated prose" in create branch
- "Regenerate the" in update branch
</verification>

<success_criteria>
sara-update SKILL.md Step 2 create branch includes summary generation with type-specific content rules and summary_max_words fallback. Step 2 update branch regenerates summary after applying change_summary. Both branches read summary_max_words from pipeline-state.json with default 50.
</success_criteria>

<output>
After completion, create `.planning/phases/05-artifact-summaries/05-02-SUMMARY.md`
</output>
