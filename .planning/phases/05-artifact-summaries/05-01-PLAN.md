---
phase: 05-artifact-summaries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/sara-init/SKILL.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "sara-init SKILL.md Step 7 pipeline-state.json template includes summary_max_words: 50 at root level"
    - "sara-init SKILL.md Step 9 CLAUDE.md behavioral rules include rule 6 for summary field generation"
    - "sara-init SKILL.md Step 9 entity schema blocks each contain a summary field"
    - "sara-init SKILL.md Step 12 all five template write calls include a summary field in frontmatter"
  artifacts:
    - path: ".claude/skills/sara-init/SKILL.md"
      provides: "Updated sara-init with summary field in all generated files"
      contains: "summary_max_words"
  key_links:
    - from: "sara-init Step 7"
      to: ".sara/pipeline-state.json (at runtime)"
      via: "Write tool call embedded in SKILL.md"
      pattern: "summary_max_words"
    - from: "sara-init Step 9"
      to: "CLAUDE.md behavioral rule 6 (at runtime)"
      via: "Write tool call embedded in SKILL.md"
      pattern: "Summary field"
    - from: "sara-init Step 12"
      to: ".sara/templates/*.md (at runtime)"
      via: "Write tool calls embedded in SKILL.md"
      pattern: "summary:"
---

<objective>
Update sara-init SKILL.md to emit the `summary` field in all three places where it generates schema artifacts: the pipeline-state.json template (Step 7), the CLAUDE.md behavioral rules and entity schema blocks (Step 9), and the five entity template writes (Step 12).

Purpose: Templates and wiki/CLAUDE.md do not exist at dev time — they are created at runtime by /sara-init. The only way to add the summary field to those files is to update the literal content embedded in sara-init SKILL.md. This is the source of truth for all schema.

Output: Updated .claude/skills/sara-init/SKILL.md with summary field threaded through Steps 7, 9, and 12.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-artifact-summaries/05-CONTEXT.md
@.planning/phases/05-artifact-summaries/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add summary field to sara-init SKILL.md (Steps 7, 9, 12)</name>
  <files>.claude/skills/sara-init/SKILL.md</files>
  <read_first>
    - .claude/skills/sara-init/SKILL.md — read in full before any edits; the three target locations are Step 7 (line ~130), Step 9 (lines ~154–292), and Step 12 (lines ~341–457)
    - .planning/phases/05-artifact-summaries/05-CONTEXT.md — D-01 through D-07 and D-16 are the locked decisions governing exact field name, content, and placement
    - .planning/phases/05-artifact-summaries/05-PATTERNS.md — Pattern assignments for each template file and CLAUDE.md show the exact before/after content required
  </read_first>
  <action>
Make four distinct edits to .claude/skills/sara-init/SKILL.md using the Edit tool. Do NOT rewrite the whole file — use targeted edits only.

**Edit 1 — Step 7: Add summary_max_words to pipeline-state.json template (per D-04)**

Find the JSON block in Step 7 that reads:
```json
{
  "counters": {
    "ingest": { "MTG": 0, "EML": 0, "SLK": 0, "DOC": 0 },
    "entity": { "REQ": 0, "DEC": 0, "ACT": 0, "RISK": 0, "STK": 0 }
  },
  "items": {}
}
```

Replace it with:
```json
{
  "summary_max_words": 50,
  "counters": {
    "ingest": { "MTG": 0, "EML": 0, "SLK": 0, "DOC": 0 },
    "entity": { "REQ": 0, "DEC": 0, "ACT": 0, "RISK": 0, "STK": 0 }
  },
  "items": {}
}
```

**Edit 2 — Step 9: Add rule 6 to CLAUDE.md behavioral rules block (per D-16)**

Find the end of the behavioral rules in the Step 9 CLAUDE.md write block. The current last rule is:
```
5. **Cross-references:** `related` fields must use entity IDs only...
   never file paths, relative links, or Obsidian wiki-links. In body prose, always use
   `[[ID|ID Title]]` (ID prefix + title or name, e.g. `[[DEC-007|DEC-007 Defer SSO to Phase 3]]`) —
   never a bare `[[ID]]` or raw ID string. Frontmatter fields remain plain IDs.
```

After rule 5 (and before the `## Entity Schemas` heading), insert:
```
6. **Summary field:** When writing or updating any wiki artifact, always generate or refresh the
   `summary` field using the type-specific content rules (REQ: title/status/description;
   DEC: options considered/chosen option/status/date; ACT: owner/due-date/status;
   RISK: likelihood/impact/mitigation/status; STK: vertical/department/role) and the
   `summary_max_words` limit from `.sara/pipeline-state.json` (default: 50 words if absent).
```

**Edit 3 — Step 9: Add summary field to each of the five entity schema blocks in CLAUDE.md**

In Step 9's CLAUDE.md content, there are five entity schema YAML blocks. For each one, insert `summary: ""` with a type-specific comment immediately after the `related: []` line:

- Requirement schema — after `related: []    # entity IDs (e.g. [DEC-001, ACT-002])`:
  ```
  summary: ""  # REQ: title, status, one-line description of what is required
  ```

- Decision schema — after `related: []`:
  ```
  summary: ""  # DEC: options considered, chosen option/recommendation, status, decision date
  ```

- Action schema — after `related: []`:
  ```
  summary: ""  # ACT: owner, due-date, status (open/in-progress/done/cancelled)
  ```

- Risk schema — after `related: []`:
  ```
  summary: ""  # RISK: likelihood, impact, mitigation approach, status
  ```

- Stakeholder schema — after `related: []`:
  ```
  summary: ""  # STK: vertical, department, role — enough to distinguish from other stakeholders
  ```

**Edit 4 — Step 12: Add summary field to each of the five template write blocks**

In Step 12, there are five Write tool calls, each embedding a template file's content. For each template, insert `summary: ""` with the same type-specific comment after the `related: []` line, matching exactly the same content as Edit 3:

- .sara/templates/requirement.md — after `related: []    # entity IDs (e.g. [DEC-001, ACT-002])`:
  ```
  summary: ""  # REQ: title, status, one-line description of what is required
  ```

- .sara/templates/decision.md — after `related: []`:
  ```
  summary: ""  # DEC: options considered, chosen option/recommendation, status, decision date
  ```

- .sara/templates/action.md — after `related: []`:
  ```
  summary: ""  # ACT: owner, due-date, status (open/in-progress/done/cancelled)
  ```

- .sara/templates/risk.md — after `related: []`:
  ```
  summary: ""  # RISK: likelihood, impact, mitigation approach, status
  ```

- .sara/templates/stakeholder.md — after `related: []`:
  ```
  summary: ""  # STK: vertical, department, role — enough to distinguish from other stakeholders
  ```

After all four edits, verify the file is syntactically consistent (no duplicate fields, no broken markdown fences) by reading the modified sections back.
  </action>
  <verify>
    <automated>
grep -n "summary_max_words" /home/george/Projects/llm-wiki-gsd/.claude/skills/sara-init/SKILL.md | head -5
grep -n "Summary field" /home/george/Projects/llm-wiki-gsd/.claude/skills/sara-init/SKILL.md | head -5
grep -c 'summary: ""' /home/george/Projects/llm-wiki-gsd/.claude/skills/sara-init/SKILL.md
    </automated>
  </verify>
  <done>
- grep finds "summary_max_words" in the Step 7 JSON block
- grep finds "Summary field" in the Step 9 behavioral rules section (rule 6)
- grep -c 'summary: ""' returns 10 (5 schema blocks in Step 9 + 5 template blocks in Step 12)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md content → generated files | sara-init writes files verbatim from embedded content; malformed YAML in SKILL.md produces malformed templates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | sara-init SKILL.md template content | accept | Files are in a git repo; history provides audit trail; no user data in templates |
| T-05-02 | Information Disclosure | summary field content | accept | Wiki is a private project repo; no external exposure in v1 |
</threat_model>

<verification>
After execution, run:
```bash
grep -n "summary_max_words" .claude/skills/sara-init/SKILL.md
grep -n "Summary field" .claude/skills/sara-init/SKILL.md
grep -c 'summary: ""' .claude/skills/sara-init/SKILL.md
```

Expected:
- summary_max_words appears once (Step 7 JSON block)
- "Summary field" appears once (Step 9 rule 6)
- `summary: ""` count = 10 (5 schema + 5 template blocks)
</verification>

<success_criteria>
sara-init SKILL.md emits summary_max_words: 50 in pipeline-state.json, rule 6 (Summary field) in CLAUDE.md behavioral rules, and summary: "" in all 10 schema/template YAML blocks (5 entity schema blocks in Step 9 + 5 template writes in Step 12).
</success_criteria>

<output>
After completion, create `.planning/phases/05-artifact-summaries/05-01-SUMMARY.md`
</output>
