---
phase: 15-lint-repair
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/sara-update/SKILL.md
autonomous: true
requirements:
  - XREF-05

must_haves:
  truths:
    - "sara-update Step 2 no longer contains a Temp ID resolution block or a substitution pass"
    - "The write loop proceeds directly from 'Initialize written_files = []' without any id_map or preview_counters logic"
    - "sara-update's final step (success path only) auto-invokes /sara-lint with no user prompt"
    - "The sara-lint auto-invocation does NOT occur on the partial-failure path or the commit-failure path"
    - "All other Step 2 write loop logic and counter increment-before-write (Pitfall 1) guard are unchanged"
  artifacts:
    - path: ".claude/skills/sara-update/SKILL.md"
      provides: "Reverted + extended skill — temp_id resolution removed, sara-lint auto-invoke added"
      contains: "Running /sara-lint"
  key_links:
    - from: "sara-update Step 2 (write loop)"
      to: "wiki artifact files"
      via: "direct write with related: [] as-is from extraction_plan"
      pattern: "Initialize.*written_files"
    - from: "sara-update Step 4 (Update Complete output)"
      to: "/sara-lint invocation"
      via: "auto-invoke on success path only"
      pattern: "sara-lint"
---

<objective>
Two modifications to sara-update:

1. **Revert** (D-03): Remove the "Temp ID resolution (before write loop)" block and its substitution pass from Step 2. The write loop proceeds directly from "Initialize `written_files = []`" — no id_map, no preview_counters.

2. **Add** (D-10): After the "Update Complete" block in Step 4 (success path only), auto-invoke `/sara-lint` with no user prompt. The user sees lint output inline as the last part of every successful sara-update run.

Purpose: Batch co-extraction does not imply semantic relatedness — temp_id resolution was the wrong mechanism. LLM semantic inference via sara-lint D-07 is correct. Auto-invoking sara-lint on every successful sara-update run ensures related[] is curated immediately after new artifacts are written.

Output: Updated `.claude/skills/sara-update/SKILL.md` with the Temp ID resolution block deleted and a sara-lint auto-invocation appended to the Step 4 success path.
</objective>

<execution_context>
@/home/george/.claude/get-shit-done/workflows/execute-plan.md
@/home/george/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-lint-repair/15-CONTEXT.md
@.planning/phases/15-lint-repair/15-PATTERNS.md

<interfaces>
<!-- Exact removal and insertion targets extracted from current SKILL.md. -->

From .claude/skills/sara-update/SKILL.md — Step 2 block to REMOVE entirely (lines 65–121):

The block begins with this heading:
```
**Temp ID resolution (before write loop)**
```
And ends with this line (inclusive — remove through the blank line before Initialize):
```
After this scan, all `artifact.related` arrays in the in-memory `extraction_plan` contain
real entity IDs only. Proceed to "Initialize `written_files = []`" and the write loop.

Do NOT write the substituted `extraction_plan` back to `pipeline-state.json` at this
point — the write loop persists counters on each create-action iteration as before.
The temp_id fields on artifact objects may be left in `pipeline-state.json` (they are
inert after resolution) or stripped — either is acceptable.
```

After removal, Step 2 must open directly with:
```
**Step 2 — Write wiki artifact files**

Initialize `written_files = []` and `failed_files = []`.
```

---

From .claude/skills/sara-update/SKILL.md — Step 4 SUCCESS path insertion point (after lines 634–642):

The current "Update Complete" success output block ends with:
```
  Output:
  ```
  ## Update Complete

  Commit: {commit_hash}
  Artifacts written: {count}
  {written_files list}

  Item {N} ({item.id}) is now complete.
  ```
```

Append AFTER this block (still within the commit-succeeds branch, before the commit-fails branch):
```
After outputting the "Update Complete" block, immediately invoke `/sara-lint` with no
arguments. Do not prompt the user — lint runs automatically as the final action of every
successful sara-update run. The user sees the lint output inline as the last part of the
sara-update session.

Output before invoking:
```
Running /sara-lint to curate related[] and Cross Links...
```

Then invoke `/sara-lint`.
```

PLACEMENT RULE: This auto-invocation ONLY occurs on the success path (where stage is written
to "complete" and exit code was 0). It does NOT appear in the commit-failure branch or in any
partial-failure path.

MECHANISM: Express this as a direct instruction to invoke `/sara-lint` in the skill prose
(not as a Task() call). The Claude agent running sara-update will invoke the sara-lint skill
as its next action.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Temp ID resolution block from Step 2</name>
  <files>.claude/skills/sara-update/SKILL.md</files>
  <read_first>
    - .claude/skills/sara-update/SKILL.md — read the full file before making any edits; the Temp ID resolution block is at lines 65–121 (beginning with "**Temp ID resolution (before write loop)**" and ending with the blank line before "Initialize `written_files = []`")
    - .planning/phases/15-lint-repair/15-CONTEXT.md — D-03 specifies the removal; the Initialize `written_files` line and everything in the write loop are retained
    - .planning/phases/15-lint-repair/15-PATTERNS.md — "D-03 removal target" section shows the full block and the correct post-removal Step 2 opening
  </read_first>
  <action>
In `.claude/skills/sara-update/SKILL.md`, locate the "Temp ID resolution (before write loop)" block in Step 2 and remove it entirely.

The block to remove starts at the subheading:
```
**Temp ID resolution (before write loop)**
```
And extends through (inclusive) the paragraph:
```
Do NOT write the substituted `extraction_plan` back to `pipeline-state.json` at this
point — the write loop persists counters on each create-action iteration as before.
The temp_id fields on artifact objects may be left in `pipeline-state.json` (they are
inert after resolution) or stripped — either is acceptable.
```
(approximately lines 65–121 in the current file)

The block encompasses:
- The "Temp ID resolution (before write loop)" heading
- The id_map initialization and preview_counters deep copy
- The preview loop (for each artifact where action == "create": determine entity_type_key, increment preview_counters, compute preview_id, set id_map)
- The "Do NOT write preview_counters" note
- The "Substitution pass:" heading and loop (for each artifact in extraction_plan: for each entry t in artifact.related: if id_map[t] exists replace; else leave unchanged)
- The unresolved-temp_id warning scan and removal loop (scan for /^[a-f0-9]{8}$/ pattern, log warning, remove entry)
- The "After this scan, all artifact.related arrays..." concluding paragraph
- The "Do NOT write the substituted extraction_plan back..." note

After removal, Step 2 must read:
```
**Step 2 — Write wiki artifact files**

Initialize `written_files = []` and `failed_files = []`.

For each artifact in `{extraction_plan}`:
[existing write loop — unchanged]
```

Use the Edit tool to remove the block. Do not alter the write loop, the counter increment-before-write pattern, or any other content of Step 2.
  </action>
  <verify>
    <automated>grep -n "id_map\|preview_counters\|Temp ID resolution\|Substitution pass" .claude/skills/sara-update/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Temp ID resolution" .claude/skills/sara-update/SKILL.md` returns empty
    - `grep "id_map" .claude/skills/sara-update/SKILL.md` returns empty
    - `grep "preview_counters" .claude/skills/sara-update/SKILL.md` returns empty
    - `grep "Substitution pass" .claude/skills/sara-update/SKILL.md` returns empty
    - `grep "unresolved temp_id\|a-f0-9.*8\|Unresolved temp_id" .claude/skills/sara-update/SKILL.md` returns empty (warning scan removed)
    - `grep -n "Initialize.*written_files" .claude/skills/sara-update/SKILL.md` — the line number is immediately after the "**Step 2 — Write wiki artifact files**" heading (no intervening resolution block)
    - `grep "Increment.*counters\.entity" .claude/skills/sara-update/SKILL.md` still returns a match (real counter increment inside write loop is unchanged)
    - `grep "CRITICAL: Entity counter increments" .claude/skills/sara-update/SKILL.md` still returns 1 match (notes section unchanged)
    - `grep "CRITICAL: Stage advances" .claude/skills/sara-update/SKILL.md` still returns 1 match (notes section unchanged)
  </acceptance_criteria>
  <done>Step 2 of sara-update no longer performs temp_id resolution or substitution. The write loop begins directly with "Initialize `written_files = []`". All existing write loop logic, counter increment-before-write (Pitfall 1 guard), and notes section are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add sara-lint auto-invocation to Step 4 success path</name>
  <files>.claude/skills/sara-update/SKILL.md</files>
  <read_first>
    - .claude/skills/sara-update/SKILL.md — read the current Step 4 section; the "Update Complete" output block is at approximately lines 629–642; the sara-lint invocation must be appended AFTER this block and BEFORE the commit-failure branch ("**If commit FAILS (exit code != 0):**")
    - .planning/phases/15-lint-repair/15-CONTEXT.md — D-10 specifies the invocation is on success path only, no user prompt, output "Running /sara-lint..." then invoke /sara-lint
    - .planning/phases/15-lint-repair/15-PATTERNS.md — "D-10 additive target" section shows the exact placement rule and mechanism note
  </read_first>
  <action>
In `.claude/skills/sara-update/SKILL.md`, locate the "**If commit SUCCEEDS (exit code 0):**" branch in Step 4. Within this branch, the "Update Complete" output block currently ends with:

```
  Output:
  ```
  ## Update Complete

  Commit: {commit_hash}
  Artifacts written: {count}
  {written_files list}

  Item {N} ({item.id}) is now complete.
  ```
```

Immediately after this block (still within the commit-succeeds branch, before the blank line that precedes "**If commit FAILS (exit code != 0):**"), append the following prose:

```
After outputting the "Update Complete" block, immediately invoke `/sara-lint` with no
arguments. Do not prompt the user — lint runs automatically as the final action of every
successful sara-update run. The user sees the lint output inline as the last part of the
sara-update session.

Output before invoking:
```
Running /sara-lint to curate related[] and Cross Links...
```

Then invoke `/sara-lint`.
```

CRITICAL placement rules:
- This block appears ONLY in the commit-succeeds branch (after stage is written to "complete")
- It does NOT appear in the commit-fails branch
- It does NOT appear on the partial-failure path (Step 2 early-stop)
- It does NOT appear on the empty-plan early-stop (Step 1)

Use the Edit tool to make a targeted insertion after the "Update Complete" output block. Do not alter any other Step 4 content.
  </action>
  <verify>
    <automated>grep -n "sara-lint\|Running.*lint" .claude/skills/sara-update/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sara-lint" .claude/skills/sara-update/SKILL.md` returns at least 2 (the prose invocation instruction and the Running... output line)
    - `grep "Running /sara-lint" .claude/skills/sara-update/SKILL.md` returns 1 match (the output-before-invoking line)
    - `grep "invoke \`/sara-lint\`\|invoke /sara-lint" .claude/skills/sara-update/SKILL.md` returns at least 1 match
    - `grep "Do not prompt the user" .claude/skills/sara-update/SKILL.md` returns 1 match (confirming no-prompt clause is present)
    - The sara-lint invocation text appears BEFORE "**If commit FAILS (exit code != 0):**" in the file — verify by line number comparison: sara-lint reference line number is lower than the commit-fails heading line number
    - `grep "STOP" .claude/skills/sara-update/SKILL.md | wc -l` is unchanged from before this task (no new STOP instructions added)
  </acceptance_criteria>
  <done>sara-update's Step 4 success path now auto-invokes /sara-lint after outputting the "Update Complete" block. No user prompt required. The invocation is absent from the commit-failure branch, the partial-failure path, and the empty-plan early-stop. The user sees lint output inline as the final part of every successful sara-update session.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extraction_plan → sara-update Step 2 | pipeline-state.json content (written by sara-extract) is the write loop input |
| sara-update → sara-lint invocation | Skill-to-skill invocation on success path; sara-lint receives no arguments |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Tampering | sara-update skill edit (removal) | accept | Changes are removals-only for Task 1; no new logic paths or input vectors introduced |
| T-15-05 | Denial of Service | sara-lint auto-invocation | accept | sara-lint is a read-then-write tool over local wiki files; no network calls; bounded by wiki size; user can skip individual findings in the lint approval loop |
| T-15-06 | Tampering | related: [] default in write loop | accept | related: [] is a safe empty default written to wiki frontmatter; sara-lint D-07 (Plan 03) curates it semantically after write; no injection risk |
| T-15-07 | Elevation of Privilege | skill-to-skill invocation (/sara-lint) | accept | sara-lint operates on local wiki files only; commit messages are templated; no external network access; same trust level as sara-update itself |
</threat_model>

<verification>
After both tasks complete:
- `grep "Temp ID resolution\|id_map\|preview_counters\|Substitution pass" .claude/skills/sara-update/SKILL.md` returns empty (zero matches)
- `grep "sara-lint" .claude/skills/sara-update/SKILL.md` returns at least 2 matches
- `grep "Running /sara-lint" .claude/skills/sara-update/SKILL.md` returns 1 match
- `grep "Increment.*counters\.entity" .claude/skills/sara-update/SKILL.md` still returns 1 match (write loop counter increment intact)
- The sara-lint invocation line appears before the "**If commit FAILS**" heading (check line numbers)
- `grep "CRITICAL: Stage advances" .claude/skills/sara-update/SKILL.md` returns 1 match (notes section unchanged)
</verification>

<success_criteria>
- sara-update Step 2: no temp_id resolution, no substitution pass, no id_map, no preview_counters; write loop starts directly at "Initialize `written_files = []`"
- sara-update Step 4 success path: after outputting "Update Complete", outputs "Running /sara-lint..." and then invokes /sara-lint
- sara-lint auto-invocation absent from partial-failure path, commit-failure path, and empty-plan early-stop
- Counter increment-before-write (Pitfall 1 guard) and all existing notes section content are unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/15-lint-repair/15-02-SUMMARY.md`
</output>
