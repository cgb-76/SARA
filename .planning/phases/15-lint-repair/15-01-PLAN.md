---
phase: 15-lint-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/sara-extract/SKILL.md
autonomous: true
requirements:
  - XREF-03

must_haves:
  truths:
    - "sara-extract Step 3 no longer assigns temp_id to any artifact in any of the four inline passes"
    - "sara-extract Step 5 no longer contains a full-mesh related[] linking block"
    - "Every artifact produced by sara-extract still carries related: [] (the default empty field is retained)"
    - "No reference to temp_id, full-mesh, or the stale-temp_id strip scan remains in SKILL.md"
    - "All other Step 3 and Step 5 logic is unchanged"
  artifacts:
    - path: ".claude/skills/sara-extract/SKILL.md"
      provides: "Reverted skill — temp_id blocks removed from Step 3 (×4) and Step 5"
      contains: "related = []"
  key_links:
    - from: "sara-extract Step 3 (four passes)"
      to: "approved_artifacts array"
      via: "related = [] default (unchanged); temp_id lines absent"
      pattern: "related.*\\[\\]"
    - from: "sara-extract Step 5"
      to: "pipeline-state.json extraction_plan write"
      via: "direct write — no mesh linking step"
      pattern: "Read.*pipeline-state.json"
---

<objective>
Revert the Phase 14 changes in sara-extract: remove the `temp_id` assignment block from all four inline extraction passes in Step 3, and remove the full-mesh related[] linking block from Step 5.

Purpose: Batch co-extraction does not imply semantic relatedness (per D-01/D-02 in CONTEXT.md). The correct mechanism is LLM semantic inference via sara-lint D-07 (Phase 15 Plan 03). The default `related: []` field is kept in each pass — only the temp_id generation and the Step 5 mesh computation are removed.

Output: Updated `.claude/skills/sara-extract/SKILL.md` with temp_id lines removed from all four Step 3 passes and the full-mesh block removed from Step 5.
</objective>

<execution_context>
@/home/george/.claude/get-shit-done/workflows/execute-plan.md
@/home/george/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-lint-repair/15-CONTEXT.md
@.planning/phases/15-lint-repair/15-PATTERNS.md

<interfaces>
<!-- Exact removal targets extracted from current SKILL.md. Executor must remove these precisely. -->

From .claude/skills/sara-extract/SKILL.md — Step 3, Requirements pass (lines 98–103):
```
- Set `temp_id` = result of Bash: `python3 -c "import secrets; print(secrets.token_hex(4))"`
  MANDATORY: use the Bash tool to generate this value. Do NOT generate inline — inline
  generation is not random and risks collisions. Each artifact gets a unique temp_id.
  After all four passes complete, verify all temp_ids in `{merged}` are unique. If any
  duplicates are found, regenerate the duplicate temp_id(s) with a new Bash call.
```
The same block (identical wording) appears in:
- Decisions pass (lines ~163–168)
- Actions pass (lines ~216–221)
- Risks pass (lines ~276–281)

In all four cases, the line AFTER the temp_id block (which MUST be retained) reads:
```
- Set `action` = `"create"`, `type` = `"requirement"`, `id_to_assign` = `"REQ-NNN"`, `related` = `[]`, `change_summary` = `""`
```
(with the appropriate type/id_to_assign values per pass)

From .claude/skills/sara-extract/SKILL.md — Step 5 full-mesh block (lines 408–431):
```
**Full-mesh related[] linking**

After all artifacts are resolved to "Accept" or "Reject":

Build the full-mesh related[] for all approved artifacts:
  For each artifact `A` in `approved_artifacts`:
    # Preserve any real IDs injected by sorter cross-reference resolutions (Step 3 option A answers)
    existing_real_ids = [entry for entry in A.related if entry does NOT match /^[a-f0-9]{8}$/]
    new_temp_ids = [B.temp_id for B in approved_artifacts if B.temp_id != A.temp_id]
    Set `A.related` = deduplicate(existing_real_ids + new_temp_ids)

For a single-artifact batch: `A.related` = `[]` (the other-artifacts set is empty — no special case needed)
For a zero-artifact batch: skip this step entirely (approved_artifacts is empty)

After the full-mesh step, strip any stale temp_id values from related[] that do not
correspond to an approved artifact (e.g. a sorter-injected cross-reference to an artifact
that was subsequently rejected in Step 4):
  approved_temp_ids = set of all A.temp_id for A in approved_artifacts
  For each artifact A in approved_artifacts:
    A.related = [t for t in A.related if t is in approved_temp_ids OR t does NOT match /^[a-f0-9]{8}$/]

This replaces the `related: []` that was set during Step 3. The temp_id values are stable
cross-reference keys — they persist in extraction_plan until sara-update resolves them to
real IDs at the start of Step 2.
```
After removal, Step 5 must open with:
```
**Step 5 — Write extraction plan and advance stage**

Read `.sara/pipeline-state.json` using the Read tool.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove temp_id assignment blocks from all four Step 3 extraction passes</name>
  <files>.claude/skills/sara-extract/SKILL.md</files>
  <read_first>
    - .claude/skills/sara-extract/SKILL.md — read the full file before making any edits; locate all four temp_id bullet blocks in Step 3 (Requirements ~lines 98–103, Decisions ~lines 163–168, Actions ~lines 216–221, Risks ~lines 276–281)
    - .planning/phases/15-lint-repair/15-CONTEXT.md — D-01 specifies exactly what to remove: the temp_id bullet block only; the `related = []` initialization line stays
    - .planning/phases/15-lint-repair/15-PATTERNS.md — "D-01 removal target" section confirms the exact block form and the retained line
  </read_first>
  <action>
In `.claude/skills/sara-extract/SKILL.md`, locate and remove the 5-line temp_id bullet block from each of the four inline extraction passes in Step 3.

The block to remove (identical in each pass, with this exact wording):
```
- Set `temp_id` = result of Bash: `python3 -c "import secrets; print(secrets.token_hex(4))"`
  MANDATORY: use the Bash tool to generate this value. Do NOT generate inline — inline
  generation is not random and risks collisions. Each artifact gets a unique temp_id.
  After all four passes complete, verify all temp_ids in `{merged}` are unique. If any
  duplicates are found, regenerate the duplicate temp_id(s) with a new Bash call.
```

Remove this block from ALL FOUR passes:
1. Requirements pass (currently ~lines 98–102)
2. Decisions pass (currently ~lines 163–167)
3. Actions pass (currently ~lines 216–220)
4. Risks pass (currently ~lines 276–280)

The line immediately after each block (which MUST remain, with its appropriate values per pass):
- Requirements: `- Set `action` = `"create"`, `type` = `"requirement"`, `id_to_assign` = `"REQ-NNN"`, `related` = `[]`, `change_summary` = `""`
- Decisions: `- Set `action` = `"create"`, `type` = `"decision"`, `id_to_assign` = `"DEC-NNN"`, `related` = `[]`, `change_summary` = `""`
- Actions: `- Set `action` = `"create"`, `type` = `"action"`, `id_to_assign` = `"ACT-NNN"`, `related` = `[]`, `change_summary` = `""`
- Risks: `- Set `action` = `"create"`, `type` = `"risk"`, `id_to_assign` = `"RSK-NNN"`, `related` = `[]`, `change_summary` = `""`

Also remove the unique-temp_id verification sentence from the "Merge and sorter dispatch" note at the end of Step 3 if it references temp_id uniqueness checking (search for "verify all temp_ids in `{merged}` are unique" — this appears within the temp_id block itself, so will be removed with the block above; confirm no orphan reference remains).

Use the Edit tool for each of the four targeted removals. Do not alter any other content.
  </action>
  <verify>
    <automated>grep -c "temp_id" .claude/skills/sara-extract/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "temp_id" .claude/skills/sara-extract/SKILL.md` returns 0 (no temp_id references remain after Step 5 block is also removed in Task 2; after Task 1 alone it may still return results from Step 5 — Task 2 clears those)
    - `grep "token_hex\|MANDATORY.*Bash.*generate" .claude/skills/sara-extract/SKILL.md` returns empty (generation instruction fully removed)
    - `grep -c "related.*\[\]" .claude/skills/sara-extract/SKILL.md` returns 4 or more — the `related = []` initialization lines in all four passes remain present
    - `grep "Set \`action\` = \`\"create\"\`" .claude/skills/sara-extract/SKILL.md` returns 4 lines (one per pass — the field-init lines are intact)
    - No pass text other than the temp_id bullet block is removed
  </acceptance_criteria>
  <done>All four inline extraction passes in Step 3 no longer assign temp_id. The `related = []` field-initialization lines remain. Artifacts produced by sara-extract will have related: [] from the field-init line (default empty, not batch-mate linked).</done>
</task>

<task type="auto">
  <name>Task 2: Remove full-mesh related[] linking block from Step 5</name>
  <files>.claude/skills/sara-extract/SKILL.md</files>
  <read_first>
    - .claude/skills/sara-extract/SKILL.md — read the current Step 5 section; the full-mesh block begins at "**Full-mesh related[] linking**" (currently lines ~408–431) and ends before "Read `.sara/pipeline-state.json` using the Read tool."
    - .planning/phases/15-lint-repair/15-PATTERNS.md — "D-02 removal target" section shows the exact block boundary and what Step 5 must look like after removal
  </read_first>
  <action>
In `.claude/skills/sara-extract/SKILL.md`, locate and remove the full-mesh linking block from Step 5.

The block to remove begins at the subheading `**Full-mesh related[] linking**` and ends at (and including) the blank line before `Read \`.sara/pipeline-state.json\` using the Read tool.`. The complete block is:

```
**Full-mesh related[] linking**

After all artifacts are resolved to "Accept" or "Reject":

Build the full-mesh related[] for all approved artifacts:
  For each artifact `A` in `approved_artifacts`:
    # Preserve any real IDs injected by sorter cross-reference resolutions (Step 3 option A answers)
    existing_real_ids = [entry for entry in A.related if entry does NOT match /^[a-f0-9]{8}$/]
    new_temp_ids = [B.temp_id for B in approved_artifacts if B.temp_id != A.temp_id]
    Set `A.related` = deduplicate(existing_real_ids + new_temp_ids)

For a single-artifact batch: `A.related` = `[]` (the other-artifacts set is empty — no special case needed)
For a zero-artifact batch: skip this step entirely (approved_artifacts is empty)

After the full-mesh step, strip any stale temp_id values from related[] that do not
correspond to an approved artifact (e.g. a sorter-injected cross-reference to an artifact
that was subsequently rejected in Step 4):
  approved_temp_ids = set of all A.temp_id for A in approved_artifacts
  For each artifact A in approved_artifacts:
    A.related = [t for t in A.related if t is in approved_temp_ids OR t does NOT match /^[a-f0-9]{8}$/]

This replaces the `related: []` that was set during Step 3. The temp_id values are stable
cross-reference keys — they persist in extraction_plan until sara-update resolves them to
real IDs at the start of Step 2.
```

After removal, Step 5 must open directly with:
```
**Step 5 — Write extraction plan and advance stage**

Read `.sara/pipeline-state.json` using the Read tool.
```

Use the Edit tool to remove the block. Do not alter any other Step 5 content (the pipeline-state.json update, the stage = "approved" assignment, the summary table output, or the zero-artifacts edge case).
  </action>
  <verify>
    <automated>grep -n "Full-mesh\|full-mesh\|temp_id\|approved_temp_ids" .claude/skills/sara-extract/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Full-mesh\|full-mesh" .claude/skills/sara-extract/SKILL.md` returns empty (heading and body both gone)
    - `grep "temp_id" .claude/skills/sara-extract/SKILL.md` returns empty (no temp_id references anywhere in the file)
    - `grep "approved_temp_ids\|existing_real_ids\|new_temp_ids" .claude/skills/sara-extract/SKILL.md` returns empty
    - `grep -n "Read.*pipeline-state.json" .claude/skills/sara-extract/SKILL.md` — the first occurrence in Step 5 immediately follows the "**Step 5 — Write extraction plan and advance stage**" heading (no intervening block)
    - `grep "stage.*approved\|approved.*stage" .claude/skills/sara-extract/SKILL.md` still returns the stage-advance line (Step 5 write logic unchanged)
    - `grep "Do NOT use Bash shell text-processing" .claude/skills/sara-extract/SKILL.md` still returns 1 match (existing constraint note unchanged)
  </acceptance_criteria>
  <done>Step 5 of sara-extract no longer performs full-mesh temp_id linking. Approved artifacts proceed to the pipeline-state.json write with their related: [] defaults from Step 3. The write logic, stage advancement, and summary table are unchanged. No temp_id references remain anywhere in the file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → sara-extract | Source documents are user-controlled content processed inline |
| extraction_plan → pipeline-state.json | In-memory JSON written with Read/Write tools only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | sara-extract skill edit | accept | Changes are removals-only from a skill file; no new logic paths, no new input vectors; the Edit tool operates on the local file system only |
| T-15-02 | Tampering | pipeline-state.json (related: [] default) | accept | related: [] is a safe empty default; no injection risk; sara-lint D-07 (Plan 03) handles semantic curation after the fact |
| T-15-03 | Information Disclosure | extraction_plan in pipeline-state.json | accept | Removing temp_id reduces the data written to pipeline-state.json; net improvement, no new disclosure risk |
</threat_model>

<verification>
After both tasks complete:
- `grep "temp_id" .claude/skills/sara-extract/SKILL.md` returns empty (zero matches)
- `grep "Full-mesh\|full-mesh" .claude/skills/sara-extract/SKILL.md` returns empty (zero matches)
- `grep -c "related.*\[\]" .claude/skills/sara-extract/SKILL.md` returns 4 or more (one per pass — default empty field retained)
- `grep "Set \`action\` = \`\"create\"\`" .claude/skills/sara-extract/SKILL.md` returns 4 lines (all four passes intact)
- `grep -n "Step 5" .claude/skills/sara-extract/SKILL.md` then `grep -n "Read.*pipeline-state" .claude/skills/sara-extract/SKILL.md` confirms the pipeline-state.json Read immediately follows the Step 5 heading with no intervening mesh block
</verification>

<success_criteria>
- sara-extract Step 3: no temp_id assignment in any of the four inline extraction passes
- sara-extract Step 5: no full-mesh linking block; stage advances directly after the approval loop
- All four passes still initialize related = [] (default empty field retained per D-01)
- Zero temp_id references remain anywhere in .claude/skills/sara-extract/SKILL.md
- All other extraction logic (sorter, approval loop, pipeline-state.json write, stage advancement) is unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/15-lint-repair/15-01-SUMMARY.md`
</output>
