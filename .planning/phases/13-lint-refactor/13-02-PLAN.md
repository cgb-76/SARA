---
phase: 13-lint-refactor
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified: []
autonomous: false
requirements: []
must_haves:
  truths:
    - "Running /sara-lint on a clean wiki exits with no findings"
    - "Running /sara-lint on a wiki with a missing schema_version field presents a D-02 finding"
    - "Accepting the D-02 fix produces a committed git change with the correct commit message format"
    - "Declining a fix leaves the file unchanged and the lint run continues to the next finding"
    - "Running /sara-lint outside a wiki directory outputs the guard error and stops"
  artifacts:
    - path: ".claude/skills/sara-lint/SKILL.md"
      provides: "Verified v2.0 skill"
      contains: "version: 2.0.0"
  key_links:
    - from: "human tester"
      to: "sara-lint skill"
      via: "Manual /sara-lint invocation in test wiki"
      pattern: "checkpoint:human-verify"
---

<objective>
Verify the rewritten sara-lint v2.0 skill end-to-end. The executor prepares a minimal test wiki with known gaps, runs `/sara-lint`, and the human confirms the lint output matches expectations: correct finding descriptions, correct per-finding prompts, correct commit messages on accepted fixes, and correct no-op behaviour on a clean wiki.

Purpose: Confirm the v2.0 rewrite is correct before the phase is marked complete.

Output: Human approval that sara-lint v2.0 behaves correctly across the acceptance scenarios.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-lint-refactor/13-CONTEXT.md
@.planning/phases/13-lint-refactor/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare test wiki with known schema gaps</name>
  <files>
    /tmp/sara-test-wiki/wiki/requirements/REQ-001.md
    /tmp/sara-test-wiki/wiki/decisions/DEC-001.md
    /tmp/sara-test-wiki/wiki/actions/ACT-001.md
    /tmp/sara-test-wiki/wiki/risks/RSK-001.md
    /tmp/sara-test-wiki/wiki/stakeholders/STK-001.md
    /tmp/sara-test-wiki/wiki/index.md
    /tmp/sara-test-wiki/.sara/config.json
  </files>
  <read_first>
    - .claude/skills/sara-lint/SKILL.md (v2.0 — read to understand what the skill checks)
  </read_first>
  <action>
Create a minimal test wiki at `/tmp/sara-test-wiki/` with the following structure and intentional gaps so that each check type fires at least once:

1. Initialise as a git repo:
```bash
cd /tmp/sara-test-wiki && git init && git config user.email "test@test.com" && git config user.name "Test"
```

2. Create directories:
```bash
mkdir -p wiki/requirements wiki/decisions wiki/actions wiki/risks wiki/stakeholders .sara
```

3. Create `.sara/config.json`:
```json
{
  "project_name": "Test Project",
  "segments": ["Enterprise", "SMB"],
  "departments": ["Engineering", "Sales"]
}
```

4. Create `wiki/requirements/REQ-001.md` — MISSING `schema_version`, `type`, `priority`, `segments`:
```markdown
---
id: REQ-001
title: User can log in
status: accepted
summary: User authentication requirement.
tags: [auth]
related: [DEC-001, BROKEN-999]
---

## Source Quote
"Users must be able to log in."

## Statement
Users must be able to log in via username and password.

## Acceptance Criteria
Login returns a session token.

## Cross Links
[[DEC-001|Logging decision]]
```

5. Create `wiki/decisions/DEC-001.md` — MISSING `schema_version`, `type`, `segments`:
```markdown
---
id: DEC-001
title: Use JWT for session tokens
status: accepted
summary: JWT chosen for stateless auth.
tags: [auth]
related: []
---

## Source Quote
"We decided to use JWT."

## Context
Needed a stateless session mechanism.

## Decision
Use JWT signed with HMAC-SHA256.

## Alternatives Considered
Session cookies — rejected due to server state.

## Rationale
JWT allows stateless verification.

## Cross Links
```

6. Create `wiki/actions/ACT-001.md` — MISSING `schema_version`, `type`, `owner`, `due-date`, `segments`:
```markdown
---
id: ACT-001
title: Write login endpoint
status: open
summary: Build the login API endpoint.
tags: [auth]
related: []
---

## Source Quote
"George will write the login endpoint by end of sprint."

## Description
Implement POST /auth/login endpoint.

## Context
Required by REQ-001.

## Owner
George

## Due Date
2026-05-15

## Cross Links
```

7. Create `wiki/risks/RSK-001.md` — MISSING `schema_version`, `type`, `likelihood`, `impact`, `owner`, `segments`:
```markdown
---
id: RSK-001
title: Session token expiry too short
status: open
summary: Risk of user experience degradation from short token TTL.
tags: [auth]
related: []
---

## Source Quote
"If token TTL is too short users will be logged out frequently."

## Risk IF/THEN
IF the token TTL is set below 15 minutes THEN users will be logged out mid-session causing high frustration.

## Mitigation
Set minimum TTL to 15 minutes with configurable extension.

## Cross Links
```

8. Create `wiki/stakeholders/STK-001.md` — MISSING `segment` (renamed from `vertical`):
```markdown
---
id: STK-001
name: George Beatty
department: Engineering
email: george@example.com
role: Developer
schema_version: '2.0'
summary: Lead developer on auth work.
tags: []
related: []
---
```

9. Create `wiki/index.md` — MISSING REQ-001 (orphaned), contains STALE-001 (stale row):
```markdown
# Wiki Index

| ID | Title | Status | Type | Tags | Last Updated |
|----|-------|--------|------|------|--------------|
| DEC-001 | Use JWT for session tokens | accepted | decision | auth | 2026-04-30 |
| ACT-001 | Write login endpoint | open | action | auth | 2026-04-30 |
| RSK-001 | Session token expiry too short | open | risk | auth | 2026-04-30 |
| STK-001 | George Beatty | — | stakeholder | | 2026-04-30 |
| STALE-001 | Deleted page | open | requirement | | 2026-01-01 |
```

10. Initial commit:
```bash
cd /tmp/sara-test-wiki && git add -A && git commit -m "test: initial wiki with known gaps"
```

Output to user:
```
Test wiki created at /tmp/sara-test-wiki

Known gaps introduced:
  D-02: REQ-001 missing schema_version, type, priority, segments
  D-02: DEC-001 missing schema_version, type, segments
  D-02: ACT-001 missing schema_version, type, owner, due-date, segments
  D-02: RSK-001 missing schema_version, type, likelihood, impact, owner, segments
  D-02: STK-001 missing segment (singular)
  D-03: REQ-001 related[] contains BROKEN-999 (no file exists)
  D-04: REQ-001 is on disk but not in wiki/index.md (orphaned)
  D-05: wiki/index.md references STALE-001 but no file exists on disk
  D-06: REQ-001 Cross Links section lists DEC-001 but related[] also has BROKEN-999 (divergence after D-03 fix, or pre-fix)

Run /sara-lint from /tmp/sara-test-wiki to verify.
```
  </action>
  <verify>
    <automated>ls /tmp/sara-test-wiki/wiki/requirements/REQ-001.md /tmp/sara-test-wiki/wiki/decisions/DEC-001.md /tmp/sara-test-wiki/wiki/actions/ACT-001.md /tmp/sara-test-wiki/wiki/risks/RSK-001.md /tmp/sara-test-wiki/wiki/stakeholders/STK-001.md /tmp/sara-test-wiki/wiki/index.md /tmp/sara-test-wiki/.sara/config.json</automated>
  </verify>
  <acceptance_criteria>
    - All seven test files exist at `/tmp/sara-test-wiki/`
    - `grep -c "schema_version" /tmp/sara-test-wiki/wiki/requirements/REQ-001.md` returns 0 (gap confirmed)
    - `grep -c "STALE-001" /tmp/sara-test-wiki/wiki/index.md` returns 1 (stale row confirmed)
    - `grep -c "REQ-001" /tmp/sara-test-wiki/wiki/index.md` returns 0 (orphaned page confirmed)
    - `grep "BROKEN-999" /tmp/sara-test-wiki/wiki/requirements/REQ-001.md` returns a line (broken ID confirmed)
    - `cd /tmp/sara-test-wiki && git log --oneline | head -1` shows `test: initial wiki with known gaps`
  </acceptance_criteria>
  <done>
    Test wiki exists at /tmp/sara-test-wiki with all seven known gaps confirmed by grep. The git repo has an initial commit.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Task 1 created a test wiki at /tmp/sara-test-wiki with intentional schema gaps covering all five check types (D-02 through D-06). The sara-lint v2.0 skill is ready to run.
  </what-built>
  <how-to-verify>
    1. Open a new Claude Code session (fresh context) at /tmp/sara-test-wiki
    2. Run `/sara-lint`
    3. Confirm the skill outputs a finding count before the loop (e.g. "Found N issues across M checks")
    4. Confirm each finding is presented individually via AskUserQuestion with header "Lint finding [N of M]"
    5. Accept the first D-02 finding (missing schema_version on any page)
    6. Confirm the skill commits the fix immediately with message matching: `fix(wiki): back-fill schema_version on {ID} via sara-lint`
    7. Skip one finding — confirm "Skipped." is output and the loop continues to the next finding
    8. Confirm D-03 finding appears for BROKEN-999 in REQ-001
    9. Confirm D-04 finding appears for REQ-001 not in wiki/index.md
    10. Confirm D-05 finding appears for STALE-001 in wiki/index.md with no disk file
    11. After all findings are processed, confirm a success summary is printed
    12. Run `/sara-lint` again — confirm it finds fewer issues (accepted fixes are gone; skipped ones remain)

    Also test the guard: run `/sara-lint` from a directory without a wiki/ folder and confirm the error message "No wiki found. Run /sara-init first." is output.
  </how-to-verify>
  <resume-signal>Type "approved" if all behaviours matched. Or describe which check failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test wiki → sara-lint | Verification uses a throwaway /tmp directory — no production data at risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06 | Tampering | /tmp test wiki writes | accept | Test files are ephemeral and contain no real data; verification is read-only for the production skill |
</threat_model>

<verification>
After checkpoint approval:

```bash
# Confirm git log in test wiki shows per-fix commits (not a batch commit)
cd /tmp/sara-test-wiki && git log --oneline | head -10

# Confirm accepted fixes are no longer flagged by a re-run
# (re-run should find fewer or zero issues for the fields that were fixed)
```
</verification>

<success_criteria>
1. Human confirms all five check types (D-02 through D-06) each produce at least one finding during the test run
2. Human confirms per-finding approval flow presents one AskUserQuestion per finding with "Lint finding [N of M]" header
3. Human confirms an accepted fix produces an immediate git commit with the correct commit message format
4. Human confirms a skipped finding outputs "Skipped." and the loop continues
5. Human confirms the wiki-guard fires correctly when wiki/ directory is absent
6. Human confirms a second /sara-lint run shows reduced findings for accepted fixes
</success_criteria>

<output>
After completion, create `.planning/phases/13-lint-refactor/13-02-SUMMARY.md`
</output>
