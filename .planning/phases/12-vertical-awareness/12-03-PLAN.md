---
phase: 12-vertical-awareness
plan: 03
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - .claude/skills/sara-init/SKILL.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "sara-init Step 3 prompt asks about segments (not verticals)"
    - "sara-init Step 6 config.json template uses 'segments' key (not 'verticals')"
    - "sara-init Step 9 CLAUDE.md template STK schema block has 'segment:' field (not 'vertical:')"
    - "sara-init Step 9 CLAUDE.md template STK summary comment reads 'STK: segment, department, role'"
    - "sara-init Step 12 stakeholder.md template has 'segment:' field (not 'vertical:')"
    - "sara-init Step 12 CRITICAL note references 'segment' and 'department' (not 'vertical')"
    - "sara-init notes section says 'Segment and department are always separate fields'"
    - "All four entity templates in Step 12 include 'segments: []' after 'related: []'"
    - "All four entity schema blocks in Step 9 CLAUDE.md include 'segments: []' after 'related: []'"
    - "Behavioral Rule 6 summary content rule updated: 'STK: segment/department/role'"
  artifacts:
    - path: ".claude/skills/sara-init/SKILL.md"
      provides: "Updated init skill with full rename + segments field additions"
      contains: "segments_array"
  key_links:
    - from: "sara-init Step 6"
      to: ".sara/config.json template"
      via: "segments key"
      pattern: '"segments"'
    - from: "sara-init Step 9"
      to: "CLAUDE.md entity schema blocks"
      via: "segments: [] field addition"
      pattern: "segments: \[\]"
    - from: "sara-init Step 12"
      to: "entity template files"
      via: "segments: [] field addition"
      pattern: "segments: \[\]"
---

<objective>
Apply the vertical → segment rename and add `segments: []` field to entity templates/schemas
in sara-init.

Purpose: sara-init is the most complex file in Track 1 (rename) with 5 distinct locations, and
it also carries the Track 2 work (adding `segments: []` to all four entity templates and all four
entity schema blocks in the CLAUDE.md template). Plan 01 must complete first so the rename
pattern is established before this more detailed file is edited.

Output: Updated sara-init/SKILL.md with all vertical→segment renames and `segments: []`
additions to entity templates + schema blocks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-vertical-awareness/12-CONTEXT.md
@.planning/phases/12-vertical-awareness/12-PATTERNS.md
@.planning/phases/12-vertical-awareness/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply vertical → segment rename in sara-init</name>
  <files>.claude/skills/sara-init/SKILL.md</files>

  <read_first>
    .claude/skills/sara-init/SKILL.md — read the full file before any edits; there are five
    distinct rename locations and they must all be found and updated correctly.
  </read_first>

  <action>
Apply Track 1 renames to `.claude/skills/sara-init/SKILL.md`. All five locations are listed below.
Apply all renames in a single pass using the Edit tool for each targeted location.

**Location 1 — Objective paragraph (line ~14–18):**
Change:
  `captures project configuration (name, verticals, departments), writes .sara/config.json`
To:
  `captures project configuration (name, segments, departments), writes .sara/config.json`

**Location 2 — Step 3 heading and prompt:**
Change the step heading:
  `**Step 3 — Collect verticals**`
To:
  `**Step 3 — Collect segments**`

Change the question text:
  `> Provide all market verticals that apply? (eg. Residential, BE\&G, Wholesale)`
To:
  `> What segments or customer groups does this project cover? (eg. Residential, BE\&G, Wholesale)`

Change the capture line:
  `Capture the user's reply, split on commas, trim whitespace, and store as \`{verticals_array}\`.`
To:
  `Capture the user's reply, split on commas, trim whitespace, and store as \`{segments_array}\`.`

**Location 3 — Step 6 config.json template:**
Change:
  `  "verticals": {verticals_array},`
To:
  `  "segments": {segments_array},`

Also update the Step 6 introductory text that references the variable name:
  (search for `{verticals_array}` in the Step 6 prose and replace with `{segments_array}`)

**Location 4 — Step 9 CLAUDE.md template — STK schema block:**
In the CLAUDE.md template written by Step 9, find the Stakeholder schema block:

```yaml
### Stakeholder

```yaml
---
id: STK-000
name: ""
nickname: ""  # colloquial name used in transcript body text (e.g. "Raj" for "Rajiwath")
vertical: ""    # from project config verticals list
department: ""  # from project config departments list
```

Change:
  `vertical: ""    # from project config verticals list`
To:
  `segment: ""    # from project config segments list`

Also update the summary comment in this schema block:
  `summary: ""  # STK: vertical, department, role — enough to distinguish from other stakeholders`
To:
  `summary: ""  # STK: segment, department, role — enough to distinguish from other stakeholders`

Also update Behavioral Rule 6 in the CLAUDE.md template — the summary content rule for STK:
  `RSK: likelihood/impact/mitigation/status; STK: vertical/department/role) and the`
To:
  `RSK: likelihood/impact/mitigation/status; STK: segment/department/role) and the`

**Location 5 — Step 12 stakeholder.md template:**
In the stakeholder.md template written by Step 12, find:
  `vertical: ""    # from project config verticals list`
Replace with:
  `segment: ""    # from project config segments list`

Also update the summary comment in this template:
  `summary: ""  # STK: vertical, department, role — enough to distinguish from other stakeholders`
To:
  `summary: ""  # STK: segment, department, role — enough to distinguish from other stakeholders`

Also update the CRITICAL note immediately after the stakeholder template:
  `CRITICAL: \`vertical\` and \`department\` MUST be two separate fields. Never merge them into a`
  `combined field. Do not add body section headings to \`stakeholder.md\`.`
To:
  `CRITICAL: \`segment\` and \`department\` MUST be two separate fields. Never merge them into a`
  `combined field. Do not add body section headings to \`stakeholder.md\`.`

**Location 6 — Notes section:**
Find the note:
  `Vertical and department are always separate fields in both .sara/config.json and entity templates.`
  (This text appears in the notes or a similar trailing location.)
Replace with:
  `Segment and department are always separate fields in both .sara/config.json and entity templates.`

After all renames, run:
  `grep -in "vertical\|verticals" .claude/skills/sara-init/SKILL.md`
Expected: zero results.
  </action>

  <verify>
    <automated>grep -in "vertical" .claude/skills/sara-init/SKILL.md || echo "PASS: no vertical occurrences"</automated>
    <automated>grep -c "segments_array" .claude/skills/sara-init/SKILL.md</automated>
    <automated>grep '"segments"' .claude/skills/sara-init/SKILL.md</automated>
    <automated>grep 'segment: ""' .claude/skills/sara-init/SKILL.md</automated>
    <automated>grep 'STK: segment, department, role' .claude/skills/sara-init/SKILL.md</automated>
    <automated>grep "What segments or customer groups" .claude/skills/sara-init/SKILL.md</automated>
    <automated>grep 'segment.*and.*department.*MUST be two separate' .claude/skills/sara-init/SKILL.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -in "vertical" .claude/skills/sara-init/SKILL.md` returns no output
    - `grep '"segments"' .claude/skills/sara-init/SKILL.md` returns exactly 1 line (config.json template key)
    - `grep 'segment: ""' .claude/skills/sara-init/SKILL.md` returns at least 2 lines (once in Step 9 CLAUDE.md schema, once in Step 12 template)
    - `grep 'STK: segment, department, role' .claude/skills/sara-init/SKILL.md` returns at least 2 lines (summary comments in Step 9 and Step 12)
    - `grep "What segments or customer groups" .claude/skills/sara-init/SKILL.md` returns 1 line (Step 3 prompt)
    - `grep 'segment.*and.*department.*MUST be two separate' .claude/skills/sara-init/SKILL.md` returns 1 line (CRITICAL note)
  </acceptance_criteria>

  <done>sara-init contains no occurrences of "vertical"/"verticals"; all rename locations applied: Step 3 prompt, Step 6 config key, Step 9 STK schema, Step 12 stakeholder template, CRITICAL note, notes section.</done>
</task>

<task type="auto">
  <name>Task 2: Add segments: [] to entity templates and schema blocks in sara-init</name>
  <files>.claude/skills/sara-init/SKILL.md</files>

  <read_first>
    .claude/skills/sara-init/SKILL.md — re-read (or use in-context state from Task 1) to
    locate the eight insertion points: four entity templates in Step 12 and four schema blocks
    in the Step 9 CLAUDE.md template. Each insertion point is after the `related: []` line.
  </read_first>

  <action>
Add `segments: []` after `related: []` at eight locations in `.claude/skills/sara-init/SKILL.md`.
This is Track 2 work: new-wiki entity templates and schema blocks must include the new field.

The YAML serialisation rule (from CONTEXT.md):
- 0 entries: `segments: []`  (flow style — use this for templates, which always start empty)
- 1–2 entries: `segments: [Residential]` or `segments: [Residential, Enterprise]`
- 3+ entries: block style

All eight insertions use the empty-array form: `segments: []   # segment names from config.segments (e.g. [Residential, Enterprise])`

**Step 9 — CLAUDE.md template: four entity schema blocks**

In the CLAUDE.md template content (Step 9), there are four schema blocks: Requirement, Decision,
Action, Risk. Each ends with `related: []` followed by `---`. Insert the segments line between
`related: []` and `---` in each of the four blocks.

1. **Requirement schema block** (after `related: []    # entity IDs (e.g. [DEC-001, ACT-002])`):
   Insert:
   `segments: []   # segment names from config.segments (e.g. [Residential, Enterprise])`

2. **Decision schema block** (after `related: []`):
   Insert:
   `segments: []   # segment names from config.segments (e.g. [Residential, Enterprise])`

3. **Action schema block** (after `related: []`):
   Insert:
   `segments: []   # segment names from config.segments (e.g. [Residential, Enterprise])`

4. **Risk schema block** (after `related: []`):
   Insert:
   `segments: []   # segment names from config.segments (e.g. [Residential, Enterprise])`

Note: the Stakeholder schema block does NOT get a `segments:` field — stakeholders have a
`segment:` (singular) field. Do not add `segments:` to the STK schema block.

**Step 12 — entity template files: four template writes**

In Step 12, sara-init writes five template files. The four artifact templates (requirement.md,
decision.md, action.md, risk.md) each contain a `related: []` frontmatter line. Insert
`segments: []` after each one:

5. **requirement.md template** (after `related: []    # entity IDs (e.g. [DEC-001, ACT-002])`):
   Insert:
   `segments: []   # segment names from config.segments (e.g. [Residential, Enterprise])`

6. **decision.md template** (after `related: []`):
   Insert:
   `segments: []   # segment names from config.segments (e.g. [Residential, Enterprise])`

7. **action.md template** (after `related: []`):
   Insert:
   `segments: []   # segment names from config.segments (e.g. [Residential, Enterprise])`

8. **risk.md template** (after `related: []`):
   Insert:
   `segments: []   # segment names from config.segments (e.g. [Residential, Enterprise])`

The stakeholder.md template is NOT modified — stakeholders have `segment:` (singular), not `segments:`.

After all eight insertions, verify the file contains exactly 8 occurrences of
`segments: []   # segment names from config.segments`.
  </action>

  <verify>
    <automated>grep -c "segments: \[\]   # segment names from config.segments" .claude/skills/sara-init/SKILL.md</automated>
    <automated>grep -n "segments: \[\]" .claude/skills/sara-init/SKILL.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "segments: \[\]   # segment names from config.segments" .claude/skills/sara-init/SKILL.md` outputs `8` (4 schema blocks + 4 templates)
    - All 8 occurrences appear after a `related: []` line (verify by reading surrounding context for each match)
    - The stakeholder template (`.sara/templates/stakeholder.md`) does NOT have a `segments:` field — the template only has `segment: ""` (singular, which was set in Task 1)
  </acceptance_criteria>

  <done>All four entity templates and all four entity schema blocks in sara-init include `segments: []` after `related: []`. Total: 8 insertions. Stakeholder template unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sara-init template → new wiki | Template content becomes the schema contract for all new wikis |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Tampering | Template field additions | accept | Additive change; `segments: []` is a safe default; no existing data affected |
| T-12-05 | Spoofing | vertical → segment rename | accept | Rename is mechanical; grep verification confirms completeness; no config.json migration per D-03 |
</threat_model>

<verification>
After both tasks complete:

```bash
# Confirm no vertical occurrences remain
grep -in "vertical" .claude/skills/sara-init/SKILL.md || echo "PASS"

# Confirm 8 segments insertions
grep -c "segments: \[\]   # segment names from config.segments" .claude/skills/sara-init/SKILL.md
# Expected: 8

# Confirm Step 3 prompt is updated
grep "What segments or customer groups" .claude/skills/sara-init/SKILL.md
# Expected: 1 line

# Confirm config.json template key
grep '"segments"' .claude/skills/sara-init/SKILL.md
# Expected: 1 line
```
</verification>

<success_criteria>
1. Zero occurrences of "vertical"/"verticals" in sara-init/SKILL.md
2. Step 3 prompt reads "What segments or customer groups does this project cover?"
3. Step 6 config.json template uses `"segments"` key with `{segments_array}` variable
4. Step 9 CLAUDE.md STK schema block has `segment: ""` field and `STK: segment, department, role` summary comment
5. Step 12 stakeholder.md template has `segment: ""` field and CRITICAL note references `segment`
6. Eight `segments: []` insertions present (4 schema blocks in Step 9 + 4 templates in Step 12)
7. No `segments:` added to stakeholder.md template (only `segment:` singular)
</success_criteria>

<output>
After completion, create `.planning/phases/12-vertical-awareness/12-03-SUMMARY.md`
</output>
