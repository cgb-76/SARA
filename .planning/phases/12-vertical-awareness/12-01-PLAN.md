---
phase: 12-vertical-awareness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/sara-add-stakeholder/SKILL.md
  - .claude/skills/sara-lint/SKILL.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "sara-add-stakeholder refers to 'segment' everywhere — no 'vertical' string remains"
    - "sara-lint STK summary rule reads 'STK: segment, department, role'"
    - "sara-add-stakeholder config sync reads and writes config.segments (not config.verticals)"
    - "sara-add-stakeholder STK page template writes 'segment:' frontmatter field (not 'vertical:')"
    - "sara-add-stakeholder AskUserQuestion header reads 'Segment' (not 'Vertical')"
  artifacts:
    - path: ".claude/skills/sara-add-stakeholder/SKILL.md"
      provides: "Updated skill with vertical → segment rename applied everywhere"
      contains: "config.segments"
    - path: ".claude/skills/sara-lint/SKILL.md"
      provides: "Updated STK summary rule"
      contains: "STK: segment, department, role"
  key_links:
    - from: "sara-add-stakeholder Step 2"
      to: ".sara/config.json segments array"
      via: "Read tool + config.segments reference"
      pattern: "config\\.segments"
    - from: "sara-add-stakeholder Step 2b"
      to: ".sara/config.json write"
      via: "append to segments array"
      pattern: "config\\.segments"
---

<objective>
Rename `vertical` → `segment` in sara-add-stakeholder and sara-lint.

Purpose: These two files contain all occurrences of the old `vertical`/`verticals` terminology
outside of sara-init and sara-update (which are handled in Plans 03 and 04). This is a pure
find-and-replace with no logic changes — every occurrence of the word `vertical` or `verticals`
is replaced with `segment` or `segments` respectively.

Output: Two updated SKILL.md files with no remaining occurrences of `vertical`/`verticals`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-vertical-awareness/12-CONTEXT.md
@.planning/phases/12-vertical-awareness/12-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename vertical → segment in sara-add-stakeholder</name>
  <files>.claude/skills/sara-add-stakeholder/SKILL.md</files>

  <read_first>
    .claude/skills/sara-add-stakeholder/SKILL.md — read the full file before making any edits;
    the current state is the source of truth.
  </read_first>

  <action>
Apply the following exact substitutions to `.claude/skills/sara-add-stakeholder/SKILL.md`.
Every change is a find-and-replace — no new logic is introduced.

**Objective line (line ~14):**
Change:
  `Capture a stakeholder's details — name (required), plus optional fields: nickname, vertical,`
To:
  `Capture a stakeholder's details — name (required), plus optional fields: nickname, segment,`

**Step 2 — config read comment:**
Change:
  `Read \`.sara/config.json\` using the Read tool. Store the \`verticals\` array for the Vertical prompt`
To:
  `Read \`.sara/config.json\` using the Read tool. Store the \`segments\` array for the Segment prompt`

**Step 2 — AskUserQuestion block:**
Change the header:
  `header: "Vertical"`
To:
  `header: "Segment"`

Change the question:
  `question: "Which market vertical? (leave blank if unknown)"`
To:
  `question: "Which segment? (leave blank if unknown)"`

Change the options comment:
  `options: [values from .sara/config.json verticals array] + ["Skip"]`
To:
  `options: [values from .sara/config.json segments array] + ["Skip"]`

Change the capture line:
  `Capture reply as \`{vertical}\`. If "Skip": set \`{vertical}\` = \`""\`.`
To:
  `Capture reply as \`{segment}\`. If "Skip": set \`{segment}\` = \`""\`.`

**Step 2b — sync logic:**
Change:
  `If \`{vertical}\` is non-empty and not already present in \`config.verticals\`: append \`{vertical}\` to the \`verticals\` array and write the updated JSON back to \`.sara/config.json\` using the Write tool.`
To:
  `If \`{segment}\` is non-empty and not already present in \`config.segments\`: append \`{segment}\` to the \`segments\` array and write the updated JSON back to \`.sara/config.json\` using the Write tool.`

**Step 4 — STK wiki page template:**
Change the frontmatter line:
  `vertical: "{vertical}"    # from project config verticals list`
To:
  `segment: "{segment}"    # from project config segments list`

Change the summary comment in the template:
  `summary: ""  # STK: vertical, department, role — enough to distinguish from other stakeholders`
To:
  `summary: ""  # STK: segment, department, role — enough to distinguish from other stakeholders`

**Notes section — first note:**
Change:
  `` `vertical` (market segment) and the functional area field are ALWAYS written as two separate ``
  `YAML fields — never combined. The functional area field name is \`department\`. This separation`
  `is a locked domain constraint (project memory).`
To:
  `` `segment` (market segment) and the functional area field are ALWAYS written as two separate ``
  `YAML fields — never combined. The functional area field name is \`department\`. This separation`
  `is a locked domain constraint (project memory).`

**Notes section — last note:**
Change:
  `New verticals and departments entered via "New..." are appended to \`.sara/config.json\` in Step 2b and committed alongside the STK page in Step 6. This keeps the config in sync so the new value appears as a selectable option in future \`/sara-add-stakeholder\` runs.`
To:
  `New segments and departments entered are appended to \`.sara/config.json\` in Step 2b and committed alongside the STK page in Step 6. This keeps the config in sync so the new value appears as a selectable option in future \`/sara-add-stakeholder\` runs.`

After all substitutions, verify with grep that no occurrence of `vertical` or `verticals` (case-insensitive)
remains in the file, with one exception: the word "vertical" as part of "vertical slice" or similar
general English terms is acceptable. In this file there are no such exceptions — every occurrence is
the domain concept being renamed.
  </action>

  <verify>
    <automated>grep -in "vertical" .claude/skills/sara-add-stakeholder/SKILL.md || echo "PASS: no vertical occurrences"</automated>
    <automated>grep -c "segment" .claude/skills/sara-add-stakeholder/SKILL.md</automated>
    <automated>grep "config\.segments" .claude/skills/sara-add-stakeholder/SKILL.md</automated>
    <automated>grep 'segment: "{segment}"' .claude/skills/sara-add-stakeholder/SKILL.md</automated>
    <automated>grep 'STK: segment, department, role' .claude/skills/sara-add-stakeholder/SKILL.md</automated>
    <automated>grep 'header: "Segment"' .claude/skills/sara-add-stakeholder/SKILL.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -in "vertical" .claude/skills/sara-add-stakeholder/SKILL.md` returns no output (zero occurrences)
    - `grep 'config\.segments' .claude/skills/sara-add-stakeholder/SKILL.md` returns at least 2 lines (Step 2 read and Step 2b write)
    - `grep 'segment: "{segment}"' .claude/skills/sara-add-stakeholder/SKILL.md` returns exactly 1 line (STK template frontmatter field)
    - `grep 'STK: segment, department, role' .claude/skills/sara-add-stakeholder/SKILL.md` returns exactly 1 line (summary comment)
    - `grep 'header: "Segment"' .claude/skills/sara-add-stakeholder/SKILL.md` returns exactly 1 line
    - `grep '"Which segment?"' .claude/skills/sara-add-stakeholder/SKILL.md` returns exactly 1 line
  </acceptance_criteria>

  <done>sara-add-stakeholder SKILL.md contains no occurrences of "vertical" or "verticals"; all renamed fields, variables, prompts, and config references use "segment"/"segments".</done>
</task>

<task type="auto">
  <name>Task 2: Rename vertical → segment in sara-lint</name>
  <files>.claude/skills/sara-lint/SKILL.md</files>

  <read_first>
    .claude/skills/sara-lint/SKILL.md — read the full file to confirm the exact location and
    wording of the STK summary rule before editing.
  </read_first>

  <action>
Apply the following exact substitution to `.claude/skills/sara-lint/SKILL.md`.

This file has exactly one occurrence of `vertical` — in the Check 1 type-specific content rules
for STK summary generation (the line that describes what to include in a stakeholder summary).

Find the line:
  `- STK: vertical, department, role — enough to distinguish from other stakeholders`

Replace with:
  `- STK: segment, department, role — enough to distinguish from other stakeholders`

This line appears in two locations in the file:
1. In the `{missing_files}` branch dry-run preview section (step listing type-specific content rules)
2. In the Proceed branch (same content rules used for generating summaries)

Apply the replacement to ALL occurrences of this line in the file.

After the edit, verify no occurrence of `vertical` remains in the file.
  </action>

  <verify>
    <automated>grep -in "vertical" .claude/skills/sara-lint/SKILL.md || echo "PASS: no vertical occurrences"</automated>
    <automated>grep 'STK: segment, department, role' .claude/skills/sara-lint/SKILL.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -in "vertical" .claude/skills/sara-lint/SKILL.md` returns no output (zero occurrences)
    - `grep 'STK: segment, department, role' .claude/skills/sara-lint/SKILL.md` returns at least 1 line
  </acceptance_criteria>

  <done>sara-lint SKILL.md contains no occurrences of "vertical"; the STK summary rule reads "STK: segment, department, role".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill file → Claude executor | Prompt text is instruction; renamed fields must be consistent or Claude will use wrong config key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | SKILL.md edits | accept | Pure text rename; no logic change; grep verification confirms completeness |
</threat_model>

<verification>
After both tasks complete:

```bash
grep -rn "vertical" \
  .claude/skills/sara-add-stakeholder/SKILL.md \
  .claude/skills/sara-lint/SKILL.md
```

Expected: no output (zero occurrences across both files).

```bash
grep -rn "segment" \
  .claude/skills/sara-add-stakeholder/SKILL.md \
  .claude/skills/sara-lint/SKILL.md | wc -l
```

Expected: at least 10 lines total (multiple segment references across both files).
</verification>

<success_criteria>
1. Zero occurrences of "vertical" in sara-add-stakeholder/SKILL.md
2. Zero occurrences of "vertical" in sara-lint/SKILL.md
3. sara-add-stakeholder config sync reads `config.segments` and writes to `segments` array
4. sara-add-stakeholder STK page template has `segment: "{segment}"` frontmatter field
5. Both files have summary comment `STK: segment, department, role`
</success_criteria>

<output>
After completion, create `.planning/phases/12-vertical-awareness/12-01-SUMMARY.md`
</output>
