---
phase: 12-vertical-awareness
plan: 04
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - .claude/skills/sara-update/SKILL.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "sara-update STK summary rule reads 'STK: segment, department, role'"
    - "sara-update notes section says 'segment and department are always separate fields'"
    - "sara-update create branch for requirement writes segments from artifact.segments"
    - "sara-update create branch for decision writes segments from artifact.segments"
    - "sara-update create branch for action writes segments from artifact.segments"
    - "sara-update create branch for risk writes segments from artifact.segments"
    - "sara-update update branch for requirement sets segments from artifact.segments"
    - "sara-update update branch for decision sets segments from artifact.segments"
    - "sara-update update branch for action sets segments from artifact.segments"
    - "sara-update update branch for risk sets segments from artifact.segments"
    - "schema_version is NOT changed — still '2.0' for all artifact types"
  artifacts:
    - path: ".claude/skills/sara-update/SKILL.md"
      provides: "Update skill writing segments field to all artifact types in create and update branches"
      contains: "artifact.segments"
  key_links:
    - from: "sara-update create branches"
      to: "wiki page frontmatter segments field"
      via: "artifact.segments write"
      pattern: "artifact\\.segments"
    - from: "sara-update update branches"
      to: "wiki page frontmatter segments field"
      via: "replace existing segments value"
      pattern: "artifact\\.segments.*replace"
---

<objective>
Add the `segments` frontmatter write rule to sara-update's eight entity branches (four create,
four update), and apply the vertical → segment rename to the STK summary rule and notes.

Purpose: This completes the pipeline — once sara-extract produces `segments` on each artifact
and sara-artifact-sorter passes it through, sara-update must write the field to the wiki page
frontmatter. Without this plan, the segments field would be extracted but never persisted.
The rename in this file is minimal (one summary rule, one note).

Output: Updated sara-update/SKILL.md with `segments:` write rules in all 8 entity branches
and the STK terminology rename applied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-vertical-awareness/12-CONTEXT.md
@.planning/phases/12-vertical-awareness/12-PATTERNS.md
@.planning/phases/12-vertical-awareness/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename vertical → segment in sara-update STK summary rule and notes</name>
  <files>.claude/skills/sara-update/SKILL.md</files>

  <read_first>
    .claude/skills/sara-update/SKILL.md — read the full file to locate all occurrences of
    "vertical" in the STK summary rule (Step 2 summary generation block) and the notes section.
  </read_first>

  <action>
Apply Track 1 renames to `.claude/skills/sara-update/SKILL.md`. There are two locations:

**Location 1 — Step 2 STK summary generation rule (in the `summary` = LLM-generated block):**

Find the line listing type-specific content rules for summary generation:
  `      - STK: vertical, department, role — enough to distinguish from other stakeholders`

Change to:
  `      - STK: segment, department, role — enough to distinguish from other stakeholders`

Note: this line appears twice in Step 2 — once in the create branch (`If write succeeds: ...`
summary rules) and once in the update branch (Regenerate the `summary` field). Apply the rename
to ALL occurrences.

**Location 2 — Notes section:**

Find the note:
  `` `vertical` and `department` are always separate fields in stakeholder pages — never merged. This is a locked domain constraint. ``

Change to:
  `` `segment` and `department` are always separate fields in stakeholder pages — never merged. This is a locked domain constraint. ``

After both renames, run:
  `grep -in "vertical" .claude/skills/sara-update/SKILL.md`
Expected: zero results.
  </action>

  <verify>
    <automated>grep -in "vertical" .claude/skills/sara-update/SKILL.md || echo "PASS: no vertical occurrences"</automated>
    <automated>grep -c "STK: segment, department, role" .claude/skills/sara-update/SKILL.md</automated>
    <automated>grep "segment.*and.*department.*are always separate" .claude/skills/sara-update/SKILL.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -in "vertical" .claude/skills/sara-update/SKILL.md` returns no output (zero occurrences)
    - `grep -c "STK: segment, department, role" .claude/skills/sara-update/SKILL.md` outputs at least `2` (create branch + update branch summary rules)
    - `grep "segment.*and.*department.*are always separate" .claude/skills/sara-update/SKILL.md` returns 1 line (notes section)
  </acceptance_criteria>

  <done>sara-update contains no occurrences of "vertical"; STK summary rule and notes section updated to use "segment".</done>
</task>

<task type="auto">
  <name>Task 2: Add segments write rule to all eight entity branches in sara-update</name>
  <files>.claude/skills/sara-update/SKILL.md</files>

  <read_first>
    .claude/skills/sara-update/SKILL.md — re-read (or use in-context state from Task 1) to
    locate all eight entity branch field-assignment blocks. The create branches each have a
    bullet list of field mappings; the update branches each have a block of "Set X = ..."
    instructions. Each branch needs one new bullet or line for segments.
  </read_first>

  <action>
Add the `segments` frontmatter write rule to all eight entity branches in `.claude/skills/sara-update/SKILL.md`.

The YAML serialisation rule (from CONTEXT.md D-08 and code_context):
- 0 entries: `segments: []`
- 1–2 entries: flow style: `segments: [Residential]` or `segments: [Residential, Enterprise]`
- 3+ entries: block style (consistent with `tags`, `related`, `source`)
- schema_version is NOT changed — all four artifact types remain at `'2.0'`

**The segments write rule to add (use this exact text in all eight locations):**

For CREATE branches (add as a new bullet in the field mapping bullet list):
```
    - `segments` = `artifact.segments` (array of segment name strings; write as flow-style YAML:
      `segments: []` for empty, `segments: [Residential]` for one entry,
      `segments: [Residential, Enterprise]` for two; use block style only if the array has 3+
      entries — consistent with `tags`, `related`, `source` in existing templates)
```

For UPDATE branches (add as a new "Set X" line in the v2.0 field update block):
```
    - Set `segments` = `artifact.segments` (array; replace existing value if present; write in
      flow style for 0–2 entries, block style for 3+ — consistent with `tags`, `related`, `source`)
```

**CREATE branch insertions (4 total):**

1. **Requirement create branch** — Find the requirement field mapping bullets. The last field
   mapping bullet before the `schema_version` or `summary` rule is the insertion point. Locate
   the block starting `- For requirement artifacts: set \`status\` = \`"open"\`` and insert the
   segments bullet after the existing requirement-specific fields (`type`, `priority`, `status`,
   `owner`, `raised-by`, `related`, `source`). Insert it before the `summary` = LLM-generated
   line or after the `related` field line — specifically: add the segments bullet after the
   line `- All other fields not supplied by the artifact: use the template default value`.
   
   More precisely: in the create branch, the field mappings are listed as a bullet list under
   "Construct the wiki page content...". The requirement-specific fields end with:
     `- For requirement artifacts: set \`status\` = \`"open"\`; do not set \`description\`...`
   Add the segments bullet immediately after the LAST artifact-type-specific bullet but BEFORE
   the `- All other fields not supplied by the artifact` line, as a new sub-rule under requirement.
   
   **Practical approach:** Find the bullet that reads:
     `- For requirement artifacts: set \`status\` = \`"open"\`; do not set \`description\`...`
   Insert after this line:
     `    - \`segments\` = \`artifact.segments\` (array of segment name strings; write as flow-style YAML: \`segments: []\` for empty, \`segments: [Residential]\` for one entry, \`segments: [Residential, Enterprise]\` for two; use block style only if the array has 3+ entries — consistent with \`tags\`, \`related\`, \`source\` in existing templates)`

2. **Decision create branch** — Find:
     `- For decision artifacts: set \`status\` = \`artifact.status\`...`
   After the full decision bullet block (which ends with `leave \`deciders\` = \`[]\``), insert
   the segments create rule.

3. **Action create branch** — Find:
     `- For action artifacts: set \`status\` = \`"open"\`, \`type\` = \`artifact.act_type\`...`
   After this bullet, insert the segments create rule.

4. **Risk create branch** — Find:
     `- For risk artifacts: set \`type\` = \`artifact.risk_type\`...`
   After this bullet (which is a long multi-field bullet), insert the segments create rule.

**UPDATE branch insertions (4 total):**

5. **Requirement update branch** — Find the block:
     `For requirement artifacts (\`artifact.type == "requirement"\`): after applying the change_summary`
   This block has bullets: `- Set \`type\` =`, `- Set \`priority\` =`, `- Set \`schema_version\` =`,
   `- Remove the \`description\` field`. Add after `- Set \`schema_version\` = \`'2.0'\``:
     `    - Set \`segments\` = \`artifact.segments\` (array; replace existing value if present; write in flow style for 0–2 entries, block style for 3+ — consistent with \`tags\`, \`related\`, \`source\`)`

6. **Decision update branch** — Find the block:
     `For decision artifacts (\`artifact.type == "decision"\`): after applying the change_summary`
   This block has bullets for `type`, `status`, `schema_version`, `Remove v1.0 fields`, `Add source`.
   Add after `- Set \`schema_version\` = \`'2.0'\``:
     `    - Set \`segments\` = \`artifact.segments\` (array; replace existing value if present; write in flow style for 0–2 entries, block style for 3+ — consistent with \`tags\`, \`related\`, \`source\`)`

7. **Action update branch** — Find the block:
     `For action artifacts (\`artifact.type == "action"\`): after applying the change_summary`
   This block has bullets for `type`, `owner`, `due-date`, `schema_version`.
   Add after `- Set \`schema_version\` = \`'2.0'\``:
     `    - Set \`segments\` = \`artifact.segments\` (array; replace existing value if present; write in flow style for 0–2 entries, block style for 3+ — consistent with \`tags\`, \`related\`, \`source\`)`

8. **Risk update branch** — Find the block:
     `For risk artifacts (\`artifact.type == "risk"\`): after applying the change_summary`
   This block has bullets for `type`, `owner`, `raised-by`, `likelihood`, `impact`, `status`,
   `schema_version`, `Remove the \`mitigation\` frontmatter field`.
   Add after `- Set \`schema_version\` = \`'2.0'\``:
     `    - Set \`segments\` = \`artifact.segments\` (array; replace existing value if present; write in flow style for 0–2 entries, block style for 3+ — consistent with \`tags\`, \`related\`, \`source\`)`

After all eight insertions, verify the file contains exactly 8 occurrences of `artifact.segments`.

**schema_version MUST NOT change** — all four types already use `'2.0'` and this phase does not
bump the version. Do not modify any schema_version line.
  </action>

  <verify>
    <automated>grep -c "artifact\.segments" .claude/skills/sara-update/SKILL.md</automated>
    <automated>grep -n "artifact\.segments" .claude/skills/sara-update/SKILL.md</automated>
    <automated>grep -c "schema_version.*2\.0" .claude/skills/sara-update/SKILL.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "artifact\.segments" .claude/skills/sara-update/SKILL.md` outputs `8` (4 create + 4 update)
    - The 8 occurrences are distributed across create and update branches for all four entity types (requirement, decision, action, risk)
    - `grep -c "schema_version.*2\.0" .claude/skills/sara-update/SKILL.md` is unchanged from before this task (no schema_version bumps)
    - Each create branch rule includes the flow/block style guidance for YAML serialisation
    - Each update branch rule includes "replace existing value if present"
  </acceptance_criteria>

  <done>sara-update writes `segments:` from `artifact.segments` to wiki page frontmatter for all four entity types in both create and update branches. Total: 8 write rules added. schema_version unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sara-update → wiki page frontmatter | segments field written from extraction artifact; array must be valid YAML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | Tampering | segments YAML serialisation | mitigate | Explicit flow/block style rule per entry count; consistent with existing tags/related fields |
| T-12-07 | Tampering | schema_version unchanged | mitigate | Explicit prohibition in action; grep verify that schema_version lines are unmodified |
</threat_model>

<verification>
After both tasks complete:

```bash
# Confirm no vertical occurrences remain
grep -in "vertical" .claude/skills/sara-update/SKILL.md || echo "PASS"

# Confirm 8 segments write rules
grep -c "artifact\.segments" .claude/skills/sara-update/SKILL.md
# Expected: 8

# Confirm STK summary rules updated
grep "STK: segment, department, role" .claude/skills/sara-update/SKILL.md
# Expected: at least 2 lines
```
</verification>

<success_criteria>
1. Zero occurrences of "vertical" in sara-update/SKILL.md
2. STK summary rule reads "STK: segment, department, role" in both create and update branches
3. Notes section reads "segment and department are always separate fields"
4. Exactly 8 `artifact.segments` references — one per entity branch (4 create + 4 update)
5. Each create branch rule specifies YAML flow/block serialisation behaviour
6. Each update branch rule specifies "replace existing value if present"
7. schema_version unchanged — all four artifact types remain at '2.0'
</success_criteria>

<output>
After completion, create `.planning/phases/12-vertical-awareness/12-04-SUMMARY.md`
</output>
