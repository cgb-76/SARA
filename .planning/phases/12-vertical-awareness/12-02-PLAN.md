---
phase: 12-vertical-awareness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/sara-extract/SKILL.md
  - .claude/agents/sara-artifact-sorter.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "sara-extract Step 3 reads config.json before the four extraction passes and stores config.segments"
    - "All four extraction passes (requirement, decision, action, risk) produce a segments field in their artifact JSON"
    - "The segments inference rule is present in all four passes: STK-attribution → keyword match → empty fallback"
    - "sara-artifact-sorter passes segments through unchanged for all artifact types in both create and update actions"
    - "The sorter output_format rules section contains the segments passthrough rule"
  artifacts:
    - path: ".claude/skills/sara-extract/SKILL.md"
      provides: "Extract skill with config read and segments field on all four passes"
      contains: "config.segments"
    - path: ".claude/agents/sara-artifact-sorter.md"
      provides: "Sorter with segments passthrough rule"
      contains: "preserve `segments` exactly as received"
  key_links:
    - from: "sara-extract Step 3"
      to: ".sara/config.json segments array"
      via: "Read tool before extraction passes"
      pattern: "config\\.segments"
    - from: "sara-extract extraction passes"
      to: "artifact JSON segments field"
      via: "STK-attribution + keyword matching inference"
      pattern: "segments.*STK.*attribution"
    - from: "sara-artifact-sorter cleaned_artifacts"
      to: "segments field"
      via: "passthrough unchanged"
      pattern: "preserve.*segments"
---

<objective>
Add `segments` field to sara-extract's four extraction passes, and add the `segments` passthrough
rule to sara-artifact-sorter.

Purpose: This implements Track 2 on the extraction side. After this plan, every artifact produced
by sara-extract will carry a `segments` array field. The inference logic (STK-attribution first,
keyword matching second, empty fallback) is embedded in the extraction pass prompt. The sorter
passes the field through unchanged to cleaned_artifacts.

Output: Updated sara-extract/SKILL.md with config read + segments on all 4 passes; updated
sara-artifact-sorter.md with segments passthrough rule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-vertical-awareness/12-CONTEXT.md
@.planning/phases/12-vertical-awareness/12-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add config read and segments field to sara-extract Step 3</name>
  <files>.claude/skills/sara-extract/SKILL.md</files>

  <read_first>
    .claude/skills/sara-extract/SKILL.md — read the full file before editing; Step 3 contains
    the four extraction passes whose exact structure determines where each insertion goes.
  </read_first>

  <action>
Make two targeted additions to `.claude/skills/sara-extract/SKILL.md`:

**Addition 1 — Config read at top of Step 3**

Find the line that marks the start of Step 3 (after the "Step 3 — Inline extraction passes and
sorter" heading and the opening paragraph "The source document is already in context..."):

Find:
  `**Requirements pass**`

Insert BEFORE this line (i.e. between the opening Step 3 paragraph and the Requirements pass heading):

```
Read `.sara/config.json` using the Read tool. Store `config.segments` for use in the `segments`
inference step of each extraction pass below.

```

(One blank line after the new paragraph, before `**Requirements pass**`.)

**Addition 2 — `segments` field bullet in all four extraction passes**

Each of the four passes ends with a field list. The last bullet in each field list is the
`action` / `type` / `id_to_assign` line. Insert the `segments` bullet IMMEDIATELY BEFORE
that final bullet in each of the four passes.

The target bullet to insert before (it appears once per pass, with slight variation):
- Requirements pass: `- Set \`action\` = \`"create"\`, \`type\` = \`"requirement"\`, \`id_to_assign\` = \`"REQ-NNN"\`, \`related\` = \`[]\`, \`change_summary\` = \`""\``
- Decisions pass: `- Set \`action\` = \`"create"\`, \`type\` = \`"decision"\`, \`id_to_assign\` = \`"DEC-NNN"\`, \`related\` = \`[]\`, \`change_summary\` = \`""\``
- Actions pass: `- Set \`action\` = \`"create"\`, \`type\` = \`"action"\`, \`id_to_assign\` = \`"ACT-NNN"\`, \`related\` = \`[]\`, \`change_summary\` = \`""\``
- Risks pass: `- Set \`action\` = \`"create"\`, \`type\` = \`"risk"\`, \`id_to_assign\` = \`"RSK-NNN"\`, \`related\` = \`[]\`, \`change_summary\` = \`""\``

Insert the following bullet before each of those four lines:

```
- Set `segments` to an array of segment name strings (zero or more):
    1. STK attribution: if `source_quote` ends with `— [[STK-NNN|…]]`, parse the STK-NNN ID
       from the attribution, read `wiki/stakeholders/{STK-NNN}.md`, extract the `segment:` field
       value, and add it as the first entry in the array.
    2. Keyword matching: scan the source passage for case-insensitive substrings matching any
       name in `config.segments`; add each matching segment name (deduplicated).
    3. Empty fallback: if neither attribution nor keyword matching resolves any segment name,
       set `segments` = `[]`.
    Deduplication: each segment name appears at most once in the array.
```

Insert this bullet block before the `action` bullet in each of the four passes. The indentation
must match the surrounding bullet list (dash prefix, no extra indentation on the first line).
The numbered sub-list (1. / 2. / 3.) uses 4-space indentation from the dash.

After making all five insertions (1 config read + 4 segments bullets), verify the file contains
exactly four occurrences of "Set `segments`" — one per extraction pass.
  </action>

  <verify>
    <automated>grep -c "Set \`segments\`" .claude/skills/sara-extract/SKILL.md</automated>
    <automated>grep -c "config\.segments" .claude/skills/sara-extract/SKILL.md</automated>
    <automated>grep -n "STK attribution" .claude/skills/sara-extract/SKILL.md</automated>
    <automated>grep -n "Keyword matching" .claude/skills/sara-extract/SKILL.md</automated>
    <automated>grep -n "Empty fallback" .claude/skills/sara-extract/SKILL.md</automated>
    <automated>grep -n "Read \`.sara/config.json\`" .claude/skills/sara-extract/SKILL.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "Set \`segments\`" .claude/skills/sara-extract/SKILL.md` outputs `4` (one per extraction pass)
    - `grep -c "config\.segments" .claude/skills/sara-extract/SKILL.md` outputs at least `1` (the config read line in Step 3)
    - `grep -c "STK attribution" .claude/skills/sara-extract/SKILL.md` outputs `4` (one inference rule block per pass)
    - `grep -c "Keyword matching" .claude/skills/sara-extract/SKILL.md` outputs `4`
    - `grep -c "Empty fallback" .claude/skills/sara-extract/SKILL.md` outputs `4`
    - `grep -n "Read \`.sara/config.json\`" .claude/skills/sara-extract/SKILL.md` shows at least one result in Step 3 (before the Requirements pass heading)
    - The `segments` bullet appears before (not after) the `action`/`type`/`id_to_assign` line in each of the four passes — verify by reading the context around each match
  </acceptance_criteria>

  <done>sara-extract Step 3 reads config.json once before the four passes; each pass produces a `segments` field using STK-attribution → keyword match → empty fallback inference logic.</done>
</task>

<task type="auto">
  <name>Task 2: Add segments passthrough rule to sara-artifact-sorter</name>
  <files>.claude/agents/sara-artifact-sorter.md</files>

  <read_first>
    .claude/agents/sara-artifact-sorter.md — read the full file to locate the `output_format`
    section where existing passthrough rules for `act_type`, `owner`, `due_date`, `risk_type`,
    etc. are defined. The new segments rule must follow the same pattern.
  </read_first>

  <action>
Add the `segments` passthrough rule to `.claude/agents/sara-artifact-sorter.md`.

The `output_format` section currently ends with the action artifact passthrough rule block:

```
- For action artifacts, preserve `act_type`, `owner`, and `due_date` exactly as received from
  the extraction pass. Do not modify, reclassify, or drop these fields. Pass them through unchanged
  to `cleaned_artifacts`.
- For action update artifacts (`action=update`, `type=action`), `act_type`, `owner`, and `due_date`
  MUST be present — copy them from the incoming create artifact unchanged. sara-update reads these
  fields for all action artifacts regardless of action.
```

Append the following two-bullet block immediately after that existing block (at the end of the
`output_format` Rules section, before the closing `</output_format>` tag):

```
- For all artifact types, preserve `segments` exactly as received from the extraction pass. Do not
  modify, reclassify, or drop this field. Pass it through unchanged to `cleaned_artifacts`.
- For update artifacts of any type, `segments` MUST be present — copy it from the incoming artifact
  unchanged. sara-update reads this field for all artifact types regardless of action.
```

The new block follows the exact prose structure and formatting of the existing passthrough rules.

After the edit, verify the file contains the segments passthrough text.
  </action>

  <verify>
    <automated>grep -c "preserve \`segments\`" .claude/agents/sara-artifact-sorter.md</automated>
    <automated>grep -n "segments.*MUST be present" .claude/agents/sara-artifact-sorter.md</automated>
    <automated>grep -c "For all artifact types" .claude/agents/sara-artifact-sorter.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "preserve \`segments\`" .claude/agents/sara-artifact-sorter.md` outputs `1`
    - `grep "segments.*MUST be present" .claude/agents/sara-artifact-sorter.md` returns 1 line
    - `grep "For all artifact types" .claude/agents/sara-artifact-sorter.md` returns 1 line
    - The new rules appear inside the `<output_format>` block (after the action artifact passthrough rules, before `</output_format>`)
  </acceptance_criteria>

  <done>sara-artifact-sorter passes `segments` through unchanged for all artifact types; both create and update artifacts carry the field forward to cleaned_artifacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sara-extract → config.json | Reads external config file; must handle missing file gracefully |
| sara-extract → STK wiki page | Reads STK page for segment attribution; must handle missing page gracefully |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | Information Disclosure | config.segments read | accept | Config is project-internal, no PII in segment names |
| T-12-03 | Denial of Service | STK page missing during attribution | accept | Inference degrades gracefully to keyword matching / empty fallback; skill does not halt |
</threat_model>

<verification>
After both tasks complete:

```bash
grep -c "Set \`segments\`" .claude/skills/sara-extract/SKILL.md
# Expected: 4

grep "preserve \`segments\`" .claude/agents/sara-artifact-sorter.md
# Expected: 1 line

grep "config\.segments" .claude/skills/sara-extract/SKILL.md
# Expected: at least 1 line (the config read in Step 3)
```
</verification>

<success_criteria>
1. sara-extract Step 3 reads `.sara/config.json` once and stores `config.segments` before any extraction pass runs
2. All four extraction passes include the `segments` field with STK-attribution → keyword match → empty fallback inference
3. sara-artifact-sorter `output_format` section contains the segments passthrough rule for all artifact types
4. sara-artifact-sorter specifies that update artifacts MUST carry `segments` unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/12-vertical-awareness/12-02-SUMMARY.md`
</output>
