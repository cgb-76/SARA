---
phase: 02-ingest-pipeline
plan: "07"
type: execute
wave: 7
depends_on: ["02-06"]
files_modified: []
autonomous: false
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03
  - PIPE-04
  - PIPE-05
  - PIPE-06
  - PIPE-07

must_haves:
  truths:
    - "User can run /sara-ingest meeting test-fixture.md and see item 1 created in pending stage"
    - "User can run /sara-ingest (no args) and see a status table listing item 1"
    - "User can run /sara-discuss 1 and the LLM identifies Raj as a known stakeholder (not unknown)"
    - "User can run /sara-discuss 1 and the LLM flags Unknown Person as an unknown stakeholder, triggering /sara-add-stakeholder"
    - "User can run /sara-extract 1 and see artifacts with source_quote citations from test-fixture.md"
    - "User can run /sara-update 1 and see a single git commit containing all artifact files and archived source"
    - "wiki/index.md is updated and wiki/log.md has an ingest event entry after /sara-update completes"
    - "pipeline-state.json shows item 1 stage=complete after /sara-update succeeds"
  artifacts:
    - path: ".sara/pipeline-state.json"
      provides: "Pipeline state showing item 1 at stage=complete"
      contains: "complete"
    - path: "wiki/stakeholders/"
      provides: "STK page for Unknown Person created by /sara-add-stakeholder"
  key_links:
    - from: "git log --oneline -1"
      to: "feat(sara): ingest MTG-001"
      via: "single commit containing all wiki artifacts"
      pattern: "feat.*sara.*ingest.*MTG"
    - from: "wiki/index.md"
      to: "all artifact IDs from /sara-update"
      via: "index rows added by /sara-update"
      pattern: "REQ-\|DEC-\|ACT-\|RISK-"
---

<objective>
Run the full SARA ingest pipeline end-to-end using the test fixture transcript and verify all seven PIPE requirements are satisfied. This is a human-driven verification checkpoint — the user executes each skill in sequence and confirms expected outputs at each stage.

Purpose: Validate that all Phase 2 skills work together correctly as a system, not just in isolation. Catch integration issues (stage transitions, inline sub-skill invocation, atomic commit, source archiving) that cannot be verified from skill prose alone.

Output: Confirmed working pipeline. All PIPE-01 through PIPE-07 requirements verified against observable outputs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ingest-pipeline/02-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pre-flight checks — verify all skills and fixture exist</name>
  <files></files>
  <read_first>
    - .planning/phases/02-ingest-pipeline/02-CONTEXT.md — D-11 through D-14 for expected error behaviours
  </read_first>
  <action>
Run the following checks to confirm all Phase 2 artifacts exist before starting the end-to-end run.

```bash
# All skill files present
ls .claude/skills/sara-ingest/SKILL.md
ls .claude/skills/sara-discuss/SKILL.md
ls .claude/skills/sara-extract/SKILL.md
ls .claude/skills/sara-update/SKILL.md
ls .claude/skills/sara-add-stakeholder/SKILL.md

# Test fixture present
ls raw/input/test-fixture.md

# Nickname field in sara-init (Phase 1 amendment)
grep "nickname" .claude/skills/sara-init/SKILL.md

# pipeline-state.json exists and has empty items (pre-test state)
cat .sara/pipeline-state.json
```

If any check fails: stop and report which artifact is missing. Do not proceed to the pipeline run until all checks pass.

Additionally verify the stage guard error paths work correctly:

Run `/sara-discuss 1` BEFORE running `/sara-ingest`. Expected output: "No pipeline item 1 found." Confirm this error fires correctly.

Run `/sara-ingest meeting nonexistent-file.md`. Expected output: file not found error + listing of raw/input/ contents. Confirm pipeline-state.json is unchanged.

Run `/sara-ingest invalid-type test-fixture.md`. Expected output: invalid type error. Confirm pipeline-state.json is unchanged.
  </action>
  <verify>
    <automated>ls .claude/skills/sara-ingest/SKILL.md .claude/skills/sara-discuss/SKILL.md .claude/skills/sara-extract/SKILL.md .claude/skills/sara-update/SKILL.md .claude/skills/sara-add-stakeholder/SKILL.md raw/input/test-fixture.md</automated>
  </verify>
  <acceptance_criteria>
    - All five SKILL.md files and test-fixture.md are confirmed present by the ls commands
    - `grep "nickname" .claude/skills/sara-init/SKILL.md` exits 0 with at least 2 matches
    - `cat .sara/pipeline-state.json` shows `"items": {}` (empty items before test run)
    - Running `/sara-discuss 1` without a prior ingest produces "No pipeline item 1 found" error, not a skill crash
    - Running `/sara-ingest meeting nonexistent-file.md` produces file-not-found error and lists raw/input/ contents; `cat .sara/pipeline-state.json` still shows `"items": {}` (no change)
    - Running `/sara-ingest invalid-type test-fixture.md` produces invalid type error; pipeline-state.json unchanged
  </acceptance_criteria>
  <done>All pre-flight checks pass. Stage guard error paths verified. System is ready for the full pipeline run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
All five Phase 2 SARA skills are implemented:
- /sara-ingest (PIPE-01, PIPE-07)
- /sara-discuss (PIPE-02, PIPE-03)
- /sara-add-stakeholder (PIPE-03)
- /sara-extract (PIPE-04, PIPE-06)
- /sara-update (PIPE-05)

The test fixture transcript is at `raw/input/test-fixture.md`.

Pre-flight checks confirmed all skills exist and error guards work correctly.
  </what-built>
  <how-to-verify>
Run the full pipeline in sequence. Verify each stage transition and output.

**Step 1 — Ingest (PIPE-01)**
Run: `/sara-ingest meeting test-fixture.md`
Expected:
- Output confirms `MTG-001 registered as item 1. Stage: pending.`
- `cat .sara/pipeline-state.json` shows `items["1"].stage == "pending"` and `items["1"].filename == "test-fixture.md"`

**Step 2 — Pipeline status (PIPE-07)**
Run: `/sara-ingest` (no arguments)
Expected:
- Output shows a markdown table with one row for item 1 (MTG-001, meeting, pending, test-fixture.md)

**Step 3 — Discuss (PIPE-02, PIPE-03)**
Run: `/sara-discuss 1`
Expected:
- LLM reads test-fixture.md and identifies stakeholders
- "Raj" is NOT flagged as unknown (matches Rajiwath Patel's expected nickname after Phase 1 amendment — NOTE: this test requires that a STK page for Rajiwath Patel exists with nickname: "Raj"; if no prior STK pages exist, Raj will be flagged — that is acceptable for first run, create the stakeholder when prompted)
- "Unknown Person" IS flagged as unknown — /sara-add-stakeholder is invoked inline
- A STK-NNN page is created in wiki/stakeholders/ for Unknown Person
- `git log --oneline -2` shows a commit for "add stakeholder STK-001" (or similar)
- LLM works through remaining blockers in priority order
- Session concludes with "Discussion complete. Run /sara-extract 1."
- `cat .sara/pipeline-state.json` shows `items["1"].stage == "extracting"` and `discussion_notes` is non-empty

**Step 4 — Extract (PIPE-04, PIPE-06)**
Run: `/sara-extract 1`
Expected:
- Each proposed artifact is presented with `Source:` field containing a quoted passage from test-fixture.md
- No artifact is presented without a source quote
- For any topic matching an existing wiki entity, the proposal shows `Action: UPDATE` not `CREATE`
- Per-artifact AskUserQuestion appears for each artifact: header "Artifact 1", "Artifact 2", etc.
- After selecting Accept/Reject/Discuss for all artifacts:
  - `cat .sara/pipeline-state.json` shows `items["1"].stage == "approved"`
  - `items["1"].extraction_plan` is a non-empty array with accepted artifact objects

**Step 5 — Update (PIPE-05)**
Run: `/sara-update 1`
Expected:
- Wiki artifact files are created in the correct directories (wiki/requirements/, wiki/decisions/, etc.)
- wiki/index.md has new rows for all created/updated artifacts
- wiki/log.md has an ingest event entry for MTG-001
- `git log --oneline -1` shows `feat(sara): ingest MTG-001 — test-fixture.md`
- `git show --stat HEAD` shows all wiki artifact files AND wiki/index.md AND wiki/log.md in one commit
- `ls raw/meetings/` shows `MTG-001-test-fixture.md` (source archived)
- `ls raw/input/` does NOT show test-fixture.md (source removed from input)
- `cat .sara/pipeline-state.json` shows `items["1"].stage == "complete"`
  </how-to-verify>
  <resume-signal>Type "approved" if all pipeline stages pass, or describe which step failed and what the actual output was.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human verification → plan continuation | User is the trust boundary; they confirm observable outputs match expectations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07-01 | Tampering | test-fixture.md content used in verification | accept | Test fixture contains no secrets, no PII, no sensitive content. Used in a local development environment. |
</threat_model>

<verification>
After human verification is complete:

```bash
# Final state check
cat .sara/pipeline-state.json | grep '"stage"'
# Expected: "stage": "complete" for item 1

git log --oneline -3
# Expected: ingest commit + STK add commit + Phase 1 init commit

ls wiki/requirements/ wiki/decisions/ wiki/actions/ wiki/risks/
# Expected: at least one artifact file in at least one directory

ls raw/meetings/
# Expected: MTG-001-test-fixture.md

ls raw/input/
# Expected: test-fixture.md NOT present (only .gitkeep or empty)

grep "MTG-001" wiki/log.md
# Expected: log entry for the ingest event
```
</verification>

<success_criteria>
1. All five PIPE requirements (PIPE-01 through PIPE-07) verified against observable pipeline outputs
2. Stage transitions occur in correct order: pending → extracting → approved → complete
3. Unknown stakeholder (Unknown Person) was created as a STK page via /sara-add-stakeholder during /sara-discuss
4. Every proposed artifact in /sara-extract had a source_quote from test-fixture.md
5. Single git commit from /sara-update containing all wiki artifacts + index + log + archived source
6. Source file at raw/meetings/MTG-001-test-fixture.md after update; raw/input/test-fixture.md removed
7. pipeline-state.json shows stage=complete for item 1
</success_criteria>

<output>
After completion, create `.planning/phases/02-ingest-pipeline/02-07-SUMMARY.md`
</output>
