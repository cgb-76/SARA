---
phase: 02-ingest-pipeline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .sara/templates/stakeholder.md
  - CLAUDE.md
  - raw/input/test-fixture.md
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03
  - PIPE-04
  - PIPE-05
  - PIPE-06
  - PIPE-07

must_haves:
  truths:
    - ".sara/templates/stakeholder.md contains a nickname field after the name field"
    - "CLAUDE.md Stakeholder schema block contains a nickname field after the name field"
    - "raw/input/test-fixture.md exists with at least one known stakeholder name, one new name with nickname, one requirement, one decision, and one topic matching an existing wiki page concept"
  artifacts:
    - path: ".sara/templates/stakeholder.md"
      provides: "Stakeholder template with nickname field — prerequisite for sara-add-stakeholder"
      contains: "nickname:"
    - path: "CLAUDE.md"
      provides: "CLAUDE.md Stakeholder schema block with nickname field — auto-loaded schema contract"
      contains: "nickname:"
    - path: "raw/input/test-fixture.md"
      provides: "Mock meeting transcript for end-to-end validation"
      contains: "Requirements"
  key_links:
    - from: "CLAUDE.md Stakeholder schema"
      to: ".sara/templates/stakeholder.md"
      via: "Both must have identical field order: id, name, nickname, vertical, department, email, role, schema_version, related"
      pattern: "nickname"
---

<objective>
Amend two Phase 1 artifacts to add the `nickname` field to the stakeholder schema, and create a test fixture file for end-to-end pipeline validation.

Purpose: The `nickname` field (D-07, D-08) enables `/sara-discuss` to match stakeholders referenced by colloquial name in body text. Without this amendment all Phase 2 skills are missing a required field. The test fixture provides a controlled transcript for validating the full ingest → discuss → extract → update pipeline.

Output: Updated `.sara/templates/stakeholder.md`, updated `CLAUDE.md` Stakeholder schema block, and a mock meeting transcript at `raw/input/test-fixture.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ingest-pipeline/02-CONTEXT.md
@.planning/phases/01-foundation-schema/01-CONTEXT.md
</context>

<interfaces>
<!-- Current content of .sara/templates/stakeholder.md — executor must read and amend -->
<!-- Established in sara-init/SKILL.md Step 12: -->
```yaml
---
id: STK-000
name: ""
vertical: ""    # from project config verticals list
department: ""  # from project config departments list
email: ""
role: ""
schema_version: "1.0"
related: []
---
```

<!-- Current CLAUDE.md Stakeholder schema block (### Stakeholder under ## Entity Schemas): -->
```yaml
---
id: STK-000
name: ""
vertical: ""    # from project config verticals list
department: ""  # from project config departments list
email: ""
role: ""
schema_version: "1.0"
related: []
---
```

<!-- Target state for both files — add nickname after name: -->
```yaml
---
id: STK-000
name: ""
nickname: ""  # colloquial name used in transcript body text (e.g. "Raj" for "Rajiwath")
vertical: ""    # from project config verticals list
department: ""  # from project config departments list
email: ""
role: ""
schema_version: "1.0"
related: []
---
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add nickname field to stakeholder template and CLAUDE.md schema block</name>
  <files>.sara/templates/stakeholder.md, CLAUDE.md</files>
  <read_first>
    - .sara/templates/stakeholder.md — read current content before editing; must see the existing field order
    - CLAUDE.md — read current content before editing; locate the ### Stakeholder section under ## Entity Schemas
    - .planning/phases/02-ingest-pipeline/02-CONTEXT.md — D-07 and D-08 specify the exact change required
  </read_first>
  <action>
Amend `.sara/templates/stakeholder.md` per D-07 and D-08: add `nickname: ""  # colloquial name used in transcript body text (e.g. "Raj" for "Rajiwath")` as a new line immediately after the `name: ""` line. The resulting field order must be: id, name, nickname, vertical, department, email, role, schema_version, related.

Do NOT add body section headings — stakeholder.md is frontmatter only (Phase 1 D-11 constraint).

Then amend `CLAUDE.md`: locate the `### Stakeholder` section under `## Entity Schemas`. Inside the fenced code block for the stakeholder schema, add `nickname: ""  # colloquial name used in transcript body text (e.g. "Raj" for "Rajiwath")` immediately after the `name: ""` line. The resulting field order in the CLAUDE.md schema block must match the template exactly: id, name, nickname, vertical, department, email, role, schema_version, related.

Note: CLAUDE.md is at the project root (not wiki/CLAUDE.md). The file is created at runtime by `/sara-init` and these amendments must be applied to that runtime file once it exists. However, the executor must also ensure the sara-init SKILL.md itself is amended so future init runs include the nickname field. Amend `.claude/skills/sara-init/SKILL.md` Step 12 (stakeholder template block at lines 424-437) and Step 9 (Stakeholder schema block in CLAUDE.md write at lines 275-288) to include the nickname field in the same position. This ensures all future `/sara-init` runs produce a nickname-aware schema.
  </action>
  <verify>
    <automated>grep -n "nickname" .claude/skills/sara-init/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "nickname" .claude/skills/sara-init/SKILL.md` exits 0 and shows TWO occurrences: one in the Step 9 CLAUDE.md write block, one in the Step 12 stakeholder template block
    - In both occurrences, the line reads exactly: `nickname: ""  # colloquial name used in transcript body text (e.g. "Raj" for "Rajiwath")`
    - In both occurrences, the nickname line appears immediately after `name: ""`
    - The field order in both blocks is: id, name, nickname, vertical, department, email, role, schema_version, related
    - No body section headings appear in the stakeholder template block (frontmatter only)
  </acceptance_criteria>
  <done>Both the stakeholder template and CLAUDE.md schema block in sara-init SKILL.md include the nickname field in the correct position. The nickname field will be present in all future /sara-init runs.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create test fixture transcript in raw/input/</name>
  <files>raw/input/test-fixture.md</files>
  <read_first>
    - .planning/phases/02-ingest-pipeline/02-RESEARCH.md — Validation Architecture section; Wave 0 Gaps note specifies fixture requirements
    - .planning/phases/02-ingest-pipeline/02-CONTEXT.md — D-07 (nickname matching requirement) to ensure fixture exercises the nickname path
  </read_first>
  <action>
Create `raw/input/test-fixture.md` with the following exact content. This is a mock meeting transcript designed to exercise all pipeline validation paths:

```markdown
---
type: meeting
date: 2026-04-27
project: ACME Platform
participants:
  - Rajiwath Patel
  - Sarah Chen
  - Unknown Person
---

# ACME Platform Integration Review — 2026-04-27

**Attendees:** Rajiwath Patel (Product), Sarah Chen (Engineering), Unknown Person (External)

## Discussion

Raj opened the meeting by reviewing the current API rate limiting situation.
He confirmed that we need to enforce tenant-level API quotas.

**Sarah** raised a concern about the current authentication token design:
the token does not carry a tenant identifier, which would be required for
per-tenant rate limiting to work.

Unknown Person (introduced as a consultant from PartnerCo) suggested reviewing
the existing architecture decision on token expiry before finalising the rate
limit approach.

## Decisions

- The team agreed that API rate limiting will be enforced at 1000 requests
  per hour per tenant. This supersedes the earlier informal cap of 5000/day.

## Actions

- Sarah Chen to update the auth token specification to include tenant_id claim
  by 2026-05-10.
- Raj to circulate the revised rate limit proposal to the wider engineering team.

## Risks

- If the auth token change is delayed, rate limiting cannot be implemented
  correctly. Risk: high impact, medium likelihood.

## Notes

- The API rate limiting requirement relates to the existing platform scalability
  discussion that was covered in prior architecture sessions.
```

This fixture satisfies all validation requirements:
- Known stakeholder by nickname: "Raj" (nickname for Rajiwath Patel)
- Known stakeholder by full name: Sarah Chen
- Unknown stakeholder: Unknown Person (new, with no existing page)
- One requirement: API rate limiting at 1000 req/hour per tenant
- One decision: rate limit enforcement supersedes prior informal cap
- One action: Sarah to update auth token specification
- One risk: auth token delay blocks rate limiting
- One topic matching existing wiki concept: "authentication token" (links to prior architecture context)
  </action>
  <verify>
    <automated>grep -c "Raj\|nickname\|Unknown Person\|requirement\|decision\|risk" raw/input/test-fixture.md</automated>
  </verify>
  <acceptance_criteria>
    - `ls raw/input/test-fixture.md` exits 0 (file exists)
    - `grep -i "rajiwath" raw/input/test-fixture.md` exits 0 (known stakeholder full name present)
    - `grep -i "\bRaj\b" raw/input/test-fixture.md` exits 0 (nickname present in body text, distinct from full name)
    - `grep -i "Unknown Person" raw/input/test-fixture.md` exits 0 (unknown stakeholder present)
    - `grep -i "rate limit" raw/input/test-fixture.md` exits 0 (extractable requirement/decision topic present)
    - `grep -i "Sarah Chen" raw/input/test-fixture.md` exits 0 (second known stakeholder present)
    - File has at least 30 lines: `wc -l raw/input/test-fixture.md` outputs a value >= 30
  </acceptance_criteria>
  <done>raw/input/test-fixture.md exists with a mock meeting transcript containing all required validation elements: known stakeholders (full name and nickname), an unknown stakeholder, extractable requirement, decision, action, risk, and a cross-link topic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan author → executor | Plan specifies file content; executor writes files as instructed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | .sara/templates/stakeholder.md amendment | accept | Additive change to a static template file; no user input; no shell execution; low risk |
| T-02-01-02 | Tampering | CLAUDE.md schema amendment | accept | Additive change to a static schema file; no user input; no shell execution; low risk |
| T-02-01-03 | Tampering | raw/input/test-fixture.md | accept | Static fixture file with known content; no secrets; used for manual testing only |
</threat_model>

<verification>
After task completion, verify amendments are consistent across both locations in sara-init/SKILL.md:

```bash
grep -n "nickname" .claude/skills/sara-init/SKILL.md
```

Expected: two lines — one in the Step 9 block (CLAUDE.md write) and one in the Step 12 block (stakeholder template write). Both lines must read: `nickname: ""  # colloquial name used in transcript body text (e.g. "Raj" for "Rajiwath")`

Verify test fixture exists and has content:

```bash
wc -l raw/input/test-fixture.md
grep "Raj\|Unknown Person\|rate limit" raw/input/test-fixture.md
```
</verification>

<success_criteria>
1. sara-init/SKILL.md contains the nickname field in both the Step 9 (CLAUDE.md schema) and Step 12 (stakeholder template) write blocks, positioned immediately after the name field
2. raw/input/test-fixture.md exists with a mock meeting transcript that exercises: nickname matching, unknown stakeholder, requirement, decision, action, risk, and cross-link topic
3. No existing Phase 1 files are deleted or otherwise broken by these amendments
</success_criteria>

<output>
After completion, create `.planning/phases/02-ingest-pipeline/02-01-SUMMARY.md`
</output>
