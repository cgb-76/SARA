---
phase: 17-document-based-statefulness
plan: 03
type: execute
wave: 2
depends_on: [17-01, 17-02]
files_modified:
  - .claude/skills/sara-discuss/SKILL.md
autonomous: true
requirements: [STF-03]
must_haves:
  truths:
    - "sara-discuss reads .sara/pipeline/{N}/state.md (not pipeline-state.json) for stage guard and item lookup"
    - "sara-discuss loads source_path from state.md frontmatter field (not from JSON items object)"
    - "sara-discuss writes discuss.md as markdown prose (not JSON discussion_notes field)"
    - "sara-discuss writes updated state.md with stage: extracting ONLY after git commit succeeds"
    - "sara-discuss git commit includes discuss.md and updated state.md (not pipeline-state.json)"
    - "sara-discuss does not read or write pipeline-state.json anywhere"
  artifacts:
    - path: ".claude/skills/sara-discuss/SKILL.md"
      provides: "Updated sara-discuss with state.md stage guard and discuss.md output"
      contains: "discuss.md"
  key_links:
    - from: "sara-discuss Step 1"
      to: ".sara/pipeline/{N}/state.md"
      via: "Read tool"
      pattern: "state.md"
    - from: "sara-discuss Step 6"
      to: ".sara/pipeline/{N}/discuss.md"
      via: "Write tool — AFTER git commit"
      pattern: "discuss.md"
---

<objective>
Rewrite sara-discuss SKILL.md to replace pipeline-state.json read/write with state.md and discuss.md file operations. The stage guard reads state.md frontmatter, discussion notes are written to discuss.md, and stage advances to extracting are written to state.md only after the git commit succeeds.

Purpose: sara-discuss is the pipeline's discussion phase. After this change, it reads item state from .sara/pipeline/{ID}/state.md and writes resolved context to .sara/pipeline/{ID}/discuss.md — a persistent markdown document instead of a JSON string field.

Output: Updated .claude/skills/sara-discuss/SKILL.md
</objective>

<execution_context>
@/home/george/.claude/get-shit-done/workflows/execute-plan.md
@/home/george/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-document-based-statefulness/17-CONTEXT.md
@.planning/phases/17-document-based-statefulness/17-RESEARCH.md

<interfaces>
<!-- Key patterns the executor must implement. From RESEARCH.md. -->

state.md frontmatter schema (written by sara-ingest, read by sara-discuss):
```markdown
---
id: MTG-001
type: meeting
filename: transcript-2026-04-27.md
source_path: raw/meetings/MTG-001-transcript-2026-04-27.md
stage: pending
created: 2026-04-27
---
```

Stage guard pattern (new):
```
Read .sara/pipeline/{N}/state.md
Parse frontmatter
If stage != "pending":
  Output error with current stage and correct next command
  STOP
If file does not exist:
  Output item-not-found error
  STOP
```

discuss.md format (written by sara-discuss):
```markdown
## Resolved Stakeholders

- Alice Wang → STK-002 (segment: Residential, role: Product Manager)
- Raj Patel → STK-003 (nickname: "Raj", segment: Enterprise)

## Source Comprehension Clarifications

- "SalesForce" refers to the CRM system used by the Sales department, not a vendor contact.
- "the new platform" = the Project Atlas initiative discussed in MTG-001.
```

Atomic commit ordering (CRITICAL — same invariant as old JSON):
1. Write discuss.md
2. git commit (discuss.md + state.md with stage: extracting)
3. ONLY if commit succeeds: state.md advances to stage: extracting
Actually: the correct order per RESEARCH Pitfall 1 is:
1. Do work (write discuss.md)
2. git add + git commit
3. If commit succeeds: write state.md with stage: extracting
4. If commit fails: output error, leave state.md unchanged (still "pending")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite sara-discuss SKILL.md with state.md + discuss.md pattern</name>
  <files>.claude/skills/sara-discuss/SKILL.md</files>
  <read_first>
    - .claude/skills/sara-discuss/SKILL.md (current full content — executor must read before modifying)
    - .planning/phases/17-document-based-statefulness/17-CONTEXT.md (D-01, D-02 locked decisions)
    - .planning/phases/17-document-based-statefulness/17-RESEARCH.md (Skill-by-Skill Change Map: sara-discuss section; Pitfall 1 — stage advance before commit)
  </read_first>
  <action>
Apply ALL of the following changes to .claude/skills/sara-discuss/SKILL.md. Read the file first, then produce the full rewritten file using the Write tool.

CHANGE 1 — Objective text:
Replace "writes the resolved context to `discussion_notes` in `pipeline-state.json` and advances the item stage to `extracting`" with "writes the resolved context to `.sara/pipeline/{N}/discuss.md` and advances the item stage to `extracting` in `.sara/pipeline/{N}/state.md`".

CHANGE 2 — Step 1 (Stage guard and item lookup):
Remove the current step that reads pipeline-state.json and looks up items["{N}"].
Replace with:

**Step 1 — Stage guard and item lookup**

Validate `$ARGUMENTS`: it must be a non-empty pipeline item ID (e.g. `MTG-001`). If empty:
Output: `"Usage: /sara-discuss <ID> where ID is a pipeline item identifier (e.g. MTG-001)."` and STOP.

Check if `.sara/pipeline/{N}/state.md` exists by attempting to read it with the Read tool.

If the file cannot be read (does not exist):
  Output: `"No pipeline item {N} found. Run /sara-ingest to register a new item, or run /sara-ingest with no arguments to see the full pipeline status."`
  STOP.

Parse the YAML frontmatter from state.md. Extract the following fields:
- `id` → store as `{item.id}`
- `type` → store as `{item.type}`
- `filename` → store as `{item.filename}`
- `source_path` → store as `{item.source_path}`
- `stage` → store as `{item.stage}`
- `created` → store as `{item.created}`

Check `{item.stage}`. Expected stage: `"pending"`.

If `{item.stage}` != `"pending"`:
  Output: `"Item {N} is currently in stage '{item.stage}'. Run /sara-discuss <ID> only when stage is 'pending'. If the item is 'extracting', run /sara-extract {N}."`
  STOP.

CHANGE 3 — Step 2 (Load source and context):
In Step 2, the current text reads: "Read `{item.source_path}` using the Read tool."
Keep this line — it now works from state.md frontmatter `source_path:` field (stored in {item.source_path} from Step 1 parse). The STK grep and config.json reads are unchanged.

Remove the old text: "Build `known_names` by running a Bash grep... Read `.sara/config.json` using the Read tool." — keep these exactly as-is. They do not reference pipeline-state.json.

Only change to make in Step 2: remove any mention of reading pipeline-state.json for item lookup. The item is already loaded from state.md in Step 1.

CHANGE 4 — Step 6 (Write resolved context and advance stage):
Remove the entire current Step 6 block (Read pipeline-state.json, update items["{N}"].discussion_notes and stage, Write JSON).
Replace with:

**Step 6 — Write discuss.md, commit, and advance stage**

Compile `{discussion_notes}` as a structured markdown document summarising all resolved context. Format as follows:

```markdown
## Resolved Stakeholders

{For each stakeholder resolved in Step 4: one line per stakeholder}
- {name} → {STK-NNN} (segment: {segment}, role: {role})

## Source Comprehension Clarifications

{For each comprehension blocker resolved in Step 5: one line per resolution}
- "{passage excerpt}" — {clarification}
```

If no stakeholders were resolved, omit the "## Resolved Stakeholders" section.
If no comprehension blockers were resolved, omit the "## Source Comprehension Clarifications" section.
If both lists are empty (no blockers existed — Step 3 found zero blockers): write a minimal discuss.md:
```markdown
## Resolved Stakeholders

(none)

## Source Comprehension Clarifications

(none — no blockers found)
```

Write `.sara/pipeline/{N}/discuss.md` using the Write tool with the compiled content.

Do NOT use Bash shell text-processing tools — use Write tool only.

Run git commit:
```bash
git add ".sara/pipeline/{N}/discuss.md"
git commit -m "feat(sara): discuss {N} — blockers resolved"
echo "EXIT:$?"
```

Check the exit code from the `echo "EXIT:$?"` output.

If commit FAILS (exit code != 0):
  Output: `"Commit failed for {N}. discuss.md has been written but the commit did not succeed. Stage remains 'pending'. Resolve the git issue and re-run /sara-discuss {N}."`
  STOP. Do NOT write state.md with stage: extracting.

If commit SUCCEEDS (exit code 0):
  Capture `{commit_hash}` by running: `git log --oneline -1`

  Read `.sara/pipeline/{N}/state.md` using the Read tool.
  Reconstruct the frontmatter with `stage: extracting` (all other fields unchanged):
  ```markdown
  ---
  id: {item.id}
  type: {item.type}
  filename: {item.filename}
  source_path: {item.source_path}
  stage: extracting
  created: {item.created}
  ---
  ```
  Write `.sara/pipeline/{N}/state.md` using the Write tool with the updated content.

  Run:
  ```bash
  git add ".sara/pipeline/{N}/state.md"
  git commit -m "feat(sara): stage {N} → extracting"
  ```

  Output:
  ```
  Discussion complete. All blockers resolved.
  discuss.md written to .sara/pipeline/{N}/discuss.md.
  Stage advanced to extracting. Commit: {commit_hash}
  Run /sara-extract {N} to proceed to extraction.
  ```

CHANGE 5 — Notes section:
Replace all notes that reference pipeline-state.json with equivalent state.md/discuss.md references:
- Remove: "pipeline-state.json is written using Read + Write tools only — never shell text-processing tools."
- Add: "state.md is updated using Read + Write tools only — never shell text-processing tools. Read the current state.md, modify stage: field in LLM memory, write the full file back."
- Add: "discuss.md is written with the Write tool only. No Bash text-processing on markdown files."
- Add: "CRITICAL — Stage advance to 'extracting' happens ONLY after the git commit of discuss.md succeeds. Writing state.md with stage: extracting before the commit would leave the item stuck if the commit fails (Pitfall 1)."
- Remove: "Stage advance to 'extracting' happens ONLY after all blockers across Priority 1 and Priority 2 are resolved." (keep but rephrase to also include the commit-ordering requirement)
- Preserve all notes about: stakeholder matching (name + nickname both fields), known_names grep approach, Priority 1 before Priority 2 ordering, AskUserQuestion usage rules, N argument format.

Write the full updated SKILL.md using the Write tool.
  </action>
  <verify>
    <automated>grep -q "discuss.md" /home/george/Projects/sara/.claude/skills/sara-discuss/SKILL.md && ! grep -q "pipeline-state.json" /home/george/Projects/sara/.claude/skills/sara-discuss/SKILL.md && echo OK || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/sara-discuss/SKILL.md does NOT contain "pipeline-state.json" anywhere (grep -c returns 0)
    - .claude/skills/sara-discuss/SKILL.md Step 1 reads ".sara/pipeline/{N}/state.md" using Read tool
    - .claude/skills/sara-discuss/SKILL.md Step 6 writes ".sara/pipeline/{N}/discuss.md" using Write tool
    - .claude/skills/sara-discuss/SKILL.md Step 6 writes state.md with stage: extracting AFTER git commit (not before)
    - .claude/skills/sara-discuss/SKILL.md git commit (Step 6) includes discuss.md
    - .claude/skills/sara-discuss/SKILL.md notes contain "Stage advance to 'extracting' happens ONLY after the git commit"
    - .claude/skills/sara-discuss/SKILL.md notes contain "Read + Write tools only" for state.md writes
    - Verify: grep -c "pipeline-state.json" .claude/skills/sara-discuss/SKILL.md → must return 0
    - Verify: grep -q "discuss.md" .claude/skills/sara-discuss/SKILL.md && echo OK
    - Verify: grep -q "stage: extracting" .claude/skills/sara-discuss/SKILL.md && echo OK
  </acceptance_criteria>
  <done>sara-discuss SKILL.md fully updated: state.md stage guard, discuss.md write, atomic commit ordering (stage advance after commit), no pipeline-state.json references</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| state.md frontmatter → skill execution | state.md is read and its fields drive behaviour; a corrupted state.md could cause incorrect stage advancement |
| discuss.md write → git commit | discuss.md content is written before commit; commit failure must leave state.md unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03-01 | Tampering | state.md frontmatter corruption | accept | LLM parses YAML natively; malformed frontmatter causes read failure handled by item-not-found guard |
| T-17-03-02 | Denial of Service | Stage advance before commit (Pitfall 1) | mitigate | Plan explicitly sequences: write discuss.md → git commit → ONLY if success write state.md stage: extracting; acceptance criteria verify the ordering note |
| T-17-03-03 | Information Disclosure | discuss.md markdown content | accept | discuss.md is a local project file containing resolved stakeholder context; no external exposure |
</threat_model>

<verification>
```bash
# No pipeline-state.json references
grep -c "pipeline-state.json" .claude/skills/sara-discuss/SKILL.md

# state.md read present
grep -n "state.md" .claude/skills/sara-discuss/SKILL.md | head -10

# discuss.md write present
grep -n "discuss.md" .claude/skills/sara-discuss/SKILL.md | head -10

# Stage ordering note present
grep -n "ONLY after" .claude/skills/sara-discuss/SKILL.md
```
Expected: first command returns 0; others return matches.
</verification>

<success_criteria>
- sara-discuss SKILL.md contains zero references to pipeline-state.json
- Step 1 reads .sara/pipeline/{N}/state.md and checks stage field
- Step 6 writes discuss.md then git commits, THEN writes state.md with stage: extracting
- Notes explicitly state the commit-ordering invariant (stage advance only after commit)
</success_criteria>

<output>
After completion, create `.planning/phases/17-document-based-statefulness/17-03-SUMMARY.md`
</output>
