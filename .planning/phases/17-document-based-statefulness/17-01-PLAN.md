---
phase: 17-document-based-statefulness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/sara-init/SKILL.md
autonomous: true
requirements: [STF-01]
must_haves:
  truths:
    - "sara-init creates .sara/pipeline/ directory with .gitkeep instead of pipeline-state.json"
    - "sara-init writes summary_max_words: 50 to .sara/config.json"
    - "CLAUDE.md generated by sara-init instructs filesystem-derivation for ID assignment (not pipeline-state.json counter increment)"
    - "sara-init success report and git add list reference .sara/pipeline/.gitkeep instead of .sara/pipeline-state.json"
  artifacts:
    - path: ".claude/skills/sara-init/SKILL.md"
      provides: "Updated sara-init with pipeline/ directory creation"
      contains: ".sara/pipeline/"
  key_links:
    - from: "sara-init Step 7"
      to: ".sara/pipeline/ directory"
      via: "mkdir -p .sara/pipeline && touch .sara/pipeline/.gitkeep"
      pattern: "mkdir.*pipeline"
    - from: "sara-init Step 6"
      to: ".sara/config.json"
      via: "Write tool"
      pattern: "summary_max_words"
---

<objective>
Rewrite sara-init SKILL.md to replace pipeline-state.json creation with the new directory-based pipeline store.

Purpose: sara-init is the entry point for every new SARA project. After this change, new projects will have .sara/pipeline/ instead of .sara/pipeline-state.json, and the generated CLAUDE.md will instruct filesystem-derived ID assignment. summary_max_words moves to config.json.

Output: Updated .claude/skills/sara-init/SKILL.md
</objective>

<execution_context>
@/home/george/.claude/get-shit-done/workflows/execute-plan.md
@/home/george/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-document-based-statefulness/17-CONTEXT.md
@.planning/phases/17-document-based-statefulness/17-RESEARCH.md

<interfaces>
<!-- Current sara-init SKILL.md key change points. Executor reads the full file first. -->

Step 6 currently writes .sara/config.json WITHOUT summary_max_words:
```json
{
  "project": "{project_name}",
  "segments": {segments_array},
  "departments": {departments_array},
  "schema_version": "1.0"
}
```

Step 7 currently writes .sara/pipeline-state.json:
```json
{
  "summary_max_words": 50,
  "counters": { "ingest": {...}, "entity": {...} },
  "items": {}
}
```

Step 9 CLAUDE.md Rule 4 currently says:
"Before assigning a new entity ID, increment the relevant counter in `.sara/pipeline-state.json`."

Step 9 CLAUDE.md Rule 6 currently references:
"the `summary_max_words` limit from `.sara/pipeline-state.json` (default: 50 words if absent)."

Step 13 success report currently lists: `.sara/pipeline-state.json`
Step 14 git add currently includes: `.sara/pipeline-state.json`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite sara-init SKILL.md with pipeline/ directory creation</name>
  <files>.claude/skills/sara-init/SKILL.md</files>
  <read_first>
    - .claude/skills/sara-init/SKILL.md (current full content — executor must read before modifying)
    - .planning/phases/17-document-based-statefulness/17-CONTEXT.md (D-01, D-02, D-03, D-05 locked decisions)
    - .planning/phases/17-document-based-statefulness/17-RESEARCH.md (Skill-by-Skill Change Map: sara-init section)
  </read_first>
  <action>
Apply ALL of the following changes to .claude/skills/sara-init/SKILL.md. Read the file first to see exact current content, then produce the full rewritten file.

CHANGE 1 — Objective text:
Replace "writes .sara/config.json and .sara/pipeline-state.json" with "writes .sara/config.json (including summary_max_words) and creates the .sara/pipeline/ directory".

CHANGE 2 — Step 5 (Create directory tree):
Add `.sara/pipeline` to the `mkdir -p` command and `.sara/pipeline/.gitkeep` to the `touch` command. The current mkdir block creates `.sara/templates` as the last entry; add `.sara/pipeline` before or after it. The touch block similarly must gain `.sara/pipeline/.gitkeep`.

CHANGE 3 — Step 6 (Write .sara/config.json):
Add `"summary_max_words": 50` to the config.json template. The updated content must be:
```json
{
  "project": "{project_name}",
  "segments": {segments_array},
  "departments": {departments_array},
  "schema_version": "1.0",
  "summary_max_words": 50
}
```

CHANGE 4 — Step 7 (Replace pipeline-state.json with pipeline/ directory):
REMOVE the entire Step 7 block that writes .sara/pipeline-state.json.
REPLACE it with a new Step 7 block:

**Step 7 — Create .sara/pipeline/ directory**

The `.sara/pipeline/` directory is already created in Step 5 (via `mkdir -p`). The `.gitkeep` file inside it (also created in Step 5) ensures the directory is tracked in git even when empty.

No additional action is required in this step. The pipeline directory is ready for use by `/sara-ingest`.

Note: Do NOT create `.sara/pipeline-state.json`. The new state backend is the `.sara/pipeline/` directory.

CHANGE 5 — Step 9 (CLAUDE.md template — update Rule 4):
In the CLAUDE.md template embedded in Step 9, replace Rule 4 text:
OLD: "Before assigning a new entity ID, increment the relevant counter in `.sara/pipeline-state.json`. Read the post-increment value and use it as the new ID (e.g. REQ-001)."
NEW: "Before assigning a new entity ID, derive the next ID by listing the relevant wiki directory and incrementing the highest existing numeric suffix. For example, to assign the next REQ ID: `ls wiki/requirements/ | grep '^REQ-' | sort | tail -1` — parse the numeric suffix from the result, increment by 1, zero-pad to 3 digits (e.g. REQ-007 → REQ-008). If no pages exist yet, start at 001. This applies to all entity types (REQ, DEC, ACT, RSK, STK)."

CHANGE 6 — Step 9 (CLAUDE.md template — update Rule 6):
In the CLAUDE.md template embedded in Step 9, replace Rule 6 text reference from `.sara/pipeline-state.json` to `.sara/config.json`:
OLD: "the `summary_max_words` limit from `.sara/pipeline-state.json` (default: 50 words if absent)."
NEW: "the `summary_max_words` value from `.sara/config.json` (default: 50 words if absent)."

CHANGE 7 — Step 13 (Success report):
Replace `.sara/pipeline-state.json` in the files created list with `.sara/pipeline/` (and note it is a directory, not a file). The directory list entry should read: `.sara/pipeline/` (pipeline state directory).

CHANGE 8 — Step 14 (git add):
Replace `.sara/pipeline-state.json` in the git add command with `.sara/pipeline/.gitkeep`.

The notes section does NOT need changes — it does not reference pipeline-state.json directly. Preserve all existing notes verbatim.

Write the full updated SKILL.md using the Write tool. Do not use Edit tool — the changes are distributed across the file and a full rewrite ensures correctness.
  </action>
  <verify>
    <automated>grep -q "pipeline/" /home/george/Projects/sara/.claude/skills/sara-init/SKILL.md && grep -q "summary_max_words" /home/george/Projects/sara/.claude/skills/sara-init/SKILL.md && grep -q "pipeline-state.json" /home/george/Projects/sara/.claude/skills/sara-init/SKILL.md && echo "FAIL: pipeline-state.json still present" || echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/sara-init/SKILL.md contains "mkdir -p" with ".sara/pipeline" in the command
    - .claude/skills/sara-init/SKILL.md contains ".sara/pipeline/.gitkeep" in the touch command
    - .claude/skills/sara-init/SKILL.md contains "summary_max_words": 50 in the config.json template block
    - .claude/skills/sara-init/SKILL.md does NOT contain ".sara/pipeline-state.json" anywhere
    - .claude/skills/sara-init/SKILL.md contains "derive the next ID by listing the relevant wiki directory" in Rule 4 of the CLAUDE.md template
    - .claude/skills/sara-init/SKILL.md contains "summary_max_words" and ".sara/config.json" in Rule 6 of the CLAUDE.md template
    - .claude/skills/sara-init/SKILL.md contains ".sara/pipeline/.gitkeep" in the git add command (Step 14)
    - Verify with: grep -c "pipeline-state.json" .claude/skills/sara-init/SKILL.md → must return 0
    - Verify with: grep -q "pipeline/" .claude/skills/sara-init/SKILL.md && echo OK
    - Verify with: grep -q "summary_max_words" .claude/skills/sara-init/SKILL.md && echo OK
  </acceptance_criteria>
  <done>sara-init SKILL.md fully updated: pipeline/ directory created instead of pipeline-state.json, summary_max_words in config.json, CLAUDE.md template uses filesystem-derived ID assignment, git add uses .gitkeep</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system → SKILL.md content | Executor writes skill file content; incorrect instructions could cause downstream skills to corrupt pipeline state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01-01 | Tampering | sara-init Step 7 | mitigate | Verify pipeline-state.json is not created by checking absence of that string in written SKILL.md; acceptance criteria enforce this |
| T-17-01-02 | Information Disclosure | CLAUDE.md Rule 4 | accept | Rule update instructs filesystem glob — no secrets involved; ls output is local filesystem only |
| T-17-01-03 | Denial of Service | mkdir -p .sara/pipeline | accept | mkdir -p is idempotent; existing directory is harmless; -p flag prevents failure if parent absent |
</threat_model>

<verification>
```bash
# Confirm no pipeline-state.json references remain
grep -c "pipeline-state.json" .claude/skills/sara-init/SKILL.md

# Confirm pipeline/ directory creation is present
grep -n "pipeline/" .claude/skills/sara-init/SKILL.md | head -10

# Confirm summary_max_words in config.json template
grep -n "summary_max_words" .claude/skills/sara-init/SKILL.md

# Confirm .gitkeep in git add
grep -n "pipeline/.gitkeep" .claude/skills/sara-init/SKILL.md
```
Expected: first command returns 0; others return matches.
</verification>

<success_criteria>
- sara-init SKILL.md contains zero references to pipeline-state.json
- Step 6 config.json template includes "summary_max_words": 50
- Step 7 creates .sara/pipeline/ directory (not pipeline-state.json)
- CLAUDE.md Rule 4 in template uses filesystem glob derivation
- CLAUDE.md Rule 6 in template references config.json for summary_max_words
- git add (Step 14) includes .sara/pipeline/.gitkeep
</success_criteria>

<output>
After completion, create `.planning/phases/17-document-based-statefulness/17-01-SUMMARY.md`
</output>
