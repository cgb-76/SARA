---
phase: 17-document-based-statefulness
plan: 04
type: execute
wave: 2
depends_on: [17-01, 17-02]
files_modified:
  - .claude/skills/sara-extract/SKILL.md
autonomous: true
requirements: [STF-04]
must_haves:
  truths:
    - "sara-extract reads .sara/pipeline/{N}/state.md for stage guard (not pipeline-state.json)"
    - "sara-extract reads .sara/pipeline/{N}/discuss.md for discussion_notes (empty string if absent)"
    - "sara-extract writes approved artifact list as headed markdown body to .sara/pipeline/{N}/plan.md"
    - "sara-extract writes state.md with stage: approved ONLY after git commit of plan.md succeeds"
    - "sara-extract git commit includes plan.md and updated state.md (not pipeline-state.json)"
    - "sara-extract does not read or write pipeline-state.json anywhere"
  artifacts:
    - path: ".claude/skills/sara-extract/SKILL.md"
      provides: "Updated sara-extract with state.md guard and plan.md output"
      contains: "plan.md"
  key_links:
    - from: "sara-extract Step 1"
      to: ".sara/pipeline/{N}/state.md"
      via: "Read tool — checks stage: extracting"
      pattern: "state.md"
    - from: "sara-extract Step 5"
      to: ".sara/pipeline/{N}/plan.md"
      via: "Write tool — AFTER approval loop"
      pattern: "plan.md"
---

<objective>
Rewrite sara-extract SKILL.md to replace pipeline-state.json operations with state.md/discuss.md reads and plan.md write. The stage guard reads state.md, discussion notes come from discuss.md (graceful empty-string fallback if absent), the approved artifact list is written as headed markdown to plan.md, and stage advances to approved in state.md only after git commit succeeds.

Purpose: sara-extract is the extraction and planning phase. After this change, it reads from .sara/pipeline/{ID}/ files and writes the canonical plan.md that sara-update will consume.

Output: Updated .claude/skills/sara-extract/SKILL.md
</objective>

<execution_context>
@/home/george/.claude/get-shit-done/workflows/execute-plan.md
@/home/george/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-document-based-statefulness/17-CONTEXT.md
@.planning/phases/17-document-based-statefulness/17-RESEARCH.md

<interfaces>
<!-- Key patterns from RESEARCH.md the executor must implement. -->

state.md stage guard (checks stage: extracting):
```
Read .sara/pipeline/{N}/state.md
Parse frontmatter
If stage != "extracting":
  Output error
  STOP
```

discuss.md read with graceful fallback:
```
Read .sara/pipeline/{N}/discuss.md
If file absent (Read returns error): {discussion_notes} = "" (continue — do not stop)
If file present: {discussion_notes} = markdown body content
```

plan.md format (headed-section-per-artifact — Claude's Discretion resolution from RESEARCH.md Pattern 4):
```markdown
## Artifact 1 — CREATE requirement

**Type:** requirement
**Title:** API rate limiting per tenant
**Action:** create
**Req type:** functional
**Priority:** must-have
**Source quote:** "we must implement rate limiting for each tenant"
**Raised by:** STK-001
**Segments:** [Residential, Enterprise]
**Related:** []

---

## Artifact 2 — UPDATE decision DEC-003

**Type:** decision
**Title:** Auth token expiry policy
**Action:** update
**Existing ID:** DEC-003
**Dec type:** architectural
**Status:** accepted
**Chosen option:** 24-hour expiry with refresh tokens
**Change summary:** Updated to reflect alignment reached in this meeting
**Source quote:** "we agreed on 24-hour token expiry"
**Raised by:** STK-002
**Segments:** [Enterprise]
**Related:** [REQ-005]
```

Note on IDs in plan.md: for create artifacts, write placeholder IDs (REQ-NNN, DEC-NNN) — actual IDs are assigned by sara-update at write time. For update artifacts, write the real existing ID (e.g. DEC-003).

Atomic commit ordering (Pitfall 1 — CRITICAL):
1. Approval loop completes
2. Write plan.md
3. git add plan.md + git commit
4. ONLY if commit succeeds: write state.md with stage: approved
5. If commit fails: output error, leave state.md with stage: extracting
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite sara-extract SKILL.md with state.md guard, discuss.md read, and plan.md write</name>
  <files>.claude/skills/sara-extract/SKILL.md</files>
  <read_first>
    - .claude/skills/sara-extract/SKILL.md (current full content — executor must read before modifying)
    - .planning/phases/17-document-based-statefulness/17-CONTEXT.md (D-01, D-02, D-04 locked decisions)
    - .planning/phases/17-document-based-statefulness/17-RESEARCH.md (Skill-by-Skill Change Map: sara-extract; Pattern 4 plan.md format; Pitfall 1; Pitfall 6 — discuss.md absent graceful fallback)
  </read_first>
  <action>
Apply ALL of the following changes to .claude/skills/sara-extract/SKILL.md. Read the file first, then produce the full rewritten file using the Write tool.

CHANGE 1 — Objective text:
Replace "Approved artifacts are written to `extraction_plan` in `pipeline-state.json` and the item stage advances to `approved`" with "Approved artifacts are written as a headed markdown body to `.sara/pipeline/{N}/plan.md` and the item stage advances to `approved` in `.sara/pipeline/{N}/state.md`".

CHANGE 2 — Step 1 (Stage guard and item lookup):
Remove the current block that reads pipeline-state.json and looks up items["{N}"].
Replace with:

**Step 1 — Stage guard and item lookup**

Validate `$ARGUMENTS`: it must be a non-empty pipeline item ID (e.g. `MTG-001`). If empty:
Output: `"Usage: /sara-extract <ID> where ID is a pipeline item identifier (e.g. MTG-001)."` and STOP.

Check if `.sara/pipeline/{N}/state.md` exists by attempting to read it with the Read tool.

If the file cannot be read (does not exist):
  Output: `"No pipeline item {N} found. Run /sara-ingest to register a new item, or run /sara-ingest with no arguments to see the full pipeline status."`
  STOP.

Parse the YAML frontmatter from state.md. Extract:
- `id` → store as `{item.id}`
- `type` → store as `{item.type}`
- `filename` → store as `{item.filename}`
- `source_path` → store as `{item.source_path}`
- `stage` → store as `{item.stage}`
- `created` → store as `{item.created}`

Check `{item.stage}`. Expected stage: `"extracting"`.

If `{item.stage}` != `"extracting"`:
  Output: `"Item {N} is in stage '{item.stage}'. Run /sara-extract <ID> only when stage is 'extracting'. Re-run /sara-discuss {N} if you need to revisit the discussion."`
  STOP.

CHANGE 3 — Step 2 (Load source, discussion notes, and wiki index):
The current Step 2 reads: "`{discussion_notes}` = `items["{N}"].discussion_notes` (already in memory from the Step 1 read of pipeline-state.json)."
Replace with:

**Step 2 — Load source, discussion notes, and wiki index**

Read `{item.source_path}` using the Read tool. This is the source document.

Attempt to read `.sara/pipeline/{N}/discuss.md` using the Read tool.
- If the file is present: set `{discussion_notes}` = the markdown body content of discuss.md.
- If the file is absent (Read returns an error): set `{discussion_notes}` = `""` (empty string). Continue — do not STOP. An absent discuss.md is valid if sara-discuss found no blockers or if discuss.md was not committed due to an earlier failure. The extraction proceeds without discussion context.

Read `wiki/index.md` HERE using the Read tool. (Unchanged from current Step 2 — keep the Pitfall 4 guard note verbatim.)

CHANGE 4 — Step 3 inline extraction passes (no changes to extraction logic):
The four extraction passes (Requirements, Decisions, Actions, Risks), the sorter dispatch, and the sorter question loop are unchanged. Preserve them verbatim. Only ensure that no references to pipeline-state.json appear in this section. (There are none in the current skill — this is a confirmation step.)

CHANGE 5 — Step 5 (Write extraction plan and advance stage):
Remove the entire current Step 5 block (Read pipeline-state.json, update items["{N}"].extraction_plan and stage, Write JSON).
Replace with:

**Step 5 — Write plan.md, commit, and advance stage**

Step 5 ALWAYS writes the full `approved_artifacts` list to `plan.md`, replacing any previously stored value. Do NOT read or merge a pre-existing `plan.md` — overwrite it unconditionally.

Compose the plan.md content. Write one `##` section per artifact in `approved_artifacts`, in order. Use the following format for each artifact:

For CREATE artifacts:
```markdown
## Artifact {N} — CREATE {artifact.type}

**Type:** {artifact.type}
**Title:** {artifact.title}
**Action:** create
**{type-specific field name}:** {value}    (e.g. "Req type:" for requirements, "Dec type:" for decisions, "Act type:" for actions, "Risk type:" for risks)
**Priority:** {artifact.priority}           (requirements only; omit for other types)
**Status:** {artifact.status}               (decisions and risks only; omit for requirements and actions)
**Chosen option:** {artifact.chosen_option} (decisions only; omit for other types)
**Source quote:** {artifact.source_quote}
**Raised by:** {artifact.raised_by}
**Owner:** {artifact.owner}                 (actions and risks only; omit for other types)
**Due date:** {artifact.due_date}           (actions only; omit for other types)
**Likelihood:** {artifact.likelihood}       (risks only; omit for other types)
**Impact:** {artifact.impact}               (risks only; omit for other types)
**Segments:** {artifact.segments as comma-separated list in brackets}
**Related:** {artifact.related as comma-separated list in brackets}
```
Omit fields that are not applicable to the artifact type (e.g. Priority only on requirements, Status only on decisions/risks). Omit fields with empty string values for optional fields (owner, due_date, likelihood, impact). Always include Type, Title, Action, Source quote, Raised by, Segments, Related.

For UPDATE artifacts, use the same format but with:
```markdown
## Artifact {N} — UPDATE {artifact.type} {artifact.existing_id}

**Type:** {artifact.type}
**Title:** {artifact.title}
**Action:** update
**Existing ID:** {artifact.existing_id}
**Change summary:** {artifact.change_summary}
... (other applicable fields)
```

Separate each artifact section with a `---` horizontal rule.

If `approved_artifacts` is empty: write a minimal plan.md:
```markdown
(No artifacts approved — extraction plan is empty.)
```

Write `.sara/pipeline/{N}/plan.md` using the Write tool with the composed content.

Run git commit:
```bash
git add ".sara/pipeline/{N}/plan.md"
git commit -m "feat(sara): plan {N} — {count_accepted} artifacts"
echo "EXIT:$?"
```

Check the exit code.

If commit FAILS (exit code != 0):
  Output: `"Commit failed for {N}. plan.md has been written but the commit did not succeed. Stage remains 'extracting'. Resolve the git issue and re-run /sara-extract {N}."`
  STOP. Do NOT write state.md with stage: approved.

If commit SUCCEEDS (exit code 0):
  Capture `{commit_hash}` by running: `git log --oneline -1`

  Read `.sara/pipeline/{N}/state.md` using the Read tool.
  Reconstruct frontmatter with `stage: approved` (all other fields unchanged):
  ```markdown
  ---
  id: {item.id}
  type: {item.type}
  filename: {item.filename}
  source_path: {item.source_path}
  stage: approved
  created: {item.created}
  ---
  ```
  Write `.sara/pipeline/{N}/state.md` using the Write tool.

  Run:
  ```bash
  git add ".sara/pipeline/{N}/state.md"
  git commit -m "feat(sara): stage {N} → approved"
  ```

Output summary table:

```
## Extraction Plan — Item {N}

{count_accepted} artifacts accepted / {count_rejected} rejected.

| # | Action | Type        | Title                              |
|---|--------|-------------|------------------------------------|
| 1 | CREATE | requirement | API rate limiting per tenant       |
| 2 | UPDATE | decision    | Auth token expiry policy           |

Run /sara-update {N} to write approved artifacts to the wiki.
```

If zero artifacts were accepted: still write the empty plan.md and advance stage to `"approved"`. Output: `"0 artifacts accepted. plan.md written (empty). Stage advanced to approved. You can still run /sara-update {N} (it will be a no-op) or re-run /sara-discuss {N} to revisit the source."`

CHANGE 6 — Notes section:
- Remove all references to pipeline-state.json and extraction_plan JSON field
- Add: "plan.md is written using the Write tool only — no Bash text-processing on markdown files."
- Add: "CRITICAL — Stage advance to 'approved' happens ONLY after the git commit of plan.md succeeds. Writing state.md with stage: approved before the commit would leave the item stuck if the commit fails (Pitfall 1)."
- Add: "discuss.md graceful fallback: if discuss.md is absent when /sara-extract runs, {discussion_notes} is set to empty string and extraction continues. The stage guard (stage must be 'extracting') confirms sara-discuss ran, but discuss.md could be absent if the discuss session failed mid-write."
- Preserve all existing notes about: sorter agent, AskUserQuestion header limit (12 chars), freeform Discuss reply, source_quote mandatory, re-run safety, N argument format.

Write the full updated SKILL.md using the Write tool.
  </action>
  <verify>
    <automated>grep -q "plan.md" /home/george/Projects/sara/.claude/skills/sara-extract/SKILL.md && ! grep -q "pipeline-state.json" /home/george/Projects/sara/.claude/skills/sara-extract/SKILL.md && echo OK || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/sara-extract/SKILL.md does NOT contain "pipeline-state.json" anywhere (grep -c returns 0)
    - .claude/skills/sara-extract/SKILL.md does NOT contain "extraction_plan" anywhere (grep -c returns 0)
    - .claude/skills/sara-extract/SKILL.md Step 1 reads ".sara/pipeline/{N}/state.md" and checks "stage: extracting"
    - .claude/skills/sara-extract/SKILL.md Step 2 reads ".sara/pipeline/{N}/discuss.md" with graceful empty fallback if absent
    - .claude/skills/sara-extract/SKILL.md Step 5 writes ".sara/pipeline/{N}/plan.md" using Write tool
    - .claude/skills/sara-extract/SKILL.md Step 5 writes state.md with stage: approved ONLY after git commit succeeds
    - .claude/skills/sara-extract/SKILL.md notes contain "Stage advance to 'approved' happens ONLY after the git commit"
    - .claude/skills/sara-extract/SKILL.md notes contain "discuss.md graceful fallback"
    - Verify: grep -c "pipeline-state.json" .claude/skills/sara-extract/SKILL.md → must return 0
    - Verify: grep -c "extraction_plan" .claude/skills/sara-extract/SKILL.md → must return 0
    - Verify: grep -q "plan.md" .claude/skills/sara-extract/SKILL.md && echo OK
  </acceptance_criteria>
  <done>sara-extract SKILL.md fully updated: state.md stage guard (extracting), discuss.md read with graceful fallback, plan.md headed-section write, atomic commit ordering, no pipeline-state.json or extraction_plan references</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| state.md stage field → extraction gate | Only items with stage: extracting should reach the four extraction passes |
| plan.md content → sara-update LLM parsing | plan.md is LLM-authored; its format determines what sara-update creates/updates in the wiki |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04-01 | Tampering | Stage advance before plan.md commit (Pitfall 1) | mitigate | Plan explicitly sequences: write plan.md → git commit → ONLY if success write state.md stage: approved; notes and acceptance criteria enforce this |
| T-17-04-02 | Tampering | plan.md content injection | accept | plan.md is authored by the LLM from user-approved artifact list; no external input injected into plan.md content |
| T-17-04-03 | Denial of Service | discuss.md absent | mitigate | Graceful empty-string fallback explicitly specified; stage guard (extracting) confirms sara-discuss ran; extraction continues without context |
</threat_model>

<verification>
```bash
# No pipeline-state.json references
grep -c "pipeline-state.json" .claude/skills/sara-extract/SKILL.md

# No extraction_plan references
grep -c "extraction_plan" .claude/skills/sara-extract/SKILL.md

# plan.md write present
grep -n "plan.md" .claude/skills/sara-extract/SKILL.md | head -10

# Stage ordering note present
grep -n "ONLY after" .claude/skills/sara-extract/SKILL.md

# discuss.md graceful fallback present
grep -n "graceful" .claude/skills/sara-extract/SKILL.md
```
Expected: first two commands return 0; others return matches.
</verification>

<success_criteria>
- sara-extract SKILL.md contains zero references to pipeline-state.json
- sara-extract SKILL.md contains zero references to extraction_plan
- Step 1 reads state.md and checks stage: extracting
- Step 2 reads discuss.md with graceful empty-string fallback if absent
- Step 5 writes plan.md with headed-section-per-artifact format
- Step 5 advances stage: approved in state.md ONLY after git commit of plan.md succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/17-document-based-statefulness/17-04-SUMMARY.md`
</output>
