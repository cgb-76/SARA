---
phase: 03-meeting-specialisation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/sara-minutes/SKILL.md
autonomous: true
requirements:
  - MEET-01

must_haves:
  truths:
    - "User can run /sara-minutes MTG-NNN on a complete meeting item and receive structured markdown minutes in the terminal"
    - "User receives an email-ready plain-text version in the same terminal response, after the markdown block"
    - "Running /sara-minutes on a non-meeting item (EML, SLK, DOC) produces a clear error naming the type and the expected type"
    - "Running /sara-minutes on a meeting item that is not in 'complete' stage produces a clear error naming the current stage and the remediation command"
    - "Markdown minutes sections (Decisions, Actions, Risks, Requirements) are populated from the wiki entity pages listed in extraction_plan — not from the transcript alone"
    - "Sections with no entities from the extraction_plan are omitted entirely from both outputs"
  artifacts:
    - path: ".claude/skills/sara-minutes/SKILL.md"
      provides: "/sara-minutes slash command skill"
      contains: "name: sara-minutes"
  key_links:
    - from: ".claude/skills/sara-minutes/SKILL.md"
      to: ".sara/pipeline-state.json"
      via: "Read tool in Step 1"
      pattern: "pipeline-state\\.json"
    - from: ".claude/skills/sara-minutes/SKILL.md"
      to: "wiki/decisions/, wiki/actions/, wiki/risks/, wiki/requirements/"
      via: "extraction_plan traversal in Step 2"
      pattern: "extraction_plan"
---

<objective>
Create the `/sara-minutes` Claude Code skill — a read-only generator that produces structured
meeting minutes from a completed pipeline item. The skill validates the item (type guard then
stage guard), aggregates wiki entity pages listed in the extraction_plan, reads the raw
transcript for attendee context, and outputs a markdown minutes block followed by a plain-text
email-ready version — all in a single terminal response. Nothing is written to disk.

Purpose: Give users a structured meeting record derived from what the pipeline actually captured
in the wiki — "what this meeting did to the wiki" — plus an email-pasteable plain-text version.

Output: `.claude/skills/sara-minutes/SKILL.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-meeting-specialisation/03-CONTEXT.md
@.planning/phases/03-meeting-specialisation/03-RESEARCH.md
@.planning/phases/03-meeting-specialisation/03-PATTERNS.md

<interfaces>
<!-- Key patterns extracted from existing skills. Executor uses these directly — no exploration needed. -->

From .claude/skills/sara-update/SKILL.md (item lookup + guard pattern):
```markdown
Read `.sara/pipeline-state.json` using the Read tool.
Validate `$ARGUMENTS`: must be a non-empty pipeline item ID (e.g. `MTG-001`). If empty:
  Output: `"Usage: /sara-update <ID> where ID is a pipeline item identifier (e.g. MTG-001)."` and STOP.
Find the item with key `"{N}"` in the `items` object
  (N is the full ID argument — for `/sara-update MTG-001`, N = `"MTG-001"`).
If no item exists with key `"{N}"`:
  Output: `"No pipeline item {N} found. Run /sara-ingest with no arguments to see the pipeline status."` and STOP.
Check `items["{N}"].stage`. Expected stage: `"approved"`.
If actual stage != `"approved"`:
  Output: "Item {N} is in stage '{actual_stage}'..." STOP.
Store `{extraction_plan}` = `items["{N}"].extraction_plan`.
```

From .claude/skills/sara-update/SKILL.md (extraction_plan traversal + wiki_dir mapping):
```markdown
For each artifact in `{extraction_plan}`:
  Determine `{wiki_dir}` from `artifact.type`:
  - `requirement` → `wiki/requirements/`
  - `decision`    → `wiki/decisions/`
  - `action`      → `wiki/actions/`
  - `risk`        → `wiki/risks/`
  For `action == "create"`: use `{assigned_id}` from artifact.
  For `action == "update"`: use `{artifact.existing_id}`.
  Read the wiki page using the Read tool.
```

From .claude/skills/sara-ingest/SKILL.md (type guard approach):
```markdown
Check `items["{N}"].type`. Expected type: `"meeting"`.  (stored as lowercase "meeting" by sara-ingest)
If type != "meeting":
  Output: "{N} is a {item.type} item. /sara-minutes only works on meeting items (MTG-NNN)." STOP.
```

pipeline-state.json items structure (written by sara-ingest):
```json
{
  "id": "MTG-001",
  "type": "meeting",
  "filename": "transcript.md",
  "stage": "complete",
  "created": "2026-04-27",
  "discussion_notes": "...",
  "extraction_plan": [
    { "action": "create", "type": "decision", "assigned_id": "DEC-001", "title": "...", ... },
    { "action": "update", "type": "action", "existing_id": "ACT-002", "title": "...", ... }
  ]
}
```

Entity fields to extract per type (from wiki page frontmatter):
- decision:     id, title, status, date — and the ## Decision body section text
- action:       id, title, status, owner, due-date
- risk:         id, title, status, likelihood, impact
- requirement:  id, title, status
- stakeholder:  id, name, role

Archive path for meeting raw files: raw/meetings/{item.id}-{item.filename}

SKILL.md four-section layout (used by all six existing skills):
1. YAML frontmatter (name, description, argument-hint, allowed-tools)
2. <objective> block
3. <process> block with bold **Step N — description** headings
4. <notes> block
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .claude/skills/sara-minutes/SKILL.md</name>
  <files>.claude/skills/sara-minutes/SKILL.md</files>

  <read_first>
    - .claude/skills/sara-update/SKILL.md — primary analog: item lookup, extraction_plan traversal, guard structure, SKILL.md layout
    - .claude/skills/sara-ingest/SKILL.md — type guard pattern: check item.type == "meeting" (lowercase)
    - .claude/skills/sara-init/SKILL.md — freeform output style reference; SKILL.md frontmatter format
    - .planning/phases/03-meeting-specialisation/03-CONTEXT.md — all locked decisions D-01 through D-08
    - .planning/phases/03-meeting-specialisation/03-PATTERNS.md — concrete code excerpts for every step
  </read_first>

  <action>
Create the directory `.claude/skills/sara-minutes/` and write `SKILL.md` with the exact content below.

**Frontmatter:**
```yaml
---
name: sara-minutes
description: "Generate structured meeting minutes and email-ready draft from a completed meeting item"
argument-hint: "<ID>"
allowed-tools:
  - Read
---
```

**`<objective>` block:**
```
Reads pipeline state and wiki artifacts for a completed meeting item, then outputs structured
meeting minutes as a markdown block followed by an email-ready plain-text version — all in one
terminal response. Nothing is written to disk or committed to git.

The minutes are structured around wiki entities actually created or updated for this meeting
item (per extraction_plan) — not a generic summary of the transcript.
```

**`<process>` block — implement these exact steps:**

**Step 1 — Item lookup and guards (type first, then stage)**

Read `.sara/pipeline-state.json` using the Read tool.

Validate `$ARGUMENTS`: must be a non-empty pipeline item ID. If empty:
  Output: `"Usage: /sara-minutes <ID> where ID is a pipeline item identifier (e.g. MTG-001)."` and STOP.

Find the item with key `"{N}"` in the `items` object (N is the full ID argument — for `/sara-minutes MTG-001`, N = `"MTG-001"`).

If no item exists with key `"{N}"`:
  Output: `"No pipeline item {N} found. Run /sara-ingest with no arguments to see the pipeline status."` and STOP.

**Type guard (D-02, D-03 — type check FIRST):**
Check `items["{N}"].type`. Expected value: `"meeting"` (lowercase, as stored by `/sara-ingest`).
If `items["{N}"].type != "meeting"`:
  Output: `"{N} is a {item.type} item. /sara-minutes only works on meeting items (MTG-NNN)."` and STOP.

**Stage guard (D-01 — stage check SECOND):**
Check `items["{N}"].stage`. Expected value: `"complete"`.
If `items["{N}"].stage != "complete"`:
  Output: `"Item {N} is in stage '{item.stage}'. Run /sara-update {N} first to complete the extraction pipeline before generating minutes."` and STOP.

Store `{item}` = `items["{N}"]`.
Store `{extraction_plan}` = `items["{N}"].extraction_plan`.

**Step 2 — Aggregate wiki entities from extraction_plan (D-04, D-05)**

Initialize four lists: `{decisions}`, `{actions}`, `{risks}`, `{requirements}` — all empty.
Also initialize `{stakeholder_ids}` = [] — list of STK IDs encountered in extraction_plan.

If `{extraction_plan}` is empty:
  Set `{no_entities}` = true. Skip to Step 3.

For each artifact in `{extraction_plan}` (aggregate BOTH `create` and `update` actions — both represent what this meeting did to the wiki):

  Determine the entity ID:
  - If `artifact.action == "create"`: use `artifact.assigned_id`
  - If `artifact.action == "update"`: use `artifact.existing_id`
  Store as `{entity_id}`.

  Determine `{wiki_path}` from `artifact.type`:
  - `requirement` → `wiki/requirements/{entity_id}.md`
  - `decision`    → `wiki/decisions/{entity_id}.md`
  - `action`      → `wiki/actions/{entity_id}.md`
  - `risk`        → `wiki/risks/{entity_id}.md`
  - `stakeholder` → skip (collect IDs separately — see below)

  Read `{wiki_path}` using the Read tool.
  Extract frontmatter fields and body section relevant to that type:
  - **decision:**     id, title, status, date; ## Decision body section text
  - **action:**       id, title, status, owner, due-date
  - **risk:**         id, title, status, likelihood, impact
  - **requirement:**  id, title, status

  Append the extracted record to the appropriate list (`{decisions}`, `{actions}`, `{risks}`, `{requirements}`).

  Collect stakeholder IDs: if `artifact.type == "action"` and `owner` field contains an STK-NNN ID, add to `{stakeholder_ids}` if not already present. Similarly collect STK IDs from `raised-by` fields of any entity type.

**Step 3 — Resolve attendees (D-04)**

For each STK-NNN ID in `{stakeholder_ids}`:
  Read `wiki/stakeholders/{stk_id}.md` using the Read tool.
  Extract `name` and `role` from frontmatter.
  Append `{name} ({role})` to `{attendees_from_stk}`.

Determine `{archive_path}`: `raw/meetings/{item.id}-{item.filename}` (the archived transcript path after sara-update ran).
Also check `raw/input/{item.filename}` as fallback if archive path does not exist.

Read the transcript file using the Read tool. Scan the transcript for attendee names, speaker labels, or participant list sections not already covered by `{attendees_from_stk}`. Add any additional names found to `{attendees_extra}`.

`{attendees}` = merge of `{attendees_from_stk}` and `{attendees_extra}`, deduplicating by name.

If no attendees can be resolved from either source, `{attendees}` = ["(attendees not recorded)"].

**Step 4 — Determine meeting date and source reference**

`{meeting_date}` = check transcript header or filename for a date. If not found, use today's ISO date (YYYY-MM-DD).
`{source_ref}` = `{item.id}` — `{item.filename}`.

**Step 5 — Compose markdown minutes (D-06, D-08)**

Compose the markdown block. Omit any section that has zero entries (D-06 — do not print empty sections).

```
# Meeting Minutes — {item.id}
**Date:** {meeting_date}
**Source:** {source_ref}

## Attendees
{for each attendee: - {name} ({role})}

## Decisions
{for each decision: - **{id}** ({title}) — {status}: {## Decision body text, condensed to 1 sentence}}

## Actions
{for each action: - **{id}** ({title}) — Owner: {owner}, Due: {due-date}, Status: {status}}

## Risks
{for each risk: - **{id}** ({title}) — {status}, Likelihood: {likelihood}, Impact: {impact}}

## Requirements
{for each requirement: - **{id}** ({title}) — Status: {status}}
```

If `{no_entities}` is true, include the Attendees section (from transcript) and add the following note after the date/source header:
`_No wiki entities were recorded for this meeting._`
Still omit empty entity sections.

**Step 6 — Derive plain-text email version (D-07)**

Apply these rules to produce `{plaintext_block}` from the markdown content:
- `# Heading` or `## Heading` → `HEADING` (all-caps, no `#` prefix)
- `**bold text**` → `bold text` (remove `**` markers)
- Bullet items (`- item`) → `- item` (keep dash; do not change to `*` or `•`)
- Blank lines preserved as-is

**Step 7 — Output both blocks (D-08)**

Output the following in a single terminal response:

```
{markdown_minutes_block}

---

## Email Version

{plaintext_block}
```

STOP — do NOT write any file, do NOT run any git command.

**`<notes>` block — include these exact rules:**

- CRITICAL — GUARD ORDER: Type check (`item.type == "meeting"`) MUST run before stage check (`item.stage == "complete"`). A non-meeting item must never reach the extraction_plan traversal. (D-03)
- CRITICAL — NO WRITES: This skill is read-only. Do NOT use the Write tool. Do NOT run git commands. `allowed-tools: [Read]` is intentional and must not be changed.
- `{N}` is the full pipeline item ID (e.g. `MTG-001`). The JSON key in `items` is that same string. For `/sara-minutes MTG-001`, look up `items["MTG-001"]`.
- `item.type` is stored as lowercase `"meeting"` by `/sara-ingest`. Check `item.type == "meeting"`, not `"MTG"`.
- Aggregate BOTH `create` and `update` actions from extraction_plan — both represent what this meeting did to the wiki.
- For `action == "create"` artifacts: use `artifact.assigned_id` as the entity ID. For `action == "update"` artifacts: use `artifact.existing_id`.
- Transcript archive path after `/sara-update` ran: `raw/meetings/{item.id}-{item.filename}`. If that path does not resolve (e.g. item was complete before archive ran), fall back to `raw/input/{item.filename}`.
- Attendee resolution is best-effort. If neither STK pages nor transcript yield attendees, output `(attendees not recorded)` rather than an error.
- Empty sections are silently omitted — never print a section heading with no content. (D-06)
- `schema_version` in wiki frontmatter is always the string `"1.0"` — treat as string, not float.
- `vertical` and `department` are always separate fields in STK pages — never merged.
- `related` fields in wiki pages use plain entity IDs. When displaying in minutes body, render as-is (no wikilinks needed in terminal output).
  </action>

  <verify>
    <automated>test -f .claude/skills/sara-minutes/SKILL.md && grep -q 'name: sara-minutes' .claude/skills/sara-minutes/SKILL.md && grep -q 'allowed-tools:' .claude/skills/sara-minutes/SKILL.md && grep -q '\- Read' .claude/skills/sara-minutes/SKILL.md && echo "PASS" || echo "FAIL"</automated>
    <automated>grep -q 'item\.type.*meeting' .claude/skills/sara-minutes/SKILL.md && grep -q 'item\.stage.*complete' .claude/skills/sara-minutes/SKILL.md && echo "guards present" || echo "FAIL guards"</automated>
    <automated>grep -q 'extraction_plan' .claude/skills/sara-minutes/SKILL.md && grep -q 'Email Version' .claude/skills/sara-minutes/SKILL.md && echo "PASS" || echo "FAIL"</automated>
    <automated>grep -q 'Write' .claude/skills/sara-minutes/SKILL.md && echo "FAIL - Write tool referenced" || echo "PASS - no Write tool"</automated>
  </verify>

  <acceptance_criteria>
    - `.claude/skills/sara-minutes/SKILL.md` exists
    - File contains `name: sara-minutes`
    - File contains `allowed-tools:` with only `- Read` listed (no Write, no Bash)
    - File contains `argument-hint: "<ID>"`
    - File contains `item.type` check before `item.stage` check (type guard first — D-03)
    - File contains `"meeting"` as the expected type value (lowercase)
    - File contains `"complete"` as the expected stage value
    - File contains `extraction_plan` traversal logic
    - File contains both `assigned_id` and `existing_id` handling (create + update)
    - File contains wiki_dir mapping: requirement→wiki/requirements/, decision→wiki/decisions/, action→wiki/actions/, risk→wiki/risks/
    - File contains `## Email Version` (or equivalent separator marking the plain-text block)
    - File contains plain-text conversion rules: headings→CAPS, bold removed
    - File contains `<notes>` block with CRITICAL guard order note
    - File does NOT contain `Write` or `Bash` in allowed-tools
    - File does NOT contain any file write instruction in the process steps
  </acceptance_criteria>

  <done>
    The sara-minutes SKILL.md exists with correct frontmatter, a six-step process implementing all
    locked decisions (D-01 through D-08), and a notes block covering all critical constraints.
    The type guard runs before the stage guard. The entity aggregation covers both create and update
    artifacts. Both markdown and plain-text outputs appear in a single terminal response.
    No file writes occur anywhere in the process.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User argument → SKILL.md process | The `$ARGUMENTS` value (pipeline item ID) is user-supplied; injected into Read tool paths |
| extraction_plan content → output | Wiki page content (user-authored + LLM-authored) is read and reflected into terminal output |
| Raw transcript → attendee output | Transcript content is read and surfaced as plain text in minutes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `$ARGUMENTS` → pipeline-state.json Read path | mitigate | SKILL.md Step 1 validates argument is a non-empty item ID matching the expected format before constructing any file path; empty argument triggers usage message and STOP |
| T-03-02 | Information Disclosure | extraction_plan entity IDs → wiki page Read paths | accept | All reads are local filesystem only; no network; wiki pages are user-owned files; no confidential data crosses a trust boundary |
| T-03-03 | Elevation of Privilege | `allowed-tools: [Read]` constraint | mitigate | SKILL.md frontmatter restricts the skill to Read only — Write and Bash are explicitly excluded, preventing any accidental wiki mutation or shell execution |
| T-03-04 | Injection | Transcript/wiki content → terminal output | accept | Output is text rendered to the Claude Code terminal only; no eval, no shell execution of content; no user-controlled content reaches a command interpreter |
| T-03-05 | Tampering | Raw transcript path construction (`raw/meetings/{item.id}-{item.filename}`) | mitigate | `item.id` and `item.filename` are sourced from pipeline-state.json (written by sara-ingest which validates filename for `/` and `..`); the path is read-only; no write occurs on this path |
</threat_model>

<verification>
1. `.claude/skills/sara-minutes/SKILL.md` exists and passes all acceptance criteria
2. `grep -q 'name: sara-minutes' .claude/skills/sara-minutes/SKILL.md` exits 0
3. `grep -q '\- Read' .claude/skills/sara-minutes/SKILL.md` exits 0
4. `grep -v 'allowed-tools' .claude/skills/sara-minutes/SKILL.md | grep -q 'Write'` exits non-zero (no Write tool in process steps)
5. Type guard text (`item.type.*meeting` or `"meeting"`) appears before stage guard text (`item.stage.*complete` or `"complete"`) in the file
6. `grep -q 'extraction_plan' .claude/skills/sara-minutes/SKILL.md` exits 0
7. `grep -q 'Email Version\|email version\|PLAIN.TEXT\|plaintext' .claude/skills/sara-minutes/SKILL.md` exits 0 — separator/email section present
</verification>

<success_criteria>
- `/sara-minutes SKILL.md` created with all six process steps
- Guards: type check first (`item.type == "meeting"`), stage check second (`item.stage == "complete"`)
- Entity aggregation reads both create and update artifacts from extraction_plan
- Both markdown and plain-text output blocks produced in single terminal response
- No Write tool, no Bash tool, no file writes anywhere in the process
- MEET-01 requirement is fully addressed
</success_criteria>

<output>
After completion, create `.planning/phases/03-meeting-specialisation/03-01-SUMMARY.md`
</output>
