---
phase: 03-meeting-specialisation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/sara-agenda/SKILL.md
autonomous: true
requirements:
  - MEET-02

must_haves:
  truths:
    - "User can run /sara-agenda and receive a plain-text agenda draft in the terminal — no files are written to disk"
    - "The skill prompts the user with a single freeform question and waits for a reply before generating"
    - "The output is plain-text only — no markdown headings (##), no bold (**), no bullet symbols (*)"
    - "The output includes: subject line suggestion, greeting, numbered agenda items (no time allocations), desired outcome statement, and sign-off"
    - "The wiki directory contains no new files after /sara-agenda completes"
  artifacts:
    - path: ".claude/skills/sara-agenda/SKILL.md"
      provides: "/sara-agenda slash command skill"
      contains: "name: sara-agenda"
  key_links:
    - from: ".claude/skills/sara-agenda/SKILL.md"
      to: "user reply"
      via: "freeform plain-text prompt in Step 1"
      pattern: "Describe the meeting"
---

<objective>
Create the `/sara-agenda` Claude Code skill — a fully stateless generator that asks the user a
single freeform question, then synthesises an email-friendly plain-text meeting agenda draft.
No files are read from the wiki; no files are written; no git commands are run.

Purpose: Give users a quick, low-friction agenda template ready to paste into an email client,
generated from their own description of the meeting.

Output: `.claude/skills/sara-agenda/SKILL.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-meeting-specialisation/03-CONTEXT.md
@.planning/phases/03-meeting-specialisation/03-RESEARCH.md
@.planning/phases/03-meeting-specialisation/03-PATTERNS.md

<interfaces>
<!-- Key patterns extracted from existing skills. Executor uses these directly — no exploration needed. -->

From .claude/skills/sara-init/SKILL.md (freeform plain-text prompt-and-wait pattern, Steps 2-4):
```markdown
Output the following question as plain text and wait for the user's reply:

> What is the name of this project?

Capture the user's reply as `{project_name}`.
```
Key: output as plain text, wait for reply, capture as named variable. No AskUserQuestion tool.

SKILL.md four-section layout (used by all six existing skills):
1. YAML frontmatter (name, description, argument-hint, allowed-tools)
2. <objective> block
3. <process> block with bold **Step N — description** headings
4. <notes> block

sara-agenda differences from sara-init:
- `allowed-tools: []` — no file reads, no file writes (fully stateless)
- Single question (not three separate prompts)
- Output is plain-text only — no markdown in the generated agenda (D-11)
- No Write tool call anywhere in the process (D-12)

D-09: "single freeform plain-text prompt — no AskUserQuestion structured fields"
D-10: "stateless — no item lookup, no wiki reads required"
D-11: output sections: subject line suggestion, greeting, numbered agenda items (no time allocations), desired outcome statement, sign-off
D-12: nothing written to disk; no wiki file, no git commit
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .claude/skills/sara-agenda/SKILL.md</name>
  <files>.claude/skills/sara-agenda/SKILL.md</files>

  <read_first>
    - .claude/skills/sara-init/SKILL.md — primary analog: freeform prompt-and-wait pattern (Steps 2-4), SKILL.md layout and frontmatter format
    - .planning/phases/03-meeting-specialisation/03-CONTEXT.md — locked decisions D-09 through D-12
    - .planning/phases/03-meeting-specialisation/03-PATTERNS.md — concrete code excerpt for freeform prompt step and plain-text output constraint
  </read_first>

  <action>
Create the directory `.claude/skills/sara-agenda/` and write `SKILL.md` with the exact content below.

**Frontmatter:**
```yaml
---
name: sara-agenda
description: "Generate an email-friendly meeting agenda draft from user-provided meeting description"
argument-hint: ""
allowed-tools: []
---
```

Note on `allowed-tools: []`: fully stateless — no Read, no Write, no Bash. If the empty array proves
invalid at Claude Code runtime, omit the key entirely. (Assumption A1 from RESEARCH.md.)

**`<objective>` block:**
```
Generates a throw-away email-friendly agenda draft from a single freeform user prompt.
No pipeline state is read. No wiki files are read or written. No git commands are run.
Output is displayed once to the terminal and discarded — nothing persists.
```

**`<process>` block — implement these exact two steps:**

**Step 1 — Collect meeting description (D-09)**

Output the following as plain text and wait for the user's reply:

> Describe the meeting: who will be attending (names and roles if relevant),
> what topics need to be covered, and what you want to achieve by the end.

Capture the user's reply as `{meeting_description}`.

**Step 2 — Generate agenda and output (D-11, D-12)**

Using `{meeting_description}`, synthesise a plain-text agenda draft.

Output plain-text only — no markdown formatting whatsoever (no `##` headings, no `**bold**`,
no `*` or `-` bullet symbols unless they are natural list dashes). Use CAPS for section labels.

Use the following structure for the output:

```
SUBJECT: [suggested email subject line derived from meeting topics]

[Greeting — e.g. "Hi team," or "Hi [name]," as appropriate from the description]

Please find the agenda for our upcoming meeting below.

AGENDA

1. [First topic from description]
2. [Second topic from description]
3. [Additional topics as needed]

DESIRED OUTCOME

[One or two sentences stating what we aim to achieve or decide by end of meeting]

[Sign-off — e.g. "Best," followed by a blank line for the sender to fill in their name]
```

Do NOT include time allocations next to agenda items (D-11 — user explicitly excluded them).

STOP — do NOT write any file, do NOT run any git command, do NOT read any wiki or pipeline file.

**`<notes>` block — include these exact rules:**

- CRITICAL — NO WRITES: This skill is fully stateless. Do NOT use the Write tool. Do NOT use the Read tool. Do NOT run Bash commands. `allowed-tools: []` is intentional.
- CRITICAL — PLAIN TEXT ONLY: The generated agenda output must be plain text. No `##` markdown headings, no `**bold**` markers, no `*` bullet symbols. Use CAPS for section labels (AGENDA, DESIRED OUTCOME, SUBJECT). Dashes as natural list separators are acceptable if the LLM produces them, but numbered items are preferred per the output structure. (D-11, Pitfall 4)
- NO TIME ALLOCATIONS: Do not include time estimates (e.g. "15 min") next to agenda items. The user explicitly does not want them. (D-11)
- SINGLE FREEFORM PROMPT: Only one question is asked. Do not use `AskUserQuestion` tool. Do not ask separate follow-up questions for each field. (D-09)
- STATELESS: Do not read `pipeline-state.json`, do not read wiki files, do not look up stakeholder pages. The output is derived entirely from the user's free-text reply. (D-10)
- NO GIT COMMIT: No commit is made. Output is throw-away. (D-12)
- `argument-hint: ""` — this skill takes no argument. If the user passes `$ARGUMENTS`, ignore it; proceed directly to the freeform prompt.
  </action>

  <verify>
    <automated>test -f .claude/skills/sara-agenda/SKILL.md && grep -q 'name: sara-agenda' .claude/skills/sara-agenda/SKILL.md && echo "PASS" || echo "FAIL"</automated>
    <automated>grep -q 'allowed-tools: \[\]' .claude/skills/sara-agenda/SKILL.md && echo "PASS empty tools" || echo "FAIL - tools not empty"</automated>
    <automated>grep -q 'Describe the meeting' .claude/skills/sara-agenda/SKILL.md && echo "PASS prompt" || echo "FAIL prompt"</automated>
    <automated>grep -q 'SUBJECT\|subject line\|Subject:' .claude/skills/sara-agenda/SKILL.md && grep -q 'AGENDA\|agenda items' .claude/skills/sara-agenda/SKILL.md && grep -q 'DESIRED OUTCOME\|desired outcome' .claude/skills/sara-agenda/SKILL.md && echo "PASS structure" || echo "FAIL structure"</automated>
    <automated>grep -qi 'time allocation\|no time\|time estimates' .claude/skills/sara-agenda/SKILL.md && echo "PASS no-time noted" || echo "FAIL no-time-allocation constraint missing"</automated>
    <automated>grep -q 'Write\|wiki\|pipeline-state' .claude/skills/sara-agenda/SKILL.md | grep -v 'Do NOT\|no wiki\|no pipeline\|STATELESS' && echo "FAIL - unexpected references to write/wiki" || echo "PASS"</automated>
  </verify>

  <acceptance_criteria>
    - `.claude/skills/sara-agenda/SKILL.md` exists
    - File contains `name: sara-agenda`
    - File contains `allowed-tools: []` (empty array — no tools permitted)
    - File contains `argument-hint: ""`
    - File contains the freeform prompt text: "Describe the meeting" (or equivalent per D-09)
    - File contains the two-step process: Step 1 (prompt + wait), Step 2 (generate + output)
    - File contains output structure with: subject line, greeting, numbered agenda items, desired outcome, sign-off
    - File contains explicit instruction that no time allocations are included (D-11)
    - File contains plain-text-only instruction (no `##`, no `**bold**`) in both Step 2 and notes
    - File contains `<notes>` block with CRITICAL NO WRITES rule
    - File does NOT instruct the skill to read pipeline-state.json or any wiki file
    - File does NOT instruct the skill to use Write tool or run git commands
    - File does NOT use `AskUserQuestion` in allowed-tools or process steps
  </acceptance_criteria>

  <done>
    The sara-agenda SKILL.md exists with correct frontmatter (empty allowed-tools), a two-step
    process implementing all locked decisions (D-09 through D-12), and a notes block covering
    all critical no-write and plain-text constraints. The skill is fully stateless: it prompts,
    waits for a reply, generates plain-text output, and stops — with no file reads or writes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User free-text reply → generated output | The user's meeting description is the only input; it is reflected into the generated agenda text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Injection | User meeting description → plain-text output | accept | Output is plain text rendered in the Claude Code terminal only; no eval, no shell execution, no file write; user-controlled content cannot reach a command interpreter |
| T-03-07 | Tampering | `allowed-tools: []` constraint | mitigate | SKILL.md frontmatter restricts the skill to zero tools — no Read, no Write, no Bash; prevents any accidental filesystem access or wiki mutation |
| T-03-08 | Information Disclosure | User-provided attendee names in output | accept | Output is displayed to the invoking user's terminal only; no data is persisted or transmitted; user controls all input content |
</threat_model>

<verification>
1. `.claude/skills/sara-agenda/SKILL.md` exists
2. `grep -q 'name: sara-agenda' .claude/skills/sara-agenda/SKILL.md` exits 0
3. `grep -q 'allowed-tools: \[\]' .claude/skills/sara-agenda/SKILL.md` exits 0
4. `grep -q 'Describe the meeting' .claude/skills/sara-agenda/SKILL.md` exits 0
5. Output structure sections present: SUBJECT, AGENDA (numbered items), DESIRED OUTCOME, sign-off
6. File contains no instruction to write files or run git commands (beyond "Do NOT" prohibitions)
7. No `AskUserQuestion` in allowed-tools
</verification>

<success_criteria>
- `/sara-agenda SKILL.md` created with all two process steps
- Fully stateless: no file reads, no file writes, no git commands
- Freeform single prompt — no structured field collection (D-09)
- Plain-text output with correct sections and no time allocations (D-11)
- Nothing written to disk at any point (D-12)
- MEET-02 requirement is fully addressed
</success_criteria>

<output>
After completion, create `.planning/phases/03-meeting-specialisation/03-02-SUMMARY.md`
</output>
