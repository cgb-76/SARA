---
phase: 03-meeting-specialisation
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/PROJECT.md
autonomous: false
requirements:
  - MEET-01
  - MEET-02

must_haves:
  truths:
    - "User has manually verified /sara-minutes produces correct structured output for a complete meeting item"
    - "User has manually verified /sara-agenda produces a plain-text agenda with no wiki file writes"
    - "Phase 3 planning docs are updated: ROADMAP.md plans marked complete, STATE.md reflects phase done"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 3 marked complete"
      contains: "Meeting Specialisation"
    - path: ".planning/STATE.md"
      provides: "State updated to reflect phase 3 completion"
      contains: "Phase 3"
  key_links:
    - from: ".claude/skills/sara-minutes/SKILL.md"
      to: "terminal output"
      via: "manual invocation against a complete MTG item"
      pattern: "sara-minutes"
    - from: ".claude/skills/sara-agenda/SKILL.md"
      to: "terminal output"
      via: "manual invocation with freeform input"
      pattern: "sara-agenda"
---

<objective>
Verify both Phase 3 skills function correctly via human review, then update the planning docs
to record Phase 3 completion. This plan is the wave 2 gate: it runs only after both skills
exist (plans 01 and 02 complete).

Purpose: Human confirmation that both skills produce correct, well-structured output before
the phase is closed. Planning docs update ensures GSD state reflects completion accurately.

Output: Human-approved verification + updated ROADMAP.md, STATE.md, PROJECT.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-meeting-specialisation/03-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify /sara-minutes and /sara-agenda produce correct output</name>

  <read_first>
    - .claude/skills/sara-minutes/SKILL.md — confirm it exists and has correct structure before asking user to test
    - .claude/skills/sara-agenda/SKILL.md — confirm it exists and has correct structure before asking user to test
  </read_first>

  <what-built>
    Both Phase 3 skills have been created:
    - `.claude/skills/sara-minutes/SKILL.md` — generates structured meeting minutes (markdown + plain-text email) from a completed MTG pipeline item
    - `.claude/skills/sara-agenda/SKILL.md` — generates a throw-away plain-text agenda from a freeform user description
  </what-built>

  <how-to-verify>
    Run these verification checks in a SARA wiki directory that has at least one complete meeting
    pipeline item (MTG-NNN with stage: complete and a populated extraction_plan).

    **Check 1 — /sara-minutes happy path (MEET-01 success criterion 1):**
    1. Run `/sara-minutes MTG-001` (or the ID of a complete meeting item)
    2. Expected: terminal output shows a markdown minutes block with Attendees, Decisions, Actions,
       Risks, and/or Requirements sections populated from the wiki entities in the extraction_plan
    3. Expected: a `---` separator and `## Email Version` section follows, containing the same
       content in plain text (CAPS headings, no `**bold**`, no `##` markers)
    4. Confirm no new files appear in the wiki directory after running

    **Check 2 — /sara-minutes type guard (MEET-01 success criterion 3):**
    1. Run `/sara-minutes EML-001` (or any non-meeting pipeline item ID)
    2. Expected: error message names the type (e.g. "EML-001 is an email item. /sara-minutes only
       works on meeting items (MTG-NNN).") — does NOT proceed to generate minutes

    **Check 3 — /sara-minutes stage guard:**
    1. Run `/sara-minutes` against a meeting item that is in `pending`, `extracting`, or `approved`
       stage (if available)
    2. Expected: error message names the current stage and instructs the user to run
       `/sara-update N` first

    **Check 4 — /sara-agenda happy path (MEET-02 success criterion 2):**
    1. Run `/sara-agenda`
    2. Expected: skill prompts with "Describe the meeting..." or equivalent freeform question
    3. Provide a test description: "Meeting with Alice (Sales) and Bob (Engineering) to discuss
       Q3 roadmap priorities and decide on the feature freeze date"
    4. Expected: plain-text agenda output with SUBJECT line, greeting, numbered agenda items
       (no time allocations), DESIRED OUTCOME section, and sign-off
    5. Confirm wiki directory contains no new files after completion

    Confirm all four checks pass before typing "approved".
  </how-to-verify>

  <acceptance_criteria>
    - Check 1: /sara-minutes produces markdown block + Email Version plain-text block in one terminal response
    - Check 2: non-meeting item produces type guard error, does not generate minutes
    - Check 3: non-complete item (if testable) produces stage guard error with remediation command
    - Check 4: /sara-agenda produces numbered agenda items, no time allocations, plain text only
    - Check 4: no new wiki files after /sara-agenda
  </acceptance_criteria>

  <resume-signal>Type "approved" to proceed to planning doc updates, or describe any issues found</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update planning docs to record Phase 3 completion</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/PROJECT.md</files>

  <read_first>
    - .planning/ROADMAP.md — read current content before editing; find Phase 3 section
    - .planning/STATE.md — read current content before editing; find phase progress table and status fields
    - .planning/PROJECT.md — read current content before editing; find Active requirements section
  </read_first>

  <action>
Update the three planning files to reflect Phase 3 completion. Make the following exact changes:

**ROADMAP.md changes:**
1. In the Phase Summary table, update Phase 3 row Status column from `Not started` to `Done`
2. In the Phase 3 Details section:
   - Change `**Plans:** [To be planned]` or current plan count line to `**Plans:** 3 plans`
   - Update the plan list to mark all three plans complete:
     ```
     Plans:
     - [x] 03-01-PLAN.md — /sara-minutes skill: type + stage guards, extraction_plan aggregation, dual-format output
     - [x] 03-02-PLAN.md — /sara-agenda skill: freeform prompt, stateless plain-text agenda generation
     - [x] 03-03-PLAN.md — End-to-end verification and planning doc updates
     ```
3. In the Progress table at the bottom, update Phase 3 row:
   - Status: `Done`
   - Completed: today's date (YYYY-MM-DD)

**STATE.md changes:**
1. Update `status:` in frontmatter to `complete` (or `done`)
2. Update `stopped_at:` to `Phase 3 complete`
3. Update `last_updated:` to today's ISO datetime
4. Update `progress` block:
   - `completed_phases`: increment to 3
   - `completed_plans`: increment to 13 (10 existing + 3 Phase 3 plans)
   - `percent`: 100
5. In the Current Status section:
   - `**Phase:**` → `3 (complete)`
   - `**Status:**` → `All phases complete — v1 milestone achieved`
6. In the Phase Progress table, update Phase 3 row to `✅ Complete (3/3 plans)`
7. Update `**Stopped at:**` line at the bottom to `Phase 3 complete`

**PROJECT.md changes:**
1. In the Requirements section, move the two Phase 3 items from `### Active` to `### Validated`:
   - `[ ] /sara-minutes N (meetings only) generates markdown minutes filed in wiki + email-ready draft — Phase 3`
   - Change to: `[x] /sara-minutes N generates structured meeting minutes (markdown + email-ready draft, output-only) — Phase 3`
   - `[ ] /sara-agenda generates an email-friendly agenda from user input (throw-away, not stored in wiki) — Phase 3`
   - Change to: `[x] /sara-agenda generates throw-away email-friendly agenda from user input — Phase 3`
2. If `### Active` section becomes empty after the move, add a placeholder:
   - `- (v2 requirements listed in REQUIREMENTS.md)`
3. Update `Last updated:` footer or equivalent metadata field to today's date

After all three files are updated, commit with:
```
gsd-sdk query commit "docs(phase-03): update planning docs after Phase 3 completion" .planning/ROADMAP.md .planning/STATE.md .planning/PROJECT.md
```

If `gsd-sdk` is not available in this context, run:
```bash
git add .planning/ROADMAP.md .planning/STATE.md .planning/PROJECT.md
git commit -m "docs(phase-03): update planning docs after Phase 3 completion"
```
  </action>

  <verify>
    <automated>grep -q '\[x\] 03-01-PLAN.md' .planning/ROADMAP.md && grep -q '\[x\] 03-02-PLAN.md' .planning/ROADMAP.md && grep -q '\[x\] 03-03-PLAN.md' .planning/ROADMAP.md && echo "PASS roadmap" || echo "FAIL roadmap"</automated>
    <automated>grep -q 'Phase 3 complete\|all phases complete\|All phases' .planning/STATE.md && echo "PASS state" || echo "FAIL state"</automated>
    <automated>git log --oneline -1 | grep -q 'phase-03\|Phase 3\|planning docs' && echo "PASS commit" || echo "FAIL commit"</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/ROADMAP.md` contains `[x] 03-01-PLAN.md`, `[x] 03-02-PLAN.md`, `[x] 03-03-PLAN.md`
    - `.planning/ROADMAP.md` Phase 3 row in Progress table shows `Done`
    - `.planning/STATE.md` `completed_phases` is 3
    - `.planning/STATE.md` Phase 3 row in phase progress table shows `✅ Complete`
    - `.planning/PROJECT.md` Phase 3 requirements appear under `### Validated` (checked `[x]`)
    - Git log shows a commit with message containing "phase-03" or "Phase 3"
  </acceptance_criteria>

  <done>
    All three planning files updated and committed. ROADMAP.md, STATE.md, and PROJECT.md accurately
    reflect Phase 3 completion. The GSD v1 milestone is fully recorded.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning doc edits → git commit | Writes to .planning/ markdown files and commits; no user data involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering | ROADMAP.md / STATE.md / PROJECT.md edits | accept | These are planning-only markdown files with no security sensitivity; human checkpoint gates this task (Task 1 must pass first) |
| T-03-10 | Denial of Service | git commit failure on planning doc update | accept | Low risk; if commit fails, files remain as uncommitted changes; user can retry manually; no data loss |
</threat_model>

<verification>
1. Human checkpoint (Task 1) passes — user types "approved"
2. `grep -q '\[x\] 03-01-PLAN.md' .planning/ROADMAP.md` exits 0
3. `grep -q '\[x\] 03-02-PLAN.md' .planning/ROADMAP.md` exits 0
4. `grep -q 'Complete.*3/3' .planning/STATE.md` exits 0
5. git log --oneline -1 shows the planning docs commit
</verification>

<success_criteria>
- Both skills manually verified by human (approved checkpoint)
- ROADMAP.md, STATE.md, PROJECT.md updated to reflect Phase 3 done
- Planning docs committed to git
- MEET-01 and MEET-02 both verified functional
- v1 milestone fully closed in planning state
</success_criteria>

<output>
After completion, create `.planning/phases/03-meeting-specialisation/03-03-SUMMARY.md`
</output>
