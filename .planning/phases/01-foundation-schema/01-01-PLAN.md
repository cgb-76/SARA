---
phase: 01-foundation-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/sara-init/SKILL.md
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-04

must_haves:
  truths:
    - "Invoking /sara-init in an empty directory creates the full directory tree without error"
    - "Guard clause aborts /sara-init when wiki/ already exists — no files are written"
    - "User is prompted separately for project name, verticals, and departments (three distinct interactions)"
    - ".sara/config.json contains project, verticals, departments, and schema_version keys after init"
    - "pipeline-state.json exists with MTG/EML/SLK/DOC ingest counters and REQ/DEC/ACT/RISK/STK entity counters all set to zero"
  artifacts:
    - path: ".claude/skills/sara-init/SKILL.md"
      provides: "The /sara-init Claude Code skill — guard clause, user input collection, directory creation, config and state file writes"
      exports: []
  key_links:
    - from: "SKILL.md guard clause"
      to: "wiki/ directory existence"
      via: "Bash [ -d wiki ]"
      pattern: "\\[ -d wiki \\]"
    - from: "SKILL.md AskUserQuestion"
      to: ".sara/config.json verticals/departments arrays"
      via: "comma-split parse"
      pattern: "verticals.*departments"
    - from: "SKILL.md Write step"
      to: "pipeline-state.json"
      via: "Write tool"
      pattern: "\"counters\".*\"ingest\""
---

<objective>
Write the first half of the `/sara-init` Claude Code skill: the SKILL.md file containing the guard
clause, three AskUserQuestion prompts (project name, verticals, departments), the mkdir -p directory
tree creation step, and the Write steps for `.sara/config.json` and `pipeline-state.json`.

Purpose: Deliver the executable core of /sara-init — the guard-before-write safety pattern and the
two persistent config files that every future SARA skill depends on.

Output: `.claude/skills/sara-init/SKILL.md` (partial — Steps 1–4 of the skill process; Plan 02
adds Steps 5–7 for wiki files and templates).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-schema/01-CONTEXT.md
@.planning/phases/01-foundation-schema/01-RESEARCH.md
@.planning/phases/01-foundation-schema/01-PATTERNS.md

<interfaces>
<!-- Locked decisions governing this skill's content. Executor must implement these exactly. -->

D-01: /sara-init collects project name, vertical list, and department list using AskUserQuestion TUI
      prompts. Verticals and departments are prompted separately (two distinct questions).
D-02: If run in a directory that already has a /wiki/ tree, /sara-init aborts with a clear error
      message. No overwrite, no partial re-init.
D-03: Project name is always collected during init. Stored in .sara/config.json and referenced
      in wiki/CLAUDE.md header.
D-04: Project config lives at .sara/config.json. Templates also live in .sara/templates/.
D-05: .sara/config.json initial structure:
      {"project": "<name>", "verticals": [...], "departments": [...], "schema_version": "1.0"}
D-07: pipeline-state.json lives at the project root. Initial structure:
      {
        "counters": {
          "ingest": { "MTG": 0, "EML": 0, "SLK": 0, "DOC": 0 },
          "entity": { "REQ": 0, "DEC": 0, "ACT": 0, "RISK": 0, "STK": 0 }
        },
        "items": {}
      }

AskUserQuestion constraint: Option headers must be 12 characters or fewer (hard limit).
Freeform rule: If user selects an option signalling free-form entry, follow up with plain text —
NOT another AskUserQuestion call.

SKILL.md frontmatter pattern (from gsd-thread analog):
---
name: sara-init
description: "Initialise a new SARA wiki in the current directory"
argument-hint: ""
allowed-tools:
  - Read
  - Write
  - Bash
  - AskUserQuestion
---

Directory tree created in one Bash call:
mkdir -p \
  raw/input \
  raw/meetings \
  raw/emails \
  raw/slack \
  raw/documents \
  wiki/requirements \
  wiki/decisions \
  wiki/actions \
  wiki/risks \
  wiki/stakeholders \
  .sara/templates
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the sara-init SKILL.md with Steps 1-4 (guard, input, directories, config files)</name>
  <files>.claude/skills/sara-init/SKILL.md</files>
  <read_first>
    - .planning/phases/01-foundation-schema/01-CONTEXT.md (locked decisions D-01 through D-08)
    - .planning/phases/01-foundation-schema/01-RESEARCH.md (SKILL.md frontmatter pattern, AskUserQuestion pattern, guard clause code, directory tree, config JSON templates)
    - .planning/phases/01-foundation-schema/01-PATTERNS.md (gsd-thread analog — inline process structure, file creation pattern)
  </read_first>
  <action>
Create the directory `.claude/skills/sara-init/` and write `SKILL.md` with the following content.
This task covers Steps 1–4 of the skill process. Plan 02 Task 1 will add Steps 5–7 via Edit.

The SKILL.md must contain:

**Frontmatter (exact):**
```yaml
---
name: sara-init
description: "Initialise a new SARA wiki in the current directory"
argument-hint: ""
allowed-tools:
  - Read
  - Write
  - Bash
  - AskUserQuestion
---
```

**Objective block:**
```
<objective>
Initialise a new SARA wiki in the current directory. Creates the full /raw/ and /wiki/ directory
tree, captures project configuration (name, verticals, departments), writes .sara/config.json and
pipeline-state.json, creates the wiki/CLAUDE.md schema contract, wiki/index.md and wiki/log.md
catalog stubs, and five entity page templates in .sara/templates/.

Run this skill once per project in an empty directory before using any other SARA commands.
</objective>
```

**Process block — Steps 1-4:**

Step 1 is the guard clause. It MUST be the very first action before any AskUserQuestion or Write:
```
**Step 1 — Guard clause**

Run the following Bash command. If it exits non-zero (wiki/ already exists), report the error to
the user and STOP — do not proceed to any further steps.

```bash
if [ -d "wiki" ]; then
  echo "Error: A SARA wiki already exists in this directory (wiki/ found). Aborting — no changes made."
  exit 1
fi
```

If the command succeeds (no wiki/ directory), continue to Step 2.
```

Step 2 collects project name. Use AskUserQuestion. Option headers must be 12 characters or fewer:
```
**Step 2 — Collect project name**

Use AskUserQuestion to ask for the project name:
- header: "Project name"  (12 chars exactly — acceptable)
- question: "What is the name of this project? (used in wiki/CLAUDE.md header)"
- options: ["My Project", "Enter name"]

If user selects an option or answers with their own text, capture the response as `{project_name}`.
If the response was "Enter name" or any free-form signal, ask for the name as plain text (not
another AskUserQuestion): "Type the project name and press Enter."
```

Step 3 collects verticals. Option headers 12 chars max:
```
**Step 3 — Collect verticals**

Use AskUserQuestion to collect market verticals:
- header: "Verticals"     (9 chars — acceptable)
- question: "List the market verticals for this project, separated by commas (e.g. Residential, Enterprise, Wholesale)"
- options: ["Residential, Enterprise, Wholesale", "My own list", "Skip for now"]

If user selects a preset option, use that string as-is. If user selects "My own list" or any
free-form signal, ask as plain text: "Type your verticals separated by commas."

Parse the response by splitting on commas, trimming whitespace from each item, and filtering empty
strings. Store the result as `{verticals_array}` (e.g. ["Residential", "Enterprise", "Wholesale"]).

If user selected "Skip for now", store `{verticals_array}` as an empty array [].
```

Step 4 collects departments. Option headers 12 chars max:
```
**Step 4 — Collect departments**

Use AskUserQuestion to collect functional departments:
- header: "Departments"   (11 chars — acceptable)
- question: "List the functional departments for this project, separated by commas (e.g. Sales, Operations, Finance)"
- options: ["Sales, Operations, Finance", "My own list", "Skip for now"]

If user selects a preset option, use that string as-is. If user selects "My own list" or any
free-form signal, ask as plain text: "Type your departments separated by commas."

Parse the response by splitting on commas, trimming whitespace from each item, and filtering empty
strings. Store the result as `{departments_array}`.

If user selected "Skip for now", store `{departments_array}` as an empty array [].
```

Step 5 creates the directory tree:
```
**Step 5 — Create directory tree**

Run the following Bash command to create the full directory structure in a single call:

```bash
mkdir -p \
  raw/input \
  raw/meetings \
  raw/emails \
  raw/slack \
  raw/documents \
  wiki/requirements \
  wiki/decisions \
  wiki/actions \
  wiki/risks \
  wiki/stakeholders \
  .sara/templates
```
```

Step 6 writes .sara/config.json with the user's input:
```
**Step 6 — Write .sara/config.json**

Use the Write tool to create `.sara/config.json` with the following content, substituting
`{project_name}`, `{verticals_array}`, and `{departments_array}` with the values collected in
Steps 2–4. Format the arrays as valid JSON arrays (e.g. ["Residential", "Enterprise"]).

```json
{
  "project": "{project_name}",
  "verticals": {verticals_array},
  "departments": {departments_array},
  "schema_version": "1.0"
}
```
```

Step 7 writes pipeline-state.json with the exact locked structure from D-07:
```
**Step 7 — Write pipeline-state.json**

Use the Write tool to create `pipeline-state.json` at the project root with the following exact
content (no variable substitution needed — all counters start at zero):

```json
{
  "counters": {
    "ingest": { "MTG": 0, "EML": 0, "SLK": 0, "DOC": 0 },
    "entity": { "REQ": 0, "DEC": 0, "ACT": 0, "RISK": 0, "STK": 0 }
  },
  "items": {}
}
```
```

After Steps 1–7 are written, add a note at the end of the file:
```
<!-- Plan 02 will add Steps 8-10: wiki/CLAUDE.md, wiki/index.md, wiki/log.md, five entity templates, and success report -->
```

The file must end with `</process>` closed after that note, with Steps 8-10 to be filled in by
Plan 02. Structure the process block so Plan 02 can insert additional steps before `</process>`.
  </action>
  <verify>
    <automated>test -f /home/george/Projects/llm-wiki-gsd/.claude/skills/sara-init/SKILL.md && grep -q 'name: sara-init' /home/george/Projects/llm-wiki-gsd/.claude/skills/sara-init/SKILL.md && grep -q 'AskUserQuestion' /home/george/Projects/llm-wiki-gsd/.claude/skills/sara-init/SKILL.md && grep -q '\[ -d "wiki" \]' /home/george/Projects/llm-wiki-gsd/.claude/skills/sara-init/SKILL.md && grep -q '"MTG": 0' /home/george/Projects/llm-wiki-gsd/.claude/skills/sara-init/SKILL.md && grep -q 'schema_version.*1.0' /home/george/Projects/llm-wiki-gsd/.claude/skills/sara-init/SKILL.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/skills/sara-init/SKILL.md` exists
    - `grep -q 'name: sara-init' .claude/skills/sara-init/SKILL.md` exits 0
    - `grep -q 'AskUserQuestion' .claude/skills/sara-init/SKILL.md` exits 0 (appears in both frontmatter and process body)
    - `grep -q 'argument-hint' .claude/skills/sara-init/SKILL.md` exits 0
    - `grep -q 'if \[ -d "wiki" \]' .claude/skills/sara-init/SKILL.md` exits 0 (guard clause present)
    - `grep -q 'Aborting' .claude/skills/sara-init/SKILL.md` exits 0 (guard clause has abort message)
    - `grep -q 'Verticals' .claude/skills/sara-init/SKILL.md` exits 0 (verticals prompt present)
    - `grep -q 'Departments' .claude/skills/sara-init/SKILL.md` exits 0 (departments prompt present, separate from verticals)
    - `grep -q 'mkdir -p' .claude/skills/sara-init/SKILL.md` exits 0 (directory creation step present)
    - `grep -q 'raw/input' .claude/skills/sara-init/SKILL.md` exits 0
    - `grep -q 'wiki/requirements' .claude/skills/sara-init/SKILL.md` exits 0
    - `grep -q '.sara/templates' .claude/skills/sara-init/SKILL.md` exits 0
    - `grep -q '"schema_version": "1.0"' .claude/skills/sara-init/SKILL.md` exits 0 (.sara/config.json content present)
    - `grep -q '"MTG": 0' .claude/skills/sara-init/SKILL.md` exits 0 (pipeline-state.json content present)
    - `grep -q '"EML": 0' .claude/skills/sara-init/SKILL.md` exits 0
    - `grep -q '"SLK": 0' .claude/skills/sara-init/SKILL.md` exits 0
    - `grep -q '"DOC": 0' .claude/skills/sara-init/SKILL.md` exits 0
    - `grep -q '"REQ": 0' .claude/skills/sara-init/SKILL.md` exits 0
    - `grep -q '"STK": 0' .claude/skills/sara-init/SKILL.md` exits 0
    - `grep -q '"items": {}' .claude/skills/sara-init/SKILL.md` exits 0
  </acceptance_criteria>
  <done>
    `.claude/skills/sara-init/SKILL.md` exists with correct SKILL.md frontmatter (name, description,
    argument-hint, allowed-tools including AskUserQuestion), objective block, and process Steps 1-7
    covering: guard clause aborting on existing wiki/, three separate AskUserQuestion prompts (project
    name / verticals / departments with ≤12-char headers), mkdir -p directory tree, .sara/config.json
    write with project/verticals/departments/schema_version, and pipeline-state.json write with
    MTG/EML/SLK/DOC and REQ/DEC/ACT/RISK/STK counters all at zero and empty items object.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → JSON config | Project name, verticals, and departments entered via AskUserQuestion flow into .sara/config.json |
| skill execution → filesystem | Skill creates directories and files in the current working directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Guard clause — wiki/ check | mitigate | Guard clause runs before any writes; if wiki/ exists, skill aborts with no side effects (per D-02) |
| T-01-02 | Tampering | User input → .sara/config.json | accept | Project name, verticals, and departments flow into JSON string values only — not into shell commands or eval contexts; no injection risk; low-value target (local config) |
| T-01-03 | Tampering | Partial init on failure | mitigate | Skill orders writes so guard clause runs first; documents recovery path (delete wiki/ and re-run) in notes block |
| T-01-04 | Spoofing | AskUserQuestion option selection | accept | AskUserQuestion is a local TUI; single-user local tool with no authentication surface; risk is negligible |
</threat_model>

<verification>
After Plan 01 completes, verify:
1. `test -f .claude/skills/sara-init/SKILL.md` — file exists
2. `grep -c 'AskUserQuestion' .claude/skills/sara-init/SKILL.md` — appears multiple times (frontmatter + body)
3. `grep -q '"MTG": 0' .claude/skills/sara-init/SKILL.md && echo pipeline-state-content-present`
4. `grep -q 'schema_version.*1.0' .claude/skills/sara-init/SKILL.md && echo config-content-present`
5. Guard clause present: `grep -q '\[ -d "wiki" \]' .claude/skills/sara-init/SKILL.md`
</verification>

<success_criteria>
- SKILL.md exists at `.claude/skills/sara-init/SKILL.md`
- Frontmatter includes all four allowed-tools: Read, Write, Bash, AskUserQuestion
- Guard clause is the first step in `<process>` and aborts with an error message when wiki/ exists
- Three separate AskUserQuestion prompts are present with ≤12-character option headers
- Directory creation step includes all 11 target directories (raw/input, raw/meetings, raw/emails, raw/slack, raw/documents, wiki/requirements, wiki/decisions, wiki/actions, wiki/risks, wiki/stakeholders, .sara/templates)
- .sara/config.json content has project, verticals, departments, and schema_version keys
- pipeline-state.json content has MTG, EML, SLK, DOC ingest counters and REQ, DEC, ACT, RISK, STK entity counters all at 0, and an empty items object
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-schema/01-01-SUMMARY.md`
</output>
