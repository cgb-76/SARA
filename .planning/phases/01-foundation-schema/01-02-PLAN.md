---
phase: 01-foundation-schema
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - .claude/skills/sara-init/SKILL.md
autonomous: true
requirements:
  - FOUND-03
  - WIKI-01
  - WIKI-02
  - WIKI-03
  - WIKI-04
  - WIKI-05
  - WIKI-06
  - WIKI-07

must_haves:
  truths:
    - "wiki/CLAUDE.md is written by the skill with the project name in the header, five entity schema blocks as fenced code, and five numbered behavioral rules"
    - "wiki/index.md stub has YAML frontmatter, a heading, and the catalog table header row"
    - "wiki/log.md stub has YAML frontmatter, a heading, and an append-only format comment"
    - "All five entity templates are written by the skill in .sara/templates/ with annotated YAML frontmatter and correct body sections"
    - "schema_version: '1.0' (quoted string) appears in all five templates and in the wiki/CLAUDE.md schema blocks"
    - "stakeholder template has separate vertical and department fields — never combined"
    - "Skill reports success listing all created files after all writes complete"
  artifacts:
    - path: ".claude/skills/sara-init/SKILL.md"
      provides: "Complete /sara-init skill — Steps 8-12 added covering wiki/CLAUDE.md, wiki/index.md, wiki/log.md, five entity templates, success report, and notes block"
      exports: []
  key_links:
    - from: "SKILL.md Step 8"
      to: "wiki/CLAUDE.md entity schemas"
      via: "Write tool — five fenced code blocks under ### headings"
      pattern: "wiki/CLAUDE.md"
    - from: "stakeholder template in Step 11"
      to: "separate vertical/department fields"
      via: "two distinct YAML keys"
      pattern: "vertical:.*department:"
    - from: "schema_version field"
      to: "all five template writes in Step 11"
      via: "quoted string value"
      pattern: 'schema_version: "1.0"'
---

<objective>
Complete the `/sara-init` SKILL.md by adding Steps 8-12: writing `wiki/CLAUDE.md` (schema contract
with behavioral rules and all five entity type schemas), `wiki/index.md` (catalog stub), `wiki/log.md`
(append-only log stub), five entity templates in `.sara/templates/`, the success report step,
and the `<notes>` block.

Purpose: Delivers the schema definitions (WIKI-01 through WIKI-07) and the CLAUDE.md behavioral
contract that every future wiki-touching skill inherits. Closes FOUND-03 (schema_version in all
templates).

Output: `.claude/skills/sara-init/SKILL.md` — complete and executable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-schema/01-CONTEXT.md
@.planning/phases/01-foundation-schema/01-RESEARCH.md
@.planning/phases/01-foundation-schema/01-01-SUMMARY.md

<interfaces>
<!-- Locked decisions governing template and wiki/CLAUDE.md content. -->

D-09: Templates live in .sara/templates/ — one file per entity type: requirement.md, decision.md,
      action.md, risk.md, stakeholder.md.

D-10: Templates use annotated frontmatter — YAML fields with inline comments showing allowed values.

D-11: Body sections per entity type:
  - Requirements: ## Description / ## Acceptance Criteria / ## Notes
  - Decisions: ## Context / ## Decision / ## Rationale / ## Alternatives Considered
  - Actions: ## Description / ## Notes
  - Risks: ## Description / ## Mitigation / ## Notes
  - Stakeholders: frontmatter only (NO body sections)

D-12: Entity schemas live in wiki/CLAUDE.md.

D-13: wiki/CLAUDE.md uses template skeleton format — fenced code blocks per entity type showing
      complete annotated frontmatter and body section headings.

D-14: wiki/CLAUDE.md contains five numbered behavioral rules:
  1. Deduplication — check wiki/index.md before creating any new entity
  2. Index maintenance — every entity write updates wiki/index.md
  3. Log maintenance — every entity write appends to wiki/log.md
  4. ID assignment — increment counter in pipeline-state.json before assigning new ID
  5. Cross-references — related fields use entity IDs only (e.g. REQ-001), never file paths

Obsidian YAML constraint: schema_version must be quoted as "1.0" — unquoted 1.0 is parsed as float.
Use 2-space indentation, no tabs.

Domain constraint: stakeholder template MUST have two separate fields — vertical and department.
Never merge them. This is a named constraint (project memory: project_sara_domain.md).

<!-- EXACT TEMPLATE CONTENT — write these verbatim into the SKILL.md -->

requirement.md content:
---
id: REQ-000
title: ""
status: open  # open | accepted | rejected | superseded
description: ""
source: ""     # ingest ID (e.g. MTG-001)
raised-by: ""  # stakeholder ID (e.g. STK-001)
owner: ""      # stakeholder ID (e.g. STK-001)
schema_version: "1.0"
tags: []
related: []    # entity IDs (e.g. [DEC-001, ACT-002])
---

## Description

## Acceptance Criteria

## Notes
---

decision.md content:
---
id: DEC-000
title: ""
status: proposed  # proposed | accepted | rejected | superseded
context: ""
decision: ""
rationale: ""
alternatives-considered: ""
date: ""          # ISO 8601 (e.g. 2026-04-27)
deciders: []      # stakeholder IDs (e.g. [STK-001, STK-002])
supersedes: ""    # DEC-NNN or empty
schema_version: "1.0"
tags: []
related: []
---

## Context

## Decision

## Rationale

## Alternatives Considered
---

action.md content:
---
id: ACT-000
title: ""
status: open  # open | in-progress | done | cancelled
description: ""
owner: ""      # stakeholder ID (e.g. STK-001)
due-date: ""   # ISO 8601
source: ""     # ingest ID (e.g. MTG-001)
schema_version: "1.0"
tags: []
related: []
---

## Description

## Notes
---

risk.md content:
---
id: RISK-000
title: ""
status: open  # open | mitigated | accepted | closed
description: ""
likelihood: ""  # low | medium | high
impact: ""      # low | medium | high
owner: ""       # stakeholder ID (e.g. STK-001)
mitigation: ""
source: ""      # ingest ID (e.g. MTG-001)
schema_version: "1.0"
tags: []
related: []
---

## Description

## Mitigation

## Notes
---

stakeholder.md content (frontmatter ONLY — no body sections):
---
id: STK-000
name: ""
vertical: ""    # from project config verticals list
department: ""  # from project config departments list
email: ""
role: ""
schema_version: "1.0"
related: []
---
---

wiki/index.md stub content:
---
maintained-by: sara
last-updated: ""
---

# Wiki Index

| ID | Title | Status | Type | Tags | Last Updated |
|----|-------|--------|------|------|--------------|
---

wiki/log.md stub content:
---
maintained-by: sara
last-updated: ""
---

# Wiki Log

<!-- Append-only. Each entry: ingest ID, date, type, filename, entities created/updated -->
---

wiki/CLAUDE.md behavioral rules and schema header:

# SARA Wiki — Schema & Behavioral Rules

**Project:** {project_name}
**Schema version:** 1.0

This file is automatically loaded by Claude Code when working in any file under `wiki/`.
All SARA pipeline commands that read or write wiki pages must follow the rules below.

## Behavioral Rules

1. **Deduplication:** Before creating any new entity, search `wiki/index.md` for an existing entity
   with the same title or similar description. Propose an update to the existing page rather than
   creating a duplicate.
2. **Index maintenance:** Every entity write (create or update) must also update `wiki/index.md`
   with the new or changed row (ID, title, status, type, tags, last-updated).
3. **Log maintenance:** Every entity write must append an entry to `wiki/log.md` recording the
   ingest ID, date, entity IDs created/updated, and source filename.
4. **ID assignment:** Before assigning a new entity ID, increment the relevant counter in
   `pipeline-state.json`. Read the post-increment value and use it as the new ID (e.g. REQ-001).
5. **Cross-references:** `related` fields must use entity IDs only (e.g. `REQ-001`, `DEC-003`) —
   never file paths, relative links, or Obsidian wiki-links.

## Entity Schemas

[Then five ### headings each with a fenced code block — same content as the five templates above.
The stakeholder block has no body section headings (D-11: frontmatter only).]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete SKILL.md with Steps 8-12 (wiki/CLAUDE.md, wiki stubs, templates, success report, notes)</name>
  <files>.claude/skills/sara-init/SKILL.md</files>
  <read_first>
    - .claude/skills/sara-init/SKILL.md (read current file — confirm Steps 1-7 are present, locate the placeholder comment <!-- Plan 02 will add Steps 8-10 --> to find the edit insertion point)
    - .planning/phases/01-foundation-schema/01-RESEARCH.md (Code Examples section lines 429-602 — authoritative template content and wiki/CLAUDE.md structure; Pitfall 2 for Obsidian YAML rules)
    - .planning/phases/01-foundation-schema/01-CONTEXT.md (D-09 through D-14 for template structure rules; D-11 for body sections per type)
  </read_first>
  <action>
Edit `.claude/skills/sara-init/SKILL.md`. Replace the placeholder comment
`<!-- Plan 02 will add Steps 8-10 ... -->` with Steps 8-12 and the notes block.

The insertion must happen before the closing `</process>` tag. The steps to insert:

**Step 8 — Write wiki/CLAUDE.md**

Instruct the executor (Claude running the skill) to use the Write tool to create `wiki/CLAUDE.md`.
The file content starts with the header block (substituting `{project_name}`), then the Behavioral
Rules section with five numbered rules, then the Entity Schemas section with five `###` subsections
each containing a fenced yaml code block. The schema blocks are identical to the five templates in
Step 11.

The exact five behavioral rules to write:
1. Deduplication: check wiki/index.md before creating any new entity; update existing pages rather than creating duplicates
2. Index maintenance: every entity write also updates wiki/index.md (ID, title, status, type, tags, last-updated)
3. Log maintenance: every entity write appends an entry to wiki/log.md (ingest ID, date, entity IDs, source filename)
4. ID assignment: increment counter in pipeline-state.json before assigning new ID; use post-increment value
5. Cross-references: related fields use entity IDs only (e.g. REQ-001, DEC-003) — never file paths or wiki-links

**Step 9 — Write wiki/index.md**

Instruct the executor to use the Write tool to create `wiki/index.md` with this exact content:
```
---
maintained-by: sara
last-updated: ""
---

# Wiki Index

| ID | Title | Status | Type | Tags | Last Updated |
|----|-------|--------|------|------|--------------|
```

**Step 10 — Write wiki/log.md**

Instruct the executor to use the Write tool to create `wiki/log.md` with this exact content:
```
---
maintained-by: sara
last-updated: ""
---

# Wiki Log

<!-- Append-only. Each entry: ingest ID, date, type, filename, entities created/updated -->
```

**Step 11 — Write entity templates**

Instruct the executor to use the Write tool to create each of the five template files in
`.sara/templates/`. Each file is written as a separate Write call. Content for each file:

`.sara/templates/requirement.md`:
```
---
id: REQ-000
title: ""
status: open  # open | accepted | rejected | superseded
description: ""
source: ""     # ingest ID (e.g. MTG-001)
raised-by: ""  # stakeholder ID (e.g. STK-001)
owner: ""      # stakeholder ID (e.g. STK-001)
schema_version: "1.0"
tags: []
related: []    # entity IDs (e.g. [DEC-001, ACT-002])
---

## Description

## Acceptance Criteria

## Notes
```

`.sara/templates/decision.md`:
```
---
id: DEC-000
title: ""
status: proposed  # proposed | accepted | rejected | superseded
context: ""
decision: ""
rationale: ""
alternatives-considered: ""
date: ""          # ISO 8601 (e.g. 2026-04-27)
deciders: []      # stakeholder IDs (e.g. [STK-001, STK-002])
supersedes: ""    # DEC-NNN or empty
schema_version: "1.0"
tags: []
related: []
---

## Context

## Decision

## Rationale

## Alternatives Considered
```

`.sara/templates/action.md`:
```
---
id: ACT-000
title: ""
status: open  # open | in-progress | done | cancelled
description: ""
owner: ""      # stakeholder ID (e.g. STK-001)
due-date: ""   # ISO 8601
source: ""     # ingest ID (e.g. MTG-001)
schema_version: "1.0"
tags: []
related: []
---

## Description

## Notes
```

`.sara/templates/risk.md`:
```
---
id: RISK-000
title: ""
status: open  # open | mitigated | accepted | closed
description: ""
likelihood: ""  # low | medium | high
impact: ""      # low | medium | high
owner: ""       # stakeholder ID (e.g. STK-001)
mitigation: ""
source: ""      # ingest ID (e.g. MTG-001)
schema_version: "1.0"
tags: []
related: []
---

## Description

## Mitigation

## Notes
```

`.sara/templates/stakeholder.md` (frontmatter ONLY — no body sections, per D-11):
```
---
id: STK-000
name: ""
vertical: ""    # from project config verticals list
department: ""  # from project config departments list
email: ""
role: ""
schema_version: "1.0"
related: []
---
```

CRITICAL: vertical and department MUST be two separate fields. Never merge them into a combined
field. Do not add body section headings to stakeholder.md.

**Step 12 — Report success**

Instruct the executor to report success to the user with:
- Project name
- A list of all 11 directories created
- A list of all files created (.sara/config.json, pipeline-state.json, wiki/CLAUDE.md, wiki/index.md, wiki/log.md, five templates)
- Next step hint: "Run /sara-ingest to register your first input document"
- Note: "/sara-init does not commit files to git. Use git add . && git commit when ready."

After inserting Steps 8-12 before `</process>`, also append a `<notes>` block after `</process>`:

```
<notes>
- wiki/CLAUDE.md is only automatically loaded by Claude Code when the active file is within the
  wiki/ subtree. Skills operating on files outside wiki/ will not inherit the schema context
  automatically.
- If /sara-init fails partway through (e.g. a permission error after the guard clause passed),
  the wiki/ directory may exist but be incomplete. Recovery: delete the partial output
  (rm -rf wiki/ raw/ .sara/ pipeline-state.json) and re-run /sara-init. The guard clause
  prevents re-init on a live repo but will also block recovery of an incomplete init that left
  wiki/ behind.
- /sara-init does not run git init or commit files. The initialised structure is ready to commit
  but the user must do so. Suggested commit: git add .sara/ pipeline-state.json wiki/ raw/ &&
  git commit -m "chore: initialise SARA wiki structure"
- Ingest types (meeting, email, slack, document) are hardcoded in SARA skill logic. They are NOT
  stored in .sara/config.json. Do not add an ingest-types key to config (per D-05).
- Vertical and department are always separate fields in both .sara/config.json and entity templates.
  Never merge them into a single field (domain constraint — see project memory).
</notes>
```
  </action>
  <verify>
    <automated>grep -q 'wiki/CLAUDE.md' .claude/skills/sara-init/SKILL.md && grep -q 'Behavioral Rules' .claude/skills/sara-init/SKILL.md && grep -q 'Deduplication' .claude/skills/sara-init/SKILL.md && grep -q 'maintained-by: sara' .claude/skills/sara-init/SKILL.md && grep -q 'requirement.md' .claude/skills/sara-init/SKILL.md && grep -q 'decision.md' .claude/skills/sara-init/SKILL.md && grep -q 'action.md' .claude/skills/sara-init/SKILL.md && grep -q 'risk.md' .claude/skills/sara-init/SKILL.md && grep -q 'stakeholder.md' .claude/skills/sara-init/SKILL.md && grep -q 'vertical:' .claude/skills/sara-init/SKILL.md && grep -q 'department:' .claude/skills/sara-init/SKILL.md && grep -c 'schema_version: "1.0"' .claude/skills/sara-init/SKILL.md | awk '{if($1>=5) print "OK"; else print "FAIL: only",$1,"occurrences"}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'Behavioral Rules' .claude/skills/sara-init/SKILL.md` exits 0
    - `grep -q 'Deduplication' .claude/skills/sara-init/SKILL.md` exits 0 (rule 1 present)
    - `grep -q 'Index maintenance' .claude/skills/sara-init/SKILL.md` exits 0 (rule 2 present)
    - `grep -q 'Log maintenance' .claude/skills/sara-init/SKILL.md` exits 0 (rule 3 present)
    - `grep -q 'ID assignment' .claude/skills/sara-init/SKILL.md` exits 0 (rule 4 present)
    - `grep -q 'Cross-references' .claude/skills/sara-init/SKILL.md` exits 0 (rule 5 present)
    - `grep -q 'maintained-by: sara' .claude/skills/sara-init/SKILL.md` exits 0 (index.md or log.md stub content present)
    - `grep -q 'Wiki Index' .claude/skills/sara-init/SKILL.md` exits 0
    - `grep -q 'Wiki Log' .claude/skills/sara-init/SKILL.md` exits 0
    - `grep -q 'requirement.md' .claude/skills/sara-init/SKILL.md` exits 0
    - `grep -q 'decision.md' .claude/skills/sara-init/SKILL.md` exits 0
    - `grep -q 'action.md' .claude/skills/sara-init/SKILL.md` exits 0
    - `grep -q 'risk.md' .claude/skills/sara-init/SKILL.md` exits 0
    - `grep -q 'stakeholder.md' .claude/skills/sara-init/SKILL.md` exits 0
    - `grep -q 'vertical:' .claude/skills/sara-init/SKILL.md` exits 0 (stakeholder vertical field present)
    - `grep -q 'department:' .claude/skills/sara-init/SKILL.md` exits 0 (stakeholder department field present — separate from vertical)
    - `grep -c 'schema_version: "1.0"' .claude/skills/sara-init/SKILL.md` returns 5 or more (once per template plus once per schema block in wiki/CLAUDE.md)
    - `grep -q 'Acceptance Criteria' .claude/skills/sara-init/SKILL.md` exits 0 (requirement.md body section present)
    - `grep -q 'Alternatives Considered' .claude/skills/sara-init/SKILL.md` exits 0 (decision.md ADR body section present)
    - `grep -q 'Mitigation' .claude/skills/sara-init/SKILL.md` exits 0 (risk.md body section present)
    - `grep -q 'notes' .claude/skills/sara-init/SKILL.md` exits 0 (notes block present)
    - `grep -q 'Plan 02 will add' .claude/skills/sara-init/SKILL.md` exits 1 (placeholder comment removed)
    - `grep -q 'sara-ingest' .claude/skills/sara-init/SKILL.md` exits 0 (success report includes next step hint)
  </acceptance_criteria>
  <done>
    `.claude/skills/sara-init/SKILL.md` is complete and executable. Steps 8-12 have been added covering:
    wiki/CLAUDE.md write (project header + 5 behavioral rules + 5 entity schema blocks), wiki/index.md
    stub, wiki/log.md stub, all five entity template writes (requirement, decision, action, risk,
    stakeholder) with annotated frontmatter and correct body sections, success report listing all
    created files, and a notes block. schema_version: "1.0" (quoted) appears in all template content.
    vertical and department are separate fields in the stakeholder template. The placeholder comment
    from Plan 01 is gone. The skill is complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md content → wiki/CLAUDE.md | Schema behavioral rules written by this skill become the enforcement contract for all future wiki-touching skills |
| Template content → entity YAML | Template frontmatter defines what fields pipeline commands will read and write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | wiki/CLAUDE.md behavioral rules | mitigate | Rules are written as authoritative prose loaded automatically by Claude Code; no code execution path; tampered rules would only affect LLM behavior, not system security |
| T-02-02 | Tampering | Entity template schema fields | accept | Templates are static files with no execution context; incorrect fields would cause data quality issues (caught at verify), not security vulnerabilities |
| T-02-03 | Information Disclosure | stakeholder.md vertical/department conflation | mitigate | Two separate YAML fields enforced; any merged field would be caught by acceptance criteria grep checks |
| T-02-04 | Tampering | Obsidian YAML parse of schema_version | mitigate | schema_version always quoted as "1.0" in all templates; Obsidian parses as string, not float; verified by grep check in acceptance criteria |
</threat_model>

<verification>
After Plan 02 completes, run the full suite from VALIDATION.md:

```bash
bash -c 'test -d wiki && test -d raw && test -f .sara/config.json && test -f pipeline-state.json && test -f wiki/CLAUDE.md && test -f wiki/index.md && test -f wiki/log.md && ls .sara/templates/*.md | wc -l | grep -q 5 && echo ALL_OK'
```

Note: This full-suite command runs against the output of `/sara-init`, not against the SKILL.md.
Run `/sara-init` in a temp directory first (Plan 03 verification checkpoint does this).

For SKILL.md content verification (runnable now):
```bash
grep -c 'schema_version: "1.0"' .claude/skills/sara-init/SKILL.md
# Must be >= 5 (one per template write + at least once in wiki/CLAUDE.md schema blocks)

grep -q 'vertical:' .claude/skills/sara-init/SKILL.md && grep -q 'department:' .claude/skills/sara-init/SKILL.md && echo "stakeholder fields OK"

grep -q 'Behavioral Rules' .claude/skills/sara-init/SKILL.md && echo "CLAUDE.md rules present"
```
</verification>

<success_criteria>
- SKILL.md is complete with Steps 8-12 and a notes block
- wiki/CLAUDE.md content in the skill includes project name interpolation, five behavioral rules, and five entity schema blocks
- wiki/index.md stub content includes the | ID | Title | Status | Type | Tags | Last Updated | table header
- wiki/log.md stub content includes the append-only comment
- All five entity templates (requirement, decision, action, risk, stakeholder) are present in the skill with correct frontmatter fields and body sections per D-11
- stakeholder template has vertical and department as separate fields (not merged)
- schema_version: "1.0" (quoted) appears at least 5 times in SKILL.md
- The placeholder comment from Plan 01 is removed
- notes block is present after </process>
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-schema/01-02-SUMMARY.md`
</output>
