---
phase: 08-refine-requirements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/sara-extract/SKILL.md
  - .claude/agents/sara-artifact-sorter.md
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05

must_haves:
  truths:
    - "sara-extract Step 3 requirements pass only extracts passages containing modal verbs from the INCLUDE list"
    - "Each extracted requirement has a `priority` field (must-have/should-have/could-have/wont-have) derived from its commitment modal"
    - "Each extracted requirement has a `req_type` field (one of six types) classified in the same inline pass"
    - "The extraction prompt contains at least three named negative examples (observation, aspiration, background context)"
    - "sara-artifact-sorter output schema includes `priority` and `req_type` fields and has a passthrough rule"
  artifacts:
    - path: ".claude/skills/sara-extract/SKILL.md"
      provides: "Updated requirements pass with modal-verb signal, MoSCoW mapping, type classification"
      contains: "priority: must-have"
    - path: ".claude/agents/sara-artifact-sorter.md"
      provides: "Updated output schema with priority and req_type passthrough rule"
      contains: "priority"
  key_links:
    - from: ".claude/skills/sara-extract/SKILL.md"
      to: ".claude/agents/sara-artifact-sorter.md"
      via: "Task() call — merged artifact array includes priority and req_type"
      pattern: "priority.*req_type"
---

<objective>
Rewrite the sara-extract requirements pass to use modal-verb anchoring as the primary extraction signal, add inline MoSCoW priority mapping and six-type classification, and fix the sara-artifact-sorter output schema to pass the two new fields through to the approval loop.

Purpose: The current requirements pass ("Extract every passage that describes a requirement — a capability, constraint, or rule…") produces false positives (observations, aspirations, background context). Anchoring on commitment modals eliminates these. The sorter schema fix ensures `priority` and `req_type` survive the sorter Task() and reach sara-update.

Output: Two modified files — sara-extract/SKILL.md (Step 3 requirements pass block replaced) and sara-artifact-sorter.md (output_format updated, passthrough rule added).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-refine-requirements/08-CONTEXT.md
@.planning/phases/08-refine-requirements/08-RESEARCH.md
@.planning/phases/08-refine-requirements/08-PATTERNS.md

<interfaces>
<!-- Existing requirements pass (lines 52–61 of sara-extract/SKILL.md) — to be replaced in full: -->

```
**Requirements pass**

Extract every passage that describes a requirement — a capability, constraint, or rule that the
system or project must satisfy. For each requirement found:
- Extract the exact verbatim passage as `source_quote` (MANDATORY — skip any requirement without a quotable passage)
- Write a short (≤10 words) noun-phrase `title`
- Set `raised_by` to the STK-NNN ID if identifiable from the source or discussion_notes; otherwise use `"STK-NNN"` placeholder
- Set `action` = `"create"`, `type` = `"requirement"`, `id_to_assign` = `"REQ-NNN"`, `related` = `[]`, `change_summary` = `""`
- Do NOT resolve create-vs-update — that is the sorter's job

Collect results as `{req_artifacts}` (JSON array; empty array if none found).
```

<!-- Current sorter output_format requirement object (lines 86–96 of sara-artifact-sorter.md) — missing priority and req_type: -->

```json
{
  "action": "create",
  "type": "requirement",
  "id_to_assign": "REQ-NNN",
  "title": "Short title",
  "source_quote": "Exact verbatim text from source document",
  "raised_by": "STK-NNN",
  "related": [],
  "change_summary": ""
}
```

<!-- Rules section note that must be added (after line 114 — after the source_quote preservation rule): -->
```
- `source_quote` must be preserved verbatim from the specialist agent output — do not modify quotes
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite sara-extract Step 3 requirements pass</name>
  <files>.claude/skills/sara-extract/SKILL.md</files>
  <read_first>
    - .claude/skills/sara-extract/SKILL.md — read the entire file to locate Step 3 (lines ~52–61); do not edit any other section
  </read_first>
  <action>
Replace the entire `**Requirements pass**` block in Step 3 (from the `**Requirements pass**` header line through `Collect results as {req_artifacts}...`) with the following new block verbatim. All other Step 3 content (Decisions pass, Actions pass, Risks pass, Merge and sorter dispatch) is unchanged.

New `**Requirements pass**` block to write:

```
**Requirements pass**

A passage IS a requirement if and only if it contains a commitment modal verb or imperative phrase
from the INCLUDE list below. Passages lacking these signals are NOT requirements regardless of topic.

  INCLUDE — these passages ARE requirements (extract them):
  - "must", "shall", "has to", "required to", "need to" → priority: must-have
  - "will" (as a commitment to future behaviour, not narrating past events) → priority: must-have
  - "should" → priority: should-have
  - "could", "may" → priority: could-have
  - "will not", "won't", "out of scope", "we won't" → priority: wont-have

  EXCLUDE — these passages are NOT requirements (do NOT extract them):
  - Observation: "Users are currently frustrated with slow load times" (describes a situation, no commitment modal)
  - Aspiration/wish: "It would be great if the system handled more users" (desire expressed without a modal commitment)
  - Background context: "The company processes approximately 10,000 invoices per month" (descriptive fact only, no system obligation)

For each requirement found, classify it into one of six types inline based on what the requirement describes:
  - `functional`     — a capability the system performs
  - `non-functional` — quality attributes and design constraints (performance, reliability, usability, security, scalability)
  - `regulatory`     — external law, standards, or mandates (GDPR, PCI-DSS, etc.) — external obligations only, not internal policy
  - `integration`    — how the system connects to external systems, APIs, or people
  - `business-rule`  — domain logic or process policy
  - `data`           — structure, retention, quality, or ownership rules for data

For each requirement found:
- Extract the exact verbatim passage as `source_quote` (MANDATORY — skip any requirement without a quotable passage)
- Write a short (≤10 words) noun-phrase `title`
- Set `raised_by` to the STK-NNN ID if identifiable from the source or discussion_notes; otherwise use `"STK-NNN"` placeholder
- Set `priority` to the MoSCoW value derived from the commitment modal (see INCLUDE list above)
- Set `req_type` to one of the six types above
- Set `action` = `"create"`, `type` = `"requirement"`, `id_to_assign` = `"REQ-NNN"`, `related` = `[]`, `change_summary` = `""`
- Do NOT resolve create-vs-update — that is the sorter's job

Collect results as `{req_artifacts}` (JSON array; empty array if none found).
```
  </action>
  <verify>
    <automated>grep -n "INCLUDE — these passages ARE requirements" /home/george/Projects/sara/.claude/skills/sara-extract/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - sara-extract/SKILL.md contains the line `INCLUDE — these passages ARE requirements (extract them):`
    - sara-extract/SKILL.md contains `priority: must-have` in the INCLUDE block
    - sara-extract/SKILL.md contains `priority: should-have` in the INCLUDE block
    - sara-extract/SKILL.md contains `priority: could-have` in the INCLUDE block
    - sara-extract/SKILL.md contains `priority: wont-have` in the INCLUDE block
    - sara-extract/SKILL.md contains `EXCLUDE — these passages are NOT requirements`
    - sara-extract/SKILL.md contains exactly three EXCLUDE negative example types: "Observation:", "Aspiration/wish:", "Background context:"
    - sara-extract/SKILL.md contains `Set \`priority\` to the MoSCoW value`
    - sara-extract/SKILL.md contains `Set \`req_type\` to one of the six types`
    - sara-extract/SKILL.md contains `"req_type"` in the output field list
    - The original "Extract every passage that describes a requirement — a capability, constraint, or rule" sentence is no longer present (replaced)
    - The `**Decisions pass**` block remains intact and immediately follows the requirements pass
  </acceptance_criteria>
  <done>The requirements pass block in sara-extract/SKILL.md Step 3 is fully replaced with the modal-verb-anchored prompt. The six INCLUDE modals, three EXCLUDE negative examples, six type labels, and both new fields (priority, req_type) are present.</done>
</task>

<task type="auto">
  <name>Task 2: Fix sara-artifact-sorter output schema for priority and req_type passthrough</name>
  <files>.claude/agents/sara-artifact-sorter.md</files>
  <read_first>
    - .claude/agents/sara-artifact-sorter.md — read the entire file to locate the output_format requirement object example and the Rules: section
  </read_first>
  <action>
Two edits to sara-artifact-sorter.md:

**Edit A — Update the requirement object example in `<output_format>`:**

Find the requirement object in the cleaned_artifacts example array. It currently ends with `"change_summary": ""`. Add two new fields after `change_summary`:

```json
    {
      "action": "create",
      "type": "requirement",
      "id_to_assign": "REQ-NNN",
      "title": "Short title",
      "source_quote": "Exact verbatim text from source document",
      "raised_by": "STK-NNN",
      "related": [],
      "change_summary": "",
      "priority": "must-have",
      "req_type": "functional"
    },
```

The decision object example (action=update) is not changed — it has no priority or req_type.

**Edit B — Add passthrough rule to the Rules: section:**

Find the line:
```
- `source_quote` must be preserved verbatim from the specialist agent output — do not modify quotes
```

Add the following rule immediately after it:

```
- For requirement artifacts, preserve `priority` and `req_type` exactly as received from the extraction pass. Do not modify, reclassify, or drop these fields. Pass them through unchanged to `cleaned_artifacts`.
```

No other edits to sara-artifact-sorter.md. The sorter's deduplication logic, create-vs-update resolution, and question generation are not changed.
  </action>
  <verify>
    <automated>grep -n "priority.*must-have\|req_type.*functional\|preserve.*priority.*req_type" /home/george/Projects/sara/.claude/agents/sara-artifact-sorter.md</automated>
  </verify>
  <acceptance_criteria>
    - sara-artifact-sorter.md contains `"priority": "must-have"` in the output_format requirement object example
    - sara-artifact-sorter.md contains `"req_type": "functional"` in the output_format requirement object example
    - sara-artifact-sorter.md contains a rule with the text `preserve \`priority\` and \`req_type\` exactly as received`
    - The `source_quote` preservation rule still appears immediately before the new priority/req_type rule
    - The decision object example in output_format does not have priority or req_type fields added
    - The sorter process steps (1–6) are unchanged
  </acceptance_criteria>
  <done>The sorter output schema includes `priority` and `req_type` on requirement objects, and the Rules section has an explicit passthrough rule for those fields. Fields will survive the sorter Task() call and reach sara-update's approval loop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill text → LLM prompt | The rewritten requirements pass becomes the LLM's instruction; vague wording could cause extraction errors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | sara-extract/SKILL.md requirements pass | accept | Skill files are author-controlled markdown; no runtime input path for injection. Changes are reviewed via git diff before commit. |
| T-08-02 | Information Disclosure | sara-artifact-sorter.md output_format | accept | Agent output is JSON returned to the skill in-context; no external persistence or network call. Low risk. |
| T-08-03 | Denial of Service | Extraction pass over-extraction | mitigate | Negative examples in the prompt explicitly block observations, aspirations, and background context — reduces false positives that would bloat the approval loop |
</threat_model>

<verification>
After both tasks complete:

1. Read sara-extract/SKILL.md and confirm the requirements pass block starts with "A passage IS a requirement if and only if..." and contains all six INCLUDE modals and three EXCLUDE examples.
2. Read sara-artifact-sorter.md and confirm the requirement object in output_format has `"priority"` and `"req_type"` fields.
3. Run: `grep -n "priority\|req_type" /home/george/Projects/sara/.claude/skills/sara-extract/SKILL.md` — should show the INCLUDE list and the two `Set` instructions.
4. Run: `grep -n "priority\|req_type" /home/george/Projects/sara/.claude/agents/sara-artifact-sorter.md` — should show the example object and the passthrough rule.
5. Confirm `**Decisions pass**` block in sara-extract/SKILL.md is intact and unchanged.
</verification>

<success_criteria>
- sara-extract requirements pass is anchored on modal verbs (D-01)
- Three explicit negative examples are present in the prompt (D-02)
- MoSCoW mapping from modal to priority is in the INCLUDE list (D-03)
- `priority` and `req_type` are assigned in the same inline pass (D-04)
- All six type labels are present with descriptions (D-05)
- sara-artifact-sorter will pass `priority` and `req_type` through to the approval loop without dropping them
</success_criteria>

<output>
After completion, create `.planning/phases/08-refine-requirements/08-01-SUMMARY.md`
</output>
