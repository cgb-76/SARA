---
phase: 08-refine-requirements
plan: 02
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - .claude/skills/sara-init/SKILL.md
autonomous: true
requirements:
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10
  - D-11

must_haves:
  truths:
    - "sara-init produces a requirement.md template with v2.0 frontmatter (type, priority fields present; description field absent; schema_version '2.0' with single quotes)"
    - "The requirement.md template body contains all seven section headings with the section matrix embedded as a YAML comment block"
    - "The CLAUDE.md Requirement schema block in sara-init Step 9 shows the v2.0 frontmatter shape with a note that the body follows the structured section format"
    - "summary field appears directly after title in the frontmatter (per D-08)"
  artifacts:
    - path: ".claude/skills/sara-init/SKILL.md"
      provides: "Updated Step 9 CLAUDE.md Requirement schema block (v2.0) + updated Step 12 requirement.md template (v2.0)"
      contains: "schema_version: '2.0'"
  key_links:
    - from: ".claude/skills/sara-init/SKILL.md"
      to: ".sara/templates/requirement.md"
      via: "Step 12 Write call — template file content is the string in SKILL.md"
      pattern: "sara/templates/requirement"
---

<objective>
Update sara-init/SKILL.md with two targeted edits: (1) replace the Requirement schema block in Step 9 (the CLAUDE.md write) with the v2.0 frontmatter shape, and (2) replace the `.sara/templates/requirement.md` template in Step 12 with the v2.0 frontmatter and structured seven-section body including the section matrix as an embedded comment.

Purpose: New SARA projects initialised after this phase must produce the v2.0 requirement template and CLAUDE.md schema block. Otherwise new projects will use v1.0 shapes (description field, no type/priority) and be incompatible with the updated sara-extract and sara-update.

Output: sara-init/SKILL.md with Step 9 Requirement block and Step 12 requirement.md template replaced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-refine-requirements/08-CONTEXT.md
@.planning/phases/08-refine-requirements/08-RESEARCH.md
@.planning/phases/08-refine-requirements/08-PATTERNS.md

<interfaces>
<!-- Current Requirement block in sara-init Step 9 CLAUDE.md write (the yaml block inside the markdown string): -->
```yaml
### Requirement

```yaml
---
id: REQ-000
title: ""
status: open  # open | accepted | rejected | superseded
summary: ""  # REQ: title, status, one-line description of what is required
source: ""     # ingest ID (e.g. MTG-001)
raised-by: ""  # stakeholder ID (e.g. STK-001)
owner: ""      # stakeholder ID (e.g. STK-001)
schema_version: "1.0"
tags: []
related: []    # entity IDs (e.g. [DEC-001, ACT-002])
---

## Description

## Acceptance Criteria

## Notes
```
```

<!-- Current requirement.md template in sara-init Step 12: -->
```markdown
---
id: REQ-000
title: ""
status: open  # open | accepted | rejected | superseded
summary: ""  # REQ: title, status, one-line description of what is required
source: ""     # ingest ID (e.g. MTG-001)
raised-by: ""  # stakeholder ID (e.g. STK-001)
owner: ""      # stakeholder ID (e.g. STK-001)
schema_version: "1.0"
tags: []
related: []    # entity IDs (e.g. [DEC-001, ACT-002])
---

## Description

## Acceptance Criteria

## Notes
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Step 9 CLAUDE.md Requirement schema block to v2.0</name>
  <files>.claude/skills/sara-init/SKILL.md</files>
  <read_first>
    - .claude/skills/sara-init/SKILL.md — read the entire file to locate Step 9 and the `### Requirement` schema block within the CLAUDE.md write string
  </read_first>
  <action>
In sara-init/SKILL.md, find Step 9 (the section that writes `wiki/CLAUDE.md`). Within the CLAUDE.md write string, find the `### Requirement` block. Replace it entirely with the following v2.0 block:

```
### Requirement

```yaml
---
id: REQ-000
title: ""
summary: ""  # REQ: title, status, one-line description of what is required
status: open  # open | accepted | rejected | superseded
type: functional  # functional | non-functional | regulatory | integration | business-rule | data
priority: must-have  # must-have | should-have | could-have | wont-have
source: ""     # ingest ID (e.g. MTG-001)
raised-by: ""  # stakeholder ID (e.g. STK-001)
owner: ""      # stakeholder ID (e.g. STK-001)
schema_version: '2.0'
tags: []
related: []    # entity IDs (e.g. [DEC-001, ACT-002])
---
```

Body follows the structured section format (Source Quote, Statement, User Story, Acceptance Criteria,
BDD Criteria, Context, Cross Links). Which sections are required/optional/omitted depends on `type` —
see `.sara/templates/requirement.md` for the section matrix and rationale.
```

Key differences from the current block:
- `summary` moved to appear directly after `title` (before `status`) — per D-08
- `description` field removed entirely — per D-07
- `type` field added after `status` — per D-09
- `priority` field added after `type` — per D-09
- `schema_version` changed from `"1.0"` to `'2.0'` (single quotes) — per D-06, YAML float prevention
- Body sections `## Description`, `## Acceptance Criteria`, `## Notes` replaced by a note pointing to the template for the section matrix

Do not change the Decision, Action, Risk, or Stakeholder schema blocks. Do not change any Behavioral Rules or GSD Phase Completion sections.
  </action>
  <verify>
    <automated>grep -n "type: functional\|priority: must-have\|schema_version: '2.0'" /home/george/Projects/sara/.claude/skills/sara-init/SKILL.md | head -10</automated>
  </verify>
  <acceptance_criteria>
    - sara-init/SKILL.md contains `type: functional  # functional | non-functional | regulatory | integration | business-rule | data` in Step 9 Requirement block
    - sara-init/SKILL.md contains `priority: must-have  # must-have | should-have | could-have | wont-have` in Step 9 Requirement block
    - sara-init/SKILL.md contains `schema_version: '2.0'` (single quotes) in Step 9 Requirement block — NOT `"1.0"` or `"2.0"` with double quotes
    - sara-init/SKILL.md does NOT contain `## Description` inside the Step 9 Requirement schema block
    - sara-init/SKILL.md contains `summary` appearing before `status` in the Step 9 Requirement frontmatter (i.e. `summary:` line comes before `status:` line within the block)
    - The Decision schema block in Step 9 still contains `schema_version: "1.0"` (unchanged)
    - The note about the structured section format and section matrix reference is present in the Step 9 Requirement block
  </acceptance_criteria>
  <done>The CLAUDE.md Requirement schema block in Step 9 shows the v2.0 frontmatter shape with type, priority, summary-before-status ordering, and single-quoted schema_version. New sara-init projects will produce CLAUDE.md files with the updated schema documentation.</done>
</task>

<task type="auto">
  <name>Task 2: Replace Step 12 requirement.md template with v2.0 body structure and section matrix</name>
  <files>.claude/skills/sara-init/SKILL.md</files>
  <read_first>
    - .claude/skills/sara-init/SKILL.md — locate Step 12 and specifically the `.sara/templates/requirement.md` write block; the template starts at the line after "`.sara/templates/requirement.md`:" and ends before "`.sara/templates/decision.md`:"
  </read_first>
  <action>
In sara-init/SKILL.md Step 12, find the block for `.sara/templates/requirement.md`. Replace the content of the Write call (the markdown string that becomes the template file) with the following complete v2.0 template:

```markdown
---
id: REQ-000
title: ""
summary: ""  # REQ: title, status, one-line description of what is required
status: open  # open | accepted | rejected | superseded
type: functional  # functional | non-functional | regulatory | integration | business-rule | data
priority: must-have  # must-have | should-have | could-have | wont-have
source: ""     # ingest ID (e.g. MTG-001)
raised-by: ""  # stakeholder ID (e.g. STK-001)
owner: ""      # stakeholder ID (e.g. STK-001)
schema_version: '2.0'
tags: []
related: []    # entity IDs (e.g. [DEC-001, ACT-002])
---

## Source Quote
> [exact stakeholder words from source document]

## Statement
The [subject] shall [verb phrase].

<!-- Section matrix — which sections are required (✓), optional (opt), or omitted (—) per requirement type:

| Section             | Functional | Non-functional | Regulatory | Integration | Business rule | Data |
|---------------------|:----------:|:--------------:|:----------:|:-----------:|:-------------:|:----:|
| Source Quote        |     ✓      |       ✓        |     ✓      |      ✓      |       ✓       |  ✓   |
| Statement           |     ✓      |       ✓        |     ✓      |      ✓      |       ✓       |  ✓   |
| User Story          |     ✓      |      opt       |     —      |     opt     |       —       |  —   |
| Acceptance Criteria |     ✓      |       ✓        |     ✓      |      ✓      |       ✓       |  ✓   |
| BDD Criteria        |     ✓      |       —        |     —      |     opt     |       ✓       |  —   |
| Context             |    opt     |       ✓        |     ✓      |      ✓      |       ✓       |  ✓   |
| Cross Links         |     ✓      |       ✓        |     ✓      |      ✓      |       ✓       |  ✓   |

Rationale:
- User Story on Non-functional: optional — usability NFRs have a natural user role, but performance/security/reliability NFRs usually don't map to a role/benefit framing.
- User Story on Integration: optional — integration requirements can have a user-facing perspective (e.g. a developer consuming an API) but often don't.
- User Story omitted on Regulatory/Business rule/Data: compliance mandates and policy rules are not user goals; forcing a user story produces artificial framing.
- BDD on Business rule: required — business rules are where Gherkin is most natural ("Given an invoice is unapproved, When payment is attempted, Then reject with error").
- BDD on Integration: optional — can work well for API contract verification but not always natural (e.g. authentication handshake sequences).
- BDD omitted on Non-functional/Regulatory/Data: non-functionals are measured, not triggered by user actions; regulatory and data requirements are compliance checklists, not behaviour scenarios.
- Context on Functional: optional — usually self-evident from Source Quote + Statement; include only when there is non-obvious rationale or design constraint.
- Context required on all other types: why a quality target, mandate, integration contract, domain rule, or data policy exists is rarely obvious from the statement alone; downstream readers need the rationale.
-->

## User Story
As a [role], I want [capability], so that [benefit].

## Acceptance Criteria
- [ ] [plain language condition]

## BDD Criteria
**Scenario: [name]**
Given [context]
When [action]
Then [outcome]

## Context
[Rationale, background, constraints not captured elsewhere]

## Cross Links
[One wiki link per related[] entry — see wikilink rule in sara-update SKILL.md]
```

The decision.md, action.md, risk.md, and stakeholder.md templates in Step 12 are not changed.
  </action>
  <verify>
    <automated>grep -n "Section matrix\|BDD Criteria\|Source Quote\|schema_version: '2.0'" /home/george/Projects/sara/.claude/skills/sara-init/SKILL.md | head -20</automated>
  </verify>
  <acceptance_criteria>
    - sara-init/SKILL.md contains `## Source Quote` section heading in the requirement.md template block
    - sara-init/SKILL.md contains `## Statement` section heading in the requirement.md template block
    - sara-init/SKILL.md contains `## BDD Criteria` section heading in the requirement.md template block
    - sara-init/SKILL.md contains `## Cross Links` section heading in the requirement.md template block
    - sara-init/SKILL.md contains the Section matrix comment block with the table header `| Section             | Functional | Non-functional |`
    - sara-init/SKILL.md contains the Rationale block within the comment (text starting `- User Story on Non-functional:`)
    - sara-init/SKILL.md does NOT contain `## Description` inside the requirement.md template block
    - sara-init/SKILL.md does NOT contain `## Notes` inside the requirement.md template block (replaced by the structured sections)
    - sara-init/SKILL.md contains `schema_version: '2.0'` (single quotes) in the requirement.md template — grep: `grep "schema_version: '2.0'" .claude/skills/sara-init/SKILL.md` returns at least 2 matches (Step 9 and Step 12)
    - sara-init/SKILL.md contains `summary: ""` appearing before `status: open` in the requirement.md template frontmatter
    - The `.sara/templates/decision.md` template in Step 12 still contains `schema_version: "1.0"` (unchanged)
  </acceptance_criteria>
  <done>The requirement.md template in sara-init Step 12 contains the v2.0 frontmatter (type, priority, summary-before-status, schema_version '2.0') and the full seven-section body with the section matrix and rationale embedded as a YAML comment block. New sara-init runs will produce templates conforming to D-06 through D-11.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill text → project files written at init time | The template and CLAUDE.md block become static files in new projects; incorrect content cannot be undone without a re-init |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering | sara-init/SKILL.md template write | accept | SKILL.md is author-controlled; changes reviewed via git diff. Template accuracy is verified by acceptance criteria before commit. |
| T-08-05 | Spoofing | YAML float parsing of schema_version | mitigate | Use single-quoted `'2.0'` in both Step 9 and Step 12; explicitly called out in acceptance criteria to prevent double-quote slip |
</threat_model>

<verification>
After both tasks complete:

1. Run: `grep -n "schema_version: '2.0'" /home/george/Projects/sara/.claude/skills/sara-init/SKILL.md` — must return at least 2 matches (Step 9 block and Step 12 template).
2. Run: `grep -n "type: functional\|priority: must-have" /home/george/Projects/sara/.claude/skills/sara-init/SKILL.md` — must return matches in both Step 9 and Step 12 sections.
3. Run: `grep -n "## Description\|## Notes" /home/george/Projects/sara/.claude/skills/sara-init/SKILL.md` — must NOT appear inside the requirement.md template block (they are fine in decision/action/risk templates).
4. Run: `grep -n "Section matrix" /home/george/Projects/sara/.claude/skills/sara-init/SKILL.md` — must return exactly 1 match in the Step 12 template comment block.
5. Confirm the decision.md template schema_version is still `"1.0"` (unchanged): `grep -A5 "sara/templates/decision" /home/george/Projects/sara/.claude/skills/sara-init/SKILL.md | grep schema_version`.
</verification>

<success_criteria>
- sara-init Step 9 CLAUDE.md Requirement block: v2.0 frontmatter with type, priority, summary before status, schema_version '2.0', body section format note (D-06, D-07, D-08, D-09)
- sara-init Step 12 requirement.md template: v2.0 frontmatter + all seven body sections + full section matrix with rationale in YAML comment (D-10, D-11)
- No changes to decision, action, risk, or stakeholder templates (scope boundary respected)
</success_criteria>

<output>
After completion, create `.planning/phases/08-refine-requirements/08-02-SUMMARY.md`
</output>
