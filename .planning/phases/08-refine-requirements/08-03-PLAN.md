---
phase: 08-refine-requirements
plan: 03
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - .claude/skills/sara-update/SKILL.md
autonomous: true
requirements:
  - D-10
  - D-11
  - D-12

must_haves:
  truths:
    - "sara-update requirement create branch writes the v2.0 structured body (Source Quote, Statement, User Story, Acceptance Criteria, BDD Criteria, Context, Cross Links) per the section matrix"
    - "sara-update requirement update branch also rewrites the full body to v2.0 structure when updating a requirement page"
    - "sara-update writes a ## Cross Links section from artifact.related[] for every requirement create and update"
    - "sara-update sets schema_version '2.0' (single quotes) on requirement pages"
    - "sara-update sets type and priority frontmatter fields from the artifact object on requirement pages"
  artifacts:
    - path: ".claude/skills/sara-update/SKILL.md"
      provides: "Updated requirement create branch (v2.0 body), updated requirement update branch (body rewrite + schema_version), Cross Links generation from related[]"
      contains: "## Cross Links"
  key_links:
    - from: ".claude/skills/sara-update/SKILL.md"
      to: "wiki/requirements/{REQ-NNN}.md"
      via: "Write tool call in Step 2 create branch"
      pattern: "## Cross Links"
    - from: ".claude/skills/sara-extract/SKILL.md"
      to: ".claude/skills/sara-update/SKILL.md"
      via: "artifact.priority and artifact.req_type fields flow from approved_artifacts to wiki page"
      pattern: "artifact.priority.*artifact.req_type"
---

<objective>
Rewrite the sara-update requirement create and update branches in Step 2 to produce v2.0 wiki pages: v2.0 frontmatter (type, priority, schema_version '2.0'), structured seven-section body per the section matrix, and Cross Links generation from artifact.related[].

Purpose: sara-update is the only skill that writes wiki artifact files. Without this change, the extraction prompt improvements (Plan 01) and the template changes (Plan 02) have no effect on actually-written wiki pages — they would still be written in the v1.0 shape.

Output: sara-update/SKILL.md with the requirement create branch body sections replaced, requirement update branch extended with v2.0 body rewrite instructions, and Cross Links generation logic added.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-refine-requirements/08-CONTEXT.md
@.planning/phases/08-refine-requirements/08-RESEARCH.md
@.planning/phases/08-refine-requirements/08-PATTERNS.md

<interfaces>
<!-- Current requirement create branch body in sara-update Step 2 (lines 145–163): -->
```
    **requirement:**
    ```
    ## Description
    > "{artifact.source_quote}" — {stakeholder_name}

    {synthesised summary of what this requirement captures, why it matters, and any constraints
     resolved during /sara-discuss}

    ## Acceptance Criteria
    {REQUIRED — derive at least one testable criterion directly from the requirement text,
     even if the source does not state it explicitly. Infer what "done" looks like for this
     requirement based on its title and source_quote. Format as a markdown checklist:
     - [ ] {criterion}
     Add further criteria for any conditions or constraints found in source or discussion notes.}

    ## Notes
    {synthesised caveats, dependencies, open questions, or related context from discussion
     notes — leave empty if none available}
    ```
```

<!-- Current requirement update branch instructions (around line 220–231): -->
```
    Read the existing file {wiki_dir}{artifact.existing_id}.md using the Read tool.
    ...
    Apply artifact.change_summary to the relevant field(s) in the frontmatter or body. Update the source field...
    Regenerate the summary field...
    Use the Write tool to overwrite...
```

<!-- Frontmatter fields sara-update must now write for requirements (from Plan 01 — new fields flow through): -->
```json
{
  "priority": "must-have",
  "req_type": "functional"
}
```
Note: artifact.req_type maps to frontmatter field `type` (the wiki page field is `type`; the artifact schema field is `req_type`).

<!-- Established wikilink convention (from sara-update SKILL.md lines 121–132): -->
```
- STK entities: display text = name only → [[STK-001|Rajiwath Patel]]  (read wiki/stakeholders/{ID}.md for name)
- REQ/DEC/ACT/RSK entities: display text = {ID} {title} → [[DEC-007|DEC-007 Defer SSO to Phase 3]]
- If title/name cannot be resolved: fall back to bare [[ID]]
```

<!-- Prose-first rule (established in sara-update SKILL.md): -->
```
Write synthesised body sections as natural language. Entity references should support the prose, not replace names.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite requirement create branch body structure in sara-update Step 2</name>
  <files>.claude/skills/sara-update/SKILL.md</files>
  <read_first>
    - .claude/skills/sara-update/SKILL.md — read the entire file to locate Step 2, specifically the `**requirement:**` create branch block and the section immediately below it (`**decision:**` block) to establish the edit boundaries
  </read_first>
  <action>
In sara-update/SKILL.md Step 2, find the `**requirement:**` block within the `**If artifact.action == "create":**` section. Replace the entire body block (from `**requirement:**` through the closing triple-backtick of the Notes section, but NOT including the `**decision:**` block that follows) with the following:

Also, in the frontmatter construction instructions (the bulleted list that sets `id`, `title`, `source`, etc.), add these two items to the list in the correct position:
- After `schema_version = "1.0"` (changing it): `schema_version` = `'2.0'` for requirement artifacts (single quotes — prevents YAML float parsing)
- Add: `type` = `artifact.req_type` (one of: functional, non-functional, regulatory, integration, business-rule, data)
- Add: `priority` = `artifact.priority` (one of: must-have, should-have, could-have, wont-have)
- Remove `description` from the field list (v1.0 field — not present in v2.0 frontmatter)

**New body block for the `**requirement:**` create branch:**

```
    **requirement:**
    ```
    ## Source Quote
    > "{artifact.source_quote}" — [[{artifact.raised_by}|{stakeholder_name}]]

    ## Statement
    {Synthesise a precise "The [subject] shall [verb phrase]." statement from the source_quote and
     discussion_notes. Use the commitment modal from the source to determine the statement's strength.
     Write one sentence in the form: "The [system/role] shall [action/behaviour/constraint]."}

    ## User Story
    {Apply the section matrix for artifact.req_type:
     - functional → REQUIRED: write "As a [role], I want [capability], so that [benefit]."
     - non-functional → OPTIONAL: write only if a user-facing perspective is natural (e.g. usability NFR).
       If not natural, omit this section header entirely — do not write an empty heading.
     - integration → OPTIONAL: write only if a developer or end-user perspective is clear.
       If not natural, omit this section header entirely — do not write an empty heading.
     - regulatory, business-rule, data → OMIT: do not write this section header at all.}

    ## Acceptance Criteria
    {REQUIRED for all types — derive at least one testable criterion directly from the source_quote.
     Infer what "done" looks like from the title and source_quote. Format as a markdown checklist:
     - [ ] {criterion}
     Add further criteria for any conditions or constraints from source or discussion_notes.}

    ## BDD Criteria
    {Apply the section matrix for artifact.req_type:
     - functional → REQUIRED: write one happy-path Gherkin scenario.
     - business-rule → REQUIRED: write one happy-path Gherkin scenario (Gherkin is most natural here).
     - integration → OPTIONAL: write if an API contract scenario is natural for this requirement.
     - non-functional, regulatory, data → OMIT: do not write this section header at all.
     Add additional scenarios ONLY when the requirement explicitly has distinct, named edge cases.
     Format:
     **Scenario: [name]**
     Given [context]
     When [action]
     Then [outcome]
     If omitting, leave this section header absent entirely — do not write an empty heading.}

    ## Context
    {Apply the section matrix for artifact.req_type:
     - functional → OPTIONAL: include only when there is non-obvious rationale or design constraint
       not captured in Statement or Source Quote. If nothing relevant, leave empty (heading only).
     - non-functional, regulatory, integration, business-rule, data → REQUIRED: write rationale,
       background, or constraints not captured in Statement or Source Quote. Why this quality target,
       mandate, integration contract, domain rule, or data policy exists. Leave empty (heading only)
       if nothing is available from source or discussion_notes — never fabricate.}

    ## Cross Links
    {Generate one wiki link per entry in artifact.related. Resolve display text per wikilink rule:
     - STK entities: [[STK-NNN|name]] — read wiki/stakeholders/{ID}.md for the name field
     - REQ/DEC/ACT/RSK entities: [[ID|ID Title]] — read wiki/index.md for the title
     - If title/name cannot be resolved: fall back to bare [[ID]]
     Write each link on its own line. If artifact.related is empty, write this heading with no
     content (heading-only — consistent with the established empty-section pattern for this skill).}
    ```
```

Do not change the `**decision:**`, `**action:**`, or `**risk:**` body blocks. Do not change the Wikilink rule block, Prose-first rule block, or the "leave empty (heading only)" convention text.
  </action>
  <verify>
    <automated>grep -n "## Source Quote\|## Statement\|## BDD Criteria\|## Cross Links\|artifact.req_type\|artifact.priority" /home/george/Projects/sara/.claude/skills/sara-update/SKILL.md | head -20</automated>
  </verify>
  <acceptance_criteria>
    - sara-update/SKILL.md contains `## Source Quote` in the requirement create branch body block
    - sara-update/SKILL.md contains `## Statement` in the requirement create branch body block
    - sara-update/SKILL.md contains `## BDD Criteria` in the requirement create branch body block
    - sara-update/SKILL.md contains `## Cross Links` in the requirement create branch body block
    - sara-update/SKILL.md contains `artifact.req_type` referenced in the section matrix conditions
    - sara-update/SKILL.md contains `artifact.priority` referenced in the frontmatter field list
    - sara-update/SKILL.md contains `type` = `artifact.req_type` in the frontmatter field construction list
    - sara-update/SKILL.md contains `schema_version` = `'2.0'` for requirement artifacts (with note about single quotes)
    - sara-update/SKILL.md does NOT contain `## Description` inside the requirement create branch body (replaced)
    - sara-update/SKILL.md does NOT contain `## Notes` inside the requirement create branch body (replaced)
    - The `**decision:**` body block immediately following the requirement block is unchanged (still contains `## Context` → `## Decision` → `## Rationale` → `## Alternatives Considered`)
  </acceptance_criteria>
  <done>The requirement create branch in sara-update Step 2 produces v2.0 wiki pages: structured seven-section body with section matrix conditions per req_type, Cross Links from artifact.related[], schema_version '2.0', type and priority from artifact fields.</done>
</task>

<task type="auto">
  <name>Task 2: Extend requirement update branch with v2.0 body rewrite and Cross Links</name>
  <files>.claude/skills/sara-update/SKILL.md</files>
  <read_first>
    - .claude/skills/sara-update/SKILL.md — locate the `**If artifact.action == "update":**` section in Step 2 (specifically the sentence "Apply artifact.change_summary to the relevant field(s)..." and the "Regenerate the summary field" sentence)
  </read_first>
  <action>
In sara-update/SKILL.md Step 2, find the `**If artifact.action == "update":**` section. After the existing sentence that reads "Regenerate the `summary` field: read `summary_max_words`..." (the summary regeneration instruction), add the following new paragraph for requirement artifacts specifically. Insert it as a new paragraph before the "Use the Write tool to overwrite..." instruction:

```
    For requirement artifacts (`artifact.type == "requirement"`): after applying the change_summary
    to frontmatter fields and regenerating the summary, also update the frontmatter to include
    the v2.0 fields from the artifact object:
    - Set `type` = `artifact.req_type` (one of: functional, non-functional, regulatory, integration, business-rule, data)
    - Set `priority` = `artifact.priority` (one of: must-have, should-have, could-have, wont-have)
    - Set `schema_version` = `'2.0'` (single-quoted string — prevents YAML float parsing)
    - Remove the `description` field from the frontmatter if present (it is a v1.0 field)

    Then rewrite the full body to the v2.0 structured section format (Source Quote, Statement,
    User Story, Acceptance Criteria, BDD Criteria, Context, Cross Links) using the same synthesis
    rules as the create branch. Synthesise section content from the updated frontmatter,
    artifact.source_quote, artifact.change_summary, and {discussion_notes}. Apply the section
    matrix (per artifact.req_type) to determine which sections to include and which to omit.

    The Cross Links section is always written last. Generate one wiki link per entry in
    artifact.related (after merging with the existing related[] array). Use the wikilink rule:
    STK → [[STK-NNN|name]], REQ/DEC/ACT/RSK → [[ID|ID Title]], fallback to [[ID]] if
    title/name cannot be resolved. Write each link on its own line. Write heading only if
    artifact.related is empty after merge.
```

This paragraph is added ONLY for requirement artifacts in the update branch. The decision, action, risk, and stakeholder update paths are not changed.
  </action>
  <verify>
    <automated>grep -n "artifact.type == .requirement.\|v2.0 structured section format\|rewrite the full body" /home/george/Projects/sara/.claude/skills/sara-update/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - sara-update/SKILL.md contains `artifact.type == "requirement"` in the update branch section
    - sara-update/SKILL.md contains `rewrite the full body to the v2.0 structured section format` in the update branch
    - sara-update/SKILL.md contains `Set \`schema_version\` = \`'2.0'\`` in the update branch requirement paragraph
    - sara-update/SKILL.md contains `Remove the \`description\` field from the frontmatter if present` in the update branch
    - sara-update/SKILL.md contains `artifact.req_type` referenced in the update branch requirement paragraph
    - sara-update/SKILL.md contains `artifact.priority` referenced in the update branch requirement paragraph
    - The "Regenerate the `summary` field" instruction for all artifact types still appears before the new requirement-specific paragraph (not removed)
    - The update branch write instruction `Use the Write tool to overwrite {wiki_dir}{artifact.existing_id}.md` still appears after the new paragraph
  </acceptance_criteria>
  <done>The requirement update branch in sara-update Step 2 will migrate pages to v2.0 body structure when updating any requirement page — consistent with D-06 ("written or updated after this phase"). Cross Links are generated from the merged related[] array using the established wikilink convention (D-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| artifact object → wiki page write | artifact.req_type and artifact.priority are LLM-assigned fields; invalid values would produce malformed frontmatter |
| artifact.related[] → Cross Links body | IDs in related[] must resolve to real wiki entities; unresolved IDs fall back to bare [[ID]] |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Tampering | sara-update requirement write | accept | SKILL.md is author-controlled; all wiki writes use Write tool only (no shell text-processing). Changes reviewed via git diff. |
| T-08-07 | Spoofing | schema_version YAML float | mitigate | Instructions explicitly require single-quoted `'2.0'`; acceptance criteria check for the quoted form specifically |
| T-08-08 | Information Disclosure | Cross Links section exposing related entity titles | accept | wiki is a private project repo; all related[] IDs are already in frontmatter — the body rendering is no additional exposure |
| T-08-09 | Denial of Service | Section matrix over-generation (too many BDD scenarios) | mitigate | Instruction limits BDD to one happy-path scenario; additional scenarios only for explicitly named edge cases |
</threat_model>

<verification>
After both tasks complete:

1. Run: `grep -n "## Source Quote\|## Statement\|## BDD Criteria\|## Cross Links" /home/george/Projects/sara/.claude/skills/sara-update/SKILL.md` — all four headings must appear in the requirement create branch body block.
2. Run: `grep -n "artifact.req_type\|artifact.priority" /home/george/Projects/sara/.claude/skills/sara-update/SKILL.md` — must appear in both the create branch (frontmatter field list and body section matrix conditions) and the update branch (new requirement-specific paragraph).
3. Run: `grep -n "rewrite the full body\|v2.0 structured section" /home/george/Projects/sara/.claude/skills/sara-update/SKILL.md` — must appear in the update branch.
4. Run: `grep -n "## Description\b" /home/george/Projects/sara/.claude/skills/sara-update/SKILL.md` — must NOT appear in the requirement block; it is acceptable in decision/action/risk blocks.
5. Confirm the decision body block is intact: `grep -n "## Decision$\|## Rationale$\|## Alternatives" /home/george/Projects/sara/.claude/skills/sara-update/SKILL.md`.
</verification>

<success_criteria>
- sara-update requirement create branch: v2.0 frontmatter fields (type from req_type, priority, schema_version '2.0'), structured seven-section body with section matrix conditions, Cross Links from related[] (D-10, D-11, D-12)
- sara-update requirement update branch: migrates to v2.0 body structure on update, sets type/priority/schema_version '2.0', generates Cross Links from merged related[] (D-06, D-12)
- decision, action, risk, and stakeholder write paths: unchanged (scope boundary respected)
</success_criteria>

<output>
After completion, create `.planning/phases/08-refine-requirements/08-03-SUMMARY.md`
</output>
